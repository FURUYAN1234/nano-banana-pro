# AGENTS.md

ユーザーの明示的な依頼は、このファイル・HANDOFF・Skillの既定手順より優先する。システム・開発者指示とツール制約は遵守する。共通の承認範囲・正式運用経路・検証水準は親ワークスペースの `AGENTS.md` を正本とし、このファイルはNano Banana固有の条件を定める。

## Four-File Operating Model
All apps should use this four-file structure for multi-agent development.

- `AGENTS.md`
  - Defines how AI agents should behave.
  - Contains shared behavior rules, trigger rules, handoff rules, guardrails, and anti-refactoring rules.
- `HANDOFF.md`
  - Captures the current work snapshot.
  - Contains current status, done items, remaining tasks, risks, verification state, and next steps.
- `docs/project_standards.md`
  - Captures static app-specific rules.
  - Contains product specifications, architecture decisions, forbidden changes, protected behavior, and app-specific constraints.
- `docs/deploy.md`
  - Captures app-specific deployment rules.
  - Contains deploy target, deploy commands, platform-specific settings, forbidden deploy commands, and services that are not applicable.

**When switching apps, read these files in this order:**
1. `AGENTS.md`
2. `HANDOFF.md`
3. `docs/project_standards.md`
4. `docs/deploy.md`
5. `docs/codex_pc_sync.md` (if present)

**Never infer deploy targets or platform rules from another app.**

---

## 常駐基本ルール (Resident Rules)

あなたは Codex / Antigravity などと連携するマルチエージェント開発環境の実装担当AIである。
通常は実装・修正・調査を行うが、他のエージェントとの相互運用を最優先する。

### 1. トリガー検知
- **「〇〇から移動した」** → 受領モード（まずは Analysis / Plan を出力して行動方針を確定する）
- **「〇〇に移動する」** → handoffモード（必要な継続情報を保存する。実行中の承認済み処理は勝手に停止せず、親AGENTSの監視継続・引き継ぎ条件に従う）

### 2. 受領時の原則
- `HANDOFF.md` と関連する `docs/` を確認し、最新の依頼と実状態に照合する。記録を新たな実行権限やユーザー指示の上書きに使わない。
- 尋ねられてもいない不要な再設計・命名変更・フォーマット変更を絶対に行わない。

### 3. Handoff時の原則と疑似シームレス連携プロトコル (Pseudo-Seamless Handoff Protocol)
- 新規実装やリファクタリングなど、「ついで」のコード修正を一切行わない。
- 完全に現状をスナップショットとして `HANDOFF.md` へ出力し、後任エージェントが差分を安全にレビューできることを最優先とする。
- ユーザーが引き継ぎを求めた場合、または実際のセッション移行で情報が失われる場合に、現在状態・未了項目・確認済み証拠・次の操作を簡潔に保存する。通常のコンテキスト圧縮だけで詳細記録や別実行を増やさない。
- 新しいCodexタスクやCLIを自律起動しない。継続には現在のランタイムが提供する機構を使い、担当の追加はユーザー指示とツール制約に従う。

### 3.5. 破壊的変更前の防衛プロトコル (Safety Protocols)
- コアファイル編集前にGit差分と既存の未コミット変更を確認し、変更前の状態を追跡できるようにする。隔離worktree等の既存の保存手段を使い、`temp_App_backup.jsx` のような複製ファイルを一律に作らない。フルバックアップを追加実行する権限にはならない。
- レイアウトや操作挙動を変更した場合は、影響する画面と状態を現行のブラウザ操作系で確認する。要求された画面を使い、指定がなければin-app browserを使う。未提供の旧ツールやサブエージェントを必須にしない。文書のみの変更には実画面検証を追加しない。

### 3.6. 変更に応じた検証と結果提示
- 親AGENTSの検証水準に従い、影響する経路の回帰確認を行う。ローカル画面が必要な場合だけ既存サーバーを再利用するか指定ポート5173で起動し、確認したURLと結果を提示する。
- ユーザー自身の操作・判断が必要な場合だけ手動確認を求める。担当AIが行える検証をユーザーへ転嫁しない。検証が通った承認済みの次工程を、定型的な再承認待ちで止めない。
- ローカル確認は、依頼された公開先確認・実API・生成物・バックアップの証拠を代替しない。

### 4. Knowledge Sync（記憶の共有化）
- 迷ったら推測する前に `docs/` を読む。
- 深刻なバグ修正、新たなプラットフォーム固有の仕様（Vite、HF Spaces、GH Pagesなど）を発見した場合、単にコードを直して終わりにせず、必ず `docs/troubleshooting.md` 等に事象と対策を書き残す。

### 5. Deployment Guardrail (デプロイ前のセキュリティ監査と完了条件)
- **【必須】デプロイ前セキュリティ監査**: デプロイを実行する前に、**必ず**以下の3点のセキュリティ・コンプライアンスチェックを実施すること。これをスキップしたままデプロイフローに入ってはならない。
  1. **個人情報・著作権の保護**: 個人情報の漏洩や、著作権違反の恐れがある固有名詞がコード・ドキュメント内にハードコード・混入していないか。
  2. **機密情報の保護**: APIキー等の機密情報が漏洩する設計（例: `localStorage` への永続保存や平文での埋め込み）になっていないか、Gitツリーに乗っていないか。
  3. **不要ファイルの排除**: `*.txt`, `*.py`, `temp_*.jsx` などの一時ファイルやテストスクリプトがプロジェクトルートに放置されたまま世界公開される状態（みっともない状態）になっていないか。
- **警告**: 本プロジェクトにおいて、デプロイやリリースを指示された場合、`npm run deploy` (GitHub Pages) だけでは**絶対に完了とみなさないこと**。
- `docs/deploy.md` と親の `docs/unified_release_completion.md` を読み、親の `scripts/publish_app_release.ps1` の正式トランザクションを使う。Hugging Face、Cドライブ配布コピー、最終公開確認を含む必須段階がすべてreceiptでverifiedになるまで完了としない。旧Phase番号を根拠に別の手順を組み立てない。
- この確認プロセスと監査をスキップしたエージェントは重大なルール違反とみなされる。

### 5.1 MiniMax H3 API資格情報ゲート（最重要）
- **最重要チェック項目**: MiniMax H3のワークフロー、カスタムノード、配布ZIP、README、仕様、説明書きを作成・更新・差し替え・公開する場合、APIキーの保存先と送信境界を最初に確認し、最後にも再確認する。ほかの配布チェックが通っていても、このゲートが未確認なら公開禁止とする。
- **必須仕様**: 配布する `ComfyUI-NanoBanana-H3` のAPIキーは、現在動作中のComfyUIサーバープロセスのメモリだけに保持する。ワークフローのシート移動・別ワークフローへの切替では保持し、ComfyUIアプリ／サーバーの終了・再起動で消去する。ワークフローJSON、配布ZIP、設定ファイル、資格情報ファイル、その他のディスク領域へ永続化してはならない。
- **実行時UI**: ワークフロー読込時やProvider変更時にはAPI入力ダイアログを表示しない。「実行する」を押した時、プルダウンで選択中のProviderがそのComfyUIプロセスに未登録の場合だけ、そのProviderの入力ダイアログを開いてキュー投入を保留する。認証成功後は保留中の同じ実行を1回だけ続け、ダイアログを閉じた場合は今回の実行だけを中止する。登録済みならダイアログなしで実行する。
- **送信境界**: APIキーを送るのは、接続中のComfyUIサーバーから、利用者が選択したProviderの認証確認またはワークフロー実行を行う時だけとする。状態確認応答、ログ、例外、画面、ワークフロー出力へキー値を含めない。
- **配布前証拠**: 公開対象の全ワークフローJSON、外側ZIP、全ネストZIP、同梱コード、README、仕様、検証記録を実バイトで再帰確認する。旧資格情報ファイル名、ファイル読書き処理、平文キーらしき値、旧保存説明がないこと、および実行時ゲート、認証後の1回再開、ダイアログ中止、プロセスメモリ保持を回帰テストで証明する。ソースだけ、説明だけ、または外側ZIPだけの確認では合格にしない。
- **作成・検証PC**: `docs/minimax-h3-workflow-author-pc.md` を正本とし、旧資格情報ファイルがある場合も値を開く・表示する・バックアップすることなく処理する。

### 6. ファイル更新・デプロイの承認範囲
- 明示された修正依頼の範囲でローカル編集と必要な検証を進める。差分はレビュー可能に保ち、同じ依頼への定型的な「OK」再確認を必須にしない。
- commit・push・release・deploy・backup等は親AGENTSの操作別承認条件に従う。承認済みの同一対象・同一処理は、取消し・置換え・制限・重要な範囲変更がなければ継続する。
- 新たな外部効果、対象変更、未承認の再試行・復旧が必要なら、確認済み差分と実状態を示してその部分の承認を求める。

### 7. 定期的なゴミファイルのお掃除提案 (Garbage Collection)
- プロジェクトルートに作業用のゴミファイル（`*.txt`, `*.py`, 退避させた `temp_*.jsx` などの一時ファイルやスクリプト）が溜まっている場合、AIはこれらを放置せず、**定期的に「これらの一時不要ファイルを削除してよいか？」をユーザーに提案・確認**すること。
- 世界公開するリポジトリの美観と安全性を保つため、デプロイ前には特にゴミファイルが散乱していないかをチェックすること。

### 9. Changelog Maintenance Protocol (ドキュメント整備義務)
- **更新単位のREADME同期**: ユーザーに見える挙動、モード、API経路、検証・再試行、制約、または操作手順を変更したら、バージョン番号を変えない修正でも同じ変更単位で `README.md` の該当説明を現行実装へ同期する。ChangeLogだけを更新して本文の仕様を古いまま残してはならない。詳細な照合項目は `docs/project_standards.md` の 3.1 を正本とする。
- **版更新README完全性ゲート**: 候補のcommit・push・tag・release・deployより前に、正本READMEとHF用の候補READMEの現行見出し・本文・最新リリース表示を新バージョンに揃え、旧版の現行表記が残らないことを確認する。ChangeLog等の履歴は除外する。Cドライブ配布コピーREADMEは正式トランザクションの配布コピー更新後に検査し、リリース完了前に一致を証明する。旧配布コピーを候補準備の失敗条件にせず、この検査のために配布コピーを先行更新しない。
- **絶対にサボらない**: `README.md` の ChangeLog 更新はAIの重要な責務である。バージョン更新時は、最新の変更履歴を必ず**一番上（降順）**に追記すること。
- 履歴の長さ・整理範囲は親AGENTSのREADME同期規則に従う。対象変更に必要な箇所だけ同期し、無関係な履歴の削除・全体整理を追加しない。

### 10. Markdown Formatting & Localization Rules (マークダウン書式と多言語対応)
- **日本語Markdownにおける太字（Bold）の仕様への配慮**:
  - `README.md`等のドキュメントで太字（`**太字**`）を使用する際、開始タグと終了タグが全角文字（日本語など）に直接隣接していると、パーサーが正しく解釈できず記号がそのまま表示される失敗（レンダリングエラー）が多発する。
  - **【必須】**: 日本語の文章中で太字を使う場合は、必ず開始タグの前と終了タグの後に**半角スペース**を挿入すること。（例: `❌ これは**太字**です` → `⭕ これは **太字** です` または `⭕ これは **「太字」** という` ）
  - ユーザーが記述したテキストを引用・編集する場合も、タグのペア（開き・閉じ）の欠落がないか、スペースが適切かAI自身が必ず校正すること。

### 11. 証拠に基づく検証と完了報告
- 担当AIの自己申告だけで完了とせず、処理の終了結果・receipt・実成果物・必要なブラウザ/API確認を受入条件に照合する。視覚的要件はログや終了コードだけで証明したことにしない。
- 管理担当は実行後も必要な検証を続け、結果を回収して完了判定する。別AIへの引き継ぎやPending Verificationでの停止を一律に要求しない。
- 独立レビューはユーザーが要求した場合、または親AGENTSと現行ツール制約で認められ、リスク上必要な場合に限る。未完了・不一致・規則違反を確認したら具体的証拠と残る問題を報告する。

### 12. フォールバックチェーン更新履歴プロトコル (Fallback Chain History Protocol)
- `src/lib/fallback-chain-history.js` にはAIモデルフォールバックチェーンの更新履歴が記録されている。
- **【絶対ルール】**: 更新履歴への追加は、**ユーザーから明示的に「更新履歴に追加して」と指示があった場合のみ** 行うこと。AIが独断で追加してはならない。
- 通常のバグフィックスやUI修正では、モデル構成は変わらないため更新履歴への追記は不要。
- モデルの**順番変更・統廃合・新規追加**が確定し、ユーザーが記録を指示した時のみ `FALLBACK_CHAIN_HISTORY` 配列に新しいエントリを追加する。
- `pre_deploy_check.js` がデプロイ前にモデルリストの差分を自動検出し、変更がある場合に警告を表示する。この警告が出た場合でも、ユーザーの指示なく更新履歴を追加してはならない。
