## Version correction / バージョン訂正

* This is a version-number correction only. No new application logic, prompt behavior, UI behavior, or provider configuration was added after `v4.10.0`.
* `v4.9.1` carries the same application logic already published as `v4.10.0`; its version identifiers were corrected because `4.9.0` must advance to `4.9.1`.
* `v4.10.0` remains as a published historical record. `v4.9.1` is the intended latest release.

* これはバージョン番号だけの訂正です。`v4.10.0` 公開後に、アプリ本体ロジック、プロンプト挙動、UI挙動、プロバイダー設定を追加・変更していません。
* `v4.9.1` のアプリ本体ロジックは、すでに `v4.10.0` として公開済みのものと同一です。`4.9.0` の次は `4.9.1` であるため、版番号識別子だけを訂正しました。
* `v4.10.0` は公開済み履歴として残し、意図した最新リリースは `v4.9.1` です。
