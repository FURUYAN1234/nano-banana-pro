# Nano Banana Pro v6.1.1

- Matched the STEP1 character-sheet selector to the full width used by the STEP2, STEP3, and STEP4 primary buttons. / STEP1のキャラクター設定画像選択ボタンを、STEP2・STEP3・STEP4の主ボタンと同じ横幅へ統一しました。
- Rewrote the STEP1 drop-zone guidance to explain both button selection and drag-and-drop, including simultaneous or later additions and optional 360-degree background images or style-setting JSON files. / STEP1のドロップ領域を、ボタン選択とドラッグ＆ドロップの両方、複数画像の同時・後からの追加、任意の360°背景画像と作風設定JSONの読込を明記する案内へ更新しました。
- Changed the STEP3 "Copy prompt (for web paste)" button from white to pale yellow while retaining its copy-complete feedback and existing behavior. / STEP3内の「プロンプトをコピーする（web貼り付け時）」ボタンを白から薄い黄色へ変更し、コピー完了表示と既存動作は維持しました。
- No upload logic, image analysis, workflow, custom node, generation prompt, or API behavior changed. The separately verified FourPanel v6.0.9 JSON and ZIP remain the current downloads. / アップロード処理、画像解析、ワークフロー、カスタムノード、生成プロンプト、API動作は変更していません。検証済みのFourPanel v6.0.9 JSONとZIPを引き続き配布します。
