# Nano Banana Pro v6.0.4

## 日本語

- 成人キャストの安全条件を維持しながら、その内部条件を画像内へ文字として描画しないようプロンプトを修正しました。旧来の目立つ年齢宣言が、指定外の英語フッターとして画像へ混入する問題を防ぎます。
- 回帰テストは、成人が20歳以上である条件と非表示指定を要求し、問題を起こした旧英文が生成プロンプトへ残らないことを確認します。
- ユーザー提供の元プロンプトとキャラクター参照2枚を使い、OpenAI GPT Image 2.5 Sunburst / xhigh・1024×1536で1回実生成しました。問題の年齢英文は再発せず、指定タイトル、7本の台詞、左右2本の透かしを目視確認しました。強い煽り、手前へ迫る拳、肩越し、最終コマのデフォルメ全身演技も維持されています。自動修正はOFFとし、追加画像生成は行っていません。
- 全374件のNodeテスト、警告0のlint、production buildを実施しました。

## English

- The adult-cast safety condition remains in place, but it is now explicitly non-visible. This prevents the previous prominent age declaration from leaking into the image as an unsolicited English footer.
- Regression coverage requires both the age-20-or-older constraint and the non-visible instruction, and rejects the exact legacy sentence that caused the visible footer.
- One real OpenAI GPT Image 2.5 Sunburst / xhigh run at 1024×1536 used the user-supplied source prompt and two character references. The age notice did not reappear. Visual review confirmed the requested title, all seven dialogue lines, both intended watermarks, and the strong low-angle, foreground-fist, over-the-shoulder, and final chibi full-body staging. Automatic repair was disabled, so no additional image generation occurred.
- Validation includes all 374 Node tests, zero-warning lint, and a production build.
