## Nano Banana Pro v5.5.2

### 日本語

- STEP4のMiniMax H3案内に、最初に選ぶべき `MiniMax H3 Reference-to-Video（R2V / Ref2VA）` ワークフローを独立した手順として追加しました。
- 同じワークフロー名を「ComfyUIの開始設定」の先頭にも表示し、以降の `ref_image_0` 接続と設定案内の前提を明確にしました。

### English

- Added an explicit STEP4 instruction to select the `MiniMax H3 Reference-to-Video (R2V / Ref2VA)` workflow before connecting the manga.
- Repeated the workflow name as the first ComfyUI setup item so the subsequent `ref_image_0` connection and settings have an unambiguous starting point.

### Verification

- Added regression coverage that requires the visible STEP4 guide to name the exact R2V / Ref2VA workflow.
