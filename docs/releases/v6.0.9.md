# Nano Banana Pro v6.0.9

- The four required STEP1 through STEP4 actions now share a light-blue style, making the minimum path easy to identify while secondary controls remain visually separate. / 必須のSTEP1～STEP4主操作を薄い青色へ統一し、補助操作と見分けやすくしました。
- Repackaged the unchanged reviewed FourPanel workflow as `FourPanel_NonLM_4step_20260912203033_v6.0.9.json` and the five-custom-node bundle as `ComfyUI_H3_FourPanel_NonLM_20260912203033_v6.0.9.zip`. / 検証済みのFourPanel生成内容は変えず、ワークフローとカスタムノード5点ZIPをv6.0.9へ揃えました。
- The workflow JSON and custom-node ZIP remain separate downloads, and the source repository still contains no distribution ZIP. / ワークフローJSONとカスタムノードZIPは別ダウンロードのまま維持し、ソースGitには配布ZIPを直接登録しません。
- T2V, I2V, and Ref2V remain in the separate `comfyui-h3-workflows` product and are unchanged. / T2V・I2V・Ref2Vは別製品`comfyui-h3-workflows`のままで変更していません。
- The supplied functionally identical candidate completed a 39-second GPU/API run; a fresh GPU run from the final v6.0.9 ZIP and another-PC execution remain unverified. / 機能が同じ受領候補は39秒のGPU/API生成を完走しましたが、最終v6.0.9 ZIPからの新規GPU生成と別PC実行は未検証です。
