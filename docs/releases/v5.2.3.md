# Nano Banana Pro v5.2.3

## 日本語

- 4コマの全パネルで、各キャラクターの衣装アイテム・基調色・差し色・柄を固定する `CROSS-PANEL WARDROBE COLOR LOCK` を追加しました。パネルごとの演出差分は背景、光、VFX、描画処理に限定し、服の色替えを防止します。
- ChatGPT Web向けの15,000文字ソフト上限を維持した圧縮経路にも同じ衣装色ロックを残します。
- 縦横比修正プロンプトは、A4のページ形状だけを再構成し、元画像の顔、髪、眼鏡、服のアイテム・色・柄、キャスト、セリフ、小道具、舞台を不変にするソース忠実度ロックを追加しました。添付した実4コマ画像をChatGPTで1024×1448へ再生成し、保持を目視確認済みです。
- 実際のキャラクターシート自動読込から、通常のフルオート4コマ生成と単一イラスト生成を `gpt-image-2` で確認しました。キャラクター識別、眼鏡状態、衣装色、手、小道具、背景の整合性を確認済みです。

## English

- Added `CROSS-PANEL WARDROBE COLOR LOCK` so each character's garment items, base colors, accents, materials, and patterns remain fixed across all four manga panels. Panel-style changes are limited to environment, lighting, VFX, and rendering treatment.
- Preserved the same wardrobe-color contract in the compact ChatGPT Web path while keeping its 15,000-character soft budget.
- Updated the aspect-ratio correction prompt with a source-fidelity lock: rebuild only the A4 page geometry while preserving faces, hair, glasses, garment items, colors, patterns, cast, dialogue, props, and setting from the source manga. Verified the behavior on an attached real four-panel image regenerated at 1024×1448.
- Verified the normal character-sheet auto-read flow with real `gpt-image-2` four-panel and single-illustration runs, reviewing character identity, glasses state, garment colors, hands, props, and setting continuity.
