## Nano Banana Pro v5.4.8

### English

- Restored the STEP4 pro helper actions: aspect-ratio correction, 2x upscale, and MiniMax H3/ComfyUI prompt copy are visible regardless of the currently rendered engine label.
- STEP4 now uses the effective OpenAI image-generation state for its provider-specific labels, links, logs, export metadata, and downloaded filename.
- Added regression coverage for both the always-visible helpers and the OpenAI state boundary.

### 日本語

- STEP4の「画像比率修正」「画像2倍アップスケール」「MiniMax H3・ComfyUI」各コピー操作を常時表示へ復旧しました。
- STEP4の表示名・Webリンク・ログ・保存メタデータ・ダウンロード名は、表示用の古いエンジン状態ではなく、実際のOpenAI画像生成状態を基準にします。
- 補助操作の表示とOpenAI状態の境界を回帰テストで固定しました。

### Verification

- Full Node regression suite, zero-warning ESLint, Vite production build, release preflight, and diff checks passed.
- The in-app browser confirmed that all three restored helper controls are visible on the STEP4 surface.
