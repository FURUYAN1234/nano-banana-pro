# Nano Banana Pro v5.2.0 - STEP1 Character Analysis Reset / STEP1キャラクター解析リセット

## 日本語

- ヘッダーの既存操作を `シナリオ（STEP2）からやり直す` に名称変更しました。
- `キャラクター解析（STEP1）からやり直す` を追加しました。API接続と選択中のエンジンを保持したまま、キャラクターシート画像、抽出済みキャラクター設定、解析ログ、作風JSON、およびSTEP2以降の依存データを消去します。
- 実OpenAI GPT-4.1のキャラクター解析完了後にSTEP1リセットを実行し、画像と抽出テキストの消去およびChatGPT Engine接続維持を確認しました。

## English

- Renamed the existing header action to `Restart from Scenario (STEP2)`.
- Added `Restart from Character Analysis (STEP1)`. It preserves the API connection and selected engine while clearing character-sheet images, extracted character settings, analysis logs, style JSON, and dependent STEP2-plus work.
- After a completed live OpenAI GPT-4.1 character-analysis run, verified that the STEP1 reset clears the images and extracted text while keeping the ChatGPT Engine connected.
