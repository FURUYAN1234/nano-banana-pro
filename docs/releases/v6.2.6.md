# Nano Banana Pro v6.2.6

- OpenAI category-news scenarios now use the OpenAI Responses API Web Search tool. / OpenAI接続時のカテゴリニュースシナリオは、OpenAI Responses APIのWeb Searchで生成します。
- The selected API remains fixed through STEP1–4: Gemini uses Gemini only; OpenAI uses OpenAI only. / 選択したAPIはSTEP1〜4を通して固定です。GeminiはGeminiだけ、OpenAIはOpenAIだけを使います。
- Verified with 481 Node tests, zero-warning ESLint, production build, pre-deploy checks, and a local in-app-browser OpenAI category-news run. / Nodeテスト481件、警告0のESLint、production build、pre-deployチェック、内蔵ブラウザーでのOpenAIカテゴリニュース実行を確認しました。
- The verification read no API-key value. The real category-news run completed through OpenAI Web Search without a Gemini fallback. / 検証でAPIキー値は読み取らず、実カテゴリニュース実行はGeminiへフォールバックせずOpenAI Web Searchで完了しました。
