# Nano Banana Pro v6.1.6

- Four-panel prompts now preserve the setting and its depth in quiet beats. Distant background may defocus, while focal faces, hands, and story props remain sharp. Blank backgrounds are no longer the default quiet-beat treatment. / 静かなコマでも場所と奥行きを保持し、遠景はぼかせる一方で、注目する顔・手元・物語上の小道具を明瞭に保つよう4コマ指示を修正しました。背景の白抜きを静かな演出の既定にはしません。
- The same direction survives long-prompt compaction in color and monochrome. Monochrome uses black-on-white halftone for distant defocus and preserves setting shapes instead of erasing them. / この演出はカラー・白黒と長文短縮後にも保持します。白黒の遠景ぼけは黒地に白の網点で表し、場所の形を消しません。
- Explicit cameras no longer receive a blanket extreme-lens override. Shoulder-over shots now keep the named foreground viewer separate from the focal speaker in depth. / 明示したカメラへ一律の極端なレンズ効果を足さず、指定した距離・高さ・傾き・遠近を保持します。肩越しでは、名前で指定した手前人物と焦点となる話者を奥行き上で分けます。
- A fresh OpenAI GPT Image 2.5 Sunburst / xhigh run produced one color and one monochrome 1024×1536 page from the same two character sheets, with automatic repair off. The returned pages restored café backgrounds and made the overhead second panel and shoulder-over third panel clearer. They still miss some camera, prop-state, incidental-text, and exact two-value requirements, so visual acceptance remains necessary. All 444 Node tests, strict lint, production build, and whitespace checks passed. / 同じキャラクターシート2枚を使い、OpenAI GPT Image 2.5 Sunburst / xhighでカラー・白黒各1枚を1024×1536で実生成しました。自動修正はOFFです。カフェ背景を残し、2コマ目の俯瞰と3コマ目の肩越しは明確になりました。一方、画角、小道具状態、未指定文字、厳密二値には未達が残るため、画像ごとの目視採否は必要です。全444 Nodeテスト、厳格lint、本番ビルド、空白差分検査に合格しています。
