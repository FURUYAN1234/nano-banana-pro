## Nano Banana Pro v5.5.8

### 日本語

- 配布ワークフローJSONを、今回提供された更新版の標準20ステップワークフローへバイト単位で同期しました。
- `ComfyUI-NanoBanana-H3` 配布ZIPを、今回提供された更新版 `__init__.py`、`h3_prompt_system.txt`、`web/nanobanana_h3.js` へ差し替えました。
- ZIPのルートフォルダと3ファイル限定の配布契約を維持し、APIキー、認証情報、モデル重み、漫画画像、生成済み動画を含めていません。
- STEP4の既存の2つの直接ダウンロード導線と、カスタムノードを先に導入してComfyUIを完全再起動する手順は変更していません。

### English

- Synced the distributed workflow JSON byte-for-byte with the newly supplied updated standard 20-step workflow.
- Replaced the `ComfyUI-NanoBanana-H3` distribution ZIP with the newly supplied `__init__.py`, `h3_prompt_system.txt`, and `web/nanobanana_h3.js` files.
- Preserved the root-folder and three-file-only ZIP contract without bundling API keys, credentials, model weights, manga images, or generated videos.
- Kept the existing two direct-download actions and the install-custom-node-first, fully-restart-ComfyUI setup sequence unchanged.
