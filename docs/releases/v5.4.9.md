## Nano Banana Pro v5.4.9

### English

- Replaced the split legacy OpenAI/Gemini state with one effective engine decision for the workflow.
- If either retained state says OpenAI, the text engine, OpenAI image route, prompt family, STEP4 display, logs, links, and recovery guidance converge to OpenAI.
- Restored provider-specific STEP4 tools without changing the established manual-repair workflow: ChatGPT keeps its manual aspect-ratio and 2x-upscale copy prompts; Gemini has its own manual aspect-ratio and 2K copy prompts for Gemini Web with the generated image attached; MiniMax H3 remains provider-independent.
- Added truth-table regression coverage for the four legacy-state combinations and a workflow guard for image-route synchronization.

### 日本語

- OpenAI/Geminiの旧二重状態を、ワークフロー全体で使う単一の実効エンジン判定に統合しました。
- どちらか片方でもOpenAI状態が残っていれば、テキストエンジン、OpenAI画像ルート、プロンプト系統、STEP4表示、ログ、リンク、救済案内をOpenAIへ同期します。
- STEP4の補助機能をAPI別に戻しました。ChatGPTには従来の比率修正／2倍アップスケール用コピー、GeminiにはGemini Webへ生成済み画像を添付して使う専用の比率修正／2K用コピー、MiniMax H3には両API共通の後処理を表示します。Geminiのアプリ内APIは通常の画像生成だけを担います。
- 旧状態4組合せの真理値表と画像ルート同期を回帰テストで固定しました。

### Verification

- Full Node regression suite, zero-warning ESLint, Vite production build, release preflight, live Gemini and OpenAI image API verification, and public release verification are required before completion.
