# Nano Banana Pro v6.1.0

- Corrected the STEP1 highlight so the large image drop area stays dark and only the compact `キャラクター設定画像を選択 (STEP 1)` control is light blue. / STEP1の広い画像ドロップ領域を黒へ戻し、小さな「キャラクター設定画像を選択 (STEP 1)」操作だけを薄青に修正しました。
- Restored the neutral gray raised edge around the STEP2 and STEP3 buttons; their button faces remain light blue. STEP4 already had the intended presentation and is unchanged. / STEP2・3はボタン面の薄青だけを残し、太い外枠を従来の薄いグレーへ戻しました。正しかったSTEP4は変更していません。
- The separately verified FourPanel v6.0.9 workflow JSON and custom-node ZIP remain the current downloads. No workflow, custom node, generation prompt, or API behavior changed. / 別々に検証済みのFourPanel v6.0.9ワークフローJSONとカスタムノードZIPを引き続き配布します。ワークフロー、カスタムノード、生成プロンプト、API動作は変更していません。
