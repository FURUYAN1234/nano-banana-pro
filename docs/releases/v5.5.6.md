## Nano Banana Pro v5.5.6

### 日本語

- Nano Bananaメニューの標準版ワークフローJSONを、標準20ステップ版「Super FURU AI 4-koma System - 4-Panel Manga to Video」へ差し替えました。配布JSONは提供された原本とSHA-256が一致し、内容を改変していません。
- 必須カスタムノード `ComfyUI-NanoBanana-H3` を、指定された3ファイルだけを含むZIPとして追加しました。ZIPのルートフォルダは `ComfyUI-NanoBanana-H3` です。
- STEP4にカスタムノード専用の直接ダウンロードボタンと、導入、ComfyUI再起動、JSON読込、利用者自身によるAPIキー認証、MiniMax H3モデル導入の順序を追加しました。
- APIキー、認証情報、モデル・LoRA重み、漫画画像、生成済み動画、Pythonキャッシュは配布物に含めていません。

### English

- Replaced the Nano Banana menu's standard workflow JSON with the supplied 20-step “Super FURU AI 4-koma System - 4-Panel Manga to Video” workflow. The distributed JSON matches the supplied source by SHA-256 and was not modified.
- Added the required `ComfyUI-NanoBanana-H3` custom node as a ZIP containing only the three specified files, with `ComfyUI-NanoBanana-H3` as the archive root folder.
- Added a separate direct-download button and ordered STEP4 instructions for custom-node installation, a full ComfyUI restart, workflow loading, user-owned API-key authentication, and separate MiniMax H3 model installation.
- API keys, credentials, model or LoRA weights, manga images, generated videos, and Python caches are not included in the distributed files.

### Verification

- Verified the exact JSON and custom-node file hashes, ZIP hierarchy, direct-download attributes, and downloaded artifacts.
- Verified the local UI and both download controls in the in-app browser, including mobile overflow, keyboard focusability, and zero browser console warnings or errors.
- Ran the full Node test suite, zero-warning ESLint, Vite production build, security scans, and whitespace diff check.
