# Nano Banana Pro v6.2.7

- STEP1〜4で実行するGemini／OpenAI APIの各試行の待機上限を10分に統一しました。GPT-6系の応答や、プロバイダー内の再試行に十分な待機時間を確保します。 / Each Gemini and OpenAI API attempt used by STEP1–4 now allows up to ten minutes, leaving adequate time for GPT-6 responses and provider retries.
- STEP4のOpenAI画像生成は従来どおり10分、Gemini画像生成も10分へ統一しました。 / STEP4 OpenAI image generation remains at ten minutes, and Gemini image generation now uses the same limit.
- APIキー接続時の事前確認はSTEP実行ではないため45秒のままです。 / The 45-second API-key preflight is unchanged because it is not a STEP execution.
