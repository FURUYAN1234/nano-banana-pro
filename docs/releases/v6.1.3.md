# Nano Banana Pro v6.1.3

- Added a fixed 12px visual gap between the STEP3 settings-file save button and the STEP4 API generation button. Button height, width, wording, color and behavior are unchanged. / STEP3の設定ファイル保存ボタンとSTEP4のAPI生成ボタンの間へ、表示環境に依存しない12pxの余白を追加しました。ボタンの高さ・幅・文言・色・処理は変更していません。
- The new regression failed before implementation and passes after it. Browser-side DOM measurement reports an actual 12px gap; the full 402-test suite, strict lint and production build pass. / 新しい回帰テストは修正前に失敗し、修正後に成功しました。ブラウザのDOM実測でも12pxの間隔を確認し、全402テスト、厳格lint、production buildが成功しています。
- The persistent STEP3 Color/Monochrome selector and shared API/Web monochrome prompt contract from v6.1.2 are unchanged. Actual prior API and Web-paste samples removed visible source hues, but their PNG channel values were not mathematically exact monochrome/binary and the Web sample did not preserve every requested cast/panel detail. / v6.1.2の保持式カラー／白黒切替とAPI・Web共通の白黒プロンプトは変更していません。既存のAPI・Web貼付実生成では目視できる元画像の色は消えましたが、PNG画素値は数学的に厳密な白黒二値ではなく、Web作例は指定した全人物・コマ構成を完全には保持しませんでした。
- No finishing-image import, pixel conversion or publisher preflight was added. The separately distributed FourPanel v6.0.9 workflow JSON and custom-node ZIP are unchanged. / 仕上げ用画像読込、画素変換、投稿先向け入稿検査は追加していません。別配布のFourPanel v6.0.9ワークフローJSONとカスタムノードZIPは変更していません。
