STEP4のプロンプト出力前AI精査で、他のSTEPと同じ形式の経過秒数を表示します。待機表示は1行で更新し、処理終了時にカウントを停止します。

キャラシートに関するログを「キャラの外見は維持し、設定資料の配置・説明文はコピーしない」に変更しました。生成指示そのものは変更していません。

STEP4 now shows elapsed seconds during the AI review before prompt output, using the same format as the other steps. The wait counter updates one line and stops when processing ends.

The character-sheet log now clarifies that character appearance is preserved while reference-sheet layouts and annotations are not copied. Generation instructions are unchanged.
