# Nano Banana Pro v6.2.9

- 生成履歴を直近10件に戻しました。フルオート・連続ループでも、品質確認後に採用された各API画像を履歴から選択してプレビューへ戻し、選択した画像を手動保存できます。 / Restored ten recent generation-history entries. In full-auto and continuous loops, each quality-selected API image can be selected from history, restored to the preview, and manually saved.
- STEP4のAPI生成ボタン直下へ、既定ONの「完成画像を自動保存（API生成のみ）」を追加しました。初回の手動生成またはフルオート開始時に保存先フォルダーを一度選ぶと、品質確認後の最終採用画像だけを実際に書き込みます。Chromeの保護によりDownloads直下は選べないため、Downloads内の専用サブフォルダーを選びます。保存先とスイッチはフルオート・ループ・STEP1/STEP2リセット中に維持し、ページ再読み込みまたは全設定クリアでは選び直し／ONへ戻ります。未対応ブラウザーや選択キャンセル時は保存済みと表示せず、手動保存を案内します。 / Added the default-on automatic final-image save switch directly below STEP4 API generation. Choose a destination folder once when manually generating or starting full-auto, then only the quality-selected final image is actually written there. Chrome protects the Downloads root, so select a dedicated subfolder within Downloads. The destination and switch survive full-auto, loops and STEP1/STEP2 resets; reloading the page or fully clearing settings requires choosing the destination again and restores the switch to on. Unsupported browsers and cancelled selection never report a save and instead guide the user to manual download.
- 手動・自動保存は同じ `AI_4koma_comic_{API}_{title}_{YYYYMMDDHHMMSS}.{extension}` を使います。履歴は現在のブラウザーセッション内だけにあり、ページ再読み込み後には残りません。 / Manual and automatic saves share `AI_4koma_comic_{API}_{title}_{YYYYMMDDHHMMSS}.{extension}`. History remains in the current browser session only and does not survive reload.
