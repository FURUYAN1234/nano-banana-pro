# Nano Banana Pro v6.2.8

- フルオートと連続ループの各周回で、STEP2に手入力した場所・衣装をクリアせず保持します。両方が空欄の時だけ、従来どおりAIが題材に合わせて決定します。 / Full-auto and continuous-loop runs now retain manually entered STEP2 location and outfit overrides; AI chooses them only when both fields are blank.
- カラー／白黒の出力モード、OpenAI画像の品質、サイズは、フルオート・連続ループ・STEP1/STEP2リセット中も維持します。カラーへ戻るのは「最初からやり直す（設定クリア）」だけです。品質・サイズは再読み込みまで保持します。 / Color/monochrome mode plus OpenAI image quality and size persist through full-auto, looping, and STEP1/STEP2 resets. Only the full settings reset returns color mode to color; quality and size persist until reload.
- 接続済みOpenAI `gpt-6-astra Web Search` で、手入力の場所・衣装を含むSTEP2シナリオを1回生成して反映を確認しました。STEP4画像生成は実行していません。 / One connected OpenAI `gpt-6-astra Web Search` STEP2 scenario run confirmed both manual overrides; no STEP4 image generation was performed.
