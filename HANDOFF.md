# HANDOFF.md

## Current Status

- **v5.4.0 combined acoustic/visual direction routing release candidate (2026-08-17)**: `効果音・演出:`, `演出／効果音:`, and `SFX / 演出:` lines are now classified as visual Action instructions instead of synthetic dialogue speakers. The generic parser protects quoted sound cues on those dedicated instruction lines, keeps them out of STRICT SCRIPT / bubble-tail / eye-line metadata, and preserves the scripted cues in visual Action. The exact user-supplied `スピーカー回収騒動` scenario completed through the visible in-app browser with OpenAI `gpt-image-2`; STEP3 contained exactly five intended dialogue bubbles and no `[効果音・演出]` bubble speaker, and the 1024x1536 final image showed no explanatory prose bubbles. Evidence: `scratch/api-speaker-recall-fix-20260817.png` (3,104,250 bytes, SHA-256 `2FFD724BCF2DE03B98C5D24E8D04A8D71E4451145F4D6820F6848B85DD5AE8CF`). Fresh local checks passed 216/216 Node tests, zero-warning ESLint, production build, strict pre-deploy checks, and diff check. No API-key value was read or stored. The user authorized the unified release transaction and a separate visible black-window full backup; completion remains forbidden until every release-receipt stage and the backup verifier pass.

- **v5.3.9 MiniMax H3 continuous-BGM prompt release candidate (2026-08-16)**: STEP4 keeps the existing MiniMax H3／ComfyUI copy action and four-step guidance, but its actual copied English prompt now exactly matches the user-supplied BGM-enforcement contract. It makes enabled BGM continuous and audience-only from `00:00.000` through the final frame, requires `MANDATORY:` in `non_diegetic_music`, and keeps `N/A` only for explicit BGM disablement. Regression coverage proves the UI copy binding, BGM clauses, and byte-for-byte parity with the supplied replacement text. The user authorized the unified release transaction and a separately verified visible black-window full backup; completion remains forbidden until every release-receipt stage and the backup verifier pass.
- **v5.3.8 MiniMax H3 ComfyUI workflow clarification release candidate (2026-08-16)**: STEP4 keeps the existing MiniMax H3／ComfyUI copy action and English prompt body, but now presents the requested four-step flow: copy the instruction and send it with the generated four-panel manga to an image-capable chat, copy the returned prompt, connect that same manga as ComfyUI `Reference Image`, then confirm framing and duration before pasting the English prompt into `Prompt`. README matches that sequence and keeps the explicit boundary that the app does not generate or send video. Regression coverage checks the copy contract, UI workflow text, and README workflow boundary. The user authorized the unified release transaction and a separately verified visible black-window full backup; completion remains forbidden until every release-receipt stage and the backup verifier pass.

- **v5.3.6 public-license structure release candidate (2026-08-14)**: Restores an explicit public licensing policy after the prior README notice only stated that no standalone license existed. Original program source code is now covered by a root MIT `LICENSE`; explicitly identified original non-code content is scoped by `LICENSE-CONTENT.md` under CC BY-NC-SA 4.0; `TRADEMARKS.md`, `OUTPUTS.md`, and `THIRD_PARTY_NOTICES.md` separate brand use, generated-output handling, and direct dependency notices. This documentation-only release does not change generation logic, API-key handling, provider selection, or image generation. The user authorized the unified release transaction and a separate visible black-window full backup; completion remains forbidden until the receipt, GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and backup verifier all pass.

- **v5.3.5 release candidate — named OTS cast-instance/API-session repair (2026-08-12)**: Root cause was a contradiction in `extractCastLimitRule`: dialogue speakers alone became foreground, so an explicitly named over-the-shoulder character was simultaneously required as camera-side foreground by the eye-line rule and assigned to the background cast list. The generic repair promotes the named OTS subject into the foreground exactly once, removes that character from the background list, preserves the dialogue speaker as main focus, and emits `OTS CAST INSTANCE LOCK`; generic RED/GREEN coverage runs for both ChatGPT and Gemini without production proper-name branches. The exact `表現規制を巡る会議` five-character scenario completed through the visible in-app browser with OpenAI `gpt-image-2`; full-resolution review found panel 1 contains five unique named people with Hikari only once as the left foreground shoulder, and panels 2–4 retain their specified cast counts and meeting staging. The first image exposed an independent `少年 -> 男性` substring rewrite inside `青少年保護`; the filter now ages up only standalone person roles, and the second real API image retained the exact dialogue. Evidence: `scratch/ots-cast-instance-final.png` (1024x1536, 2,567,441 bytes, SHA-256 `90CB48BBAED9D9B25029E73186DE25575AD8A3C26AE2D3EB6B63C53BCC2FBF1E`). The same investigation fixed first-submit API preflight from 15 seconds × 2 to 45 seconds × 3 using the same in-memory input, and added workflow epochs so stopped full-auto STEP2/STEP3 results cannot overwrite a manual scenario. Fresh checks passed 210/210 Node tests, zero-warning ESLint, Vite production build, and diff check. No key value was read or stored. The user authorized the unified release transaction followed by a separate visible black-window full backup; completion remains forbidden until the receipt, GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and backup verifier all pass.

- **v5.3.4 published-status synchronization (2026-08-12)**: Corrects only release metadata and current-state documentation after v5.3.3 shipped. README now identifies v5.3.4 as the current implementation and latest release; the v5.3.3 HANDOFF record below is updated from its pre-release candidate wording to its actual published outcome. The image-quality failsafe behavior is unchanged. This follow-up must use the same unified release and full-backup evidence gates as any other patch release.

- **v5.3.3 image-quality failsafe released (2026-08-12)**: STEP4 saves and displays the first image before QA. A concrete person/hand/prop-orientation/bubble failure creates at most one issue-limited repair candidate; a passing repair replaces the original, while a still-NG repair or repair-image request failure restores the saved original and returns success to the full-auto chain with a concrete warning. An `unverified` QA result does not spend another image call and also keeps the original. Initial image API/auth/transport/missing-image failures still stop because no usable artifact exists. TDD covers original PASS, repaired PASS, repaired NG, unverified QA, repair request failure, and prompt preservation. Source commit `aa839a1` was published as annotated tag/GitHub Release `v5.3.3`; Pages, Hugging Face, and `C:\\nano-banana-pro-main` (tree SHA-256 `67D2BE956CB8ADF4C9E3C4BAC69B63DDE706D6EEE52B8A05C5310D1E0754ED99`) passed independent verification. The separate visible full backup `antigravity_full_backup_2026-08-12_010015.zip` passed seven-app parity and matching local/Drive SHA-256 `9C04122335CCC5815BA1026435EAF3C465C350A1E8D743BF6B7BE82021E20051`.

- **v5.3.2 functional-surface orientation release candidate (2026-08-11)**: Functional faces now derive their intended target from the scripted action instead of the holder: reading/operating targets self, submitting/presenting/showing targets the recipient, and explicit camera presentation targets the camera. Tabletop pages may correctly be face-up, with the text baseline oriented to the intended reader. Explicit overhead/hand-detail/close-up cameras are preserved without an invented shoulder; explicit OTS readers survive ChatGPT prompt compaction with rear head/shoulder foreground and their screen front visible to both reader and camera. The visible QA now treats the viewer as the physical camera and forbids the previous false claim that an OTS viewer must not see the reader's screen. README now includes a decision table for phones, monitors, nameplates, documents, cards, books, maps, tickets, and forms, covering reader, recipient, tabletop, OTS, and explicit camera-presentation cases. Exact bank-scenario evidence: `scratch/functional-surface-orientation-api/bank-orientation-pass-action-target-ots.png`; `gpt-image-2` completed after 151 seconds and `gpt-4.1` QA returned PASS. The exact 5,870-character single-image copy prompt also completed through `gpt-image-2` after 136 seconds and is saved as `scratch/functional-surface-orientation-api/single-image-copy-prompt-api-output.png`; the four-panel bank QA is not applicable to that scenario-free helper image. Fresh local verification passed 198/198 Node tests, ESLint, production build, diff check, and current-version pre-deploy checks. The user authorized the unified release transaction and a separate visible black-window full backup; completion remains forbidden until source, annotated tag, bilingual GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and backup verification all pass.

- **v5.3.1 API session and manga quality gate release candidate (2026-08-11)**: Fixed first-entry API connection handling with one verified browser-memory session and one bounded retry for transient preflight failures; no credential value or bootstrap flag is persisted. Replaced the seven-field dynamic-background path with lightweight `Location` plus explicit time/weather continuity, subordinate to anatomy, hand/arm laterality, prop ownership, and bubble accuracy. STEP4 now displays the image before one visible combined quality review for malformed or extra arms/hands, side swaps, prop ownership, non-dialogue bubble text, speaker-name leakage, and extra text; it never auto-regenerates after a failed review. In the in-app browser, two independent real OpenAI `gpt-image-2` 1024x1536 runs completed without API re-entry or reload; both `gpt-4.1` visual reviews returned PASS and manual inspection found exact dialogue, no speaker-name bubble labels, coherent prop ownership, and no obvious extra arms. Fresh candidate verification: 191/191 Node tests, zero-warning ESLint, production build, and diff check. User authorized the unified release transaction and full backup; completion remains forbidden until the receipt, GitHub Release, Pages, Hugging Face, C-drive release copy, and backup verifier all pass.

- **v5.3.0 generic hand/prop kinematics release candidate (2026-08-10)**: The verified generic hand/prop kinematics fix is synchronized as v5.3.0 across package metadata, runtime header, HTML title, README current-version statement/changelog, and Hugging Face metadata. The current user has authorized the unified release transaction and a full backup; completion is forbidden until the persisted receipt verifies the source, exact annotated tag, bilingual GitHub Release, GitHub Pages, Hugging Face, C-drive release copy, public verification, and full-backup stages.

- **Generic hand/prop kinematics fix locally verified (2026-08-10)**: Replaced the pointing-, two-hand-, placement-, and arm-cross-specific prompt branches with one situation-agnostic `HAND / PROP KINEMATICS LOCK` shared by ChatGPT/OpenAI and Gemini prompt families. STEP3 no longer rewrites a scripted `両手で` action to `片手で`; the contract instead assigns subject-relative anatomical left/right arms, one simultaneous role/contact per hand, one actor per prop, final-state support for released objects, and palm/thumb/finger/wrist continuity. Long ChatGPT prompts keep a compact equivalent without exceeding the 15,000-character Web-copy budget, and compression preserves existing camera, eye-line, evidence, background, wardrobe, title, and watermark semantics. TDD reproduced the old action rewrite and missing generic coverage; the exact supplied `偽画像が被災地を騒がす` scenario produced a 13,231-character live STEP3 prompt containing the generic lock and no old special-case locks. In the in-app browser, one real OpenAI `gpt-image-2` call completed after 157 seconds following one streamed partial. Full-resolution review of `scratch/generic-hand-kinematics-api/generated-disaster-center.png` found coherent two-hand phone holding in panel 2, newspaper pointing in panel 3, and phone-plus-peace-sign staging in panel 4 with no obvious extra, detached, mirrored, or grossly malformed hand. The PNG is 1024x1536, 2,872,299 bytes, SHA-256 `0C2DE257924C2D84ACC40E5922418E002E79027097D91E3826839359AE696F71`. No key value was read or stored. No commit, push, deploy, release, or backup was authorized or performed.

- **v5.2.9 README version consistency (release candidate, 2026-08-10)**: Corrected the README statement that still called v5.2.7 the current implementation after v5.2.8 had shipped. README, package metadata, HTML title, runtime version constant, and Hugging Face README now agree on v5.2.9. This patch changes public release documentation/version metadata only; it does not alter the normal manga-composition variety behavior, provider route, or image-generation behavior. The user required a fresh unified release and full backup because the prior published README was inconsistent. Completion remains forbidden until the receipt, public pages, C-drive release copy, and backup verifier all pass.

- **v5.2.8 manga composition variety and truthful image-wait copy (release candidate, 2026-08-10)**: Normal STEP2 now requires subject-relative horizontal azimuth in addition to shot size, elevation, and tilt; STEP3 carries a provider-specific page lock and per-panel staging assist. The page allows at most one flat front-on panel, varies shoulder/hip/face axes, and staggers two-hand actions in depth or height without replacing the requested action, cast, props, dialogue, or background. The exact office-server-leak scenario completed in the in-app browser through OpenAI `gpt-4.1` and `gpt-image-2`: the 14,885-character STEP3 prompt retained four camera/staging assists and the final 1024x1536 image used oblique low-angle, over-the-shoulder, foreground close-up, and oblique-wide panels. The image-wait overlay now says `通常2〜10分`, matching the runtime log. Fresh verification passed 198/198 tests, zero-warning ESLint, Vite production build, and diff checks. The user authorized the unified v5.2.8 release and full backup; completion remains forbidden until the receipt, both public surfaces, C-drive release copy, and backup verifier all pass.
- **v5.2.7 season-aware outfit selection and in-app-browser API proof (release candidate, 2026-08-09)**: News mode now derives a Japan-default spring/summer/autumn/winter hint from `targetDate`, while manual mode uses only explicit topic cues. Outfit priority is explicit user clothing; event-specific clothing; profession/safety/location/weather/indoor environment; date season; ordinary auto-selection. STEP2 shows a read-only hint such as `季節目安: 夏（対象日付から自動）`; no season control was added. Automatic scenario validation rejects empty/ambiguous outfits plus obvious summer winterwear or winter summerwear unless the context supplies a cold/warm environmental exception, and reports the exact seasonal reason before a bounded retry. Indoor ice-rink, heated-indoor, pool/sauna, safety-equipment, and explicit-outfit exceptions are covered. The existing high-severity dependency findings were also repaired; current `npm audit` reports 0 vulnerabilities. In the visible in-app browser at `http://127.0.0.1:4176/`, the user-configured OpenAI route used target date `2026-08-09`, news mode, no custom outfit, and `2026年8月の日本の屋外夏祭り会場（夜、蒸し暑い）`. STEP2 produced `Outfit: 浴衣（yukata）` with no seasonal-outfit retry; STEP3 carried that exact outfit plus the cross-panel wardrobe lock; STEP4 reached a streamed partial and final OpenAI `gpt-image-2` success. Full-resolution review confirmed four panels at a lantern-lit summer festival, all characters in yukata, and no winter coats or scarves. Evidence: `scratch/season-aware-outfit-api/generated-summer-festival.png` (1024x1536 RGB PNG, 3,210,838 bytes, SHA-256 `B340DA9B1D860A29A222E3EA8EA4F916E58E367BF6CD3DFBD58693D4B143191E`) and `scratch/season-aware-outfit-api/generated-result-viewport.png`. A second real in-app-browser run used target date `2026-12-09`, news mode, no custom outfit, and `2026年12月の日本の屋外クリスマスマーケット（夜、雪、厳しい寒さ）`. The UI changed to `季節目安: 冬（対象日付から自動）`; STEP2 produced `Outfit: 防寒着（winter clothes）` with no seasonal-outfit retry or summer-wear term; STEP3 locked that outfit in all four panels; and STEP4 completed after one streamed partial. Full-resolution review confirmed snow and an outdoor Christmas market plus coats/jackets, scarves, and gloves across the cast, with no swimsuits, short sleeves, sleeveless tops, or tank tops. Evidence: `scratch/season-aware-outfit-api/generated-winter-market.png` (1024x1536 RGB PNG, 3,109,176 bytes, SHA-256 `B780AC6686BC94D827AF4D736C291299323B477D18CFF8A4F809118FC7064AF9`). STEP2 did issue an unrelated exhausted `DYNAMIC_BACKGROUND: background_time_conflict` quality warning; the winter outfit validator itself passed and the approved scenario consistently retained night, snow, and cold. The first summer STEP4 attempt exposed browser `Failed to fetch`; `src/lib/openai.js` now retries once without streaming only for that browser-network failure, and the successful live reruns prove the normal streamed route still works. Fresh verification passed 194/194 Node tests, zero-warning ESLint, Vite production build, `npm audit` with 0 total vulnerabilities, PNG verification, and visual inspection. No key value was read or stored. User authorized the unified v5.2.7 release followed by full backup; completion remains forbidden until the receipt, both public surfaces, the C-drive release copy, and backup verification all pass.
- **v5.2.6 combined visual-direction bubble routing and OpenAI wardrobe-lock safety fix (release candidate, 2026-08-09)**: Compound labels such as `身体・表情・演出:` are now handled as visual directions across speaker detection, action extraction, structured sections, and placement inference, so their prose stays in Action and cannot become a synthetic speaker or oversized bubble. A separate live `gpt-image-2` rejection was traced by block bisection to the negated wardrobe phrase `remove garments`; both the full and compact ChatGPT/Gemini prompt contracts now preserve the same wardrobe continuity with positive keep-language and never emit that phrase. RED then GREEN coverage includes the supplied mayonnaise-style combined-label failure and the shared wardrobe contract. Fresh verification passed 181/181 Node tests, zero-error ESLint, Vite production build, and `git diff --check`. In the in-app browser, the unchanged pre-fix 10,269-character full prompt reproduced the safety rejection, while normal STEP3 from the updated code produced an unedited 10,294-character prompt with four panels/four dialogue blocks, zero negated garment-removal phrases, and the positive wardrobe lock. That exact STEP3 output completed through `gpt-image-2` after a streamed partial; full-resolution review confirmed the four-panel exhibition manga and only the intended short dialogue bubbles, with no visual-direction prose bubble. No API key value was read or stored. User authorized the unified v5.2.6 release followed by full backup; completion remains forbidden until the receipt, both public surfaces, the C-drive release copy, and backup verification all pass.
- **v5.2.5 content-aware title and safer OpenAI image-generation release candidate (2026-08-08)**: Removed the unconditional ` !?` append from STEP2 and now preserve the generator's intentional title ending: none, `!`, `?`, or `!?`. The scenario prompt explicitly discourages punctuation as a default while retaining it when the title is genuinely a surprise, question, or both. The final ChatGPT/OpenAI prompt path now generically normalizes plain school/minor-coded traits, childlike proportion wording, and overly biological anatomy wording into adult-safe instructions; declares all characters as adults aged 20 or older; sends the supported OpenAI image `moderation: "low"` option; classifies the current safety-rejection wording; preserves only the in-memory OpenAI key across Vite HMR; and reopens the key modal if an in-memory key is missing. No key value is persisted or inspected. Fresh full verification passed 179/179 Node tests, zero-error ESLint, Vite production build, and `git diff --check`. In the in-app browser, normal manual STEP2 used the new topic `成人の会社員5人が商店街の福引で巨大なぬいぐるみを当て、持ち帰り方に困るドタバタ劇`; OpenAI `gpt-4.1` generated the 1,664-character scenario and unpunctuated title `商店街福引で大誤算`. STEP3 built a 13,363-character production prompt with four verbatim dialogue blocks, an adult-age declaration, and zero residual targeted risk phrases. A single STEP4 call reached streamed partial and final completion through `gpt-image-2`, rendered at 1024x1536, with no safety rejection or retry. Browser visual review confirmed the exact title, four horizontal panels, the shopping-arcade lottery setting, giant teddy bear, and five adult office workers; it was not the stale pancake result. Evidence: `scratch/title-variety-full-api/browser-title-and-top.png` and `scratch/title-variety-full-api/browser-four-panels.png`. The user authorized the unified v5.2.5 release followed by full backup; completion remains forbidden until every receipt stage, both public surfaces, the C-drive release copy, and backup archive/hash/parity/lock checks are verified.
- **v5.2.4 Markdown visual-section dialogue/placement isolation release candidate (2026-08-07)**: The supplied `国税職員の不祥事連発!?` scenario used Markdown blocks such as `**表情:**`, `**身体/間合い:**`, `**演出:**`, `**背景:**`, and `**セリフ:**`. Those pure section headers and quoted visual descriptions were previously eligible for speaker inference, which polluted both `STRICT SCRIPT LOCK` and `PLACEMENT/IDENTITY` with prose such as `口が小さく「あ」の形` and synthetic names such as `**表情`. The parser now tracks structured scenario-section boundaries: visual-section contents remain in Action, only the dialogue section feeds bubbles, and structural headers are excluded from placement inference. RED then GREEN coverage was added for the shared parser and both ChatGPT/Gemini prompt families. Fresh verification passed 170/170 Node tests, zero-error ESLint, Vite production build, and `git diff --check`. In the in-app browser, two character sheets already parsed through OpenAI `gpt-4.1` supplied all five named characters; the exact 2,967-character scenario produced a 14,653-character corrected STEP3 prompt with four required dialogue lines, four verbatim bubble entries, zero prose leakage, and zero Markdown placement leakage. A fresh OpenAI `gpt-image-2` run completed after 157 seconds. Full-resolution review found exactly one intended bubble in each of the four panels and no expression/body/direction/background prose rendered as dialogue or captions. Evidence: `scratch/api-dialogue-markdown-fix/2026-08-07-gpt-image-2-clean.png` (2,912,895 bytes; SHA-256 `4CB4AA6C79DDC75701DA251C2BB87DBBE1128C4F73A09E8F2191F2DACE62CDE4`). No API key value was read or stored. User authorized the unified release followed by full backup; do not mark this candidate released until its receipt, public surfaces, release copy, and backup all verify.
- **Cross-panel wardrobe color continuity verified through the normal full-auto character-sheet path (2026-08-07)**: The supplied four-panel result exposed a prompt-contract conflict: named-character identity/outfit continuity was required while each `PANEL STYLE LOCK` could change an unscoped palette. The local fix adds a shared `CROSS-PANEL WARDROBE COLOR LOCK` to ChatGPT/OpenAI and Gemini prompt families, confines panel-style palette changes to environment/lighting treatment/VFX, and keeps the 15,000-character ChatGPT Web-copy budget through equivalent compact wording. TDD RED reproduced the missing contract; focused tests passed 9/9 and fresh full checks passed 167/167 Node tests, zero-warning ESLint, Vite production build, and `git diff --check`. The earlier 2,260-character edited prompt run is retained only as diagnostic evidence and is not the acceptance proof. After the user correctly rejected that shortcut, the in-app browser used the normal untouched workflow: two real character-sheet images already loaded in STEP1, OpenAI `gpt-4.1` automatically parsed five named characters, full-auto selected current categories and generated the 1,534-character `冷やし風呂大行列!?` scenario with `Outfit: 銭湯用の浴衣・ラフな部屋着`, STEP3 automatically produced an unedited 13,419-character ChatGPT-family prompt containing the wardrobe lock and no unscoped panel palette instruction, and STEP4 submitted that exact prompt to real `gpt-image-2`. Generation completed without policy editing after 174 seconds at 1024x1536. Full-resolution review passed across all four panels: Miku retained blonde-to-pink hair and the same pale blue patterned yukata; Rin retained brown twin tails, round glasses, and the same pale blue patterned yukata; Saeko retained long black hair and the same blue-gray patterned yukata; Akari retained the orange bob and the same pale lavender/blue patterned yukata; Hikari retained the blonde bob, round glasses, and the same pale blue patterned yukata. Lighting and per-panel style changed, while garment base colors stayed recognizable. A subsequent real `gpt-image-2` single-illustration check used the same parsed five-character cast and explicitly named each canonical yukata color/pattern; all five identities, glasses states, garment colors, hands, and the single-image layout passed full-resolution review. The ratio-correction copy prompt now has a source-fidelity lock that retains the original four-panel manga's face, hair, glasses, outfit items/colors/patterns, cast, dialogue, props, setting, and time while rebuilding only A4 geometry; focused regression plus the fresh full suite passed 168/168. In the in-app ChatGPT browser, the user-supplied `ChatGPT Image 2026年8月7日 15_55_00.png` was attached and regenerated through the corrected A4 prompt. The resulting 1024x1448 PNG preserved all four panel beats, the five source identities, glasses states, per-panel outfit colors, props, and vertical dialogue while keeping an even A4 page layout. Acceptance evidence: `scratch/api-wardrobe-color-lock/2026-08-07-gpt-image-2-normal-full-auto-character-sheets.png` (3,229,001 bytes), the in-app STEP4 generated result, and `C:\Users\sx717\AppData\Local\Temp\browser-use\assets\6e150fa8-42e2-448e-8ff4-667a5d57eda8\18dcd93f822d3b86`. No key value was read or stored. The user then authorized the unified release and separate full backup; `v5.2.3` must fail forward from the already tagged/released `v5.2.2` and remain incomplete until the receipt, public surfaces, release copy, and backup all verify.
- **v5.2.2 shared single-image/four-panel quality-contract release candidate (in progress, 2026-08-06)**: The existing four-panel prompt remains intact; its cast, scenario, setting, and camera requirements are not replaced. A shared image-quality contract now supplies the same setting depth, motivated lighting, anatomy/prop ownership, localized clothing-fold shadows, and clean-surface constraints to the existing ChatGPT/Gemini four-panel families and the new single-image copy prompt. The exact copy-button text produced a real five-person classroom illustration in ChatGPT Web. A real in-app-browser OpenAI `gpt-image-2` four-panel run completed at 1024x1536 and visually retained coherent classroom staging, lighting, props, and anatomy. Fresh local verification passed 166/166 Node tests, ESLint, production build, and `git diff --check`. The authorized next stages are the unified `v5.2.2` release transaction and a separate visible black-window `-NoGui` full backup. Exact five-character identity matching in the four-panel proof remains unverified because that test upload contained only three character references.
- **v5.2.1 staging-gag dialogue-routing release candidate (in progress, 2026-08-05)**: The v5.2.0 release already exists, so the subsequent generic parser correction must fail forward as v5.2.1. `ギャグ演出:` / `演出ギャグ:` labels are now structural metadata rather than speakers, including Markdown bullets, whitespace, separator, and matching bracket forms. Exact real-API evidence is saved at `scratch/api-gag-direction-fix/final-generalized-fix-1024x1536.png`: OpenAI `gpt-image-2` completed a fresh 1024x1536 four-panel image after 138 seconds, with the panel-2/3/4 direction prose absent from dialogue bubbles. Release candidate validation, the unified release receipt, public Pages/Hugging Face/release-copy verification, and the separately authorized visible black-window full backup remain pending. No credential is stored in source, notes, or this record.
- **v5.2.0 reversed gag-direction dialogue-routing fix (locally verified, 2026-08-05)**: The supplied `美術部カノジョ新作話題!?` output exposed that `ギャグ演出:` was not recognized as the existing `演出・ギャグ:` structural label. STEP3 therefore emitted `B1->[ギャグ演出]` and placed the complete panel-2/3/4 direction prose into `Dialogue (verbatim bubbles)`. RED reproduced the exact `羞恥` / `観察中…` / `バグ多発！` direction sentences as speech bubbles. The parser now recognizes both label orders plus Markdown bullets, whitespace, separator, and matching bracket variants; excludes those structural labels from speaker inference; preserves their body as visual Action; and strips the label/wrapper before prompt assembly. Focused coverage passes 26/26 across parser plus ChatGPT/Gemini prompt families; fresh full verification passes 162/162 Node tests, zero-warning ESLint, Vite production build, and `git diff --check`. In the in-app browser, the exact scenario's final STEP3 prompt contained 4 Dialogue lines / 5 real bubbles, zero direction prose in Dialogue, zero gag-label speakers, all three panel-2/3/4 direction bodies in Action, and zero remaining structural labels. A fresh post-fix OpenAI `gpt-image-2` run completed after 138 seconds with `[COMPLETE] Image successfully generated.` and a new 1024x1536 image. Full-resolution review found no rectangular scenario-description captions: only the five real dialogue bubbles were present, while `羞恥`, `観察中…`, `バグ多発！`, `バグ`, and `現実ショック` remained visual effects. Evidence image: `scratch/api-gag-direction-fix/final-generalized-fix-1024x1536.png`. No commit, push, release, deploy, release-copy change, or backup was authorized or performed.
- **v5.2.0 STEP1 character-analysis reset release candidate (in progress, 2026-08-05)**: The header now names the existing retained-character reset `シナリオ（STEP2）からやり直す` and adds `キャラクター解析（STEP1）からやり直す` between it and the full reset. The new action reuses the downstream reset, then clears character-sheet images, extracted `castList`, analysis trace, style JSON, copy state, and drag state without clearing the API key, selected engine, or engine flags. RED then GREEN regression coverage passed; fresh local validation passed 158/158 Node tests, zero-warning ESLint, and Vite production build. In the in-app browser, OpenAI GPT-4.1 completed character analysis for two uploaded sheets; the new STEP1 action removed the image thumbnails and extracted text while `✅ ChatGPT Engine` remained connected. User authorized unified v5.2.0 release followed by a visible black-window full workspace backup; public release, release-copy, and backup verification are pending.
- **v5.1.9 manga dialogue-boundary release candidate (in progress, 2026-08-05)**: STEP3 now treats structural labels such as `表情・身体`、`動作`、`ポーズ`、`姿勢`、`目線`、`視線` as directions rather than speakers, preventing descriptive prose from becoming a speech bubble. Same-speaker stage quotes are deduplicated only when they are near-identical to a formal dialogue line, so distinct dialogue remains available. The exact supplied scenario completed through the user-configured OpenAI routes in the in-app browser: STEP3 produced 2/2/3/3 dialogue entries without structural-label bubbles, and `gpt-image-2` returned a visible 1024x1536 four-panel manga without the oversized prose bubble or duplicate final-panel dialogue. Fresh local verification passed 156/156 Node tests, zero-warning ESLint, Vite production build (existing Browserslist/chunk-size warnings only), and `git diff --check`. The user authorized the unified v5.1.9 release followed by a fresh full workspace backup; public release, release-copy, and backup evidence are pending.
- **v5.1.7 non-blocking scenario-quality fix (release in progress, 2026-08-04)**: The production v5.1.6 path still let `requestSafeScenarioContent` throw after a content-hygiene rejection, bypassing the shared three-attempt scenario candidate selector and stopping STEP1. v5.1.7 integrates `SCENARIO_CONTENT` into the same quality checks and retry instructions as dynamic background, visual evidence, manual exclusions, and final-panel staging. The attempt budget is explicitly three total calls (initial attempt plus two retries), and progress now renders `試行 2/3` / `試行 3/3`. Exhausted content-hygiene quality checks return a warning and the highest-scoring usable candidate rather than terminating the workflow. RED reproduced the terminal exception; GREEN focused coverage passed 42/42 and the full suite passed 153/153, with zero-warning ESLint, Vite build, and `git diff --check` also passing. Fresh in-app-browser evidence used OpenAI `gpt-4.1` for scenario generation and all-seven-category scenario enhancement, then built STEP3 and completed STEP4 through real `gpt-image-2`; the final Base64 image rendered at 1024x1536 after one streamed partial and the log reached `[COMPLETE] Image successfully generated.` Visual review confirmed a four-panel game-shop manga retaining the update poster, game packages, manual/controller or cabinet evidence, and active final-panel staging. The authorized unified release must now be verified on Pages, Hugging Face, and `C:\nano-banana-pro-main`, followed by a separate fresh full backup and independent archive/hash/parity/lock verification.
- **v5.1.6 API-refresh protection release candidate (in progress, 2026-08-04)**: Vite/Fast Refresh no longer replays the mount-time API bootstrap against an already connected in-memory engine. The guard stores only a boolean on `window`, never an API key, and prevents the existing no-persistence path from clearing the OpenAI/Gemini key during a development update. STEP2 scenario enhancement still makes bounded quality retries without turning an exhausted soft-quality check into a terminal error: it retains the highest-scoring safe enhanced candidate, or the original scenario only when every candidate breaks a non-negotiable editing contract, and then continues to STEP3 and STEP4 with a concrete warning. In the in-app browser, the prior fixed candidate completed OpenAI GPT-4.1 all-category enhancement, STEP3 prompt construction, and visible `gpt-image-2` 1024x1536 output. Focused API-refresh and scenario-continuation tests, zero-warning lint, build, and the full suite are being run for this final candidate. User authorized unified release followed by a separate fresh full backup; v5.1.5's Hugging Face receipt is pending reconciliation and this final candidate's publication and backup evidence are pending.
- **v5.1.1 visual anatomy QA released (2026-08-04)**: The post-generation vision anatomy acceptance gate, one corrective regeneration, and fail-closed second rejection were published as source commit `cc105216eb88e3de94974bdfbdfb5e0753acfbaf`, annotated tag and bilingual GitHub Release `v5.1.1`. The persisted release receipt `backups/release_receipts/nano-banana-pro-v5.1.1-2026-08-04-anatomy-qa.json` has every stage verified. In-app-browser checks found both Pages and Hugging Face at `v5.1.1`, GitHub's rendered release contains the Japanese QA note, and independent `verify_app_release.ps1` verified the Release copy `C:\nano-banana-pro-main` against the Release ZIP tree hash. The separately authorized visible `-NoGui` full backup is being refreshed after this completion record.
- **Visible limb-anatomy QA local follow-up (2026-08-04)**: The earlier real `gpt-image-2` result exposed an apparent third hand on Saeko in panel 2. The root cause was not the existing prompt locks (which already required two arms/two hands) but an acceptance gap: STEP4 displayed the returned image without examining it. `src/lib/image-anatomy-qa.js` now asks the configured text/vision provider to review the finished four-panel image panel-by-panel for visible extra, missing, detached, or wrong-body arms/hands. A malformed QA response fails closed; one rejected image is automatically regenerated with an anatomy repair lock, and a second rejection is surfaced as an error instead of displayed. Focused RED/GREEN coverage plus the full Node suite passed (132/132), along with ESLint, Vite build, and `git diff --check`. A fresh user-credentialed external-browser OpenAI run completed `gpt-image-2` followed by real `gpt-4.1` vision QA; the live log recorded `[QA] Vision anatomy review (1/2)` then `PASS`. Full-resolution visual inspection of `generated-result-anatomy-qa.png` found no visible third hand on panel-2 Saeko. No commit, push, deploy, release, or backup was performed.
- **Final-panel activity diversity local follow-up (2026-08-04)**: Non-surreal endings now reject a passive multi-character lineup (including a moving lead with silent observers) and retry the complete scenario with story-specific actions across foreground, midground, and background. Both ChatGPT and Gemini image prompts receive the same active-staging lock; the ChatGPT copy compactor applies after final safety sanitization so the 15,000-character Web-copy soft budget remains intact. Focused RED/GREEN coverage and the full Node suite passed (126/126), as did ESLint with zero warnings, Vite production build, and `git diff --check`. A fresh local real-provider run used user-entered UI credentials and character sheets: OpenAI `gpt-4.1` generated the scenario, and `gpt-image-2` completed the four-panel image. Visual review confirmed distinct physical actions in every panel; the final panel has Miku posing, Rin clutching a book, Akari handing a book to a child, Saeko holding her head over the list, and Hikari raising a map, rather than a straight-line lineup. The output also retained the mobile-library bus, donation books, reception table, and sorting labels. The browser used for this completion was external because the user had already entered the API setup there after an earlier browser-choice error; this is not in-app-browser evidence. No commit, push, deploy, release, or backup was performed.
- **v5.1.0 rich-panel composition correction release candidate (2026-08-04)**: The rich-panel implementation from the mistakenly numbered v5.0.10 is retained, but v5.0.9 must advance to v5.1.0 under the single-digit minor/patch rule. Both provider-family image prompts require one fixed environmental anchor and at least two further physical setting cues in every panel. VFX must stay behind or around the cast and may not replace the location or story evidence. Faces, eye direction, silhouettes, hands, and key actions remain crisp and visually separated from rich lower-contrast, softer-edged backgrounds; broad blank walls, flat gradients, and black voids are disallowed except where physical emptiness is explicitly scripted. The ChatGPT compact Web-copy path preserves the same rules within its empirical 15,000-character budget. The release updater and predeploy check now reject two-digit minor/patch components and enforce carry from `…9`; RED/GREEN contract coverage passed. The prior live OpenAI `gpt-image-2` result remains the implementation evidence. User authorized correction deployment and a separate visible `-NoGui` full backup; corrected public release, release-copy verification, erroneous v5.0.10 withdrawal, and new backup evidence are next.
- **v5.0.9 dynamic-background release candidate (2026-08-04)**: Removed the STEP2 background preset dropdown, `background-rag.js`, and the fixed location dictionary. Each scenario now creates and validates a complete per-story background contract: space and circulation; foreground/midground/background layers; time, weather, and primary light; atmosphere; interaction props; two to three fixed anchors; and contradictions to avoid. A valid scenario must retain an exact anchor in every panel, use at least two anchors across the page, and may not change declared time or weather (including afternoon-to-sunset / `GOLDEN_HOUR` drift). Both provider-family image prompts carry the same dynamic continuity lock. RED/GREEN regression coverage was added; fresh full checks pass 118/118 Node tests and ESLint with zero warnings. A live OpenAI `gpt-4.1` scenario for a passenger-ship launch first exposed the noon-to-sunset defect; the generic validator was extended, the same topic was regenerated with a stable sunny daytime harbor, and `gpt-image-2` produced a 1024x1536 four-panel result that visually retained the passenger ship, flower wreath, boarding equipment or port staff, and paper streamers across the page. User authorized the unified release transaction and separately authorized a visible `-NoGui` full backup; commit, public release, release-copy verification, and new backup evidence are next.
- **v5.0.8 visual-evidence priority release candidate (2026-08-03)**: `VISUAL STORY EVIDENCE LOCK` now follows `STRICT SCRIPT LOCK` immediately and precedes `ART / RENDERING QUALITY` / provider style guidance in both prompt families; the trailing duplicate insertion and the blank separation before it were removed. Regression coverage requires `STRICT SCRIPT LOCK` < evidence lock < style guidance for ChatGPT/OpenAI and Gemini. Fresh local verification passed 116/116 Node tests, ESLint with zero warnings, Vite production build, and `git diff --check`. A live OpenAI `gpt-image-2` run completed at 1024x1536 after the same reordered editable prompt was submitted; browser review confirmed an electronics-store battery aisle with a Game Gear, AA battery packs, battery shelves, and a basket piled with batteries, with no laundromat/generic-scene substitution. User authorized the unified release transaction and separately authorized a visible `-NoGui` full backup; collect receipt, public/release-copy proof, fresh archive/manifest, parity/hash, and lock-release evidence before completion.
- **v5.0.7 story-fit background and visual-evidence release candidate (2026-08-03)**: Automatic STEP2 generation no longer preselects a curated background before the scenario exists. The model chooses one concrete setting from the actual topic and also emits 3-5 drawable `VisualEvidence` nouns that prove the specific event, places at least two distinct evidence types across at least two panel actions, and retries once when that contract is missing. STEP3 carries those terms into both provider prompts through `VISUAL STORY EVIDENCE LOCK`; explicit curated presets still add continuity anchors when selected. TDD RED reproduced the missing-evidence path, focused tests passed 20/20, the full suite passed 115/115, ESLint and Vite build exited 0, `git diff --check` passed, and production source contains no launch-ceremony sample terms. A real connected OpenAI run used `gpt-4.1` to derive `Location: 港の桟橋` and `VisualEvidence: 旅客船、テープカット、紙テープ、式典用花輪、船員` from a passenger-ship launch prompt that did not enumerate props. `gpt-image-2` completed at 163 seconds with a 1024x1536 PNG. Full-resolution browser review found passenger ships, ceremonial flower wreaths, uniformed crew/staff and a ribbon-cutting stage in panel 1; a ceremonial ribbon in panel 2; a flower wreath and paper streamers in panel 3; and a passenger ship, paper streamers, flower wreath and staff in panel 4. The event is visually recognizable as a launch ceremony, all panels remain at the port, no laundromat appears, and browser error/warning logs were empty. Commit, push, release, deploy, release-copy update, and the user-requested full backup are the next authorized stages.
- **v5.0.6 background RAG continuity release candidate (2026-08-02)**: STEP2 now exposes 24 curated, safety-filtered background presets while preserving the existing free-text location input. Hybrid auto-selection ranks safe presets against the topic and mood, selected locations carry stable environmental anchors into scenario construction, and both ChatGPT/OpenAI and Gemini image prompts add a four-panel background continuity lock for location, weather, time, lighting, and recurring props. Fresh local checks passed 111/111 Node tests, ESLint with zero warnings, production build, and `git diff --check`. The in-app browser completed a real OpenAI `gpt-4.1` scenario and `gpt-image-2` 1024x1536 image using `雨の駅前`; visual review confirmed a vertical four-panel manga with the station departure board and wet reflective tiles recurring across panels. The user authorized the unified v5.0.6 release transaction followed by the separate full backup; external publication and backup evidence are pending.
- **v5.0.5 arm-cross prop-release release candidate (2026-07-31)**: Same-character prop placement followed by crossed arms now emits a generic final-pose lock: the prop remains self-supported, both hands release it, and exactly two arms/two hands stay naturally connected to the character. The lock is limited to the same actor, so separate characters' prop handling and arm-cross poses remain unchanged. RED then GREEN regression coverage covers ChatGPT and Gemini prompt construction; fresh local checks passed 105/105 Node tests, ESLint, production build, and `git diff --check`. A real OpenAI `gpt-image-2` 1024x1536 four-panel run verified Saeko with two hands, natural crossed arms, and a self-supported file without limb/prop merging. User authorized the unified release transaction followed by the separately authorized visible `-NoGui` full backup; external evidence is pending.
- **v5.0.4 staging-and-gag bubble-routing release candidate (2026-07-31)**: Compound stage-direction labels such as `演出・ギャグ:` no longer become speakers, and quoted sound lettering in acoustic context (for example `「ギュッ」という擬音`, `「カサッ」と擦れる音`, and `「ブンッ」という風切り音`) no longer becomes dialogue. The visual Action instructions retain the staging/SFX content without the meta label. RED/GREEN regression coverage now fixes this behavior in dialogue extraction and strict ChatGPT/Gemini prompt construction. Fresh local checks passed: 103/103 Node tests, ESLint with zero warnings, production build, strict pre-deploy security/version checks, and `git diff --check`. The exact supplied four-panel scenario completed through real OpenAI `gpt-image-2`; visual review found only speaker-owned dialogue bubbles, with visual SFX and no isolated right-side non-dialogue bubble. User authorized the unified release transaction followed by the separately authorized `-NoGui` full backup; external evidence is pending.
- **v5.0.3 prompt-edit and stream-recovery release candidate (2026-07-26)**: STEP 4 now exposes an editable final-prompt field; the edited `finalPrompt` is used unchanged by copy actions and both provider image requests. OpenAI image streaming now retains the most recent valid partial image and renders it if the transport drops before the final SSE event. RED then GREEN coverage verifies the stream-drop recovery; the focused prompt-editor test verifies the UI/data path. Fresh local checks passed: 101/101 Node tests, ESLint with zero warnings, production build, strict pre-deploy checks, and `git diff --check`. In-app-browser real API verification completed random scenario generation, direct prompt editing, and final image generation with OpenAI `gpt-image-2` and Gemini `gemini-3.1-flash-image`. User authorized the unified release transaction followed by visible-window `-NoGui` full backup; publication and backup evidence are now being collected.
- **v5.0.2 ambient-sound bubble-routing release candidate (2026-07-25)**: Quoted ambient sounds such as `スプーンの「カチャ」という音` and `椅子の軋む「ギシ…」という音` are no longer promoted into speaker-owned speech bubbles. Their quoted sound lettering is removed while grammatical scene action remains (`スプーンの音` / `椅子の軋む音`). RED then GREEN regression coverage covers dialogue extraction and both ChatGPT/Gemini strict-script prompts. The exact user scenario passed a real OpenAI `gpt-image-2` run and a real Gemini `gemini-3.1-flash-image` run; both completed without extra `カチャ` or `ギシ…` bubbles. One initial Gemini request returned no image part, but the identical unchanged retry returned an 864x1232 JPEG, so this is recorded as a transient provider empty response rather than a parser defect. Unified release and the separately authorized visible-window `-NoGui` full backup are in progress.
- **v5.0.1 scenario-enhancement selection release candidate (2026-07-25)**: Replaces the final empty STEP2 enhancement-grid slot with an accessible `全カテゴリを選択` toggle. It selects or clears all seven enhancement categories; the selected state uses a bright lime glow, explicit `全選択中（全カテゴリを解除）` label, and clear-off instruction. Fresh verification passed: 94/94 Node tests, ESLint with zero warnings, production build, pre-deploy checks, and git diff --check. The local OpenAI API path generated a character-sheet scenario and completed all-category enhancement. User authorized the unified release transaction followed by a visible, -NoGui full backup; publication and backup evidence remain pending.
- **Fresh Gemini STEP2 enhancement and STEP4 image proof (2026-07-24)**: Local port 5173 used the exact `AI苦手分野4連発 !?` original scenario with Gemini `gemini-3.5-flash`, character sheets loaded, and all six non-background enhancement categories selected (`expressions`, `body`, `effects`, `camera`, `dialogue`, `gag`). The first response completed in 24 seconds at 1,793 characters with no corrective retry. A fresh independent `validateScenarioEnhancement` pass returned `ok: true`, no issue codes, and all six selected categories in `changedCategories`; title, Logline, Location, Outfit, Punchline, four-panel structure, and speaker order were preserved. Dialogue changed in two of four lines (`……なんか、手、丸くない？` / `ねえ、これ流行る？`), proving the dialogue category was effective, while background enhancement remained off. STEP4 then completed through the real Gemini image API using `gemini-3.1-flash-image` and returned one 864x1232 JPEG. Visual inspection confirmed four panels, all four intended main dialogue lines, no printed speaker-name prefixes, and the unchanged white `精神と時の部屋` setting. Gemini also produced stochastic image-level artifacts (occasional duplicated cast, short stray text, a misdirected bubble tail, and weaker-than-requested exaggeration); the user explicitly accepted these as tolerable Gemini hallucination for this parity check. The API calls had no failure; one pre-run console warning came from the existing reset path calling `setActiveEngine("")`, which immediately defaults to Gemini and did not affect either verified request.
- **Local dialogue/bubble routing fix awaiting publication (2026-07-24)**: Image-prompt dialogue now separates printable text (`TEXT`) from non-printable speaker/tail metadata (`TAILS`), explicitly forbids printed speaker names, and requires each tail tip to terminate at the mapped speaker's mouth/head silhouette. Attributable spoken quotes embedded in `状況:` (including generic roles such as a host/presenter) are promoted to dialogue while being removed grammatically from visual action; reaction-only and unowned narration quotes remain excluded. The exact `世界最小スフィンクス公開 !?` local OpenAI `gpt-image-2` run produced a 1024x1536 image with no printed speaker names, the panel-2 center bubble routed to Akari, and the host's panel-4 `この続きは次号で！` bubble present. The same prompt pasted into ChatGPT Web omitted only the host bubble while retaining the other two improvements, confirming a remaining stochastic image-model omission rather than parser loss. Local checks passed: 91/91 tests, ESLint with zero warnings, production build, pre-deploy/security checks, and `git diff --check`. No commit, push, deploy, release, release-copy update, or backup is authorized/completed for this local fix.
- **v4.9.8 documentation-alignment release candidate (2026-07-24)**: README implementation details now explicitly describe the seven STEP2 enhancement categories, selected-scope protection, dialogue/gag separation, response validation, one corrective retry, and fail-closed behavior. Application behavior is unchanged from v4.9.7. The candidate must complete the unified release transaction before the separately authorized visible-window `-NoGui` full backup runs.
- **v4.9.7 released (2026-07-24)**: Replaced the permissive whole-scenario rewrite with selected-category contracts, structural/diff validation, one corrective retry, and fail-closed acceptance. `セリフ書換` now requires an actual dialogue change while rejecting explanatory over-expansion and softened punchlines; `ギャグ演出` is a separate control that preserves dialogue unless dialogue rewriting is also selected. Metadata (including `Topic:` and omitted optional fields), panel structure, speakers, unselected dialogue/camera/emotion/background mutations, calm-tone escalation, and new anatomy/body-horror escalation are guarded. Exact user-supplied `AI苦手分野4連発 !?` browser/API proof on local port 5173 used OpenAI gpt-4.1 with every enhancement except background: all six selected categories changed, validation returned no issues, and the four final dialogue lines were `いくわよ。` / `手がパンみたいになってる……。` / `下が見えすぎる……。` / `これ、逆に新しいかも。`. Unified receipt `backups/release_receipts/nano-banana-pro-v4.9.7-2026-07-24.json` completed with every stage verified: source commit `6eb76205fda5cf766533a17866f98c5601e7f106`, annotated tag and GitHub Release `v4.9.7`, GitHub Pages commit `29c9f720ae1657d435f21e3d20108fc6821355f8`, Hugging Face commit `beadb0a6c1be46d1e6502894a0c63d25d293e57a`, and `C:\nano-banana-pro-main` synchronized from the verified Release ZIP.
- **Gemini parity proof for the same STEP2 fix**: The user connected Gemini and uploaded the same character sheets. Local port 5173 used `gemini-3.5-flash` with the exact same scenario and the same six categories (background OFF). The first response was rejected as `dialogue_overexpanded`; the automatic corrective retry returned a 1,388-character scenario that passed with all six selected categories changed, no validation issues, and dialogue `……始めるわよ。` / `……なんか、丸くない？` / `地面が、遠い……。` / `これ、流行るかな。`. Browser error count was zero. This proves the retry/validation path works for both OpenAI and Gemini providers.
- **v4.9.6 release candidate**: Adds localized crisp triangular cel-shaded shadows at overlapping and pinched clothing folds for both provider prompt families, with panel-level priority for light shirts, blouses, jackets, and sleeves. It preserves cast, script, layout, material, and lighting constraints, avoids geometric garment patterns, and retains the instruction in compact ChatGPT Web-copy prompts. Local verification passed: 79 tests, lint, build, diff check, and strict pre-deploy checks. The user completed live OpenAI and Gemini image generations; OpenAI quality is preferred, while Gemini is accepted as structurally non-broken. Release, public verification, and the separately requested visible-window/no-API-input full backup remain in progress.
- **v4.9.3 release status**: v4.9.2 exposed an HF deploy-runner defect: it deleted the Space `Dockerfile` and `nginx.conf`, producing HTTP 404 at the retired static host. The forward-fix is published as source commit `5a2430c`, tag/release `v4.9.3`, GitHub Pages (HTTP 200, title `Nano Banana Pro v4.9.3`), and the current Hugging Face Docker host `https://furuyan-nano-banana-pro.hf.space` (HTTP 200 for root and SPA fallback; runtime SHA `57c3382`; `Dockerfile` and `nginx.conf` present). `scripts/deploy_hf.ps1` now preserves both runtime files and stops before cleaning if either is missing; `tests/deploy-hf-runtime-preservation.test.mjs` is the regression guard. The canonical local release copy `C:\nano-banana-pro-main` is verified at `4.9.3`; the prior `4.9.1` copy was preserved at `C:\nano-banana-pro-main.pre-v4.9.3-20260719`. Full backup is the remaining final operation.
- **v4.9.2 release in progress**: the two-hand prop ownership lock is implemented locally. It fixes a generic depth/anatomy failure in which a character closing a document with both hands could acquire an apparent third hand from behind. The lock requires exactly two arms and two hands connected to the owning character's shoulders, keeps the prop at that character's torso, and forbids back-emerging or cross-body limbs. The exact Rin/document regression passed in both prompt families and a live OpenAI `gpt-image-2` generation; panel 2 showed no third hand. Current evidence before publication: 60/60 tests, ESLint, Vite build, `pre_deploy_check.js`, and `git diff --check` passed. Commit/push/deploy/release/local-release-copy sync/full backup remain pending.
- **v4.9.1 correction release status**: `v4.10.0` was an erroneous increment: `4.9.0` must advance to `4.9.1`. Its GitHub Release and tag are removed; `v4.9.1` is the latest release. GitHub Pages returns HTTP 200 with title `Nano Banana Pro v4.9.1`; the Hugging Face Docker host returns HTTP 200 for HTML, CSS, JS, and SPA fallback; `C:\nano-banana-pro-main` reports package/title `4.9.1` with no nested folder. The verified final full backup is `C:\Users\sx717\Antigravity\backups\antigravity_full_backup_2026-07-16_191303.zip` (SHA-256 `9E4A94EDA871E8E91734EB9284AEEBB6898674ED6C0C267B4F1CA1C65B68E6F6`); its Google Drive mirror hash matches.
- **Published application payload**: The generic hand-action allocation fix is the already-published application logic from source commit `73a7cfb`; no new application behavior was introduced while correcting the version identifiers to `v4.9.1`. The current release correction must describe this accurately and must not claim that `v4.9.1` newly added the behavior.
- **Version**: v4.9.0 Gemini-only rear-foreground staging fix is released and deployed from commit `2abb7e1`. GitHub Pages and Hugging Face Spaces serve `Nano Banana Pro v4.9.0` with `assets/index-9CEHV2dI.js`; the bilingual GitHub Release and `C:\nano-banana-pro-main` release copy are verified.
- **Previous public version**: v4.8.9 conversational depth staging release.
- **Version**: v4.8.8 conversational eye-line auto-staging fix was released and deployed.
- **Version**: v4.8.7 Super FURU title alignment is released and deployed.
- **Previous public version**: v4.8.6 enhanced-scenario narration bubble extraction fix was released, deployed, and backed up.
- **Post-v4.8.2 local fix status**: Completed as v4.8.3. The serious provider prompt-routing bug found after the user's style/footer report is fixed by explicit provider families (`chatgpt` / `gemini`) in `src/lib/prompt-assembler.js` and `src/hooks/useMangaWorkflow.js`; prompt assembly branches on normalized `providerFamily`, not the legacy `enableChatGPTMode` boolean. ChatGPT Web copy and OpenAI API generation share the same ChatGPT-family final prompt; Gemini Web copy and Gemini API generation share the same Gemini-family final prompt. Generated-image history is capped to the latest item to reduce huge Data URL memory pressure after the in-app browser crash. Free-form EMOTION descriptors are normalized to existing strong style locks. Fresh ChatGPT Web regeneration from the final rebuilt prompt remains unverified.
- **Latest changes**:
  - v4.8.7 release prep: README/app-footer references to the old long system title were aligned to `Super FURU AI 4-koma System`; `Nano Banana 2` and `ChatGPT Images 2.0` remain only where they describe supported generation engines. The deploy title precheck now accepts `Super FURU AI 4-koma System` as an allowed app title.
  - v4.8.6 release prep: enhanced-scenario narration/action lines with quoted group reactions are no longer promoted into speech-bubble speakers. Panel 4 keeps only explicit character dialogue, so quoted direction text such as a group scream or breath-stopping narration stays visual/action-only. Regression coverage was added in `tests/panel-utils-dialogue.test.mjs`; local STEP3/STEP4 browser proof generated a 1024x1536 PNG without the long narration bubble.
  - v4.8.5 release prep: STEP4 image aspect ratio correction prompt copy button now hides its base label while the temporary `繧ｳ繝斐・螳御ｺ・ feedback is visible, matching the 2x upscale copy-button feedback behavior and preventing text overlap. Regression test `tests/upscale-prompt-ui.test.mjs` now locks this copied-feedback state.
  - v4.8.4 release prep: STEP4縺ｮChatGPT 2蛟阪い繝・・繧ｹ繧ｱ繝ｼ繝ｫ譯亥・譁・・蜈磯ｭ縺ｫ `笨ｨ` 繧定ｿｽ蜉縺励∵｡亥・譁・→繧ｳ繝斐・逕ｨ繝懊ち繝ｳ縺ｮ隕冶ｪ肴ｧ繧帝ｫ倥ａ縺溘よ里蟄倥・闍ｱ隱槭い繝・・繧ｹ繧ｱ繝ｼ繝ｫ繝励Ο繝ｳ繝励ヨ縲√さ繝斐・蜃ｦ逅・∵ｯ皮紫菫ｮ豁｣繝励Ο繝ｳ繝励ヨ縲∫判蜒冗函謌舌Ο繧ｸ繝・け縺ｫ縺ｯ螟画峩縺ｪ縺励３egression test `tests/upscale-prompt-ui.test.mjs` now requires the emoji-prefixed sentence and still forbids copying the social-post title/source name.
  - Gemini stability/quality hardening: the Gemini-family prompt now adds a generic `GEMINI STABILITY / QUALITY LOCK` to reduce flat-template output, invented dominant cast/silhouette figures, shadow-as-new-character errors, dialogue duplication, extra warning phrases, and punctuation normalization. The old Gemini rule forcing Japanese dialogue to end with `縲Ａ was removed because it conflicted with verbatim script lock. The generic `classic anime styles` constraint was also replaced with a selected-style/PANEL STYLE LOCK preservation rule so Gemini does not collapse selected visual styles back to a generic anime look.
  - Provider-family architecture repair: `PROMPT_PROVIDER_FAMILIES` and `normalizePromptProviderFamily` now make Gemini/OpenAI prompt selection explicit; `useMangaWorkflow` derives the family from the active engine/API state and passes it to `buildMangaPrompt`; provider parity tests forbid Web/API split prompt variables and forbid prompt assembly branching on `enableChatGPTMode`.
  - Browser memory hardening: `src/lib/generation-history.js` caps generated image history to the latest item so giant PNG data URLs are not accumulated in the DOM.
  - Free-form EMOTION normalization: hyphen/plus/space-separated descriptors are mapped to existing strong style locks instead of silently falling back to NORMAL.
  - v4.8.1 release prep after v4.8.0: ChatGPT image prompts now stay in a web-paste-safe range by compacting repeated global/per-panel control text while preserving title, dialogue, watermarks, visual action detail, art-quality rules, cast limits, and identity/glasses locks. Dialogue labels now explicitly say `Dialogue (verbatim bubbles)` and the text rules forbid paraphrase, synonyms, softening, and added/omitted words.
  - Generic identity parser hardening: `- Character [Name]: ... glasses/no glasses ...` cast lines now feed the Identity Matrix, so glasses locks do not disappear when the cast list is already in cleaned Character-line form.
  - Generic dialogue extraction follow-up: spoken quotes embedded in action narration are extracted alongside explicit dialogue, source order is preserved, and visual text/handwriting/SFX/mood labels stay out of speech-bubble Dialogue.
  - Generic action-quote de-duplication follow-up: fallback extraction now skips unowned narration quotes and short action-quote fragments already covered by explicit dialogue, preventing extra speech bubbles such as partial setup phrases while preserving distinct spoken quotes with an inferred speaker.
  - Generic title punctuation fix: the scenario title line is captured as a full line and terminal punctuation such as `!?` is preserved instead of being stripped by prompt assembly or Step 4 rebuild.
  - Highest-priority anti-hardcoding rule documented: root `AGENTS.md` and `docs/project_standards.md` now explicitly forbid production hardcoding of proper nouns, sample words, character names, sample dialogue, product/place names, trend words, and one-off prompt fragments when fixing bugs.
  - Regression tests added for embedded spoken quote extraction, visual quote exclusion, handwriting text exclusion, title punctuation preservation, and ChatGPT prompt length/critical-content preservation.
  - Production source scan for the user's sample-specific terms returned no matches; sample terms appear only in regression test fixtures.
  - v4.8.2 API key preflight fix: entering an arbitrary string or a shortened/invalid OpenAI-style key no longer unlocks STEP 1. API entry now requires provider format validation plus a live provider preflight (`Gemini /v1beta/models` or `OpenAI /v1/models`) before setting the app's connected state. Failed provider errors are sanitized so API key fragments are not rendered in the UI. The modal now labels typed keys as unverified format guesses until preflight succeeds, saved Gemini keys are re-verified on startup, and the header/control bar/STEP1 receive an empty API-key prop while any API modal is open so stale state cannot look connected.
  - Web/API prompt parity guard: `copyPrompt` copies `finalPrompt`, API generation starts from `overridePrompt || finalPrompt`, and both `generateImageWithOpenAI` and `generateImageWithImagen` receive that same `currentPrompt`. Regression tests forbid introducing separate `webPrompt` / `apiPrompt` style variables for ChatGPT/OpenAI or Gemini.
  - ChatGPT/Gemini prompt budget note: the roughly 15,000-character ChatGPT prompt target is an empirical soft budget for manual ChatGPT Web paste, not an official OpenAI API limit and not a guarantee. Keep it separate from API transport guards such as the OpenAI image request hard cap in `src/lib/openai.js`. Do not apply that empirical ChatGPT Web budget to Gemini-family prompts; Gemini-family prompts must be allowed to remain longer when the scenario/cast/style guidance needs it.
  - STEP2 downgrade rollback: the attempted STEP2 scenario emotion-diversity guard was removed after the user rejected STEP2 spec-down. Do not re-add STEP2 restrictions or scenario-generator constraints without explicit approval.
  - ChatGPT Web style prompt restoration: the ChatGPT-family image prompt no longer collapses the selected `styleCore` into `Base style: full-color TV anime`; it now carries the full style guidance into the Web/API final prompt.
  - A4 export rollback: the extra `A4 PNG繧偵ム繧ｦ繝ｳ繝ｭ繝ｼ繝・(1024ﾃ・448)` button and deterministic A4 canvas export were removed from STEP4. Only the raw generated image download remains.
  - Strict scenario/script guard: both ChatGPT-family and Gemini-family final prompts now include `STRICT SCRIPT LOCK` before `PANEL DESCRIPTIONS`, state that a different story is a failed output, forbid replacing/rewording/omitting/adding dialogue, and make lower camera/layout/cast/style instructions yield to the locked scenario/title/dialogue.
  - Action extraction control-token leak fix: `extractActionOnly` now strips bracketed control tags such as `[EMOTION: ...]` and `[Camera: ...]` from `Action (visual only)` while preserving the actual situation/action body. This prevents hidden routing/camera/emotion tokens from being sent to image models as visual content.
  - Gag-intent additive overlay: when a comedy/gag emotion tag maps to a serious/dark style such as `HORROR`, the style is preserved and only a short comedy-expression allowance is appended. This avoids flattening dramatic comedy into plain chibi gag while still rescuing the gag intent.
  - Action quote placeholder cleanup: `extractActionOnly` no longer injects English placeholder strings such as `the listed dialogue content` or `a non-text ambient sound effect` into visual action text. Explicit visible writing contexts now include poster text, while spoken quote fragments and dangling speech verbs such as `縲ゅ→蜻溘″縲～ are removed from action text without polluting the prompt.
- **State**: v4.8.7 title alignment is released and deployed. Source commit `b8e604b6fdb208185028a62277171f1e605acc0d` was pushed to `origin/main`; tag `v4.8.7` points to that commit; GitHub Release `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.7` is published as a non-draft, non-prerelease release. GitHub Pages `gh-pages` commit `49bf0b65348660d2e6e67ded711c116cef7582a1` is deployed by successful Pages run `28789326242`; live Pages returns title `Nano Banana Pro v4.8.7`, asset `assets/index-BYfBYppp.js`, `SUPER FURU AI 4-KOMA SYSTEM`, and no old long-title variants. Hugging Face Spaces commit `9c5ec83` is pushed and the live static URL returns the same title/asset/footer checks. `C:\nano-banana-pro-main` is synced from the v4.8.7 GitHub Release ZIP to package version `4.8.7`, `index.html` title `Nano Banana Pro v4.8.7`, new footer/precheck title strings present, no old long-title variants, and no nested duplicate folder. Other README-only app references were committed and pushed: Story Maker `8790983`, Short Movie `085663a`, Character Sheet `a09d149`, Comic Translation `413309b`, and Background `098eee6`; Viral Radar had no old-title occurrence. User-requested PS1 full backup is the next operation.

## Pending Verification Tasks
- [x] Local STEP2 scenario-enhancement exact-sample proof: OpenAI gpt-4.1 on port 5173 changed all four dialogue lines, preserved metadata/speakers/calm tone, changed all six selected categories, kept background enhancement OFF without a background-mutation violation, and passed the post-response validator on the first attempt.
- [x] Local STEP2 Gemini parity proof: gemini-3.5-flash initially failed `dialogue_overexpanded`, then passed the built-in corrective retry with all six selected categories changed, background OFF, no validation issues, and zero browser errors.
- [x] Local STEP2 scenario-enhancement verification: targeted 10/10 and full 89/89 tests, ESLint, Vite build, and `git diff --check` passed.
- [x] v4.8.7 title-scope proof: all seven app folders and known local `*-main` copies returned no old long system-title variants; model/engine mentions are intentionally preserved.
- [x] v4.8.7 version prep: `node scripts\update_version.cjs 4.8.7 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was kept to version-only changes.
- [x] v4.8.7 local checks: `git diff --check -- . ':!dist'`, `node scripts\pre_deploy_check.js`, full `node --test tests\*.mjs` 40/40, `npm run lint -- --max-warnings=0`, `npm run build`, and `dist` title-string scan passed. Built asset is `assets/index-BYfBYppp.js`; `dist` contains `Nano Banana Pro v4.8.7` / `SUPER FURU AI 4-KOMA SYSTEM` and no old long-title variants.
- [x] v4.8.7 commit/push/tag/GitHub Release: source commit `b8e604b6fdb208185028a62277171f1e605acc0d` pushed to `origin/main`; tag `v4.8.7` points to that commit; GitHub Release is published at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.7`.
- [x] v4.8.7 GitHub Pages deploy proof: `npm run deploy` published `gh-pages`; the first Pages deployment failed transiently, then explicit Pages rebuild run `28789326242` succeeded. Live Pages returns title `Nano Banana Pro v4.8.7`, asset `assets/index-BYfBYppp.js`, `SUPER FURU AI 4-KOMA SYSTEM`, and no old long-title variants.
- [x] v4.8.7 Hugging Face Spaces deploy proof: `npm run deploy:hf` pushed commit `9c5ec83`; live static HTML/JS return title `Nano Banana Pro v4.8.7`, asset `assets/index-BYfBYppp.js`, `SUPER FURU AI 4-KOMA SYSTEM`, and no old long-title variants.
- [x] v4.8.7 release ZIP and `C:\nano-banana-pro-main` sync proof: GitHub Release ZIP was downloaded and synced; `C:\nano-banana-pro-main\package.json` is `4.8.7`, `index.html` title is `Nano Banana Pro v4.8.7`, new footer/precheck title strings are present, old long-title variants are absent, and `C:\nano-banana-pro-main\nano-banana-pro-main` does not exist.
- [ ] v4.8.7 requested PS1 full backup proof.
- [x] v4.8.6 local issue proof: in-app browser STEP3 rebuilt the supplied enhanced scenario and Panel 4 `Dialogue (verbatim bubbles)` contained only the explicit final speaker dialogue; the quoted group reaction/narration stayed out of speech bubbles. STEP4 OpenAI image generation completed successfully with a 1024x1536 PNG and browser warn/error logs 0.
- [x] v4.8.6 version prep: `node scripts\update_version.cjs 4.8.6 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was kept to version-only changes.
- [x] v4.8.6 local checks: `node --check src\lib\panel-utils.js`, `node --check tests\panel-utils-dialogue.test.mjs`, full `node --test tests\*.mjs` passed 40/40, `npm run lint -- --max-warnings=0` passed, `npm run build` passed with existing Browserslist/chunk warnings only, `git diff --check -- . ':!dist'` passed, and `node scripts\pre_deploy_check.js` passed.
- [x] v4.8.6 commit/push/tag/GitHub Release: source commit `d34f1865290f498df793b34b8bc2371bf4bd488c` pushed to `origin/main`; tag `v4.8.6` points to that commit; GitHub Release is published at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.6`.
- [x] v4.8.6 GitHub Pages deploy proof: `npm run deploy` published `gh-pages`; latest Pages build is `built` for `gh-pages` commit `04527b2bcf912601ceae182da44f7e5dbc0e38ce`; live Pages returns title `Nano Banana Pro v4.8.6`, asset `assets/index-Dxq0x6kQ.js`, and the group-narration guard strings.
- [x] v4.8.6 Hugging Face Spaces deploy proof: `npm run deploy:hf` pushed commit `5823b5e`; live static HTML/JS return title `Nano Banana Pro v4.8.6`, asset `assets/index-Dxq0x6kQ.js`, and the group-narration guard strings.
- [x] v4.8.6 release ZIP and `C:\nano-banana-pro-main` sync proof: GitHub Release ZIP was downloaded and synced; `C:\nano-banana-pro-main\package.json` is `4.8.6`, `index.html` title is `Nano Banana Pro v4.8.6`, and `C:\nano-banana-pro-main\nano-banana-pro-main` does not exist.
- [x] v4.8.6 requested PS1 full backup proof: visible PowerShell launched `scripts\backup_full.ps1 -NoGui`; final ZIP `backups\antigravity_full_backup_2026-07-06_090640.zip` is 136,373,907 bytes with SHA256 `311533A9B9314DF4DB3B1F072CE7405323285CFBE25AF4A044AB217B10953CED`; ZIP inspection found 15,132 entries, `testzip=OK`, required entries including `backup_meta.json`, `git_repos.txt`, `git_repos_detailed.json`, `restore_full.ps1`, `backup_full.ps1`, `root_files/AGENTS.md`, `root_files/PLAN.md`, `coordination_files/manifest.json`, and `git_worktree_snapshots/snapshot_index.json`; `root_files/PLAN.md` and `coordination_files/files/nano-banana-pro/HANDOFF.md` contain v4.8.6; Drive copy at `E:\繝槭う繝峨Λ繧､繝暴Antigravity_Backups\antigravity_full_backup_2026-07-06_090640.zip` has the same size/hash; `antigravity-system` ledger commit `99ec05f503ed370915d4daa1a122e11a5588fe4e` matches `origin/master`.
- [x] v4.8.5 focused regression proof: `node --test tests\upscale-prompt-ui.test.mjs` passed 3/3 and requires the ratio-fix button base label to be hidden while `繧ｳ繝斐・螳御ｺ・ is shown.
- [x] v4.8.5 version prep: `node scripts\update_version.cjs 4.8.5 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was kept to version-only changes.
- [x] v4.8.5 local checks so far: full `node --test` suite passed 39/39; `npm run lint -- --max-warnings=0` passed; `npm run build` passed with only existing Browserslist/chunk warnings; `git diff --check -- . ':!dist'` passed.
- [x] v4.8.5 local browser proof on port 5173: in-app browser loaded `http://127.0.0.1:5173/?codexVerify=v485-local-title` with title `Nano Banana Pro v4.8.5`; harness click proof showed the ratio-fix button `innerText` as `笨・繧ｳ繝斐・螳御ｺ・, the original-label span hidden, and the copied-feedback span visible/absolute.
- [x] v4.8.5 `node scripts\pre_deploy_check.js` after this HANDOFF update passed.
- [x] v4.8.5 commit/push/tag/GitHub Release: commit `2c437b57d297315dc0ec4e56a633bff8d23a3094` pushed to `origin/main`; tag `v4.8.5` points to that commit; GitHub Release published at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.5`.
- [x] v4.8.5 GitHub Pages deploy proof: `npm run deploy` published `gh-pages`; build `1078237152` reached `built`; live `https://furuyan1234.github.io/nano-banana-pro/` returned title `Nano Banana Pro v4.8.5`, asset `assets/index-CHp4AeiZ.js`, and JS contains `v4.8.5`, `逕ｻ蜒乗ｯ皮紫菫ｮ豁｣繝励Ο繝ｳ繝励ヨ繧偵さ繝斐・`, and `繧ｳ繝斐・螳御ｺ・.
- [x] v4.8.5 Hugging Face Spaces deploy proof: `npm run deploy:hf` committed `4c73daa` to `hf-nano-banana-pro`; live `https://furuyan-nano-banana-pro.static.hf.space/` returned title `Nano Banana Pro v4.8.5`, asset `assets/index-CHp4AeiZ.js`, and JS contains `v4.8.5`, `逕ｻ蜒乗ｯ皮紫菫ｮ豁｣繝励Ο繝ｳ繝励ヨ繧偵さ繝斐・`, and `繧ｳ繝斐・螳御ｺ・.
- [x] v4.8.5 release ZIP and `C:\nano-banana-pro-main` sync proof: GitHub Release ZIP was downloaded and synced; `C:\nano-banana-pro-main\package.json` is `4.8.5`, `index.html` title is `Nano Banana Pro v4.8.5`, and `C:\nano-banana-pro-main\nano-banana-pro-main` does not exist.
- [x] v4.8.5 requested PS1 full backup proof: visible PowerShell launched `scripts\backup_full.ps1 -NoGui`; final ZIP `backups\antigravity_full_backup_2026-07-04_182348.zip` is 136,368,750 bytes with SHA256 `ADD00CCB1CB250A7F9EFC948DAA38EA649EA5112376EFAE55CC14B98DF4489CD`; ZIP inspection found 15,131 entries, `testzip=OK`, and required entries including `backup_meta.json`, `git_repos.txt`, `git_repos_detailed.json`, `restore_full.ps1`, `backup_full.ps1`, `root_files/AGENTS.md`, `root_files/PLAN.md`, `coordination_files/manifest.json`, and `git_worktree_snapshots/snapshot_index.json`; Drive copy at `E:\繝槭う繝峨Λ繧､繝暴Antigravity_Backups\antigravity_full_backup_2026-07-04_182348.zip` has the same size/hash; `antigravity-system` ledger commit `9a2c897d0a8e5e594ff1968257a960bb7cdd71fd` matches `origin/master`.
- [x] v4.8.4 RED/GREEN proof: `node --test tests\upscale-prompt-ui.test.mjs` failed before the UI sentence had the `笨ｨ` prefix, then passed after the sentence was updated.
- [x] v4.8.4 version prep: `node scripts\update_version.cjs 4.8.4 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was limited to version-only changes; ChangeLog entries were pruned to the latest 15-ish entries.
- [x] v4.8.4 local checks so far: full `node --test` suite passed 38/38; `npm run lint -- --max-warnings=0` passed; `npm run build` passed; `git diff --check -- . ':!dist'` passed; source scan found no `Eris_Create_Lab` or `GPT Native Super Resolution Ver2.1` strings.
- [ ] v4.8.4 local browser proof on port 5173.
- [ ] v4.8.4 `node scripts\pre_deploy_check.js` after this HANDOFF update.
- [ ] v4.8.4 commit/push/tag/GitHub Release.
- [ ] v4.8.4 GitHub Pages deploy proof.
- [ ] v4.8.4 Hugging Face Spaces deploy proof.
- [ ] v4.8.4 release ZIP and `C:\nano-banana-pro-main` sync proof.
- [ ] v4.8.4 requested PS1 full backup proof.
- [x] v4.8.3 local pre-release verification: `node scripts\pre_deploy_check.js`, full `node --test` suite (36/36), `npm run lint -- --max-warnings=0`, `npm run build`, `git diff --check -- . ':!dist'`, and node syntax checks on touched JS files passed.
- [x] v4.8.3 local browser proof: in-app browser on `http://127.0.0.1:5173/?codexVerify=v483-local` loaded `Nano Banana Pro v4.8.3`, rendered `Super FURU AI 4-koma System v4.8.3`, had no A4 export button, and reported zero console warnings/errors.
- [x] v4.8.3 commit/push/tag/release: commit `f66d7b2b506e65111b2d42f9629cd499de9b33cc` was pushed to `origin/main`; tag `v4.8.3` was pushed; GitHub Release was created at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.3`.
- [x] v4.8.3 GitHub Pages proof: live HTML returned title `Nano Banana Pro v4.8.3`, asset `assets/index-BoDe6TcM.js`, and deployed JS contains `v4.8.3`, `STRICT SCRIPT LOCK`, and `GEMINI STABILITY / QUALITY LOCK` while excluding the old `Japanese dialogue MUST end with a period` rule and `A4 PNG` label.
- [x] v4.8.3 Hugging Face Spaces proof: `npm run deploy:hf` committed `b9b598e`; live static HTML returned title `Nano Banana Pro v4.8.3`, asset `assets/index-BoDe6TcM.js`, and deployed JS matched the same lock/removal checks.
- [x] v4.8.3 release ZIP/local sync proof: GitHub Release ZIP was downloaded and synced to `C:\nano-banana-pro-main`; package version is `4.8.3`, HTML title is `Nano Banana Pro v4.8.3`, and there is no nested duplicate folder.
- [x] v4.8.3 backup policy: no full backup was run because the user did not explicitly request backup for this deploy.
- [x] Post-v4.8.2 RED/GREEN regression proof for ChatGPT full-auto prompt routing: the new test first caught the forced Gemini branch, then passed after routing full-auto prompt assembly through `enableOpenAIApi || enableChatGPTMode`.
- [x] Post-v4.8.2 per-panel style-lock regression proof: non-normal style blocks now include `PANEL STYLE LOCK` guidance for visibly distinct linework, palette, shading, and background/VFX while preserving identity/dialogue/layout/clean skin rules.
- [x] Post-v4.8.2 focused local checks: `node --test tests\prompt-budget.test.mjs tests\prompt-title.test.mjs tests\panel-utils-dialogue.test.mjs tests\full-auto-chatgpt-mode.test.mjs tests\prompt-style-exceptions.test.mjs`, `git diff --check`, `npm run lint -- --max-warnings=0`, and `npm run build` passed.
- [x] Post-v4.8.2 local browser proof after the ChatGPT prompt-routing/style-lock fix: `http://127.0.0.1:5173/?codexPromptRouteStyleLockFix=1` loaded with title `Nano Banana Pro v4.8.2`, rendered `Super FURU AI 4-koma System v4.8.2`, and reported zero browser console errors.
- [x] Web/API prompt parity proof for both provider families: `tests\prompt-provider-parity.test.mjs` verifies Web copy uses `finalPrompt`, API generation uses `currentPrompt` derived from `finalPrompt`, OpenAI/Gemini both send that same prompt, and Web/API split prompt variable names are blocked.
- [x] STEP2 rollback proof: the STEP2 scenario emotion-diversity production rules were removed, and the enforcing `tests\scenario-emotion-diversity.test.mjs` regression was deleted.
- [x] Strict scenario/script-lock proof: `tests\prompt-script-lock.test.mjs` first failed before the lock existed, then passed after both provider-family prompts placed `STRICT SCRIPT LOCK` before detailed panel descriptions and made story/dialogue changes a failed output.
- [x] Prompt budget wording and provider-boundary proof: ChatGPT prompt-budget regression tests now describe the roughly 15,000-character target as an empirical Web-copy soft budget, not an official API limit. `tests\prompt-budget.test.mjs` also verifies a Gemini-family manga prompt can exceed that soft budget while keeping the Gemini footer, style-lock markers, and a late stress marker, so the ChatGPT Web-copy budget cannot silently cap Gemini.
- [x] Provider-family architecture proof: `tests\prompt-provider-parity.test.mjs` and `tests\full-auto-chatgpt-mode.test.mjs` now require explicit `providerFamily` routing and forbid falling back to the legacy ChatGPT-mode boolean for provider selection.
- [x] Browser Data URL memory proof: `tests\generation-history-memory.test.mjs` verifies generated image history keeps only the latest data URL.
- [x] Latest local checks after the control-token leak and gag-overlay follow-up: combined provider/dialogue/style/script/budget/title/history/A4-removal/Web-quality tests passed 23/23; `node --check` on `src\lib\panel-utils.js`, `src\lib\prompts.js`, `src\lib\prompt-assembler.js`, and `src\lib\openai.js` passed; `npm run lint -- --max-warnings=0` passed; `npm run build` passed; `git diff --check -- . ':!dist'` passed with only the existing CRLF warning for `src/lib/safety-filters.js` and no content diff in that file.
- [x] Latest local checks after action-quote placeholder/dangling-verb cleanup: `tests\panel-utils-dialogue.test.mjs` passed 10/10; combined provider/dialogue/style/script/budget/title/history/A4-removal/Web-quality tests passed 25/25; `node --check src\lib\prompts.js`, `node --check src\lib\panel-utils.js`, `node --check src\lib\prompt-assembler.js`, `node --check src\lib\openai.js`, `npm run lint -- --max-warnings=0`, and `npm run build` passed.
- [x] Fresh real OpenAI API image generation after the ChatGPT-family prompt-routing/style-lock repair: after the user entered the API key in the UI, the in-app browser generated the restored `AI闡嶺ｽ懈ｨｩ隲紋ｺ峨∫樟蝣ｴ豺ｷ荵ｱ!?` scenario successfully. Treat this as API confirmation only, not the final target. It was produced before the final dangling-verb cleanup, so it is not proof of the latest rebuilt prompt.
- [x] User-provided ChatGPT Web image `C:\Users\sx717\Downloads\ChatGPT Image 2026蟷ｴ6譛・7譌･ 17_31_45.png` was inspected against the scenario/final prompt, not against another image. Verdict: practical scenario-faithful pass direction. Title, setting, main beats, required dialogue, poster text, cookie beat, and dark final panel are present; remaining gaps are minor spelling/punctuation drift and not all five characters being equally visible in panels 1/3. This is not a perfect literal-lock proof.
- [ ] Fresh ChatGPT Web regeneration from a prompt rebuilt after the final dangling-verb cleanup remains unverified.
- [x] Fresh Gemini API verification after the stability/quality hardening: STEP3 rebuilt the Gemini prompt in the in-app browser with `GEMINI STABILITY / QUALITY LOCK`, `Do NOT normalize punctuation`, and the selected-style/PANEL STYLE LOCK preservation rule; it no longer contained the old `Japanese dialogue MUST end with a period` or `classic anime styles` fixed rule. Two Gemini API generations completed without browser/API errors. The verified scenario `繧上◆縺ゅａ繧医ｊ霆ｽ縺・ヱ譏・?` followed the title, 4-panel structure, core beats, and required dialogue direction better than the earlier `AI闡嶺ｽ懈ｨｩ隲紋ｺ荏 failure, and did not show the prior black-silhouette invented-person or dialogue-duplication failure. Remaining gap: Gemini's drawing quality still looks flatter and more generic than ChatGPT/OpenAI Web; further prompt-only pressure is likely to overconstrain composition/dialogue rather than close the model-quality gap. Next quality step should be post-generation QA/retry or manual Gemini Web generation with attached character/background references, not more prompt lines.
- [x] Local prompt assembly proof for the failing scenario shape.
- [x] Production source scan for sample-specific hardcoded terms.
- [x] Local checks and build before release prep (`node --test tests\panel-utils-dialogue.test.mjs tests\prompt-title.test.mjs`, `node --check`, `git diff --check`, `npm run lint -- --max-warnings=0`, `npm run build`).
- [x] Local prompt budget regression proof: the latest long ChatGPT sample now emits a 13,841-character prompt and keeps title/dialogue/watermarks/tategaki/glasses locks, `THINGS TO AVOID`, stronger verbatim dialogue locking, and explicit Panel 4 dialogue without promoting unowned action quotes.
- [x] Local checks after prompt budget/action-quote fix: `node --test tests\panel-utils-dialogue.test.mjs tests\prompt-title.test.mjs tests\prompt-budget.test.mjs`, `git diff --check -- . ':!dist'`, `npm run lint -- --max-warnings=0`, `npm run build`, and local HTTP 200 on `http://127.0.0.1:5173/`.
- [x] Final docs validation after this HANDOFF update: `node scripts\pre_deploy_check.js` passed.
- [x] `node scripts\pre_deploy_check.js` before release commit.
- [x] GitHub Pages deploy verification for v4.8.1: live URL returned title `Nano Banana Pro v4.8.1`, asset `assets/index-DLlziH4s.js`, and deployed JS contained `v4.8.1`, `THINGS TO AVOID`, and `Dialogue (verbatim bubbles)`.
- [x] Hugging Face Spaces deploy verification for v4.8.1: live URL returned title `Nano Banana Pro v4.8.1`, asset `assets/index-DLlziH4s.js`, and deployed JS contained `v4.8.1`, `THINGS TO AVOID`, and `Dialogue (verbatim bubbles)`.
- [x] GitHub Release creation (v4.8.1): `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.1`.
- [x] Release ZIP download and `C:\nano-banana-pro-main` local sync: package version `4.8.1`, HTML title `Nano Banana Pro v4.8.1`, no nested duplicate folder.
- [x] v4.8.2 API preflight tests: invalid arbitrary text and shortened OpenAI-looking keys are rejected locally without network access; provider-shaped keys must pass live provider preflight; invalid provider responses are rejected without exposing API key fragments.
- [x] v4.8.2 UI lock regression tests: the API modal must not label unverified keys as a running engine, main UI receives an empty API-key prop while an API modal is open, and saved keys are preflight-verified before restoring connected state.
- [x] v4.8.2 local verification: `node --test tests\api-key-preflight.test.mjs tests\api-key-ui-lock.test.mjs tests\panel-utils-dialogue.test.mjs tests\prompt-title.test.mjs tests\prompt-budget.test.mjs`, `npm run lint -- --max-warnings=0`, `npm run build`, and `git diff --check -- . ':!dist'` passed.
- [x] v4.8.2 browser proof on `http://127.0.0.1:5173/?codexApiPreflightV482Browser=1`: entering a shortened OpenAI-looking dummy key shows `OpenAI蠖｢蠑擾ｼ域悴讀懆ｨｼ・荏, never shows `Engine 縺ｧ襍ｷ蜍描, fails with the Japanese error `API繧ｭ繝ｼ縺ｮ蠖｢蠑上′豁｣縺励￥縺ゅｊ縺ｾ縺帙ｓ縲・..`, keeps the header `譛ｪ謗･邯啻, keeps `main` blurred and pointer-locked, and does not render the raw OpenAI provider error or the typed key fragment.
- [x] Production source scan for the user's sample-specific terms and API key patterns returned no matches.
- [x] `node scripts\pre_deploy_check.js` after this HANDOFF update.
- [x] Commit/push v4.8.2 API preflight fix: `5f9ad7acc09011ee224a35a25acb9e704d66d1bc`.
- [x] GitHub Pages deploy verification for v4.8.2: `https://furuyan1234.github.io/nano-banana-pro/?v=20260624-v482-api-preflight-jp` returned title `Nano Banana Pro v4.8.2`, asset `assets/index-BPKQnvko.js`, Japanese API-key errors and unverified labels present, raw OpenAI error text and old English format error absent.
- [x] GitHub Pages live UI API-error proof for v4.8.2: `https://furuyan1234.github.io/nano-banana-pro/?v=20260624-v482-live-ui-api-error` rejected a shortened OpenAI-looking dummy key with Japanese format error, kept `譛ｪ謗･邯啻, kept `main` blurred/pointer-locked, and did not show raw provider errors or `Engine 縺ｧ襍ｷ蜍描.
- [x] Hugging Face Spaces deploy verification for v4.8.2: `https://furuyan-nano-banana-pro.static.hf.space/?v=20260624-v482-api-preflight-jp` returned title `Nano Banana Pro v4.8.2`, asset `assets/index-BPKQnvko.js`, Japanese API-key errors and unverified labels present, raw OpenAI error text and old English format error absent.
- [x] Hugging Face Spaces live UI API-error proof for v4.8.2: `https://furuyan-nano-banana-pro.static.hf.space/?v=20260624-v482-live-ui-api-error` rejected a shortened OpenAI-looking dummy key with Japanese format error, kept `譛ｪ謗･邯啻, kept `main` blurred/pointer-locked, and did not show raw provider errors or `Engine 縺ｧ襍ｷ蜍描.
- [x] GitHub Release creation (v4.8.2): `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.2`.
- [x] Release ZIP download and `C:\nano-banana-pro-main` local sync for v4.8.2: package version `4.8.2`, HTML title `Nano Banana Pro v4.8.2`, API preflight test present, no nested duplicate folder.
- [x] v4.8.3 final docs status: this HANDOFF and root `PLAN.md` now record the released state and remaining unverified ChatGPT Web regeneration.

## Next Steps
- For future prompt-routing/style-diversity work, keep ChatGPT Web and OpenAI API on the same ChatGPT-family prompt, and keep Gemini Web and Gemini API on the same Gemini-family prompt. Do not split them by surface.
- Treat scenario/title/dialogue mismatch as a hard failure, not a tolerable style variation. The model must not replace the story with a different conflict, setting, sequence, ending, or punchline.
- Do not reintroduce the removed STEP2 emotion-diversity/spec-down rule unless the user explicitly asks for it. The next visual check should focus on whether the ChatGPT Web copy prompt preserves scenario/dialogue/style/identity in a fresh Web generation.
- Run a full backup only when the user explicitly requests backup.
