# HANDOFF.md

## 現在のステータス (v4.3.8 - デプロイ確認待ち)
- **Completed** (完了)
  - シナリオ生成プロンプトを本物の v4.2.7 コミット時の内容に完全復元。
  - セリフが一言だらけになってシナリオがスカスカになるバグを防ぐため、対話の充実化（簡潔かつテンポの良いコミカルな会話）を getScenarioPrompt 内で強化。
  - ト書き（状況描写）が薄くなり画像生成AIが背景やアクションのディテールを拾えなくなるのを防ぐため、ト書きの描写量を80〜150文字程度確保するルールを追加。
  - 学校外（うどん店などの店舗や観光地）でのサエコの「校則違反没収オチ」を厳重に禁止するルールを `getScenarioPrompt` に追加し、ペルソナ引力による薄いオチを修正。
  - 感情絵柄（HIGH_SATURATION, IMPACT）でのビビッドな極彩色を維持しつつ、不自然な虹色のスピードラインや多色ノイズを `NOISE REDUCTION` セクションのネガティブ指定（除外プロンプト）で強力に排除。
  - バージョン番号を **v4.3.8** に引き上げ、`package.json`, `index.html`, `src/lib/constants.js`, `README.md` (ChangeLog) の同期を完了。
  - 本番ビルドテスト (`npm run build`) による文法・ビルドエラーが無いことを確認。
  - 不要な一時ファイル (`src/lib/temp_v4.2.7_real.js`) のクリーンアップを完了。
- **In Progress** (進行中)
  - 本番環境（GitHub Pages）へのデプロイおよび GitHub Release 等のリリースフローの実行。

## 今回の変更内容 (v4.3.8)
- [x] **シナリオ対話・ト書きの充実化とペルソナ引力抑制**
  - セリフが一言だらけになるのを防ぎ、テンポの良いコミカルな対話を指示。
  - ト書きを詳細（80〜150文字）にし、背景やポーズのディテール・色彩表現を向上。
  - 学校外での「校則違反没収」ペルソナオチを厳禁化。
- [x] **極彩色の維持と色彩ノイズ（虹色スピードライン）排除**
  - 感情絵柄のビビッドな彩度をそのまま維持。
  - ネガティブプロンプトにて `multicolored speed lines` や `rainbow streaks` を強力に除外。
- [x] **バージョン同期**
  - アプリ全体でバージョンを `v4.3.8` に同期。

---

## 残りのタスク (Remaining Tasks)
- [ ] 本番環境（GitHub Pages）へのデプロイ (`npm run deploy`)
- [ ] 変更内容のコミットとプッシュ (`git add -A && git commit` & `git push origin main`)
- [ ] タグ打ち (`git tag v4.3.8` / `git push origin v4.3.8`) および GitHub Release 作成
- [ ] ZIP検証と C ドライブローカル展開バックアップ (`C:\nano-banana-pro-main` の差し替え)
- [ ] フルバックアップスクリプト (`backup_full.ps1`) の実行

---
