## Note本文・図版・履歴の再照合と修正完了 — 2026-09-25

- 対象は既存Note `https://note.com/happy_duck780/n/ndf063558c1f5`。ユーザー承認済みの訂正を更新ボタンで公開し、公開ページを読み戻した。今回アプリの再デプロイ・新規バックアップは実施していない。
- 全工程図と思考図を各1枚のv6.6.0へ差し替え。記事旧図はv6.4.2/v6.4.1、ローカルPNGも古かったため、生成元で台詞/話者分離・横長縦1列・衣装連続性・最大3回修正・警告付き最良候補・生成ごとの最終1件保持を同期し、両SVG/PNGを再生成した。READMEの現行版表示も6.6.0へ同期。これらのローカル文書差分は未コミット。
- 記事本文はAPIサイズ、白黒の汎用原稿ルール、STEP3再構築、品質修正の初回含む最大4枚と停止、Web/APIの区別、現行公開先をコードと照合して訂正。編集途中の重複文と見出しへの本文混入を除去した。
- 更新履歴の日付・版番号189見出しを点検。太字内の改行0件。本文とリンクを保持し、途中切れのv6.1.7末尾のみ既存リリース文のCRLF正規化の事実へ復元した。
- 公開後DOM検査: 全工程図1枚・思考図1枚・旧図0枚、本文画像15点・埋め込み20件を維持。編集前の本文リンク42件に欠落なし（公開ページでは画像拡大リンク等が追加される）。公開画面で図のv6.6.0表示、新旧履歴の日付/説明改行を目視確認。
- `node --test tests/workflow-diagram.test.mjs` 4/4成功。新しい現行契約テストは修正前に失敗、修正後に成功。`git diff --check`成功。文書/図のみの修正なので有料API生成・アプリ全体の再テストは追加していない。

## v6.6.0 公開完了: 台詞・コマ割り・衣装連続性 — 2026-09-25

- 最新依頼: 受け入れた現状で公式デプロイ、FourPanel note更新、フルバックアップを実施。対象はNano Banana Pro。前回からの未公開台詞・コマ割り修正と最新の汎用衣装連続性修正を含む。リモートv6.5.9タグが既に存在するため、v6.6.0として完了。
- 修正: 服と付属品の有無/数/形/取付位置を人物ごとに一度確定し全コマ共有。台本上の着脱等と遮蔽/画面外/短縮を区別する。両provider・カラー/白黒・資料画風・長文でも保持。圧縮は衣装原文を上位に残し、機械注入した同一衣装文だけを各Actionから重複排除する。
- 検査: `wardrobe_continuity` は参照画像の有無に依存しない。異なる2コマの同じ可視部位・異なる状態・2つの人物同定根拠・台本上の変更なしを要求し、曖昧なら未確認として修正対象から除外。衣装比較報告の欠落も未確認。単一場面にコマ間比較を要求しない。
- 証拠: 修正前は新規重点テスト3件が失敗、修正後は78/78成功。全体729/729成功（`output/wardrobe-full.log`）。資料画風の実際の分岐 `SeriousDocumentary` を含む8件も成功。Gemini既存の無条件 `NO changes` と台本上の着脱の矛盾もRED→修正し、最終関連24件・lint/build・`git diff --check`成功。既存の大容量chunk警告のみ残る。
- 承認済み追加1枚: ユーザーの「OK」後、同じシナリオ・参照2枚・OpenAI Sunburst/xhigh・2240×3168・自動修正OFFで実行。STEP3再構築後15,687字の指示に汎用衣装ルールが入ったことを確認し送信。人物名・特定衣服を使った補正はなし。目視ではアカリの肩まわりは1/3/4コマで一致し、サスペンダーの途中出現なし。横長4コマ縦1列・7台詞縦書き・話者名混入なしを維持。ただしリンのボトムは台本のパンツでなくスカートであり、全衣装の完全一致とはしない。実画像はOpenAIのみ、Geminiはローカル契約検証のみ。
- 内部QA: 同じ画像の再検査まで完了、画像は計1枚のみ。衣装比較の報告欠落・人物対応・小道具の向きは未確認として警告付き採用。報告欠落を見逃さない新ガードが実際に動作した。返却例補強後の回帰テストを含む最終QA関連95/95成功（`output/wardrobe-final-qa.log`）。この返却例補強後の実API再検査は未実施であり、自動検査まで合格とは主張しない。API値は取得・表示・保存せず、内蔵ブラウザの同一タブと接続を保持。
- 公開結果: v6.6.0公式release/deploy完了。Pages `https://furuyan1234.github.io/nano-banana-pro/`、GitHub Release `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v6.6.0`。コミット `ea036b5241b6fd1fca6a300fb334c0c211a68010`。公式transaction receipt `C:\Users\sx717\Antigravity\backups\release_receipts\nano-banana-pro-v6.6.0-2026-09-25_203220.json`。公開コピー `C:\nano-banana-pro-main`、7/7コピー検証済み。
- Note: 既存記事 `https://note.com/happy_duck780/n/ndf063558c1f5` を更新。v6.6.0タイトル・履歴を公開後readbackで確認。画面は「記事が公開されました」と完了を示した一方、記事には「あなたにだけ表示されています」と表示されたため、既存の本人限定公開範囲を変更せず維持。
- Full backup: 公式経路で `FULL_BACKUP_VERIFIED apps=7`。`C:\Users\sx717\Antigravity\backups\antigravity_full_backup_2026-09-25_203649.zip` (133 MB)、対応manifestあり。ローカルとDriveのSHA-256は `F60E8DB3FF7A1D72E40AB300EC4AB06CA3F792F00C4490AC0B96A84B78E120BD` で一致し、`.backup_full.lock` は解放済み。

## ローカル修正・画像検証済み: 台詞境界・縦書き検査・縦1列コマ割り — 2026-09-25

- 依頼: 話者名のフキダシ混入／横書き化を汎用修正。追加でWeb貼付画像が横長縦1列でなく2×2になる問題も修正する。今回のデプロイ・note・バックアップは未実施。
- 原因: 閉じた「」を抽出できない台詞行がコロン形式へフォールバックし、話者名ごと印字本文になった。JSON引用符未エスケープ、Geminiの制御語置換、長文圧縮による本文変形も再現した。ChatGPT用PAGEには縦積み横長コマ指定が欠落し、Geminiにだけ存在していた。
- 共通境界: `bubble-text.js`で本文をJSONとして往復・括弧検査。STEP2構造検証、STEP3完成、コピー、Web渡し、OpenAI/Gemini API送信前で不正本文を拒否する。印字本文と尻尾の話者メタデータを分離し、正当な本文内の名前・引用・制御語は保持する。
- コマ割り: 共通PAGE契約で4本の全幅横長コマを縦1列に指定し、2×2・横並びを禁止。カラー／白黒、両provider、圧縮経路で保持する。
- 追加原因: 角括弧なしの `Camera:` 行をカメラ抽出が無視し、既定の望遠指定などが台本のローアングルと競合していた。両provider共通の抽出で明示カメラを保持し、台詞内のCamera文字列は除外する回帰テストを追加した。
- 実API初回: 内蔵ブラウザの既存タブ、ユーザー入力済みOpenAI接続と参照2枚を使用。提供された失敗プロンプトから同じ場面・台詞を復元し、4コマ目だけ壊れた話者ラベル／括弧を正した入力でSunburst/xhigh・2240×3168・自動修正OFFを1枚実行。目視では7台詞が縦書き、4コマ目の名前混入なし。4本の横長コマ、人物同一性、俯瞰／ローアングル／傾き／奥行きとギャグの演技を確認。細部の描画忠実性を全面合格とまでは判定しない。
- 自動検査: 初回の縦書き誤判定を受け、台詞中の隣接2文字の画像座標と判定の一致を必須化。根拠不足は修正対象でなく未確認にし、台本なしの転記にも同じページのコマ拡大を渡す。手動編集で行頭を字下げしても本文・縦書き検査を維持する。
- 承認済み追加1枚: ユーザーの「好い」後、修正版をSTEP3から再構築し、同じ参照2枚・Sunburst/xhigh・自動修正OFFで生成。実寸2240×3168。目視で横長4コマ縦1列、7台詞すべて縦書き、4コマ目に話者名なし。俯瞰／低い視点／背後の前景／4コマ目の超広角と身体演技は保たれ、4コマ目の料金パネルも描かれた。既定望遠との競合は解消。内部QAの横書き誤判定も再発なし。ただしリン等の服装細部は台本と不一致、参照一致と4コマ目小道具の向きは内部QAで未確認のため、画像全体の完全合格ではない。
- 検証: 全体723/723成功（`output/dialogue-full-verified.log`）。重点49件、カメラ関連46件、圧縮契約7件、最終画像QA関連59件も成功。最終lint/build・`git diff --check`成功。buildの既存大容量chunk警告は残る。実画像はOpenAIのみで確認し、Web版への貼付生成／今回のGemini実画像生成は未実行。旧コピーへ修正は遡及しないため、Web用はSTEP3再構築後の文章を使う。今回の長い検証台本は16,285字で、15,000字のWeb目安は絶対上限ではなく、必須台本は切り捨てない。
- 秘密情報: APIキー値は取得・表示・保存しない。内蔵ブラウザのタブと接続を保持する。

## ローカル候補: A4大の既定化と俯瞰時の頭切れ防止 — 2026-09-25

- 最新依頼: 画像サイズの今後の既定をA4大2240×3168へ変更し、実API画像の1コマ目でミクの頭部・髪が上端に切れた問題を汎用修正する。
- 実API確認: GPT-6 Lunaでシナリオを作成し、GPT Image 2.5 Sunburst/xhigh・A4標準1120×1584・参照キャラシート2枚・自動修正OFFで1枚生成した。カメラの高低・左右・寄り引き・奥行きと、レンズ側へ手のひらを向けない演技は目視で確認できた一方、1コマ目のミクの頭切れが残ったため画像全体は不合格。APIキー値は取得・記録していない。
- 追加実API確認: ユーザー承認済みの追加1枚をGPT Image 2.5 Sunburst/xhigh・A4大2240×3168・同じ参照2枚・自動修正OFFで生成した。目視では1コマ目のミク（左の金髪）の頭髪がコマ内へ収まり、枠線による切断・枠内外の位置ずれは見えない。添付スクリーンショットの原寸再確認で、生成画像内の大きな題名は白地・黒文字で長方形の黒枠なし。上側の黒帯と小さい白題名は画像外のアプリUI。内蔵QAの「黒枠付き第5コマ」は、白い題名帯と極太ゴシック文字を誤認した偽陽性として撤回する。人物参照一致と作中印字の一部はQA上`unverified`。背景分離規則は画像送信後に追加されたため当該画像では未検証。
- 根本修正: A4合成はコマ内部を切り取っておらず、生成元と画像QAに枠際の輪郭連続性契約がなかった。通常は頭上余白を取る一方、絵的に有効なら枠外への飛び出しを許可し、その場合は頭・髪を一続きの前景として枠線を背後へ回し、切断・断片重複・枠内外の位置ずれを禁止する。共通品質契約、俯瞰SHOT EXECUTION、長文短縮、画像QAの独立boundary軸へ同じ条件を入れた。明示された顔アップの切り取りは保持する。A4大の既定化も旧実装でRED、新実装で焦点12/12 GREEN。
- 追加品質規則: 被写界深度ぼけ後も主役人物が背景へ埋もれる場合、指定照明・場所の根拠・白黒トーンを保ったまま背景色をさらに薄く低彩度にし、人物のGペン輪郭を強める共通規則を4コマと1枚絵へ追加。1枚絵Webコピーは旧8,000字テストを廃止し、添付ファイル化を避ける経験的10,000字ソフト上限へ変更。現行出力は8,278字。APIガード32,000字とは分離。
- ローカル検証: 背景分離、1枚絵10,000字、枠際生成・QA、4コマ長文圧縮を含む最終焦点76/76、全Node 685/685、ESLint警告0、production build、`git diff --check`が成功。1枚絵プロンプト実測8,278字。build警告は既存のBrowserslist更新案内、Gemini静的/動的import、chunk-sizeのみ。
- 未完了境界: 頭切れ修正は追加有料API画像の目視で確認済み。背景分離の新規則は送信後追加のため実画像未検証で、さらに有料生成する承認はない。release/deploy、公開note、配布コピー、バックアップは依頼範囲外。

## v6.5.8 Web用制作情報JSONとAPI画像メタデータの経路分離・公開作業 — 2026-09-25

- 最新依頼: v6.5.4でWeb貼付用JSONがAPI画像用メタデータ経路へ誤統合され、画像生成前は無効な空白帯に見える回帰を修正する。文章ルールだけに依存せず、全設計でルール違反と手抜き設計をコード境界・正負テスト・証拠ゲートにより止める。
- 根本原因: Web用の事前準備レコードと、実際のAPI画像・モデル・出力ハッシュを持つAPI画像来歴を「制作情報」という名称だけで同一視した。v6.5.4の変更でWeb側も`generatedImage`、API用ビルダー、`api_image_generation`を要求し、最終プロンプト完成時点で保存できた従来契約を壊した。画面用途と履歴を照合する回帰テストがなかった。
- 修正: Web用`buildWebGenerationMetadata`とAPI用`buildGeneratedImageMetadata`を分離。Web用は出力画像・APIモデル・フォールバック・生成ID・出力ハッシュを受け取らず、`record_type: web_generation_companion`／`workflow_mode: manual_web_generation`を記録する。API用は`record_type: api_image_generation`と実画像ハッシュを保持する。Web用ボタンは`finalPrompt`だけで有効になり、ラベルとエラー状態もAPI画像保存から分離した。
- 機械的防止: `tests/generated-image-metadata.test.mjs`へ、出力画像なしのWebレコード生成、API専用項目の不在、Webボタンの有効条件、WebハンドラからAPIビルダー・画像変換・`generatedImage`依存を排除する正負テストを追加。グローバル`C:\Users\sx717\.codex\AGENTS.md`へ、全Codex設計でルールをコード境界・正負テスト・監査証拠に変換し、未強制の絶対ルールを完了扱いしない規則を追加した。
- 検証: 回帰は旧実装で欠落export、API用`record_type`欠落、禁止API項目の受理をそれぞれRED確認後GREEN化。焦点13/13、全Node 679/679、ESLint警告0、production build、`git diff --check`が成功。内蔵ブラウザでOpenAI `gpt-6-luna`のSTEP2とSTEP3を実行し、画像0件の状態でWeb用JSONボタンが`disabled=false`・`opacity=1`、APIボタンまで12pxであることを確認。ボタン操作後に`保存完了！`へ変化し、以前の薄い空白帯は解消した。API画像生成は本修正の受入条件ではないため実行していない。
- 公開境界: ユーザーはv6.5.8の公式release/deploy、既存FourPanel note `ndf063558c1f5`更新、公式フルバックアップを明示承認済み。公開・note・バックアップはそれぞれ独立した証拠で完了判定する。


## v6.5.7 STEP4制作情報案内の配置修正・公開作業 — 2026-09-25

- 最新依頼: 手動JSON保存欄とAPI生成欄の間にあった制作情報の説明と空きを整理し、内蔵ブラウザで実API生成を確認後、公式release/deploy、既存FourPanel note `ndf063558c1f5`更新、公式フルバックアップまで完遂する。
- 修正: 制作情報の説明を手動JSON保存欄からAPI生成ボタン直下へ移し、`生成画像には、安全化した制作情報を保存します。`へ短縮した。API生成ボタンの下余白を削除し、説明とAPI設定を一続きの操作群にした。制作情報の実データ、APIキー・参照画像本体・人物／場所の解析全文を保存しない条件は変更していない。
- 実API検証: Codex内蔵ブラウザでSTEP2を実行し、`gpt-6-luna`から下方向フォールバックした`gpt-5.6-luna`を採用。STEP4はGPT Image 2.5 Sunburst/xhigh・A4標準1120×1584で初回と最大3回の自動修正を完走し、最良候補を品質警告付きで採用した。タイトル、全台詞、手、小道具はアプリQAで合格。2コマ目の肩越し構図は自動QAで未解消警告が残ったため、画像全体の完全合格とは扱わない。画面ではAPI生成ボタン、制作情報の説明、API設定の順と、不要な空きがないことを確認した。
- 公開境界: ユーザーはv6.5.7の公式release/deploy、既存note更新、公式フルバックアップを明示承認済み。

## README・公開noteの関連システム／シナリオモデル同期 — 2026-09-24

- 最新依頼: Nano Banana Proの関連システムへ物語バズメーカーを追加し、シナリオルーチンの説明をGPT-6追加後の実装へ合わせる。今後は各アップデートでREADMEと公開noteを現行仕様と照合し、齟齬があれば修正する。今回はGitHubとローカル配布コピーまで同期する。
- 確認結果: READMEのOpenAIルートが旧6モデルのままで、現行カタログのGPT-6 Sol／Luna、GPT-5.6 Terra／Lunaが欠落していた。公開noteはGPT-6 Sol／Lunaの選択と下方向フォールバックを説明済みだが、全候補順と物語バズメーカーへの導線がない。
- 変更境界: ドキュメントと同期検査だけを更新する。API実行、アプリ挙動、バージョン番号、GitHub Release、Pages、フルバックアップは変更しない。公開noteの編集は操作直前の確認後に限定する。

## v6.5.6 肩越し前景人物と吹き出し方向の整合 — 2026-09-24

- 最新依頼: 2コマ目で台詞と人物の対応は合っているのに、日本語の右→左読順と肩越し構図が逆転した実画像を根本修正し、Lunaで実API画像検証後、公式release/deploy、FourPanel note `ndf063558c1f5`更新、公式の可視黒い窓フルバックアップまで完遂する。
- 根本原因: Cameraに「手前の人物」が明記されていても、肩越し主体の抽出がその表現を認識せず、台詞順の2人目を前景の肩へ選んでいた。このためCamera本文とEYE-LINE lockが互いに矛盾し、明示済みのB1右／B2左配置への画像モデル追従も不安定にしていた。
- 汎用修正: 肩越しCamera内の`手前`・`前景`・`foreground`・`camera-side`と結び付くキャストを先に採用し、既存の背後指定や台詞順フォールバックより優先する。明示された背面・肩越し契約は通常QAと独立して前景の後頭部／肩とカメラ位置を再監査する。具体的カメラ不具合をOCR由来の未確認読順より先に修正し、修正版で構図が直り、吹き出し順の退行や新たな具体的不具合がなければその候補を優先採用する。固有名、特定コマ、台詞のベタ書きは行わない。
- 実API検証: 開発版の既定`gpt-6-luna`で同じ肩越し条件を再構築し、Sunburst/xhigh・A4標準1120×1584を1枚生成。2コマ目は右前景の人物を後頭部・肩から捉え、左中景の相手とスクリーンへ奥行きを作り、右側B1／左側B2の日本語読順も保持した。独立カメラ監査は合格、具体的不具合0件。手指・眼鏡・一部小道具は証拠不足の`unverified`として残し、追加の有料修正画像は生成していない。履歴はこの生成の最終画像1件だけ増加。画面上の35px帯は透かしではないため「フッター」へ改称した。
- 公開境界: ユーザーはv6.5.6の実API確認、公式release/deploy、既存note更新、公開後の公式フルバックアップを明示承認済み。修正前保全バックアップ`2026-09-24_195844`は7アプリ・ローカル/Drive同一SHAで独立検証済み。

## v6.5.3 面文字と発話の分離・公開作業 — 2026-09-24

- 対象: 下記「ローカル修正・API検証済み: 面文字の誤発話と周辺バグ」の修正。ユーザーが公式deploy、既存FourPanel note `ndf063558c1f5`の更新、公式の可視黒い窓フルバックアップを明示依頼した。
- 修正と画像検証: 全Node `660/660`、警告0 lint、production build、実API画像1120×1584で対象の誤吹き出し解消を確認。衣装と一部動作の画像モデル追従差は残る。追加生成は行わない。
- 次工程: v6.5.3を公式release transactionで公開検証し、noteの更新履歴を狭く追記・公開readbackしてから、`backup_launch.bat`を可視黒い窓で実行し独立検証する。工程ごとの証拠を追記する。

## v6.5.2 STEP2 GPT-6 Sol/Luna routing and price display — 2026-09-23

- 最新依頼: STEP2で利用可能なGPT-6 Sol／GPT-6 Lunaをフォールバック候補へ組み込み、料金・括弧書き表示を整えたうえで、Nano Banana Proを公式deploy、既存FourPanel note `ndf063558c1f5`更新、公式の可視黒い窓フルバックアップまで完遂する。
- 実装: 既定のGPT-6 Astraは維持し、フォールバックは選択位置より後ろだけへ進む。順序は`gpt-6-astra → gpt-6-sol → gpt-5.6-sol → gpt-5.6-terra → gpt-6-luna → gpt-5.6-luna → GPT-4.1系 → GPT-4o`。GPT-6／GPT-5.6系は現行Chat Completionsパラメータを使用する。選択肢の括弧書き、参考単価、除外条件（ツール、キャッシュ、税、長文コンテキスト、処理モード差額）を2026-09-23の公式短文コンテキスト・標準処理価格に同期した。
- 実API: OpenAIでGPT-6 Lunaを明示選択し、STEP2の開始・最終採用とも`gpt-6-luna`で完走。最初のシナリオ生成は入力19,790／出力5,482トークン、ペイオフ監査は入力4,086／出力1,080トークンで、当該検証ではGPT-6 Astraを使用していない。
- 候補検証: 関連19/19、全Node 648/648、ESLint警告0、production build、`git diff --check`、strict pre-deployが成功。buildには既知のBrowserslist期限、mixed import、bundle-size警告だけが残る。v6.5.2のコミットは`7ff5fca`。公式transactionは初回にこの記録の版未更新で検証停止したため、この追記を同コミットへamendしてから、同じ公式手順を再実行する。

## 自由入力の内部ラベル隔離・結末固定 — 2026-09-23

- 最新依頼: どの自由入力テーマでも題名・台詞へ「手入力」が混ざる問題を根本修正し、同じ種類の設定漏れが残っていないか監査する。ローカル実装・実API／実画像確認を完了後、ユーザーがv6.5.1の公式release/deploy、既存FourPanel note `ndf063558c1f5`更新、公式の可視黒い窓フルバックアップまで明示承認した。追加の有料生成は行わない。
- 根本原因: 自由入力時の内部カテゴリ値を`手動入力`へ固定し、その値をニュース用Topic Lockへ無条件挿入していた。ニュース専用の日付・鮮度・話題選定規則も自由入力へ混在し、Topic欠落時は内部カテゴリが題名フォールバックになった。さらにSTEP3が解決済みの結末より生成文の`Punchline`を優先し、シリアス判定済みでも`感動詐欺`等へ戻る経路があった。
- 汎用修正: 自由入力とニュースのプロンプト契約を分離し、自由入力はユーザー提供題材だけをTopic Lockへ渡す。UI・入力方式・内部プレースホルダー・前回題材を作品内容にしないSource Boundaryを追加し、タイトル欠落時はユーザー題材へ戻す。原文にない`手入力`、`自由入力`、`ユーザー提供トピック`、`ユーザー提供の創作題材`、`最新ニュース`、`Custom Scenario`、`Generic Background`等が生成メタデータ、本文、SNS解説へ残れば共通サニタイザーで除去する。品質ループ上限後もfatal終了へ迂回せず、品質点の最も高い候補を保持し、残存ラベルだけを決定的に補正して再検査する。原文自体に同語がある正当な題材は許可する。
- 結末固定: 内部で確定した結末種別をSTEP2の表示、ペイオフ検査、STEP3組立の正本にする。`SeriousAuto`はシリアス候補だけを採用し、固定選択と非互換なモデル出力は選択値で補正する。既存の短縮ラベル（例: `静寂型`）は同義として保持する。
- シュール契約: 通常ギャグの因果回収ゲートを静寂型（シュール）へ強制しない。物・設備・舞台の大破壊、因果の飛躍、支離滅裂、不条理を、真顔・沈黙・間で目に見える笑いにできれば、伏線、自然な予測、`shift_kind`、新事実だけを理由に失格へしない。説明・標語だけで終わる出力は従来どおり不合格。内部ラベル隔離をギャグの無難化へ流用しない。
- 実API・実画像: 自由入力`真夜中の市役所で巨大プリンが理由なく庁舎を押し潰す`、固定オチ`静寂型 (シュール)`でSTEP2を実行。運用上は検証時Terraを明示選択すべきところ、誤ってAstraで本生成＋ペイオフ監査を実行したため、このモデル選択ミスは再発禁止。生成物`庁舎が潰れても押印`は破棄せず採用し、原因説明・伏線回収・教訓・夢オチなし、庁舎の大破壊と無言の押印を保持した。同じシナリオを再生成せずSTEP3へ渡し、Sunburst/xhigh・A4標準1120×1584で画像1枚を生成。タイトル、2コマ目の巨大プリンによる庁舎破壊と`ぐしゃっ`、3・4コマ目の瓦礫内の無言押印、4コマ目の5人を目視確認。内蔵QAは具体的欠陥を検出せず、手指・面文字・小物面向きの証拠不足だけを`unverified`として最良候補を警告付き採用した。追加画像生成なし。ユーザー提供のWeb貼付版も同じタイトル、4コマ、全コマ無言、2コマ目だけの`ぐしゃっ`、破壊規模、3コマ目2人、4コマ目5人と押印継続を満たし、両版とも因果説明を後付けしなかった。Web版は擬音が左、API版は右などの構図揺れだけで、狙いは維持した。
- 検証モデル運用: 公開時の初期選択はAstra、開発・実API検証はTerraを明示選択し、検証後はAstraへ戻す。既定値がAstraであることを、検証モデル選択の代用にしない。
- 回帰: `tests/manual-topic-isolation.test.mjs`を追加。自由入力プロンプトの隔離、ニュース規則の維持、欠落Topicフォールバック、内部ラベル出力の改善・最良候補保持・決定補正と原文明示時の許可、非互換オチの遮断、STEP3の解決済み値優先をRED→GREENで固定した。`tests/scenario-payoff-quality.test.mjs`へシュール専用判定、`tests/sns-explanation.test.mjs`へSNS解説の入力方式メタ語隔離を追加。全Node `648/648`、警告0 lint、production build、strict pre-deployが合格。buildに既知のBrowserslist期限・mixed import・large chunk警告だけ残る。

## v6.5.0 実体人物と劇中複製の分離 — 2026-09-22

- 最新依頼: 3コマ目のヒカリ、4コマ目のアカリが等身大で重複した実画像を根本修正し、同一シナリオのAPI版とWeb貼付版を比較する。両方で直っていれば公式release/deploy、既存FourPanel note `ndf063558c1f5`更新、公式の可視黒い窓フルバックアップまで完遂する。
- 根本原因: 既存の`CAST INSTANCE LOCK`は名前ごとに1体を要求していたが、Camera、Action、台詞、リアクションに分散した同一人物の記述を同じ身体へ再結合する指示が弱かった。さらに4コマ目は実体5人と箱内の極小5人を同じ`TOTAL 5`で表し、意図したミニチュアと禁止すべき等身大複製が矛盾していた。
- 汎用修正: 各人物への全記述を同じ等身大の実体1体へ統合する。台本に明示されたミニチュア、鏡像、写真、画面内人物だけを`DIEGETIC REPLICA LAYER`へ分離し、指定容器・面の内側にある小型表現に限定する。QAも`cast_instances`と`cast_replicas`を別々に要求し、欠落、場所外、等身大化を合格にしない。
- ローカル回帰: 失敗を再現するテストを先に追加し、汎用人物名による抽出、ChatGPT/Gemini両プロバイダの最終プロンプト、実体／複製別QA、Web短縮上限を確認。関連98/98、全Node 638/638、警告0 lint、production build、strict pre-deploy、`git diff --check`が成功。版表示、README、release noteはv6.5.0へ同期した。
- 実画像: 内蔵ブラウザのSunburst/xhigh API・A4標準1120×1584・自動修正OFFで1枚生成。3コマ目は実体5人ちょうど、4コマ目は実体5人ちょうど＋展示箱内の極小5人で、ヒカリ／アカリの等身大重複なし。ユーザー提供の同一Web貼付版も同じ構成で合格し、API/Web比較条件を満たした。
- 公開境界: ユーザーは、API版とWeb貼付版の双方で人物重複が解消したため、公式release/deploy、既存FourPanel note `ndf063558c1f5`更新・公開、公式の可視黒い窓フルバックアップを明示承認済み。

## v6.4.9 作中面文字と発話の分離 — 2026-09-22

- 最新依頼: 案内板の`空襲警報による配送遅れ`がサエコの吹き出しに見える不具合と、実台詞の話者対応混乱を汎用修正し、同一シナリオをWeb貼付版とAPI版で比較する。問題がなければ公式deploy/release、FourPanel note更新、黒い窓フルバックアップまで完遂する。
- 根本原因: `hasSpokenQuotePostContext()`が`「...」の文字を読み`の素の`読み`を発話手掛かりとして扱い、案内板・画面・メニュー等の列挙文字を吹き出しへ昇格させていた。名前付き面の文字・文言・表示・案内・内容・項目・メニュー・ラベルを読む／確認する文脈は視覚情報として除外し、`読み上げる`等の明示発声は維持した。
- 回帰: 汎用名と汎用表示文字を使ったパーサー回帰、ChatGPT/Gemini両プロバイダの最終プロンプト回帰を追加。重点54/54、全96 Node test files、警告0 lint、production buildが成功。公開候補はv6.4.9へ同期済み。
- 実画像: 内蔵ブラウザで同じ`キーウの品切れ`を再構築。最終プロンプトは2コマ目の案内板文字をActionに保持し、台詞をヒカリ／サエコの2本だけへ修正。Web貼付版では作中文字と2吹き出しを分離。Sunburst/xhigh API版はA4標準1120×1584へ正規化し、2コマ目の読順転記が合格。生成モデル由来のしっぽ位置揺れは残るため実画像の目視確認を継続する。
- 料金: OpenAI公式Docsを2026-09-22に再確認。Astra $10/$50、Sol $4/$20、Terra $2/$12、Luna $0.20/$1.20、Sunburstは画像入力$8（キャッシュ$2）・出力$30、テキスト入力$5（キャッシュ$1.25）USD／100万トークンで据え置き。STEP2の基準日だけ2026-09-22へ更新した。
- 公開境界: ユーザーはv6.4.9の公式release/deploy、既存FourPanel note `ndf063558c1f5`更新、公式の可視黒い窓フルバックアップを明示承認。無関係な未追跡`public/diagrams/workflow-preview.png`は候補へ含めない。

## v6.4.8 4コマ目ペイオフ・背景文字・人物重複防止・FourPanel H3更新 — 2026-09-22

- 最新依頼: 4コマ目を標語で終わらせず意外な帰結へ寄せる汎用品質ゲートを追加し、Web/APIのA4比率と背景文字を揃え、同一人物の重複を全キャスト共通で防止する。供給されたFourPanel H3 ZIPをNano Banana側へ登録し、内蔵ブラウザの実API検証後、公式release/deploy、FourPanel note更新、公式黒い窓フルバックアップまで完遂する。
- 人物重複: 各コマの`CAST COUNT`直後へ、名前ごとに身体シルエット1体・奥行き位置1か所を割り当て、前景／背景や端の見切れをまたぐ2体目を禁止する汎用`CAST INSTANCE LOCK`を追加。画像QAも名前ごとの`cast_instances`、観測数、身体位置、識別手掛かりを要求し、期待値1との差を`cast_count`として報告する。ヒカリ固有のベタ書きではない。
- 実画像: 同じ修正版プロンプトをWeb貼付とSunburst/xhigh API・A4標準1120×1584・自動修正OFFで各1枚生成。API履歴9件目とユーザー提供Web画像の双方で4コマ目は5人を各1回だけ描画し、ヒカリ重複なし。背景文字量、2コマ目OTS、画面密度はほぼ同等。APIのOCRは「一ミリ」を「ニミリ」と未確認扱いにしたが、人物重複の具体的不具合は検出されなかった。
- FourPanel H3: 供給ZIP`ComfyUI_H3_FourPanel_NonLM_20260922104144.zip`（SHA-256 `4BF332F4A42349DDB569EC825EC578668CCB5E2E3D427FD5AA65AEAC7BF36566`）を同版Release assetへ、ワークフロー`FourPanel_NonLM_4step_20260922104144.json`（SHA-256 `6AE722C158E940CB4C69D8675E60B01EDD2ECD5044FF68040BD8FBEC19B7DC27`）をPages配布へ登録。供給verifierは全105ファイル合格。通常経路6区間・36秒・864×480・24fps、manifest complete/pass、184回帰、音声再精錬4ステップ・denoise 1.0。T2V/I2V/Ref2V製品とnote`n15e732b3147b`は対象外。
- 検証: 供給verifier 105/105、全Node 633/633、strict lint、production build、`git diff --check`、アプリpre-deployが合格。内蔵ブラウザのSTEP4で新ZIP/JSON名、音声4ステップ・denoise 1.0、6区間36秒、184回帰、同梱／別途依存、3文書名を確認した。
- 残工程: クリーンな公式preflight、公式v6.4.8 transaction、FourPanel note`ndf063558c1f5`のブロック安全更新と公開読戻し、公式黒い窓フルバックアップと独立検証。

## v6.4.7 公開既定Astra固定・STEP4公式料金表示 — 2026-09-22

- 最新依頼: 検証時に選んだTerraが公開時のSTEP2既定へ持ち越される不具合を直し、公開時は常にAstraへ戻す。STEP4はOpenAIシナリオボタンと同様に最新API料金をボタン内へ表示し、ボタン下の説明文は削除する。内蔵ブラウザで実API検証後、公式deploy/release、FourPanel note更新、公式フルバックアップまで完遂する。
- 根本原因と修正: `nano-banana-pro:scenario-model-id`を`localStorage`へ保存・復元していたため、検証用Terraが次回公開アクセスでも初期値になっていた。モデル選択を現在のページのReact stateだけへ限定し、再読込／次回アクセスは常に`gpt-6-astra`から開始する。失敗時のみ現在位置より下位へ進む既存フォールバックは維持した。
- STEP4: 選択中のGPT Imageモデルごとに公式API参考単価を生成ボタン内へ表示し、基準日を2026-09-22とした。Sunburst/Flareは画像入力$8（キャッシュ$2）・出力$30、テキスト入力$5（キャッシュ$1.25）、GPT Image 2は画像入力$4（キャッシュ$1）・出力$15、テキスト入力$2.50（キャッシュ$0.625）、すべてUSD／100万トークン。旧`最終プロンプトから毎回、新規画像を生成します`は削除した。
- 現在の証拠: 重点回帰はRED後に22/22 GREEN。内蔵ブラウザでTerra選択→再読込によりAstra復帰を確認。実STEP2 APIは`固定開始モデル: gpt-6-astra`、`最終採用モデル: gpt-6-astra`で完走し、フォールバックなし。STEP3後のSTEP4ボタンに料金と基準日が表示された。STEP2説明3段落はcomputed styleでmargin 0、line-height 12px、全体約104px→36pxを確認。Sunburst/xhigh・自動修正OFFの実STEP4を1回行い、A4標準1120×1584、タイトル103px・4コマ全体1446px・フッター35pxで完走した。公式release、note、フルバックアップは最終API・ローカルゲート後の次工程。
- 追加不具合と修正: ユーザー提供の実画像は1864×2696（比率0.691）と1024×1536（2:3）で、漫画原稿のA4系比率ではなかった。内部でA4・2:3・3:4が競合していたため、共通`manga-manuscript-format.js`へ210:297、1120×1584、2240×3168を一元化。現行GPT Image 2.5／2の任意寸法対応を使ってA4を直接要求し、プロンプト・正規化・保存も同じ契約へ統一した。A4重点回帰はRED→GREENで30/30。A4契約追加でWebコピー上限を4〜7文字超える2ケースは、吹き出し配置文の後付け後にも圧縮を再適用して解消した。
- 4コマ目の手増殖: 添付の旧v6.4.6プロンプトは、ヒカリへ「眼鏡を直す」「左手でハンカチ拭き」「右手で指す」を同じ最終瞬間に残す3役競合だった。シナリオ作成規則を人物ごと左右2役までへ限定し、組立時に過去の未指定手動作＋明示左右を検出したコマへ、最終左右だけを描き3本目を禁止する共通ルールを挿入する。QAも人物ごとの全可視手を棚卸しする。ユーザー提供の品川駅シナリオ全文を実際の4コマ目へ入れたChatGPT/Gemini回帰をRED→GREEN化。変更後の全Node 620/620、警告0 ESLint、production build、`git diff --check`、strict pre-deployが成功。さらに内蔵ブラウザで同じ全文を実API生成し、保存済み自動修復設定により初回＋限定修復の2候補を生成。採用画像はA4標準1120×1584で、アプリQAと目視の双方で4コマ目に余分・結合・欠落した手がないことを確認した。残る警告はOCRの「二十代／二十台」不確実性と人物参照・厳密なカメラ構図の未検証項目のみ。

## v6.4.6 背景ポスター・看板の自然な図柄保持 — 2026-09-21

- 最新依頼: 背景のポスターや看板を面ごと空白・灰色・ぼかし・モザイクにする過剰な文字回避を汎用修正し、実API確認後にv6.4.6を公式release/deploy、FourPanel note更新、公式の黒い窓フルバックアップまで完遂する。
- 根本原因: 共通の文字制約が未指定面を一律に`unlettered`と要求し、画像モデルが文字だけでなく面全体の図柄・配色・枠・アイコンまで消す解釈を許していた。Web/API共通の文字制約で、自然な非文字デザインを保持しつつ、明確な語句・数字・URLの捏造だけを禁止するよう修正した。題名・人物名・コマ番号・固有の看板文言による個別ルールはない。
- TDD・実API: 旧`unlettered`文言を検出し、新しい保持／禁止条件を両プロバイダーへ要求する回帰をRED→GREEN化（6/6）。STEP2は今回の検証指定どおり`gpt-5.6-terra`、STEP4はGPT Image 2.5 Sunburst/xhigh・1024×1536で1ジョブ実行。タイトル、全4コマ台詞、手、小道具、カメラ、100/1402/34pxのページ領域とv6.4.6透かしを確認し、ユーザーが生成画像を目視して合格と判断した。全Node 615/615、警告0のESLint、production build、`git diff --check`、版同期、機密・不要物監査も成功した。
- 次工程: クリーンな公式preflight、公式release、公開readback、FourPanel note `ndf063558c1f5`の狭い履歴更新、公式フルバックアップと独立検証を行う。無関係な未追跡`public/diagrams/workflow-preview.png`は公開候補へ含めない。

## v6.4.5 単独吹き出しの話者側配置・しっぽ終点固定 — 2026-09-21

- 最新依頼: `食事キャンセルの影`の3コマ目を基準に、単独吹き出しを話者サエコ側の空きへ置き、短いしっぽをサエコの口元／頭部へ結ぶ。複数吹き出しだけ従来の日本語右→左順を維持し、同じシナリオをAPIとWeb貼付で各1回実検証する。
- 根本原因: 単独吹き出しだけが強いB番号→話者終点ロックの対象外で、さらにChatGPT最終プロンプト圧縮時に`mouth/head`と近接人物への再割当禁止が落ちていた。全吹き出しを共通の終点ロックへ通し、単独は話者側の空き＋最短しっぽ、複数はB1右端＋後続を左へ置く汎用ルールへ分離した。題名・人物名・コマ番号・台詞の個別ハードコードはない。
- ローカル証拠: RED→GREEN回帰を追加。関連117/117、全Node 614/614、ゼロ警告ESLint、production buildが成功。既知のBrowserslist期限、mixed import、bundle-size警告だけ残る。
- 実画像証拠: 同じシナリオ・参照2枚で、APIはGPT Image 2.5 Sunburst/xhigh 1024×1536を1ジョブ実行し、Web貼付はユーザーが同時に1回実行した。両方とも3コマ目の単独吹き出し本体が左のサエコ側へ置かれ、短いしっぽが黒髪のサエコ前景へ向き、アカリへの誤接続は見られない。APIの内蔵QAもPanel 3の台詞・カメラを`ok`とし、`bubble_speaker`問題を報告しなかった。
- 残る警告: APIジョブはPanel 4の読順を検出して同一ジョブ内限定修正を1回行い、最良候補を警告付き採用した。最終QAにはPanel 4の「確保」を「確認」と転記した疑い、人物参照・背景サイン・面向きの未確認が残るため、API画像を全画質PASSとは扱わない。本件のPanel 3話者側配置としっぽ終点はAPI/Web両面で確認済み。検証後のSTEP2選択は通常運用の`gpt-6-astra`へ戻した。ユーザーはv6.4.5の公式release/deploy、FourPanel note更新、公式の黒い窓フルバックアップを明示承認済み。

## v6.4.4 QA文字領域分類・公開工程 — 2026-09-21

- 最新依頼: 作中の冊子文字を吹き出しとして数えるQA誤検出を汎用修正し、実API再検証、v6.4.4化、公式preflight、release/deploy、FourPanel note更新、公式フルバックアップまで完遂する。ユーザーはこの一連の外部操作を明示承認済み。
- 根本原因: 独立読順QAが画像内の文字領域を吹き出し、印刷物、キャプション、効果音へ分類せず、返却された全項目を吹き出しとして信頼していた。さらに初回修正では印刷物にも台詞用の中心座標を要求し、正しく印刷物と分類できてもコマ全体を未確認へ落とした。
- 汎用修正: 全可読領域へ `region_kind` と画素根拠 `container_evidence` を要求し、`speech_balloon` だけを読順へ使う。紙、冊子、看板、画面、ボード、包装などは矩形枠があっても `printed_object` として除外する。印刷物には台詞用の中心座標や判読可能な文字を要求せず、容器が判別できない場合だけ `uncertain` とする。失敗例の題名・人物名・台詞はproduction codeへ入れていない。
- TDD: 印刷物が判読不能かつ中心座標なしでも、1個の実吹き出しだけで読順が合格する回帰をRED→GREENで追加。実APIで見つかった無言コマへのしっぽ一覧要求も、吹き出し契約がゼロなら空一覧を正しく受理する汎用回帰としてRED→GREEN化。関連41/41テストが成功。
- 実API第1回: Terra作成済みの同じシナリオ、GPT Image 2.5 Sunburst/xhigh 1024×1536、参照2枚、自動修正OFFで生成履歴5→6。冊子の「全11話」を吹き出しとして数える誤検出は消えたが、初回修正が印刷物の座標を要求したため2コマ目は転記不完全。新画像の1コマ目では「五晩」が「五夜」へ変わる別の実生成ミスをQAが検出した。これを成功画像とは扱わない。
- 実API最終検証: STEP2は検証用の`gpt-5.6-terra`、STEP4はGPT Image 2.5 Sunburst/xhigh 1024×1536、参照2枚で実行。最初の画像要求はポリシー拒否となり、既存の同一ジョブ内自動補正1/5で成功した。看板「A出口」は吹き出しへ混入せず、実吹き出しはPanel 1〜3すべて読順`ok`、無言Panel 4もセリフ照合`ok`。全可視手端点は5本指・靴への置換なし、タイトルは無枠、ページ配置はタイトル100px／可変コマ全体1402px／透かし34pxでフッターを保持した。画像QAはキャラクター参照と小道具面の証拠不足を`unverified`として残したため、全画質合格とは扱わない。
- バージョン候補: package、lock、画面定数、HTML、READMEをv6.4.4へ同期し、`docs/releases/v6.4.4.md`を追加。READMEは、可変コマ高を保つページ正規化、追加ボタン撤去、印刷文字を除外する読順QAへ同期した。
- 次工程: 全テスト、lint、build、セキュリティ監査、コミット、クリーンpreflight、公式release、公開readback、FourPanel note `ndf063558c1f5`の狭い履歴更新と公開readback、公式フルバックアップと独立検証を行う。無関係な未追跡 `public/diagrams/workflow-preview.png` は公開候補へ含めず、公開工程後に元位置へ戻す。

## 吹き出し読順・しっぽ経路の共通修正と実API確認 — 2026-09-21

- Root cause: `extractPlacementRule` treated any Camera/Action foreground/background wording as a complete horizontal-position override. In the failed panel 3 this removed the stable dialogue-order speaker X layout even though no speaker-specific left/right positions were authored, and the image model then arranged bubbles by nearby speakers in reverse reading order.
- Generic fix: preserve explicit named left/right staging, but otherwise assign dialogue speakers right→center→left while retaining every scripted foreground/background layer, scale, framing and contact. This is shared logic with no scenario title, panel number, character name or dialogue hardcoding.
- Tail fix: every bubble now starts its tail on the lower, speaker-facing half near lower-center, uses the shortest unobstructed corridor, never crosses/overlaps/passes over heads, faces, hair or dialogue text, and ends at the mapped mouth/head. QA requires grounded root/path/endpoint evidence and rejects a crossed head/face/hair/text route even when the endpoint speaker is correct.
- Live proof: rebuilt STEP3 for the unchanged Terra-authored `踊る全11話放送` scenario, then generated one GPT Image 2.5 Sunburst/xhigh 1024×1536 candidate with two character references and automatic repair OFF. The rendered panel 3 physical left→right bubble text is リン／ヒカリ／サエコ, matching required Japanese right→left dialogue order; panel 4's single Akari tail leaves the lower bubble edge on a short route and no longer passes over her head. The title is plain/unframed and the footer remains intact.
- Remaining warnings: QA could not fully ground per-panel character-reference and some hand inventories. Its panel-2 bubble transcription also included the in-scene booklet text `全11話`, so panel 2 remained `unverified`; visual inspection found the actual single speech bubble. This candidate proves the targeted panel-3 order and panel-4 tail-path fixes, not a global full-quality pass.
- Verification: RED→GREEN regressions cover depth-only versus explicit named left/right staging and crossed-head tail paths. Related suites pass 115/115; full Node suite 610/610, ESLint, production build and `git diff --check` pass. Build retains the known Browserslist-age, mixed-import and bundle-size warnings.
- Delivery boundary: local implementation plus the requested live API validation only. No release/deploy/note/backup is authorized. Preserve the unrelated untracked `public/diagrams/workflow-preview.png`.

## 固定ページ正規化・タイトル枠除去・5本指QA — 2026-09-21

- User correction: panel heights must follow content; preserve the designed title lettering. Fixing every panel to 300px was a requirements error. Local-only scope; no publish/release/backup authority.
- Live failure: STEP2 adopted Terra for this verification; one STEP4 Sunburst/xhigh image was generated. The compositor failed frame detection, used proportional cuts and split faces/dialogue across neighboring panels. QA reported unverified dialogue in panels 2–4. This is a failed result, not a successful layout validation.
- Current local implementation: a conservative three-region normalizer uses the native 2:3 output tier instead of forcing A4. Standard output is 1024×1536 with title 100px, the whole undivided panel block 1402px, footer 34px and 6px side inset. Large output remains 1536×2304 with every value scaled by 1.5 (150/2103/51px and 9px inset), so selecting the large API size never downscales the result. It draws the complete panel block once, preserving variable panel heights, people, balloons and the model-rendered title. Ambiguous detection or any fit requiring crop/distortion fails closed and retains the raw candidate. The raw original remains separately downloadable.
- Prompt correction: the two watermark strings are footer-exclusive and may appear exactly once each. They are forbidden inside story panels and on in-scene pages. The first compact-layout API run duplicated the English watermark inside panel 3 because the test topic itself asked the scenario to depict a page-bottom credit; that run is rejected as final evidence.
- Integration: new API four-panel candidates are normalized before image QA, display and history storage. Existing displayed four-panel images can use the same local action without another generation. Single-image mode does not expose the action. README describes conditional normalization and raw preservation rather than the stale prompt-only/raw-only behavior.
- Superseded evidence: the earlier 1024×1448 harness and first current API run proved preservation of the undivided variable-height panel block, title bitmap, original download and footer detection, but also exposed excessive margins (about 51px sides, 24px title vertical padding and 15px footer vertical padding). Those values are no longer accepted. Fresh focused/full checks and one compact live API proof are required before completion.
- Latest live proof: STEP2 adopted `gpt-5.6-terra` for this verification and STEP4 generated exactly one Sunburst/xhigh image at 1024×1536 with two character references, automatic repair OFF and continuous generation OFF. Normalization applied as title 100px / undivided panel block 1402px / footer 34px. All four frames stayed intact with content-driven variable panel heights, narrow margins, and one uncropped footer watermark. The scenario selector was restored to the normal `gpt-6-astra` selection after the run.
- Rejected artwork: visual inspection found a closed black rectangle around the title and only four total digits on Hikari's large foreground hand in panel 4. The built-in QA had incorrectly accepted the hand, so this API result is failed evidence despite the correct page proportions. No second paid generation was started after this discovery.
- Local follow-up: generation prompts now forbid a title box/border/rule/banner and explicitly require one thumb plus four fingers even on foreground or foreshortened hands. Image QA now records `hand_geometry` for every prominent hand, requires visible plus naturally occluded digits to total five, rejects confirmed four-digit hands as `anatomy`, and leaves missing/uncertain evidence unverified. The page normalizer detects an aligned rectangular title rule and excludes it while retaining the model-rendered title glyphs.
- Same-artifact proof: applying the updated normalizer to the first 1024×1536 API artifact removed the title rectangle while keeping the original title lettering, four-panel bitmap and footer intact.
- Post-fix paid proof: one newly authorized Sunburst/xhigh 1024×1536 image used the Terra-authored `迷子ピューマ` scenario, two character references, automatic repair OFF and continuous generation OFF. It has an unframed model-rendered title, variable-height intact panels, narrow fixed page margins, one uncropped footer, and no observed four-digit prominent hand; QA also recorded all large/foreground hands as five digits. This proves the targeted page/title/hand improvements on this sample.
- Remaining image defects in the last artifact: panel 3 does not satisfy the specified rear Saeko OTS camera, and visual inspection found the rightmost `菓子をしまいなさい。` tail fails to terminate at Saeko and appears to point into the right-side group; the middle Miku tail is also ambiguous. The internal QA caught the camera but incorrectly accepted bubble-speaker tails, so that image is not a full quality pass.
- Bubble-tail QA root fix: the parser no longer trusts the reviewer's copied `expected_speaker` or prose `ok`. It extracts the authoritative B-number/text/speaker map from the submitted final prompt, requires every B-number exactly once, and requires an independently reported normalized tail-tip plus assigned-speaker mouth/head anchor and an explicit endpoint relation. A substituted expected speaker, wrong/empty endpoint, ambiguous/missing endpoint, or claimed contact with separated coordinates forces `bubble_speaker`/`unverified` even when the reviewer returns PASS.
- SNS explanation fix: the common prompt already requested a short heading, so this was not a new-model routing omission. The shared formatter failed to enforce it. `buildSnsExplanation` now receives the confirmed scenario title, prepends `【タイトル】` before the body for every model/provider, and normalizes a matching existing heading without duplication. The current browser copy was updated to `【迷子ピューマ】` before the body.
- Local verification: the four focused layout/prompt/QA files pass 47/47 tests; the SNS explanation suite passes 11/11 after a reproduced RED failure; bubble-tail/evidence tests pass 36/36 after a reproduced RED failure; the full Node suite passes 607/607; zero-warning ESLint, production build and `git diff --check` pass. Build retains the known Browserslist-age, mixed-import and bundle-size warnings.
- Delivery boundary remains local implementation plus requested live API verification. Unrelated `public/diagrams/workflow-preview.png` is preserved. No release/deploy/note/backup is authorized.

## v6.4.3 STEP2 model selection, shoulder-camera fix, and QA refinement — 2026-09-21

- Root cause: `extractExplicitRearSubject` mixed explicit shoulder matches with a generic rear-position fallback. With cast order `リン, サエコ`, the negated camera phrase `リンの背後へ回り込まず` could therefore steal the foreground role from the explicit `サエコの反射帯付き安全ベストの肩越し`. A single-speaker panel also returned before emitting the rear-camera eye-line/depth lock.
- Fix: resolve an explicitly named shoulder owner before any generic rear fallback, inspect the complete canonical cast for Camera-only participants, include that owner in depth assignment, and preserve the rear-camera lock for a single-speaker panel. The change only constrains ownership and physical occlusion; expressive height, tilt, foreshortening, full-body acting, lighting, depth, and panel contrast remain active.
- Live API: the in-app browser ran STEP2 with `gpt-5.6-terra` and STEP4 with `GPT Image 2.5 Sunburst / xhigh`. The final prompt correctly assigned foreground/OTS ownership to サエコ and background to リン. In the selected rendered image, panel 3 shows サエコ's reflective-vest shoulder in the foreground and rear-facing リン beyond it; the chest-held record and hands do not appear through リン's torso, and normal proportions remain. Visual inspection also confirmed varied shot height/distance/tilt, expressive faces and body acting, motivated light, layered depth, and a distinct quiet panel-3 beat followed by a wide tilted punchline.
- Remaining image warning: built-in QA exhausted its three automatic limited repairs and retained the best candidate. Panels 2 and 4 still contain the unscripted readable background sign `整備用具庫`; this does not invalidate the shoulder-camera fix. No additional paid generation was started.
- Verification before the QA refinement: focused prompt tests 31/31, full Node tests 592/592, ESLint, production build, and `git diff --check` passed. After the environmental-text and eyewear changes, focused QA tests 64/64 and the full Node suite 593/593 passed. Existing build warnings for Browserslist age, mixed static/dynamic import, and bundle size remain.
- Release scope: the user subsequently authorized the v6.4.3 official release/deploy. The release includes the persisted STEP2 model picker and descending fallback/cost display, the shoulder-camera fix, harmless environmental-text tolerance, and per-panel eyewear evidence. Note publication and full backup remain outside this authorization.

## Web貼り付け・人体接続指示の保持 — 2026-09-20

- 対象: 四コマ生成指示。左右の手足を人物ごとに追跡し、自然な遮蔽・画角外と不自然な欠損を区別する指示を、Web用の最終短縮後も保持する。所属不明の手足、家具付近の浮いた手、接続不良を対象とし、Action・カメラ・クロップ・短縮遠近法を維持する。全身を無理に見せたり演技を平坦化したりしない。
- 状態: ローカル修正済み。関連46テスト、全体578/578テスト、変更ファイルのESLint、production build、diff checkを通過。OpenAI実APIで短縮版指示の残存、手足の接続、動きと構図の維持を確認した。
- 境界: ユーザーがv6.4.2のcommit・push・公式deploy・既存FourPanel note更新を明示承認。フルバックアップは依頼されていないため実行しない。

## v6.4.1 STEP4ポリシー拒否の内部自動修正 — 2026-09-20

- 状態: ローカル実装と依頼された実API検証まで完了。画像APIのコンテンツポリシー拒否時は、拒否理由を文章APIで解析してプロンプトを修正し、画像APIを最大5回まで順次再実行する。最初の成功で停止し、非ポリシーエラーへ変わった場合も停止する。上限到達後だけ選択UIを表示する。
- 保持: 新規生成に失敗しても直前の成功画像を消さず、画像履歴、元プロンプト、各修正版の順序を作業中の状態へ保持する。STEP4の上限到達案内には、各試行で文章APIと画像APIを再利用することを明示した。
- ローカル証拠: 新規の決定的テストで、最大5回、途中成功で停止、非ポリシー失敗で停止、失敗時の表示画像保持、UI文言を確認。全Nodeテスト573/573、strict lint、production build、diff checkが成功。buildのBrowserslist経過・import混在・chunkサイズ警告は既存。
- 実API: 内蔵ブラウザでOpenAI接続後、STEP2 `gpt-6-astra`、STEP3、STEP4 `GPT Image 2.5 Sunburst / xhigh`を完走。通常生成ではポリシー拒否は起きず、新しい5回ループは実APIでは未発火。既存品質QAは吹き出し順を理由に3回の限定修正を行い、最終的に警告付き最良候補と生成履歴4件を保持した。目視では題名、全台詞、人物、手、小物、4コマ構成を確認し、2コマ目だけ台詞の左右順が未合格。
- 公開境界: 今回の依頼はローカル実装とAPI検証。release/deploy、GitHub反映、note更新、バックアップは実行しない。

## v6.4.0 STEP進捗表示と次操作導線 — 2026-09-20

- 状態: リリース候補を準備済み。STEP1のキャラクター画像解析中は実際の経過表示窓へ移動し、STEP2〜STEP4は前工程の完了後に次に操作できる主ボタンを画面下端寄りへ表示する。手動入力・手動スクロールでは表示位置を動かさない。
- 実証: OpenAIを接続した内蔵ブラウザでSTEP1解析を実行し、実際の解析完了とSTEP2主ボタンの画面下端表示を確認した。ローカル検証はNodeテスト569/569、strict lint、production build、diff check、公式preflightが成功。
- 公開境界: ユーザーは公式release/deployと既存FourPanel note `ndf063558c1f5`更新を明示承認。フルバックアップは今回依頼されていないため、`-RunFullBackup`およびバックアップ起動を行わない。

## v6.3.8 STEP2進行表示・README英日対応 — 2026-09-19

- 状態: リリース候補を準備中。STEP2の実行中はSTEP2ボタン直下の実進捗・エラーログだけを残し、古いシナリオ結果、Generation Preview、STEP3を隠す。完了後に結果・プレビュー・STEP3を戻し、既存の控えめな明暗案内でSTEP3を示す。
- 実証: OpenAI GPT-6 AstraでSTEP2を一回実行し、実行中の非表示状態と完了後の表示復帰を内蔵ブラウザで確認した。昭和の商店街という背景指定と、異世界の騎士服・魔法使い服という明示衣装は、生成台本で別々に保持された。最終画像の衣装再現はこのシナリオ検証だけでは保証しない。
- 変更: `src/App.jsx`、`src/components/Step2Panel.jsx`、`tests/step2-progressive-disclosure.test.mjs`、README。READMEは既存の画像QA・読順・救済操作説明へ対応する英語も追加する。
- 公開境界: ユーザーは公式release/deployと既存FourPanel note `ndf063558c1f5`更新を明示承認。フルバックアップは今回見送りのため、`-RunFullBackup`およびバックアップ起動を行わない。

## v6.3.7 漫画品質・再試行・操作案内・README改善 — 2026-09-19

- 状態: v6.3.7候補のローカル実装、README完全同期、依頼された実API検証まで完了。ユーザーは公式release/deploy、FourPanel note更新、黒い窓の公式フルバックアップを承認済み。公開・note・backupの完了証拠はroot `PLAN.md`へ記録する。キー値は確認していない。
- 実装: 出典・参考文の吹き出し化を共通パーサーで防止。ギャグ／シリアス／両ドキュメンタリーの強化規則、Acting Identity Lock、焦点整理、強いGペン、後頭部・関節・手足・小道具所有、カメラ／距離／視線／場所・時間連続性を共通プロンプトへ追加した。明示衣装は時代・文化より優先し、未指定時だけ舞台から自然推定する。
- 新キャラ: 物語上必要な役職人物は登場可能。役職名を人数に含め、再登場時は同じ人物の年齢・服装・小道具・位置と動作の連続性を保持する。今回の商店主も各コマの人数制約と継続対象になった。
- 文字・QA: 吹き出しTEXTを不変レイヤーとして一字ずつ照合する指示へ変更。看板、包装、背表紙、画面などの可読文字を実画像から全件転記し、未知文字や一覧欠落は合格にせず`unverified`へ送る。
- UI／堅牢性: STEP3が空の間はコピー以降を非表示。次に押せるSTEPは1.6秒周期で明度0.84〜1.18を滑らかに往復し、reduced-motionでは静止表示する。STEP4は「毎回新規生成」と明記し、既存画像のQA再検査ボタンは自動修正ONかつ明確な不合格時だけ、元画像ダウンロード直下に表示する。強化APIの後続失敗時は既に検証済みの最良候補を警告付きで保持し、同じ失敗修正の反復は打ち切る。READMEへ現行機能・物語アルゴリズム・モデル経路・限界を追記。
- Web貼付実証: 白黒を選び直してSTEP3を再構築し、Webコピーボタンの成功表示を確認。実出力は18,879文字で、`explicit outfit overrides setting era/culture`、リンの`waist-length twintails`／リボン／大きな丸眼鏡、`immutable TEXT`全字照合、`GUEST CONTINUITY`を保持し、添付資料名は含まなかった。
- 実API（カラー4コマ）: STEP2はOpenAI `gpt-6-astra`で、昭和映画館最終日のミク／リンによるシリアス台本を生成。GPT Image 2.5 Sunburst/xhigh・1024×1536は、全台詞と閉館看板、カメラ距離差、演技差、人体・手・半券、最終コマの背面と静かな余韻を実画面で確認した。内部QAはタイトル・台詞・手・カメラを合格、小道具面の一部を`unverified`としたが、実画面では台詞と主要小道具を確認して採用した。
- 実API（1枚絵）: 共通経路で人物の焦点、照明、奥行き、演技、人体を確認。黒板・包装・本へ未指定文字が出たため、上記の可読文字全件転記規則を追加した。修正後の追加有料生成は行っていない。
- 実API（白黒4コマ）: カラーと同じ映画館台本・人物・衣装・構図で比較。純白・黒・限定網点、強いGペン、カメラ距離差、演技差、人体・手・半券を確認した。初回の2コマ目吹き出し位置をQAが検出し、2回の自動修正候補から読順を退行させない候補を採用。最終実画面では題名、閉館看板、全台詞が一致し、2コマ目も右側配置になった。内部QAの`しー！`転記は`unverified`だったが、実文字は`し！`と確認したため採用した。
- 追加根本修正: シナリオの一度きりの時間表現を全コマへ複製しない、無言コマのAction人物をABSENTにしない、`演技癖`見出しを人物名として扱わず行動規則として渡す、白黒指定時に台本内の全編カラー命令を白黒へ正規化する。
- ローカル検証: Nodeテスト564/564、strict lint、production build、`git diff --check`が成功。実画面ではSTEP2主ボタンの明度0.84〜1.18変化と、不合格でない状態で再検査ボタンが非表示であることを確認。buildのBrowserslist経過・import混在・chunkサイズ警告は既存。
- Friday評価: MCP/HTTPに加えてDocker/Neo4j、DeepSeek/Mem0設定を要求し、今回の共通プロンプト／QA修正には過剰。既存構造で根本修正できたためインストール・設定・外部送信を行わなかった。

## Web貼り付け共通プロンプト・読順QA — v6.3.6、2026-09-18

- 最新依頼: API修正だけでなくWeb貼り付け用プロンプトそのものを直す。ローカル修正とコピー実証が現在の優先対象。条件付きdeploy・FourPanel note・黒い窓バックアップは未実行。
- 共通ソース修正: 各台詞の直後へB1 [RIGHTMOST]、最終B [LEFTMOST]、途中Bは直前Bの左と明記し、同じ番号で尻尾の話者を固定。短縮処理でも残す。姓付き人物名の短縮名、背景の「他のN人」、自由文キャストの特徴、gray hair/sunglassesを解釈し、ActionとABSENTの衝突・特徴欠落を修正。シナリオに明示された結末を再解決し、シリアス作品へ古いギャグ設定が混入しないようにした。
- Web実証: ユーザーがキャラシート5枚を再投入済み。既存食品包装シナリオのSTEP3を構築し、実際の「web貼り付け時」コピーボタンからクリップボードを読み、表示文と一致を確認。Panel 3はB1="袋はここで開く？" [RIGHTMOST] / B2="ああ、ここで受け止めよう。" [LEFTMOST]、尻尾はユウ/エムバ。SERIOUS INTENT、キャスト特徴、人数5/2/3/5も確認。内蔵ブラウザ5173を再読み込みせず保持。
- API失敗証拠: 正解プロンプトを渡さず画像だけで吹き出し文字と横座標を転記する独立QAを追加。既存画像のPanel 3左右逆転を実APIで検出した。その後の旧修正解析による3回の修正は全て不合格で警告付き保持。解析AIが質問と返答の左右を逆に指示することをログで確認したため、読順修正指示は元台詞順からコードで組み立てるよう変更。現在の組み立て方式での新画像は未検証であり、画像修正完了・合格とは扱わない。
- QA実装: 現在画像を再検査できるボタン、独立した文字座標比較、既知の読順を悪化させる候補の拒否、読順修正の優先、原文を切らない32,000文字上限処理。3回とも不合格なら最良候補を警告付き採用する既存方針を維持。
- ローカル検証: strict lint、production build、diff check成功。全543 Nodeテスト成功。buildには既存のBrowserslist経過・import混在・chunkサイズ警告あり。
- 次工程: Webコピー文の修正と実画像の合格を区別し、条件付き公開工程は直接の画像合格確認後のみ。

## 台詞順序と吹き出し尻尾の話者固定 — v6.3.5、2026-09-17

- 変更: 4コマの台詞を右から左、上から下の読順へ固定し、吹き出しの尻尾を指定話者の口・頭へ結び付ける共通ルールを強化した。人物の位置やカメラによる近さで台詞・話者・尻尾を入れ替えない。
- QA: ChatGPT/Geminiの短縮プロンプトにもパネル別の話者・尻尾対応を残し、画像品質QAは話者違いを独立した`bubble_speaker`として検出する。内部QAラベル、画像内の実文字、採否は分けて確認する。
- 検証: 全522 Nodeテスト、strict lint、production build、`git diff --check`が成功。修正済みプロンプトを用いたGPT Image 2.5 Sunburst/xhigh・1024×1536の実画像1枚を目視し、右側の先行台詞、左側の最終台詞、最終尻尾の左向き、俯瞰・肩越し・アオリ・望遠寄りの差を確認した。自動修正はOFFで追加生成なし。
- 公開境界: ユーザーはv6.3.5の公式デプロイ、既存FourPanel note `ndf063558c1f5`更新、黒い窓の公式フルバックアップを承認済み。完了証拠をroot `PLAN.md`へ追記する。

## SNS投稿用の解説・出典リンク — v6.3.4、2026-09-17

- v6.3.4では、SNS解説の出典・参考リンクを漫画の台詞・登場人物・配置・画像プロンプトへ混入させない共通フィルタを追加。実際の台詞と明示された小道具文字は保持する。focused/full tests、lint、build、出典行を含む実API入力で確認済み（API画像は安全判定で拒否）。

- 変更: STEP3の上に、SNS投稿用の編集可能な解説欄とコピーボタンを追加した。カテゴリ検索では実際に使った検索出典だけを表示し、自由入力では本文に含まれるURLだけを表示する。URLがない自由入力には参考リンク欄を出さない。
- 内容: 解説はシナリオ・画像プロンプトから分離しており、演出強化を行っても保持する。新しい題材、リセット、結末変更、連続作成では前の解説を消す。創作の自由入力は、本文末の改行後に独立した丸括弧で注意書きを付ける。
- 検証: OpenAIのWeb Searchを使うカテゴリ検索と、URLなしの自由入力を実APIで確認し、STEP3表示、出典表示の有無、画像生成内容を目視した。後者は俯瞰・肩越し・アオリを含む4コマになり、絵がヌルくなっていないことも確認した。関連18 Nodeテスト、lint、production build、diff checkが通過している。
- 公開境界: ユーザーはv6.3.3の公式リリース、既存FourPanel note `ndf063558c1f5`更新、黒い窓の公式フルバックアップを承認済み。公開完了の証拠はroot `PLAN.md`へ追記する。

## 手動保存専用・見せ場に応じた強パース — v6.3.0、2026-09-16

- 変更: ブラウザーのフォルダー書き込みによる完成画像の自動保存を撤去した。品質確認後の最終採用API画像は、現ブラウザーセッション内で直近10件の生成履歴へ残る。履歴から任意の画像をプレビューへ戻し、従来のタイムスタンプ付き手動ダウンロードだけを使うため、保存先選択やDownloadsのフォルダー権限は不要。
- 演出: 強い遠近感は、物語上の見せ場に合う手・顔・重要な小道具・環境の奥行きから焦点を選ぶ。足だけを手前へ突き出す構図を既定にせず、走る・踏み込む・蹴るなど脚そのものが見せ場のときだけ、支持脚と重心が読める足の短縮遠近法を使う。明示されたCamera/Action、台詞、人物、小道具は上書きしない。
- 検証: 新しい回帰テストは旧規則で失敗し、STEP2シナリオとChatGPT/Gemini双方の最終プロンプトが同じルールを保持することを確認した。生成履歴・手動保存・フルオート入力保持を含む重点25 Nodeテスト、全487 Nodeテスト、strict lint、production build、diff checkが通過。ローカル画面でもSTEP4生成ボタン直下に自動保存スイッチがなく、品質・サイズ設定が直後に続くことを確認。OpenAI GPT Image 2.5 Sunburst/xhighの実画像1枚は、顔・手・本・書架の奥行きを使い、足を前景へ突き出す構図にならなかった。内蔵QAは小道具向きのみ`unverified`で元画像を保持し、手動ダウンロードと生成履歴(1)を確認した。
- 公開境界: ユーザーは公式リリース、既存FourPanel note `ndf063558c1f5`更新、変更なしの黒い窓フルバックアップを承認済み。Hugging Faceは正式リリース契約・バージョン更新・公開検証から外し、Spaceは削除しない。共通契約はHF有効化を拒否し、HF工程なしのGitHub Pages、GitHub Release、Cドライブ公開版コピーで完結する。root `PLAN.md`のv6.3.0項目を現在の進捗正本とする。

## 生成履歴と最終画像の実ファイル自動保存 — v6.2.9、2026-09-16

- 変更: 生成履歴を直近10件へ復元し、品質確認後に採用したAPI画像だけを履歴へ追加する。履歴サムネイルから任意の画像をプレビューへ戻し、従来の手動保存を使える。STEP4直下の「完成画像を自動保存（API生成のみ）」は既定ONで、最初の手動生成またはフルオート開始時に一度選んだ保存先へ最終採用画像を実書き込みする。
- 保存先: ChromeはDownloads直下をWebページの書き込み先として保護するため、Downloads内の専用サブフォルダーを選ぶ。保存先とスイッチはフルオート、連続ループ、STEP1/STEP2リセットで維持し、ページ再読み込みまたは全設定クリアで選び直す。未対応ブラウザー、選択キャンセル、書き込み失敗では保存済みと表示せず手動保存を案内する。
- 実API: OpenAI GPT Image 2.5 Sunburst / xhigh・1024×1536、自動修正OFFで2枚生成。OpenAI画像生成、品質確認、履歴2件、履歴からの選択、`C:\\Users\\sx717\\Downloads\\Nano Banana Pro`へのタイムスタンプ付きPNG2ファイルの実書き込みを確認した。両標本の台詞パネル位置にはQA警告が残ったため、追加画像生成はせず元画像を保持した。これは保存・履歴機能の実証であり、台詞位置の完全な画質採用証拠ではない。
- ローカル証拠: 重点36 Nodeテストと全Nodeテスト（終了コード0）、strict lint、production build、diff check成功。既存のBrowserslist経過、dynamic/static import混在、chunkサイズのbuild警告は残る。
- 公開境界: ユーザーはv6.2.9の公式リリース、既存FourPanel note `ndf063558c1f5`更新、変更なしの黒い窓フルバックアップを承認済み。公式pre-deploy checkはHANDOFFの現行バージョン不足で一度停止したため、この記録を追加後にリリース前検査から再開する。

## 職業制服の役割保持と白黒三階調 — v6.2.5、2026-09-16

- 原因と修正: 日本語の`制服`を一律にフォーマル服へ変える安全変換と、全員へ同じ衣装を当てる最終指示が、勤務中の職業制服まで消していた。学校制服だけを安全変換の対象に限定し、職業制服・作業服・安全装備を人物ごとの勤務役割へ結び付け、来訪者・非勤務者の私服と分離した。Serious Documentary、ChatGPT/Gemini、カラー/白黒、Web短縮でも同じ割当を保持する。
- 白黒修正: 網点ぼかしの反復が顔・明るい背景まで灰色にしていた。白地・黒ベタ・範囲を限定した網点/ハッチングの三階調へ整理し、肌の明部、明るい壁・天井・布地を白く残す。遠景は線を減らし白い間隔を広げ、画像QAとWeb短縮も同じ条件を保持する。
- 実API: OpenAI GPT Image 2.5 Sunburst / xhigh・1024×1536、自動修正OFF、同一の警察署シナリオと参照2枚でカラー/白黒を各1枚生成。両方ともアカリは全4コマで勤務中の警察官制服、ヒカリは来訪者の私服を保持。白黒は顔、白い壁・天井・布地の白地を残し、背景全面の灰色ベールなし。内部QAの表面文字/小道具向き`unverified`と白黒2コマ目の眼鏡誤検出は画像内文字でも採否でもなく、目視では警察官に眼鏡なし。追加修正生成0回。
- ローカル証拠: Nodeテスト479/479、strict lint、production build、diff check成功。buildのBrowserslist経過・import混在・chunkサイズは既存警告。今回の2標本は衣装割当と白黒明部の退行防止を確認するもので、全ての台詞・小道具・表面文字の完全性や統計的な失敗率低下は保証しない。
- 公開境界: ユーザーはv6.2.5の公式リリース、既存FourPanel note `ndf063558c1f5`更新、変更なしの黒い窓フルバックアップを承認済み。完了証拠はroot `PLAN.md`へ追記する。

## Model Chainの実行経路連動 — v6.2.5、2026-09-16

- ユーザーの恒久要件: APIモデル経路を更新した場合、画面のModel Chainも同じ定義から自動で追随させる。OpenAI文章の通常・STEP2シナリオ・Visionは `src/lib/openai-model-routes.js`、Geminiの通常文章・Vision・画像生成は `src/lib/gemini-model-routes.js` に集約し、実行側と表示側 `fallback-chain-history.js` が参照する。OpenAI STEP4は `openai-image-settings.js` の初期選択・明示選択・代替選択から表示を組み立てる。
- `tests/fallback-chain-history.test.mjs` はOpenAIとGeminiの表示が各実行経路を参照していることを確認し、`pre_deploy_check.js` はこのテストを必須実行する。経路を増減・並べ替えた場合、表示を別途複写して更新する必要はない。履歴の追記は、ユーザーが公開履歴に残すよう明示した場合だけ行う。

## Model Chain同期 — v6.2.3、2026-09-16

- 表示用のModel Chainを実行時のSTEP2 OpenAI経路へ同期し、GPT-6 Astra、GPT-5.6 Sol、GPT-4.1系の順番を明示した。STEP4はSunburstを認証済み時の初期選択、Flareを明示選択、GPT Image 2.0を2.5利用不可時の初期選択として区別する。
- `pre_deploy_check.js` は通常テキスト、STEP2シナリオ、Vision、Gemini画像、OpenAI画像設定をまとめてスナップショットと比較する。`tests/fallback-chain-history.test.mjs` がSTEP2とSTEP4表示の回帰を検出する。
- ユーザーは公式リリース、既存FourPanel note更新、黒い窓の公式フルバックアップを明示承認済み。公開・バックアップの現在証拠は完了後に追記する。

## 4コマプロンプトの優先順位整理 — v6.2.2、2026-09-16

- 依頼: 人物の複製・衣装逸脱・眼鏡欠落の診断を受け、過密なプロンプトの改良を行う。
- 範囲: 通常4コマの自動的な構図・画風ノルマと重複指示を整理し、人数・同一性・眼鏡・衣装・台詞・明示Camera/Action・選択画風・白黒媒体・物理整合を保護する。カラー/白黒、両プロバイダとWeb短縮を共通確認する。
- 境界: 通常4コマの実装・API検証と、ユーザーが明示した v6.2.2 の公式リリース、既存FourPanel note更新、黒い窓の公式フルバックアップ。1枚絵/H3、モデル、UIは対象外。
- 結果: 水平方位・身体演技・画風差の数値ノルマを削除し、場面に応じた演出へ変更。共通画風指示と長文時の重複身体演技説明を整理し、必須内容を保護する優先順位を両プロバイダへ追加。明示正面にも斜め向きを足す競合を修正。既存のカメラ演出差し込み、手・小道具の共通物理ルール、選択画風・白黒描線・頭身指定は維持。
- 検証: 変更前に4回帰失敗を確認し、変更後は全468 Nodeテスト、警告0 lint、build、diff check成功。長文fixtureのカラー/白黒×両プロバイダをHEADと比較し、Action/Dialogue行が一致。ビルドには既存のBrowserslist経過・動的/静的import混在・chunkサイズ警告あり。長い台本では既存の15,000文字Web目安を超える場合があり、必須内容を切り捨てて合わせてはいない。
- 実API: 同じ保存済み台本「百歳祝いは合算で」、同じキャラシート2枚、OpenAI GPT Image 2.5 Sunburst / xhigh / 1024×1536でカラー1枚と白黒1枚を生成。追加修正生成0回。両方ともAPIと内蔵QAがCOMPLETE、ブラウザエラー0件。目視では全コマの指定人数を維持し、4コマ目はアカリ・リン・ヒカリの3人だけでクローンなし。リンとヒカリは眼鏡あり、他3人は眼鏡なし。全員に明確な制服はなくスマートカジュアルを維持。見える手足に明白な逆向き・増殖・分離はない。
- 採否/制限: 今回の2標本では、報告されたクローン・制服化・眼鏡欠落は再現せず、改良方向は実画像でも有効。ただし白黒4コマ目のアカリは指定の「左手で皿、右手で菓子」ではなく菓子を両手で持っており、手の形は自然でも動作指定は未達。足はほぼ画面外のため、足向きの改善は今回未検証。内蔵QAのPASSだけでなく目視を採否根拠とする。2枚だけでは失敗率低下を統計的に断定できない。
- 引継ぎ: 保存済み台本を保つ場合はSTEP3から再構築し、自動演出の変更はSTEP2から反映する。公開の正本は公式リリースreceipt、noteの公開読戻し、フルバックアップのmanifest・hash・lock解放で確認する。追加有料生成は不要。

## v6.2.1 参考衣装のシナリオ独立 — リリース進行中、2026-09-15

- 対象: キャラシートの制服・私服が、題材と無関係にSTEP2のシナリオや画像へ戻る経路を修正する。顔・髪・性格・関係性、手入力衣装、学校行事と職業制服は維持する。
- 変更: シナリオ生成・演出強化へ渡すキャラ情報から衣装欄を分離し、生成済みの学生役を学校制服の根拠に使わない。画像プロンプトのキャスト抽出とフォールバックでも、確定Outfitがある場合は参考衣装を復活させない。JSON形式のシナリオ応答でもOutfit等のメタデータを保持する。
- 実証: 衣装欄を空欄にした長寿祝い・市民会館の新規STEP2で、GPT-6 Astraが「きれいめの私服」を選択。GPT Image 2.5 Sunburst/xhigh・1024×1536でカラー／白黒を各1枚生成し、両方で私服を確認。小道具の個数、全身フレーミング、細部の同一性は未達のため、衣装修正の証拠と完成原稿の全条件達成を混同しない。
- 次: v6.2.1の公式リリーストランザクション、note `ndf063558c1f5`の狭い更新と公開読戻し、変更なしの`backup_launch.bat`による可視フルバックアップおよび独立検証を実行する。

## v6.1.8 GPT-6 STEP2経路と小道具QAのリリース候補 — 2026-09-15

- リリース候補: ユーザーは今回のGPT-6 STEP2経路・画像QA改善について、deploy、既存FourPanel note更新、黒い窓フルバックアップを明示承認。GPT-6 AstraはSTEP2のシナリオ作成と任意強化だけに限定し、GPT-5.6 Solと既存GPT-4.1系へ段階フォールバックする。キャラクター解析、プロンプト査読、画像生成のモデル経路は変更しない。
- 画像品質: 額縁などの二面小道具は「読ませる表」と「操作する裏」を区別し、裏面を操作する人物を誤って破綻扱いしない。画像根拠が不足する場合は検出済み欠陥ではなく`unverified`に留める。長文圧縮後も最終コマの非整列、能動的な身体動作、読める顔演技を保持する。
- 現在の証拠: ユーザーが投入したOpenAI接続でGPT Image 2.5 Sunburst/xhigh・1024×1536のカラー実画像を生成し、完全白背景への退行なし、奥行き・肩越し・デフォルメ差・額縁前景を確認。画像内文字は内部QAラベルと区別して目視する。原画像・実送信プロンプト・入力・reviewは既存の`output/`履歴に保存済み。ローカル453テスト、strict lint、production build、diff check成功。
- 次: v6.1.8の公式リリーストランザクション、note `ndf063558c1f5`の狭い更新と公開読戻し、変更なしの`backup_launch.bat`による可視フルバックアップおよび独立検証を連続実行する。

## v6.1.7 追加の緩急・個別演技改良とAPI再検証 — 2026-09-15

- v6.1.7リリース候補: ユーザーがdeploy、FourPanel note更新、黒い窓フルバックアップを明示承認。v6再検証で確認したCRLF修正、日本語俯瞰の認識、背景省略中の見下ろし形状、個別視線優先／未指定時の相互視線を共通処理へ反映。v5と同じ台本・簡潔キャスト、ユーザー再投入の参照2枚、稼働中の最新版STEP3→STEP4でSunburst/xhigh・1024×1536のカラー白黒各1枚。追加修正0回、両版QAまでCOMPLETE。`output/reading-rhythm-20260915-v6/`へ原PNG・実送信指示・入力・review・X原稿を保存。私服・場所、4本のCamera／状況、6台詞、人数5/5/6/5、新指示の保持とハッシュ／寸法を検査。全447テスト・strict lint・build・diff check成功。v5の服装欠落は解消し、肩越しと個別の手の演技は読めるが、2コマの強い俯瞰と最終コマの非整列配置は未達。小道具・未指定文字・厳密二値も残り、完成原稿の全面採用は保留。保存サービス正常終了、進行中の画像生成なし。in-appタブ11に白黒結果、5173は稼働中。公開・note・backupはこの候補の正式トランザクションとして進行する。
- 対象: 通常4コマ。ユーザーは描き込むコマと大胆な背景省略・デフォルメの差、カメラ・相互関係・個別演技を追加改良した上でのAPI再検証を依頼。「喧騒」は「検証」の誤入力で、喧騒を加える依頼ではない。
- 変更: シナリオ生成、背景／カメラ強化、両プロバイダの画像指示と白黒・長文短縮へ、明示的な抽象コマの背景省略と必要な相手の反応・個別演技の保持を反映。通常の場所描写と参照画風・頭身指定を維持する。既存シナリオの台詞・基本動作を保持してCamera／背景／演出を具体化する。
- v5の結果（履歴）: 追加改良を反映したカラー・白黒各1枚をSunburst/xhighで実API生成、両方COMPLETE、追加修正0回。`output/reading-rhythm-20260915-v5/`へ原PNG・実送信指示・台本・比較review・X原稿を保存。密度差とカラー全身引きは改善、2コマ俯瞰は後退。開発サーバー停止中の旧画面を保持し現行ソースで別途組立てたが、フォームのCRLFで場所・私服が欠落した確認不足あり。条件を揃えた比較として不採用。共通組立入口で改行を正規化し、失敗再現→修正後成功、全446テスト・strict lint・build成功。当時の修正後指示は `*-repaired-NOT-GENERATED.txt`。その後ユーザーが再修正・再生成を承認し、上記v6を実施。v5原画像と実送信指示は変更していない。

## 4コマの視線誘導と密度の強弱 — ローカル実装・検証済み、2026-09-15

- 最新の背景・カメラ再検証は実施済み（v4、2026-09-15）。場所・奥行き・明暗を残す被写界深度へ通常4コマの共通指示／短縮処理を修正し、肩越しの手前人物と奥の話者を同じ前景へ置く競合も一般処理で修正。全444テスト、strict lint、build、diff check成功。台本はCamera／背景／演出だけを具体化し、他の全行を保持した。
- 承認されたカラー・白黒各1枚を同じSunburst/xhigh、参照2枚、1024×1536で実API生成。追加修正0回、両方QAまでCOMPLETE。`output/reading-rhythm-20260915-v4/` に原PNG2枚、実行プロンプト、詳細review、X原稿を保存し寸法・ハッシュ・プロンプト文字数を確認。保存サービス正常終了。タブ1に白黒結果を保持。背景白抜きは解消し、2コマ目の俯瞰と3コマ目の肩越しは改善。一方、1コマ目の引き不足、4コマ目の正面寄り構図、白黒の背景トーン・本の開閉・厳密二値条件などは未達。生成と保存の完了を画質全条件の達成と混同しない。
- v3の背景白抜きを改善とした講評は撤回し、旧review／X原稿の先頭へ不採用訂正を追記。v1〜v3原PNGと実行プロンプトは不変。今回の画像呼び出し2回は消化済みで、さらに有料再生成する承認はない。今後の具体的な採否・追加修正はv4 reviewの未達項目を基準にする。1枚絵、H3、commit/push、公開、backupは実施していない。
- v6.1.6の公開・note更新・フルバックアップは完了: ユーザーはdeploy、FourPanel note更新、公式の黒い窓フルバックアップを明示承認。公式リリースはcommit `d6decfd7ae63f35942f66146bf48bb77687ade08`、GitHub Release/tag `v6.1.6`、Pages commit `15bc5cb071d12bf5c24f19e64d3a8af50a276fd6`、Hugging Face commit `0618d58a344fa382203b59c534397e5966df6267`、Cドライブ配布コピーまで完了し、receiptは`backups/release_receipts/nano-banana-pro-v6.1.6-2026-09-15_120904.json`。公開FourPanel note `ndf063558c1f5` はv6.1.6タイトル・本文・太字履歴を読み戻した。`output/` は原PNG・プロンプト・講評のローカル保存先として.gitignoreへ追加し、公開ソースには混在させない。リリースnotesは`docs/releases/v6.1.6.md`。変更なしの黒い窓公式ランチャーが終了し、`verify_full_backup.ps1 -ExpectedVersion 6.1.6` は`FULL_BACKUP_VERIFIED apps=7`を返した。manifestは`backups/antigravity_full_backup_2026-09-15_122014.manifest.json`、ローカル／Drive SHA-256は`B15346B72F1B15610FEF67C926505D8AC1B70AA6C75A5E1F49AB491CFC07A02D`で一致。
- 最新依頼「再検証して。その上で最終講評を」は完了（2026-09-15）: 同じ参照2枚・Sunburst/xhighでカラー1枚＋白黒1枚、追加画像修正0回、両方COMPLETE。`output/reading-rhythm-20260915-v3/` に原PNG2枚・最終プロンプト・比較 `review.md`・X用 `x-post.md` を保存しハッシュ／寸法確認。保存サービスは正常終了。前回より3・4コマ目の余白が増え、白黒の人数超過は今回はなし。ただしカラーの両掌・POP文言、カメラ、白黒の白地肌・厳密二値などは未達。X投稿、公開、backup、製品コード追加はなし。
- v3の証拠上の制限: 既存シナリオ強化は成功表示でも全4本の状況行を落としたため不採用。元の状況・台詞を復元し、Codexが台本のCamera／背景／演出を具体化した。補助人数は「5人を」では欠落するため入力を「5人が」に直して再構築。誤人数から画像生成していない。最終人数5/5/6/5を確認。完全自動成功やモデル更新比較ではなく、各1枚の演出修正込み比較。最初の批評対象の元作品は未提供。追加有料生成は未承認。タブ1（127.0.0.1:5173）に白黒結果を保持。
- 再改善の現状（最新依頼「課題が残るなら解消して」）: 古い全コマ高密度・歪み必須指示を修正し、静かな背景省略・脇役の主張抑制・指定手動作優先・未指定文字禁止をシナリオ／強化／両プロバイダの最終指示と短縮処理へ反映。442テスト、strict lint、build、diff check成功。原画像は保持。公開・backup・1枚絵・H3は対象外。
- 承認済みの再検証2回は実施済み: カラー1枚＋白黒1枚、GPT Image 2.5 Sunburst/xhigh、1024×1536、同じ参照2枚・台本。全Action/Camera/Dialogueは前回と一致。追加修正生成0回、両方COMPLETE。`output/reading-rhythm-20260915-v2/` にPNG2枚とプロンプト2本、詳細 `review.md` を保存しハッシュ確認。一時保存サービスは正常終了。カラーQAは応答解析失敗、白黒QAはsurface_text未確認を保持。
- v2時点の未解消事項（その後の再検証結果は上記v3を参照）: カラー2コマ目の両手を広げる演技は改善したが、背景密度・未指定文字・最終コマの頬杖は残った。白黒3コマ目は手前の頭部が6人分見え、指定5人を超えた。厳密二値でもなかった。これを受けてユーザーが台本整理後のv3各1枚を承認し、実施済み。

- 対象・依頼: 通常4コマの吹き出し、構図、情報密度が均されるという指摘への改良。元画像は未提供のため、個別作品の評価ではなく現行生成経路の矛盾を修正する。
- 範囲: シナリオ・選択カテゴリの演出強化・両プロバイダ/API/Webの4コマプロンプト、回帰テスト、README。4コマ等分レイアウト、台詞・人物・小物・明示演出を保つ。1枚絵、H3、公開、バックアップは対象外。
- 確認済み: 全コマの背景要素数と余白制限、Geminiカメラの一律な歪み追加、吹き出しの中央/頭上固定、カメラ未指定時のChatGPTフォールバック位置未更新。
- 受入条件: 見せ場と静かなコマ、主な注視対象、台詞順と尾の所有関係、密度差を最終プロンプトまで維持。指定カメラと静かな間を上書きせず、文字数圧縮でも契約を保持。ローカル回帰・lint・buildを確認し、実画像の効果は未検証として区別する。
- 結果: 見せ場・静かな間・注視対象・密度差の共通4コマ指示を追加。背景量の一律指定を修正し、吹き出しの高さと余白を許可。Geminiの指定外カメラ効果追加を解消し、ChatGPTカメラ未指定時のフォールバック位置を更新。選択カテゴリの演出強化とREADMEも同期。
- 証拠: 新規6回帰が修正前にRED、修正後GREEN。全Nodeテスト437/437、`npm.cmd run lint -- --max-warnings 0`、`npm.cmd run build`、`git diff --check` 成功。両プロバイダ×カラー/白黒×Auto/SeriousDocumentaryと15,000文字Webコピーの保持条件を確認。buildにはBrowserslistデータ経過、動的/静的import混在、chunkサイズの警告あり。
- API検証の追加依頼（2026-09-15）: 同じ台本・キャラクターでカラーとモノクロを各1枚実API生成し、改善内容と結果の講評を提示する。自動修正はOFF。公開・バックアップは対象外。現在の版番号は6.1.5のまま、変更は未コミット。元からあった下記v6.1.5完了記録の未コミット差分を保持。
- 追加修正: 実APIで作った台本の「5人が／5人は」を補助人数指示が認識せず、非話者をABSENT扱いする矛盾を発見。登録キャスト人数と一致する無修飾の人数主語を読み取り、台詞内の数字・別集団・人数不一致は除外する一般処理へ修正。2回帰を追加しRED→GREEN、全Nodeテスト439件、strict lint、build、diff check成功。
- 実API完了: キャラシート2枚と同じgpt-4.1台本「無料おかわりの静かな罠」を使用。修正後STEP3の人数指示5/5/6（店員含む）/5、ABSENT・MONOLOGUE矛盾なし。OpenAI GPT Image 2.5 Sunburst / xhigh、1024×1536でカラー1枚・白黒1枚、追加修正生成0回。両方とも画像とQAのCOMPLETEを確認。キー値の読出しなし。
- 保存・評価: `output/reading-rhythm-20260915/` にPNG原本2枚、使用プロンプト2本、詳細な `review.md` を保存。カラー2,695,537 bytes / SHA256 b417b3cdbcc0c67a1d188cd4c830f5946dd295c7e5eed09ad1ff2db96df204e4、白黒3,328,884 bytes / SHA256 37411d99b629db6578460cb1fcce2eec731aeba23d37c716c7d3fb7a21a17d6a。通常ダウンロードを確認できず、一時ループバック保存フォームで元PNGバイト列を受け渡し、保存サービスは正常終了。画像加工なし。
- 講評: 2コマ目の賑やかさと4コマ目の冷めた表情・寄りに差があり、白黒はデフォルメ／背面の差が明確。一方、静かなコマも人物5人と背景で高密度。カラーのミクの両手動作等は台本と不一致、背景に未指定英字もある。モノクロQAの眼鏡欠落2件は実画像に眼鏡が見えるため誤検出。白黒は目視で色漏れなしだが、RGB不一致62.44%・差>5が0.79%・最大差21で厳密二値ではない。旧版対照なしなので改善幅の断定不可。今回の依頼は完了、追加生成・公開・バックアップなし。

## v6.1.5 serious-mode and compact-UI release — 2026-09-13

- Scope: generic stale-ending invalidation and Serious Documentary final-prompt guards; serious/gag enhancement-mode routing; compact 28px STEP2 ending selector with explicit menu cue and 12px execution gap; compact STEP4 help plus 11px labels/text and 26px quality/size selects.
- Live proof: the in-app browser completed STEP2, displayed `シリアス演出`, rebuilt the Serious Documentary STEP3 prompt, and generated one 1024×1536 OpenAI GPT Image 2.5 Sunburst / xhigh image with both character sheets. Visual review found normal proportions and one consistent reference-sheet style in all four panels, including the ending; no chibi conversion. Internal QA retained surface orientation as unverified and performed no repair generation.
- Local proof: full Node suite 431/431, zero-warning lint, production build, diff check and browser console warning/error check pass. Computed browser values are 28px/12px for the ending select, 12px separation before STEP2, and 11px labels/text with 26px selects for API quality/size.
- Delivery: official receipt `backups/release_receipts/nano-banana-pro-v6.1.5-2026-09-13_152323.json` completed for source commit `2c2a2145d4761636308abd2fb1bb15f868df1c32`, GitHub Release/tag `v6.1.5`, Pages commit `0944df36c398ad3aaac9c79923b89640e0422af2`, Hugging Face commit `43b17de573cb168e687bcd4f9173ed1f5c1df593`, and the C-drive release copy. FourPanel note `ndf063558c1f5` is publicly updated and verified with the v6.1.5 title/body, serious/gag routing guidance, compact bold date/version history, 47 figures, and retained H3 links. The established visible black-window full backup completed and closed; independent verification returned `FULL_BACKUP_VERIFIED apps=7` for `backups/antigravity_full_backup_2026-09-13_153843.manifest.json`, with matching local/Drive SHA-256 `DB05553B404943CA022FA93DE83220DC65A911F620E390901A682F7A037B5DFB`.

## v6.1.4 documentary endings and FourPanel v5.9.9 — released; local stale-mode guard verified, 2026-09-13

- Target: publish the source-faithful Serious Documentary and Gag Documentary endings, the generic source-fact ledger/timeline guard, and the supplied FourPanel v5.9.9 JSON/three-node ZIP plus standard-H3 clipboard prompt. The user explicitly authorized the official deploy, existing FourPanel note update and established visible black-window full backup on 2026-09-13.
- Generic-system boundary: production logic derives facts, dates, cast, dialogue, clothing and art-style constraints from arbitrary inputs. It contains no sample character names, fixed cast size or sample news facts. Internal fact-ledger labels are excluded from visible image text.
- Live evidence: OpenAI GPT Image 2.5 Sunburst / xhigh received the exact STEP3 prompt and both supplied character sheets and returned a 1024x1536 serious-documentary page. The page kept one consistent character-sheet art style across all four panels and visibly represented the October/6 p.m./staffing-cost/learning-place facts; the internal QA kept surface orientation as unverified rather than inventing evidence.
- Verification: the full suite passes 419/419, strict lint, production build, pre-deploy version sync and diff whitespace checks pass. The exact v5.9.9 ZIP is 608,677 bytes with SHA-256 `38D4734100FD9575AC9236CC4BC1BBEF4AD627FCEB1310134021292348131BCF`; its workflow SHA-256 is `5E1258949CCB5AFCC029E02F192B7F1A1F703989524195319CD29D0227D1B4AC`.
- Released delivery: source commit `00bb90dce6fcb5ea51341e533d50c6102a1786a0`, GitHub Release `v6.1.4`, Pages, Hugging Face and release copy completed; note `ndf063558c1f5` was published and publicly verified. The unchanged visible backup launcher completed, and independent verification reported `FULL_BACKUP_VERIFIED apps=7` for `backups/antigravity_full_backup_2026-09-13_135956.zip`; local/Drive SHA-256 is `F99906A267A3E4BEC2A356A74D4357F7D8E0EAE4C4A40E28D0E5E7F0D84A65A0`.
- Local unreleased follow-up: changing the ending selector now invalidates the prior STEP2/STEP3/current-image state. Serious Documentary validates the final prompt after STEP3 review and immediately before Web copy, API generation and policy-switch copying. It rejects missing all-panel reference-style lock plus generic comedy/chibi/panel-style/proportion/style-difference directives; no sample character, location, dialogue or article fact is encoded.
- Follow-up verification: RED regression reproduced the stale gag prompt; GREEN focused tests pass 18/18, full tests 422/422, strict lint, production build and diff whitespace checks pass. In the in-app browser, the exact 16,649-character STEP3 prompt had one all-panel art-style lock, four per-panel acting-only locks and zero comedy/chibi/panel-style/proportion-switch directives. One OpenAI GPT Image 2.5 Sunburst / xhigh 1024x1536 run used the two retained character sheets, background 0, repair source 0 and automatic repair OFF. Visual result: all four panels use normal proportions and one character-sheet-like style, including Panel 4; no chibi conversion. Internal QA found Panel 1 exact text omission/order failure, so art-style acceptance is `satisfied` while exact dialogue/text acceptance is `unmet`. The generated image remains displayed in local in-app tab 5. No second image generation was run.
- Delivery boundary for the follow-up: local edit plus one real API proof only. No new version bump, commit, push, release, note edit or backup has been authorized or performed for this follow-up.
- Local STEP2 selector UI follow-up: the ending selector now has a compact 28px-high cream selection face with 12px dark text, amber border, permanent right-side chevron, and a `▼ 選択メニュー` cue. The settings row uses a dedicated 12px bottom gap so the selector no longer touches the STEP2 execution button. Interaction/state logic is unchanged. The new regression failed against the zero-gap/dark control and passes after the compact correction. In-app-browser computed styles confirmed `height: 28px`, `font-size: 12px` and the 12px separation, and the visible screenshot showed the selector and STEP2 button as distinct rows. This is local-only; no API image generation, version bump or external delivery is part of this UI follow-up.
- Local enhancement/help-text follow-up: the seventh STEP2 enhancement category now resolves from the same ending-mode policy used by scenario and image prompt generation. Serious Documentary displays/sends `シリアス演出`, strengthens tension, silence, timing, reaction and conclusion without changing art style or normal proportions, and rejects CHIBI/COMEDY emotion tags; other endings retain `ギャグ演出`. STEP4 metadata uses the same resolved label. API quality/size and Web image correction/upscale explanatory copy now uses the same 10–11px scale as the video and safety guides. The API quality/size labels are 11px and their select controls are 26px high with 11px text; generation settings are unchanged. In-app-browser computed styles confirmed those exact values, while the serious scenario, `シリアス演出` label, final prompt and enabled STEP4 controls were all visible. Focused UI/mode checks pass 9/9; the preceding unchanged full suite remains 430/430, strict lint and production build passed. No STEP4 image generation was run. Delivery remains local only; do not version, publish, or back up this follow-up without a new explicit request.

- Target: publish the completed v6.1.2 STEP3 color/monochrome work plus the v6.1.3 follow-up that adds a fixed 12px gap between the settings-file save button and the STEP4 API generation button. The user re-authorized the new official deploy, FourPanel note update and established visible black-window full backup on 2026-09-13.
- Acceptance: default color; explicit color/monochrome selector; selection retained through STEP1/STEP2 resets, full settings reset returns to color; switching invalidates the old prompt without automatic assembly. STEP3 rebuilds the same scenario in the selected mode.
- Shared API/Web prompt: pure white lit face/skin, pure black ink/fills, black-on-white regular dots/hatching for tones; replace color-specific style/reference directives without changing dialogue, camera, acting or cast. No finishing-image import or pixel-conversion feature.
- Additional direction: simplify crowded nonessential background textures/screens without dropping location anchors or story clues. Emphasize pressure-sensitive G-pen taper, bold/fine line hierarchy, solid blacks/white cutouts, foreshortening and diagonal staging, while retaining quiet beats and explicit Camera/Action.
- Status: local implementation is complete. The mode choices are compact 24px controls: selected is white, inactive is distinct gray, and both labels are black. Browser inspection confirmed selection/copy behavior and the shared prompt contents. The first no-reference ChatGPT Web monochrome output was rejected after a visible chromatic accent remained in a hair-end region. A second authorized Web run attached both supplied character sheets and added a generic whole-page chroma audit; it still left a faint pink tint in light hair-end/interior strands, so it was also rejected for monochrome compliance.
- Changed paths: STEP3 UI/CSS and App wiring; workflow mode state/invalidation/reset; `manga-render-mode.js`; mode-aware prompt templates/assembler/emotion blocks; image QA and source-image repair medium preservation; README; two monochrome regression test files.
- Generic chroma-leak fix: monochrome assembly strips source-color adjectives from arbitrary cast/outfit reproduction data while retaining names, hair structure, glasses, garment form and other non-color identity traits. Its Identity Matrix checks face/eye shape, hairstyle/length, glasses and stable ink/tone assignments instead of hair color. A mandatory final whole-page chroma audit rejects any hue/tint/sepia/colored fringe/continuous grey anywhere. Existing sample-specific names in the production character-analysis/scenario/QA prompts were replaced with generic rules/placeholders.
- Follow-up fix: Web compaction ran before the monochrome marker was prepended, and shortened away the scenario-color precedence while preserving unqualified reference skin/hair reproduction. Pass the explicit mode through that early compaction; retain ink-only reference, skin, QA and scenario priority at every compaction stage. Both provider prompts now request a black printing plate, map every source-color region/accent to fixed ink/screens, and repeat a short ink check in each panel. Only repeated authored constraints are folded to retain the Web budget; source dialogue/Action and style-specific drawing instructions remain intact.
- Verification: the new reference/compaction regression failed before the fix; current focused monochrome tests pass 13/13, full Node suite 398/398, strict lint, production build and diff whitespace check pass. Existing Browserslist/chunk/import warnings remain. A fresh in-app-browser OpenAI API run was completed after this follow-up with the STEP3 prompt rebuilt in monochrome mode and both supplied character sheets attached. The visible result is clean monochrome with no coherent hair-tip/source-color region, but the returned RGB PNG is not mathematically achromatic/binary: 1,018,065 of 1,572,864 pixels have unequal channels, mostly 1-3 levels apart; maximum channel delta is 19. Therefore visual source-hue elimination passes this sample, while exact pixel-level monochrome remains unmet without post-processing.
- Superseded Web diagnostic: the earlier Web run used the old conversation prompt plus a manually authored generic follow-up. It was not an exact STEP3 rebuilt-output test. Its `scratch/web-mode-qa/monochrome-with-refs.png` result retained faint pink in light hair-end/interior strands and is not evidence for the current prompt.
- Fresh Web-paste proof after the compaction/reference-priority fix: sent the complete freshly rebuilt 16,062-character STEP3 prompt and both supplied character-sheet images together in one official ChatGPT Web message. ChatGPT converted the long pasted prompt into a text attachment while retaining both image attachments in that same message. The saved result is `scratch/web-mode-qa/monochrome-web-paste-fresh-with-two-references.png` (3,024,042 bytes, SHA-256 `B128E7D5C396D3CA498F87B76E57560EE6326F2CF716A995163A1ADA919C24AB`). Visual review passes source-hue removal: no pink/blonde/blue/brown tint is visible in hair ends, eyes or skin, and the page uses black linework, white regions, dot tone and hatching. Pixel audit still fails exact mathematical monochrome/binary output: 983,918 of 1,572,568 pixels (62.57%) have unequal RGB channels, maximum delta 18. Story/cast adherence is imperfect: the page emphasizes three main characters, adds festival/tax-booth signage, splits one panel internally and does not visibly preserve all five requested cast members. No further paid generation was run.
- Live API proof after the compaction/reference-priority fix: the in-app browser used the freshly rebuilt 16,062-character STEP3 prompt, not an old prompt plus follow-up. Prompt inspection found `BLACK INK PLATE`, `SOURCE COLOR BOUNDARIES`, and `SCENE COLOR PRIORITY` once each, `PANEL INK CHECK` four times, and no unqualified `Reproduce reference face, hair, eyes, skin, accessories` phrase. STEP4 sent two character sheets and no background/repair image to `gpt-image-2.5-sunburst` at xhigh, returning a 1024x1536 PNG after about 73 seconds. The saved result is `scratch/api-mode-qa/monochrome-api-with-two-references-fixed.png` (3,583,255 bytes). Visual review: no visible pink/blonde/blue/brown source hue remains; light hair ends are rendered with black lines/white gaps or dot tone, lit faces are white, and all four panels use strong G-pen-like black strokes, differentiated tone/hatching, and readable simplified backgrounds. Pixel audit: mean RGB 164.266/164.322/164.360, 64.73% of pixels differ in at least one channel, 1.83% differ by more than 5 levels, 0.06% differ by more than 10, maximum delta 19. Treat this as a visual monochrome pass and an exact-pixel monochrome fail; no second paid generation was run.
- STEP4 copy/layout follow-up: removed the API-only reference-supplement implementation note, changed the idle generation label to `APIで画像をアプリ内で生成する（STEP4）`, and moved the shared editable-prompt guidance directly below the thinking/status log and above the prompt editor. Focused source-order/copy regression passes 3/3; strict lint and diff whitespace checks pass. Local in-app-browser inspection confirmed the thinking/status log, guidance and editor in that visual order, exactly one new-label button, zero removed-note occurrences, and no browser warning/error logs.
- STEP3 prompt-editor heading follow-up: shortened `最終プロンプト（直接編集できます）` to `最終プロンプト`; the adjacent `DYNAMIC ENGINE V1.2.3` label and the separate editable-prompt guidance remain unchanged. Focused STEP4 copy tests pass 4/4; strict lint and diff whitespace checks pass. Local in-app-browser readback found the new heading once, the engine label once, the separate guidance once, the old heading zero times, and no browser warning/error logs.
- v6.1.2 was released successfully before the spacing follow-up. The v6.1.3 source uses explicit wrapper padding so the gap remains visible even when the disabled-state overlay is present; local DOM measurement reported 12px. The new regression failed before the fix and passes after it; the full suite passes 402/402, strict lint and production build pass.
- Delivery sequence: validate and commit v6.1.3 on canonical `main`, run the official unified release transaction, update and publicly verify FourPanel note `ndf063558c1f5`, then run the unchanged visible black-window full backup and independent verifier. Live image generation is already complete; prompt wording alone does not prove two-value raster or publisher submission compliance.

## v6.1.1 STEP1 full-width selector and drop guidance — 2026-09-12

- STEP1のキャラクター設定画像選択面を`w-full`にし、STEP2・3・4の主ボタンと同じ横幅へ統一する。ドロップ領域の案内は、STEP1ボタンまたはドロップ、複数枚の同時・後からの追加、任意の360°背景画像と作風設定JSON読込を明記した指定文へ置換する。
- 追加依頼として、STEP3内の「プロンプトをコピーする（web貼り付け時）」ボタンだけを白から薄い黄色へ変更する。固定ヘッダーの1枚絵用ボタン、コピー完了表示、処理は変更しない。
- 変更対象は`src/components/Step1Panel.jsx`、`src/components/Step4Panel.jsx`、`src/index.css`、対応テスト、ユーザー向け版情報・READMEだけ。入力処理、画像解析、生成プロンプト、API、FourPanel v6.0.9配布物、H3 T2V/I2V/Ref2Vは変更しない。
- Release notes: `docs/releases/v6.1.1.md`。公開、FourPanel note `ndf063558c1f5`、黒い窓フルバックアップの進行状態はroot `PLAN.md` を参照。

## v6.1.0 primary-step display correction — 2026-09-12

- v6.0.9でSTEP1の広いドロップ領域とSTEP2・3の太い外枠まで青くした誤りを修正した。STEP1は小さな「キャラクター設定画像を選択 (STEP 1)」操作だけ薄青、STEP2・3はボタン面だけ薄青で太枠は従来の薄いグレー、STEP4は変更なし。
- ローカル画面でSTEP1操作面274×25px／ドロップ領域透明、STEP2・3の底辺6pxが`rgb(203, 213, 225)`であることを確認。FourPanelワークフローJSONとカスタムノードZIPは検証済みv6.0.9配布をそのまま使用する。
- Release notes: `docs/releases/v6.1.0.md`。公開、note、訂正版バックアップの進行状態はroot `PLAN.md` を参照。

## v6.0.9 primary-step button release candidate — 2026-09-12

- STEP1の画像投入、STEP2のシナリオ生成、STEP3のプロンプト構築、STEP4の画像生成だけを共通の薄い青色へ変更。補助操作と実行ロジックは変更していない。
- FourPanel配布の生成グラフはv6.0.8から変更せず、現行アプリと導線を揃えるためワークフローJSON・5ノードZIP・英日併記READMEをv6.0.9へ再梱包した。最終v6.0.9 ZIPからの新規GPU生成と別PC実行は未検証。
- Release notes: `docs/releases/v6.0.9.md`。公開、note、黒い窓フルバックアップの進行状態はroot `PLAN.md` を参照。

## v6.0.8 released — 2026-09-12

- STEP4のFourPanel NonLM配布をv6.0.8へ更新。単独ワークフローJSONはPages/Hugging Face、カスタムノード5点ZIPはGitHub Release専用アセットとして分離し、アプリの2ボタンを維持した。現在のGitソースツリーから全配布ZIPと旧ワークフローを除去した。
- 新機能は、基本5秒に収まらない台詞だけ最大15秒へ延長するモードと、複合AV不合格時の構造化ショット差し替え、同一区間2回の映像不合格後の話者中心再構図。確定台詞・時刻・人物・音・H3生成BGMを保持する。
- 同梱PlagueKind内のLightX2V由来部分へApache-2.0本文と出典表示を追加。Git READMEとZIPのREADMEを一文内の`English / 日本語`形式へ統一した。T2V/I2V/Ref2Vは対象外。
- 配布元ソースcommit `5c9ae9f`、tag `fourpanel-v6.0.8`。最終ZIP 693,070 bytes / SHA256 `7ffe7566d865959447aef68601c5158af55ae54fcfdb429c7984270c6f2d62ac`。144 payload files、package 15 testsが合格し、APIキー・個人パス・モデル・利用者メディア・ネストしたアーカイブを含まない。
- 受領候補の39秒GPU/API生成と音声確認は供給元証拠。最終v6.0.8 ZIPからの新規GPU生成と別PC実行は未検証。リリース、FourPanel note `ndf063558c1f5`、黒い窓フルバックアップの進行状態はroot `PLAN.md` を参照。Release notes: `docs/releases/v6.0.8.md`。

## v6.0.6 release candidate — 2026-09-12

- 成人キャストの安全条件を、画像内へ印字しない内部制約として明示した。旧 `IMPORTANT AGE DECLARATION: All characters depicted in this image are adults aged 20 or older.` は出力しない。
- ユーザー提供の元プロンプトとキャラクター参照2枚を使い、OpenAI GPT Image 2.5 Sunburst / xhigh・1024×1536で1回実生成。問題の年齢英文は再発せず、指定タイトル、7本の台詞、2本の透かし、強い煽り・拳の遠近・肩越し・最終コマのデフォルメ演技を目視確認した。自動修正OFFで追加画像生成なし。
- STEP4のAPI品質・サイズ折り畳みボタンへ、現在選択中のモデル・品質・サイズを常時表示し、選択変更を即時反映する。Google AI使用時は自動設定表示とする。
- 黒い生成実行ボタンは `画像を生成する (STEP 4)` だけを表示し、モデル・品質・サイズの情報は白い設定ボタンだけに置く。
- リリース、FourPanel note `ndf063558c1f5`、黒い窓フルバックアップの進行状態はroot `PLAN.md` の「Nano Banana STEP4 API setting summary v6.0.6」を参照。Release notes: `docs/releases/v6.0.6.md`。

## v6.0.3 release candidate — 2026-09-12

- カメラの高さ・傾き・強い遠近感、全身の誇張、コマ間の演出差を積極的に許可する。人物、台詞、小道具、明示Action、静かな間は保持する。舞台に自然な背景人物は許容し、主役の明白な複製と区別する。
- 実OpenAI画像生成では俯瞰、肩越し、低い煽り、静かな寄りの落差と身体の勢いを確認。足先を必ず画面内へ収める指定は2回の比較でも保証できなかったため、完全な全身収まりやモデル一般の改善は主張しない。
- 現在のリリース、note、黒い窓フルバックアップの進行状態はroot `PLAN.md` の「Nano Banana expressive camera and acting restoration」を参照。Release notes: `docs/releases/v6.0.3.md`。

## v6.0.2 release candidate — 2026-09-12

- STEP4ログの「参照画像キャラシート再現禁止」を「キャラの外見は維持し、設定資料の配置・説明文はコピーしない」に修正。プロンプト出力前のAI精査待ちに、他STEPと同じ経過秒数の1行更新を追加。生成指示・モデルは変更なし。現在の検証・デプロイ・note・黒い窓フルバックアップの状況はroot PLAN.mdの「Nano Banana STEP4 wait counter and log wording」を参照。

## v6.0.1 release candidate — 2026-09-11

Current live delivery status: see root PLAN.md, Nano Banana four-panel quality reference API evaluation. User authorized final API verification then release, FourPanel note update and full backup even if image-quality issues remain. Automatic STEP3 review, bounded API candidate comparison/print fallback and STEP4 collapsible UI are included. Final real Sunburst/xhigh 1024x1536 output completed; QA JSON could not be parsed, original retained with warning, spine-lettering issue remains. Do not claim complete geometry repair or live fallback success. Release notes: docs/releases/v6.0.1.md. Prior v6.0.0 delivery is complete; this is a new versioned transaction.

# HANDOFF.md

## Current Status

- **v6.0.3 expressive camera and acting (2026-09-12)**: Camera height, tilt, foreshortening, full-body acting and panel-to-panel contrast are actively permitted while cast, dialogue, props, explicit actions and quiet beats stay protected. Natural background people are allowed; clear main-cast clones remain defects. Real Sunburst/xhigh four-panel trials showed stronger high/low/OTS/close contrast and acting energy. Exact head-to-feet containment remained unreliable, so the release does not guarantee it. Root `PLAN.md` owns the authorized release, FourPanel note `ndf063558c1f5`, and visible full-backup status. Release notes: `docs/releases/v6.0.3.md`.

- **v6.0.0 comedy-aware prompt review (2026-09-11)**: Root PLAN.md entry "Nano Banana four-panel quality reference API evaluation" owns live delivery status. Optional AI review preserves surreal intent and adjusts auxiliary directions only; quality warnings retain outputs. Real text-API trials preserved the gag and removed conflicting auxiliary directions. Image accuracy remains imperfect. Release notes: docs/releases/v6.0.0.md.

- **v5.9.9 Gemini character references (2026-09-10)**: Root PLAN.md entry "Nano Banana Gemini character-reference parity" owns current evidence and delivery status. Two real reference-image runs completed; output defects remain. The user explicitly requested release, note update and full backup despite those limits. This release does not claim flawless rendering.

- **v5.9.8 OpenAI initial image selection (2026-09-10)**: Root PLAN.md entry "Nano Banana v5.9.8 OpenAI initial image selection" owns the current implementation, real API verification, release, note and full-backup status. The verified models response determines Sunburst/xhigh or 2.0/high at key connection; image errors do not automatically switch models or retry.

- **v5.9.7 four-panel distribution and HF recovery (2026-09-10)**: The live task record is the root `PLAN.md` entry "Four-panel NonLM v5.9.7 supplied-package audit and release". It owns package audit evidence, release status, the corrected note target `ndf063558c1f5`, and the explicitly requested visible full backup. Refer to that entry for current completion state; the older v5.9.6 backup exclusion below does not apply to this task.

- **v5.9.6 reference-image and H3 distribution candidate (2026-09-10)**: OpenAI character/background references and source-image repair are implemented; the real two-character-sheet Sunburst/xhigh run completed with QA PASS (repair OFF). H3 v1.1.7 ZIP and separate JSON include five folders and corrected package instructions. Full suite: 310/310; warning-free lint passed. Release and note publication are authorized; full backup is excluded. Current detailed state and evidence limits: `docs/superpowers/plans/2026-09-10-openai-reference-image-editing-and-web-guidance.md`, section 12. Publication is not yet complete.

- **v5.9.5 four-panel type-hierarchy release candidate (2026-09-09)**: Separates page-title type from dialogue type in both provider families: title is extra-bold condensed Japanese Gothic, while vertical speech bubbles use regular, slender Japanese manga Mincho-style lettering and explicitly reject bold Gothic/sans-serif. A single real GPT Image 2.5 Sunburst/xhigh four-panel generation retained the intended bubble weight; its QA reported unrelated wardrobe mismatches, with repair disabled so no extra image was generated. Local full suite passed 283/283, lint and production build passed. The user authorized the official unified release, a new full backup, and the existing note article update. Root PLAN owns the live release record; do not publish the note without action-time confirmation.

- **v5.9.4 STEP4 image selection and conservative repair (2026-09-09)**: Current task state and release/backup evidence are maintained in root PLAN.md, under Nano Banana GPT Image 2.5 selection and comparison. Adds five model/quality choices, Sunburst/xhigh default based on four live comparisons, manual 2.0 verification fallback help, one optional repair with original-versus-repair comparison, and silent Action actor parsing correction. Local tests pass 282/282 and strict lint passes. Publication and full backup are not yet complete; consult the live PLAN entry before continuing.

- **v5.9.3 HF binary-upload recovery (2026-09-08, in progress)**: User authorized correcting the unpublished HF deployment, stopping the v5.9.2 resume, and retrying full backup after repair. The Hub rejected regular ZIP blobs even below 10 MB. All current ZIPs now use LFS; unpublished deployment commits are preserved under a recovery branch before rebuilding from origin/main. Focused regression failed before the fix and passes 2/2 afterward. README current-version text is synchronized. Existing v5.9.2 tags remain unchanged. Official v5.9.3 release and independent public/backup evidence are pending. Root PLAN owns live coordination.

- **v5.9.2 current-public-ZIP classification recovery candidate (2026-09-07)**: The v5.9.1 recovery correctly preserved the blocked local HF history, but its size classifier still resolved attribute-relative paths outside `public/` and stopped on an obsolete, absent ZIP. It now evaluates only ZIPs that exist under `public/downloads`; the seven extant packages are detected, including the supplied 421,717-byte FourPanel ZIP, which is classified as ordinary Git. Focused HF deployment regression passes 2/2. Full validation, official v5.9.2 release/public verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.9.1 HF failed-LFS recovery candidate (2026-09-07)**: The v5.9.0 source, tag, GitHub Release, and Pages stages completed, but HF still rejected the old local-only LFS history before the new small-ZIP path could push. The official deploy route now detects that exact state, preserves the blocked commits under a local recovery ref, resets only the local HF checkout to its current `origin/main`, and rebuilds the requested distribution. No published HF history is rewritten. Focused HF deployment regression passes 2/2. Full validation, official v5.9.1 release/public verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.9.0 small-HF-distribution recovery candidate (2026-09-07)**: The v5.8.9 fail-forward transaction reached verified source, tag, GitHub Release, and Pages stages, but Hugging Face rejected the separate LFS object transfer for the supplied 421,717-byte ZIP. The deployment path now derives every current distribution asset from `public/.gitattributes`, preserves ZIP/JSON/sidecar bytes, removes stale LFS rules for ZIPs at or below 10 MiB, and reserves LFS/Xet only for larger future files. The focused HF deployment regression passes 2/2. Full validation, official v5.9.0 release/public verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.8.9 HF distribution-attribute recovery candidate (2026-09-07)**: The v5.8.8 unified release reached verified source, tag, GitHub Release, and Pages stages, but its HF deploy failed before push because `scripts/deploy_hf.ps1` only tracked the historical r8 ZIP and byte rules. The recovery derives every current ZIP LFS target and workflow/sidecar `-text` rule from `public/.gitattributes`, preserving Pages as ordinary static bytes while using the HF LFS/Xet bridge. RED/GREEN deployment regression passed 2/2. Full validation, official v5.8.9 release/public verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.8.8 supplied 2026-09-07 Fused4＋SLA external-API distribution candidate (2026-09-07)**: Replaces the two public STEP4 download targets with the supplied `FourPanel_Fused4_SLA_API_20260907` ZIP and its separately served workflow JSON. The ZIP is 421,717 bytes / SHA-256 `1020857E4DBE0B65603D23721D073242FD24F66D33257F1D73CC3DA0EE7B16D4`; its 26-node/28-link workflow is 73,107 bytes / SHA-256 `2839ED348E9334807A9BA7E807D297C32CBAEE8824691943C9D00DE1E7EADEBB`. The supplied SHA256 manifest covers every payload, with no unsafe paths or credential artifacts. The setup ZIP and individual workflow deliberately remain separate URLs. Focused distribution tests pass 4/4, full Node tests pass 273/273, lint is clean, and the local STEP4 browser check shows both new filenames. Official unified release, public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.8.7 supplied H3 Fused4step＋SLA r8 distribution candidate (2026-09-06)**: Replaces the separate public workflow JSON and bundled-three-custom-node ZIP with the corrected `Super-FURU-AI_四コマ_非LLM_Fused4step_SLA_配布用_20260906_r8` package. The supplied outer ZIP SHA-256 is `5EFF0F1EB9EBB74BFEFF7DDFEEAE4782A4BFFC696A5948A7E845B5319C66C31C`; all 86 payload files match the internal manifest, and no unsafe paths, credential-value patterns, legacy credential names, or credential disk writes were found. The r8 workflow is 26 nodes/28 links with SHA-256 `2445668CBDF5E2169B5FB5B709B174A6E658CCBDAE2B8BC6DC8BF20FBAA9B337`, internal distribution version r8, and no stale r5/r7 labels. It retains four video steps and audio refinement at two steps/denoise 0.5 while adding Japanese dialogue-reading review, audio auditing, and faulty-shot-only visual replacement. The bundled folders are `ComfyUI-NanoBanana-H3`, `ComfyUI-MiniMax-H3-Long-Video`, and `ComfyUI-Spectrum-MiniMax-H3`; `ComfyUI-H3-AudioRefine` and `ComfyUI-PlagueKind-Nodes` remain ZIP-external requirements linked directly from STEP4 and README. Focused distribution/UI/HF tests pass 14/14. Full validation, browser inspection, official unified release, Pages/Hugging Face public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window backup remain pending.

- **v5.8.3 supplied H3 target-match distribution candidate (2026-09-05)**: Replaces the separate public workflow JSON and three-node setup ZIP with the supplied `Super-FURU-AI_4koma_H3_非LLM_対象一致修正_配布用_20260905-151249` package. The outer ZIP SHA-256 is `AC1EDA6956D9EDD7C6173A1BA079B1EC9F57F9FA2DED877739D8DD2ACF515CD8`; the extracted 27-node/29-link workflow JSON SHA-256 is `7B658CE7CB58337AD16068D99C0362F924A45FCFDFF1E94ADC4FE04AD7C354CF`. Static inspection found no embedded archives, model/media payloads, credential values, disk/browser credential persistence, or setup-script network/process commands; the package's setup script was not executed. Focused tests, full validation, official unified release, Pages/Hugging Face public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window backup remain pending.

- **v5.8.2 bounded facial-acting release candidate (2026-09-05)**: Adds one shared facial-acting contract for STEP2 scenario creation, optional expression enhancement, and both four-panel provider prompts. Readable main faces are described with story-supported physical cues selected from brow, eyelid/eye opening, gaze target, mouth shape, and head/torso angle without forcing close-ups, front-facing poses, or camera gaze; scripted camera, action, conversational eye-line, hands, and props remain higher priority. Focused regression coverage verifies both provider prompts and expression-only routing. The full Node suite, strict lint, production build, pre-deploy check, and one real OpenAI `gpt-image-2` four-panel generation passed; the generated page showed stronger expression progression, while manual inspection also confirmed the documented model limit because one panel hid the main face and one supporting character posed toward a phone. Official unified release, Pages/Hugging Face verification, C-drive release-copy verification, and the explicitly requested full backup remain pending.

- **v5.8.1 supplied H3 audio-two-step distribution candidate (2026-09-05)**: Replaces the separate public workflow JSON and three-node setup ZIP with the supplied `Super-FURU-AI_4koma_H3_非LLM_音声2step_配布用_20260905-081753` package. The outer ZIP SHA-256 is `33EFD6C1985A08E9905956673BE68CE0D54E9BC6712AA4FA0CEEB8A7B3FF053B`; the 27-node/29-link workflow JSON SHA-256 is `4ECE221D7882E91D25F986C34F1EDB4D4E8A36EA81E08D1036EDC369DC3E5F2C`. All 80 supplied manifest entries matched. The three custom-node trees are byte-identical to the preceding bundle; the workflow changes audio refinement from four to two steps while retaining denoise 0.5 and eight video steps. Focused tests pass 6/6; the full Node suite, strict lint, production build, and in-app local UI inspection are complete. Official unified release, Pages/Hugging Face public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window backup remain pending.

- **v5.8.0 latest H3 custom-node bundle candidate (2026-09-04)**: The supplied 14:00:19 bundle outer SHA-256 is `3F036794B88E065098385317861471673E49896F02015D518355B552AE8F144E`; its 27-node/29-link workflow is byte-identical to the already public v5.7.9 JSON (`B1158A516112362A8C23A88461879F15B7C0C2BCCBDF363F94F0BF7FABA2680E`). Only the three-node bundle is updated. The meaningful code changes identify `<Subject N>` before dialogue timing and add a pre-generation Japanese dialogue-text and reading review. Recursive literal-key and legacy credential-store scans passed; focused download/HF preservation tests passed 6/6. Release, public verification, and explicitly requested visible full backup remain pending.

- **v5.7.9 supplied recommended H3 BGM distribution candidate (2026-09-04)**: Replaced the current MiniMax H3 public assets with the supplied 2026-09-04 11:13:55 recommended fully variable-duration workflow and its three-custom-node bundle. The workflow is preserved at `B1158A516112362A8C23A88461879F15B7C0C2BCCBDF363F94F0BF7FABA2680E` and the ZIP at `45435DFDC671AEF508E3881BA0ED079586749AC75F4147AFF92AD8C547B83F78`. Removed only superseded H3 ZIP/JSON files from the current distribution tree; Git history is unchanged. The supplied workflow uses five seconds per retained dialogue turn with no upper duration cap and falls back to 30 seconds only when no dialogue remains. Focused tests and the full suite passed 269/269; strict lint and a production build passed; release, public-byte verification, and the explicitly requested visible full backup remain pending.

- **v5.7.8 Hugging Face workflow-byte preservation candidate (2026-09-04)**: The completed v5.7.7 transaction published the supplied ZIP byte-for-byte on Pages and Hugging Face, but an additional direct download audit found that the new workflow JSON was 102,112 bytes / SHA-256 `4CDB259561AF9C5C87B09484F6B116CE2C4CFDB0D113AA6F13E0D55B40CDC120` on Pages and 99,565 bytes / SHA-256 `226ECC52E8C0C0C51EB72D1EF4199521B4D64203BBDF9A7AF9B47D09414F4459` on Hugging Face. Root cause: the protected HF-specific `.gitattributes` tracked the new ZIP through LFS but did not mark the new JSON `-text`, so Git normalized its CRLF bytes on commit. A RED/GREEN regression now appends the exact JSON preservation rule before `git add`; focused HF tests pass 2/2. The full suite passes 269/269; strict lint, production build, pre-deploy, diff check, and a headed-browser v5.7.8 UI/zero-console-error check pass. The official v5.7.8 transaction, repeated Pages/HF byte checks, and the visible black-window full backup remain pending.

- **v5.7.7 stable auto-duration H3-generated-BGM distribution release (2026-09-04)**: Replaced the two STEP4 distribution links with separate direct downloads for the supplied stable workflow JSON and its three-custom-node setup bundle. The supplied outer ZIP matches its sidecar SHA-256, all 71 internal manifest entries match, the exact workflow parses as 27 nodes / 29 links, and recursive secret/credential scans found no bundled API key, model, input image, or generated video. `ComfyUI-NanoBanana-H3` is MIT-licensed; Long-Video remains GPL-3.0-only and Spectrum GPL-3.0-or-later. Receipt `nano-banana-pro-v5.7.7-2026-09-04_024911.json` verified source `edfd5bd417470620373c1fc41d02c969191ed735`, Pages `56d17581d3775b8475bef8bae9ef42f3471ad531`, HF `519e584116d43b6c020c0b848ac1836563212947`, and C-copy tree SHA-256 `D3B0124DF4CECEA111C3D10758FB41B728083AF592F4702798517589BD20F731`. A separate public-byte audit then found only the HF workflow JSON was line-ending-normalized; v5.7.8 supersedes the HF byte-delivery portion before backup.

- **v5.7.6 Hugging Face LFS attribute preservation release candidate (2026-09-04)**: The v5.7.5 source, tag, GitHub Release, and Pages deployment completed, but the HF stage failed because `scripts/deploy_hf.ps1` preserved the Space-specific `.gitattributes` during cleanup and then overwrote it from `dist/` during copy. That removed the existing Fused ZIP's LFS rule, staged the 628,086-byte ZIP as a regular Git blob, and triggered the Hub binary pre-receive rejection. A RED/GREEN regression now requires the copy phase to preserve both `.gitattributes` and `README.md`; the focused test passes 2/2. The rejected HF commits remain only on local recovery refs, and HF `main` is restored to the public v5.7.4 commit `abe111725dbd6db4ea55f2da80f354f426c9e6cd`. Full local verification, a v5.7.6 unified fail-forward release, public-byte/C-drive verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.7.5 variable-duration Hybrid b25 distribution release candidate (2026-09-03)**: Replaces the public workflow and distribution bundle with the separately supplied `MiniMax-H3_四コマ自動可変尺_今回成功版_配布用_20260903.zip` content. The public workflow retains its supplied SHA-256 `5EA044A38897410F05CB7051C04C924A0254207218977FFE2794D06A516E1D97`, 27 nodes, and 29 links. The bundle contains three raw custom-node folders: the workflow-specific original `ComfyUI-NanoBanana-H3` under GPL-3.0-or-later, `ComfyUI-MiniMax-H3-Long-Video` under GPL-3.0-only, and `ComfyUI-Spectrum-MiniMax-H3` under GPL-3.0-or-later. Its license scope excludes ComfyUI, model weights, external APIs, and user inputs/outputs. Keys remain process-memory-only and are absent from the workflow and bundle. Focused bundle/UI tests, lint, build, and local browser checks are complete; unified release, public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.7.3 ComfyUI process-memory credential release completed (2026-09-03)**: Removed the plaintext credential-file implementation and established process-memory-only storage. Receipt `nano-banana-pro-v5.7.3-2026-09-03_104602.json` verified source commit `0fc14fff360179bbb558709d28d7aacde9040d62`, public bundle/workflow bytes, and the release copy. The visible full backup produced `antigravity_full_backup_2026-09-03_104956.zip` with matching local/Drive SHA-256 `32443EFE5D9510D160D30FD5C309E81BAACECDC5E64CF3B99E19E09EFE7C5BDB`. v5.7.4 supersedes only the dialog timing and latest supplied distribution bytes.

- **v5.7.2 Hugging Face binary-delivery recovery completed (2026-09-03)**: The full unified transaction completed with receipt `nano-banana-pro-v5.7.2-2026-09-03_101658.json`. The source, tag, GitHub Release, Pages, Hugging Face LFS/Xet delivery, C-drive copy, and final public checks passed. v5.7.3 supersedes it solely to remove the ComfyUI credential-file behavior and correct every active description before the requested full backup.

- **v5.7.1 supplied MiniMax H3 Fused Turbo/Spectrum/SLA distribution release candidate (2026-09-03)**: The existing latest-workflow route now serves the supplied 126,687-byte, 28-node/32-link Fused Turbo INT8 ConvRot workflow unchanged (SHA-256 `F8973537727A0DD1B5DAAC8F2D73D28BC7EF08D259C4C9C03F88AB7278586F0C`). It uses Spectrum, H3 SLA Attention, RES Multistep, Simple, and 8 steps, with registered metadata for four separately downloaded models. The separate custom-node/setup download is a sanitized 623,474-byte bundle (SHA-256 `870024CC9EC51955ABE8AF8B45145200DFFE11D7611DA5D3A0224F12BE0AAF9C`) containing all four supplied custom-node packs and supporting documents; only the stale nested `h3_prompt_system.txt.bak-20260820` backup was excluded, while the attachment remains untouched. STEP4 and README document folder-level replacement, no old/new mixing, ComfyUI restart, provider/key handling, and the two distinct download files. Focused distribution tests passed 6/6, the full Node suite passed 270/270, zero-warning ESLint passed, and the Vite production build completed. Pre-deploy, local browser proof, unified release/public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.7.0 supplied MiniMax H3 Spectrum workflow release candidate (2026-09-02)**: The existing latest-workflow button now serves the newly supplied 93,219-byte Hybrid b25 Turbo v4 LoRA 8-step JSON unchanged (SHA-256 `83DEA9A22988F06BFC3D7B406D2EB3BC031DB60621F15EA5A2DEC617F6249CAE`). The graph has 28 nodes and 32 links, adds `SpectrumApplyMiniMaxH3` v0.2.23 between the LoRA and guider/scheduler paths, and uses `minimax_h3_video_vae_int8_convrot.safetensors`. STEP4 and README now identify the separate `ComfyUI-Spectrum-MiniMax-H3` dependency and exact VAE filename. The custom-node ZIP remains a distinct 21,832-byte download with SHA-256 `8FD57A8EEE78A1B2F0929EA12A0EF635D7D63E6DB24D73BE6B4D6C8E5258EF4E`. Local browser proof shows v5.7.0, both visible buttons, distinct same-origin URLs, the Spectrum setup guidance, and no console warnings or errors; local HTTP bytes match both expected hashes. Focused workflow tests passed 6/6, the full Node suite passed 270/270, strict ESLint passed, and the Vite build passed. The v5.7.0 release also includes the already-committed v5.6.9 parser false-positive fixes. Unified release, public-byte verification, C-drive release-copy verification, and the explicitly requested visible black-window full backup remain pending.

- **v5.6.9 dialogue/staging parser release candidate (2026-08-29)**: Fixed two shared parser false positives: topical prose mentioning viewers, readers, or characters is no longer copied into every panel as a user staging lock, and an unregistered descriptive category ending in `のキャラ` is no longer emitted as a speech-bubble speaker. Explicit eye-line instructions and established generic roles such as `司会者` remain supported. Regression coverage was added for both paths. Local proof: focused tests, full Node suite (270/270), ESLint, Vite build, pre-deploy check, and a real OpenAI `gpt-image-2` four-panel generation. The final prompt contained zero staging-lock/article/rogue-speaker occurrences; the 1024×1536 output rendered only the six supplied Japanese dialogue lines vertically, and in-app visual QA passed. Unified release and the explicitly requested visible black-window full backup are pending.

- **v5.6.8 MiniMax H3 supplied workflow release candidate (2026-08-28)**: The existing latest-workflow download route now serves the supplied 27-node, 31-link Hybrid b25 Turbo v4 LoRA 8-step JSON unchanged (SHA-256 `C43A72EE7853BA7066EA09F660674698AA9EFB15628611F48D4B9C24493727BC`). The custom-node ZIP remains a separate button and asset. The supplier artifact parsed successfully; its content scan found no API-key value, authentication token, or private key. Focused download tests and the full Node suite passed; strict ESLint, Vite build, pre-deploy check, and diff check passed. A local v5.6.8 Vite server returned both files with HTTP 200; the workflow bytes matched the supplier and differed from the custom-node ZIP. The browser automation runtime could not reach this Windows localhost, so browser-UI proof is unverified. Unified release, public-byte verification, and the explicitly requested visible black-window full backup are pending.

- **v5.6.7 conditional cinematic techniques and character-sheet QA release candidate (2026-08-28)**: A fail-closed library can replace only existing fixed-length camera slots with at most two physically supported cinematic depth, shadow, motion, focus, reflection, refraction, or miniature-scale cues across a four-panel page, and one compact router cue for the single-image Web-copy protocol. Explicit camera, cast, action, anatomy, props, and readable text always win; unsupported or conflicting scenes retain the baseline prompt exactly. STEP4 visual QA now receives the generated candidate first and attached character sheets afterward, so it can identify visible outfit and identity mismatches as `character_reference` issues before its single bounded repair. Fresh local checks passed 268/268 Node tests, ESLint, Vite build, pre-deploy checks, and diff check. Real OpenAI `gpt-image-2` evidence passed for a four-panel page after the QA detected and corrected a vest-uniform mismatch, and for the correctly parameterized single-image route; full-resolution review confirmed correct sailor uniforms, character identity, layout, dialogue where applicable, and coherent anatomy. Unified release, public verification, release-copy verification, and the explicitly requested visible black-window backup are in progress.

- **v5.6.6 MiniMax H3 workflow distribution release candidate (2026-08-26)**: The public Hybrid b25 MiniMax H3 workflow keeps its existing filename and STEP4 download route but now matches the supplied 27-node, 31-link JSON exactly (SHA-256 `2CA5573BEBBC2C30E9A58BE01941B761BAA936181B4F2D6F66F929BACCAA8606`). Its custom-node types and model metadata remain compatible. Local validation, official unified release, public verification, and the explicitly requested visible full backup are pending.

- **v5.6.5 dialogue-contract retry release candidate (2026-08-26)**: STEP2 now validates printable speech-bubble dialogue with the same extraction logic used by final-prompt assembly, so quoted visual-direction text cannot masquerade as panel dialogue. Candidates that fail the dialogue contract are regenerated automatically up to three total attempts. If all attempts fail, the application blocks the no-dialogue result from STEP3 and STEP4 and gives an actionable instruction to rerun scenario generation; this hard contract cannot fall through as a normal quality warning. Local validation, official unified release, public verification, and the explicitly requested full backup are pending.

- **v5.6.3 published-workflow byte-preservation release candidate (2026-08-26)**: The v5.6.2 public button refresh is retained, and the static-site output now carries a root `.gitattributes` rule so the supplied `Super-FURU-AI-4koma-H3-Hybrid-b25-Turbo-v4-LoRA-8step-v1.json` remains byte-for-byte identical on GitHub Pages. This is required for the specified SHA-256 `8A95FA2D2F38FDBB40F5533D82F3318A19223829DEA42583EDC7B12C37D30011`; the separate custom-node ZIP remains `8FD57A8EEE78A1B2F0929EA12A0EF635D7D63E6DB24D73BE6B4D6C8E5258EF4E`. Focused verification, official unified release, public byte verification, and the explicitly requested visible full backup remain pending.

- **v5.6.4 Pages dotfile publication release candidate (2026-08-26)**: The existing byte-preservation rule is now included in the `gh-pages` publish set through `--dotfiles`; without it, the public branch lacked `.gitattributes` and normalized the supplied JSON. The v5.6.4 unified release must confirm the GitHub Pages workflow has SHA-256 `8A95FA2D2F38FDBB40F5533D82F3318A19223829DEA42583EDC7B12C37D30011`, alongside the unchanged custom-node ZIP SHA-256 `8FD57A8EEE78A1B2F0929EA12A0EF635D7D63E6DB24D73BE6B4D6C8E5258EF4E`, before the requested visible full backup.

- **v5.6.2 two-download-button distribution release completed (2026-08-26)**: The Nano Banana menu now keeps two separate same-origin download actions: `ComfyUI-NanoBanana-H3-Latest-2026-08-26.zip` for the custom node and `Super-FURU-AI-4koma-H3-Hybrid-b25-Turbo-v4-LoRA-8step-v1.json` for the Hybrid b25 workflow. STEP4 and README give the custom-node replacement, `user/default/workflows`, full-restart, node-4 Provider, model, and no-secrets guidance without embedding credentials. The supplied ZIP SHA-256 is `8FD57A8EEE78A1B2F0929EA12A0EF635D7D63E6DB24D73BE6B4D6C8E5258EF4E`; the JSON SHA-256 is `8A95FA2D2F38FDBB40F5533D82F3318A19223829DEA42583EDC7B12C37D30011`. The official receipt verified all release stages, but Pages normalized the JSON line endings, so v5.6.3 supersedes it for byte-exact distribution.

- **v5.6.1 MiniMax H3 one-click model metadata and FURU workflow-guide release candidate (2026-08-24)**: The distributed `Super-FURU-AI-4koma-H3-Turbo-v4-LoRA-8step.json` now carries exact source URL, SHA-256, target directory, and filename metadata for all five required weights: Ref2VA diffusion model, Qwen3VL text encoder, video VAE, audio VAE, and the Turbo v4 LoRA. Its two left-side Markdown guidance notes, STEP4 helper, and README are bilingual and state the FURU four-panel manga-to-video contract: four contiguous panel-order shots retaining panel-derived cast, dialogue ownership, actions, setting, and punchline; each visible character has a role-appropriate movement, while the workflow removes balloons and composites its title and fixed end credit after H3. The workflow tells users that registered missing models show Download after opening it, while browser-based ComfyUI can require moving a browser download to the displayed model folder. Focused regression coverage was RED against absent hashes/LoRA metadata and is GREEN at 7/7; full local validation, browser, unified release, public verification, Downloads backup copy, and black-window full backup verification remain pending.

- **v5.6.0 MiniMax H3 Turbo v4 LoRA distribution release candidate (2026-08-23)**: Replaces the dedicated 20-step workflow with `Super-FURU-AI-4koma-H3-Turbo-v4-LoRA-8step.json` (27 nodes, 31 links, SHA-256 `C35BAC4B788FBD17166EF3C10BD3E36B3FA7784800F2EC5DFC38F37511ABA42F`) using Turbo v4 LoRA strength 1.0, Euler, Beta, and 8 steps. The standard-template copy prompt matches the supplied 22,266-character text exactly (SHA-256 `392C22212BB33A72CA48DAD4D3AAF9368E0537400372B6ACE67931946263EB5A`) and keeps the Shot 1 title. The dedicated shared custom node derives per-panel `panel_cast[N]`, validates `overlay_title` fail-closed, removes it before H3 conditioning, and composites the title once after decoding. The rebuilt ZIP contains exactly the three documented files, excludes the stale ComfyUI API branch, and has SHA-256 `119D95D8CCB8B4EBFA3CC8D934078848C23196F0DA51C3C971E3B8789D0BDA8B`. Focused tests passed 14/14, the full Node suite passed 247/247, ESLint passed with zero warnings, and the local in-app browser showed v5.6.0, both separated routes, exact same-origin download links, no horizontal overflow, and no console warnings or errors. Independent re-review found no Critical or Important issues. Unified release and the authorized visible black-window full backup are pending.

- **v5.5.9 MiniMax H3 guide release candidate (2026-08-23)**: STEP4 now introduces MiniMax H3 as the Reference-to-Video route for turning the generated four-panel manga into video before presenting either action. It lets users choose the manual ComfyUI standard workflow or the dedicated all-in-one workflow first, while retaining the established prompt-copy, custom-node download, and workflow-JSON download order. The README and v5.5.9 bilingual release notes reflect the same boundary. The new UI-route regression first failed for the missing introduction and then passed. The full Node suite passed 246/246, ESLint passed with zero warnings, Vite production build passed (existing Browserslist age and chunk-size warnings only), and the local in-app browser showed v5.5.9 plus the introduction and all three H3 actions with no browser console warnings or errors. Unified release, fresh public verification, and the requested visible black-window full backup remain pending.

- **v5.5.8 workflow/custom-node distribution release candidate (2026-08-23)**: Replaces both public download artifacts with the newly supplied Y-drive sources. The workflow remains the 26-node/30-link, no-`PreviewImage`, `simple`/20-step standard version and now matches source SHA-256 `433D5A2364ACE1422D92E7B1B54B91825FF1158D6C4F471D1B5DABB1971BED6F`. The custom-node ZIP contains exactly `ComfyUI-NanoBanana-H3/__init__.py`, `h3_prompt_system.txt`, and `web/nanobanana_h3.js`; its file hashes match the supplied folder and the ZIP SHA-256 is `D676558E780915734DCE15855CBE890BB20C1FC3F3022531344635775F364154`. Source and packaged-asset scans found no API keys, credentials, model weights, LoRA weights, manga images, or generated videos. Focused distribution tests passed 5/5, the full Node suite passed 245/245, zero-warning ESLint and Vite production build passed, and the local app showed v5.5.8 plus both same-origin download URLs. Unified release, fresh public byte verification, and the requested visible black-window full backup remain pending.

- **v5.5.7 workflow-distribution synchronization release completed (2026-08-23)**: Replaced the stale 27-node/31-link download asset with the user-supplied 26-node/30-link standard workflow, removing `PreviewImage` and preserving source SHA-256 `9F21C48D7BD30B920F055ECF1B16969BFA709063780DD4C60053258DF39F563B`. Official receipt `C:\Users\sx717\Antigravity\backups\release_receipts\nano-banana-pro-v5.5.7-2026-08-23_065934.json` verified preflight, validation, build, source push, annotated tag/GitHub Release, Pages, Hugging Face, C-drive release copy, and public verification for source commit `9499cd34c2b566b59d48b327ae24b46c8224d97d`. The newly supplied v5.5.8 artifacts supersede those download bytes.

- **v5.5.6 standard workflow and custom-node distribution release completed (2026-08-21)**: Replaces the Nano Banana menu's distributed workflow with the user-supplied unmodified standard 20-step JSON, adds `ComfyUI-NanoBanana-H3.zip` with exactly `__init__.py`, `h3_prompt_system.txt`, and `web/nanobanana_h3.js` under the required root folder, and adds a separate direct-download button plus ordered install/restart/API/model guidance. Local exact-hash, ZIP hierarchy, security, focused tests, full 245/245 Node suite, zero-warning ESLint, production build, desktop/mobile DOM, keyboard, console, and in-app-browser download checks passed. Official receipt `C:\Users\sx717\Antigravity\backups\release_receipts\nano-banana-pro-v5.5.6-2026-08-21_210231.json` verified source commit `e5d59cb38ab6688627baa6d1a26939f188bf1186`, annotated tag/GitHub Release `v5.5.6`, Pages commit `e3aa20968a52e97d688e43f87b7c2849c3ad8597`, Hugging Face commit `91661f142db662aaf8d3b40a41aed1d51b5478f4`, C-drive release copy, and public surfaces. Fresh public Pages downloads matched the source JSON SHA-256 `E7E653EBEEFA44602A72B7615BBA1541A1D0FDE0FC7412EAA72E91A1071DE3A6` and ZIP SHA-256 `A05A2FA7A3572D9F5F8CC86CC711E3D8F3C420BA3E96F7EF1DD8E362D099C7B3`; the ZIP held exactly the required three paths. The separate visible `backup_full.ps1 -NoGui` run produced `C:\Users\sx717\Antigravity\backups\antigravity_full_backup_2026-08-21_211239.zip` (138,690,462 bytes), and the independent verifier returned `FULL_BACKUP_VERIFIED apps=7` with matching local/Drive SHA-256 `A0E1555B86C89DB6C4E8B0722D40B882B993B623094E628D14346BF48F36C647`, readable required restore content, seven-app parity, and released lock.

- **v5.5.5 body-acting and single-image quality-path release candidate (2026-08-21)**: Four-panel generation now treats reference-sheet poses as identity reference rather than recurring character gestures. The scenario and prompt paths require varied action families, action phase, weight-bearing support, hand roles, and contact targets; repeated default pointing, lens-facing hand/prop thrusts, and strikes against a supporting surface are bounded unless explicitly scripted or used as a running gag. Default eye-level framing is prohibited unless explicit or story-required, while normal dialogue remains counterpart-facing rather than lens-facing. The shared one-shot Web-copy prompt keeps the same body-acting baseline. A real `gpt-image-2` four-panel run confirmed four distinct actions, partner-facing dialogue, and varied non-eye-level cameras. A real one-shot API exercise exposed that the downstream QA repair path always requested a four-panel replacement; it is now mode-aware and, on a second real run, retained one scene and one character rather than introducing panels, a new setting, or new cast. The one-shot test prompt was deliberately scenario-free and both original and repair still received an arm-anatomy warning, so it is not evidence of perfect anatomy. Fresh checks passed 243/243 Node tests, ESLint, Vite production build, and diff check. Unified release and the separately requested visible black-window full backup are authorized; completion requires a new v5.5.5 release receipt, Pages/Hugging Face/C-drive public verification, and `FULL_BACKUP_VERIFIED apps=7`.

- **v5.5.4 public workflow-download release candidate (2026-08-21)**: In-app-browser verification of the v5.5.3 public Pages button found that the cross-origin GitHub Raw URL did not emit a browser download event because the browser ignored the `download` attribute. The STEP4 UI now uses the same-origin Pages workflow asset, while the workflow-internal Raw GitHub distribution URL remains unchanged. A focused regression was RED against the cross-origin URL and GREEN after the correction. The user authorized the continued unified release and a fresh visible black-window full backup; completion requires a v5.5.4 receipt, public button click evidence, and independently verified backup.

- **v5.5.3 ComfyUI workflow-distribution release candidate (2026-08-20)**: STEP4 now separates two MiniMax H3 routes: the standard ComfyUI R2V / Ref2VA route copies only the authoring prompt, while the end-to-end route downloads a dedicated ComfyUI workflow JSON for Nano Banana image conversion, H3 prompt creation, H3 video generation, and deterministic credit composition. The native download control was verified in the in-app browser; the saved JSON matched the distributed asset SHA-256 and retained the public sample image as the LoadImage default while clearing the H3 and Nano Banana prompt fields. The distributed workflow documents required MiniMax H3 assets, custom nodes, and Gemini, OpenAI, and LM Studio options without containing API keys, personal images, or generated videos. The user explicitly waived a real MiniMax H3 run and Nano Banana API execution for this release. Candidate validation: 234/234 Node tests, zero-warning ESLint, production build, workflow sanitation, and diff check passed. The user authorized the unified public release and the separate visible black-window full backup; completion requires the release receipt, Pages/Hugging Face/C-drive public verification, and independently verified full backup.

- **v5.5.2 MiniMax H3 workflow-selection release candidate (2026-08-19)**: The STEP4 MiniMax H3 helper now explicitly tells the user to select or open the `MiniMax H3 Reference-to-Video (R2V / Ref2VA)` workflow before connecting the manga. The same workflow name appears both as an ordered menu step and as the first item in the ComfyUI settings panel; it then directs image output only to `ref_image_0`. The user authorized the v5.5.2 follow-up release and visible black-window full backup after the v5.5.1 receipt reached verified status across all stages.

- **v5.5.1 MiniMax H3 active-performance release candidate (2026-08-19)**: Keeps the v5.5.0 dialogue-first contract—one reference image, no subtitles, no default BGM, normal Japanese orthography, speaker-bound mouth movement, balloon removal, and title/credit boundaries—while restoring active character performance and varied camera paths. Every visible character receives a role-appropriate action unless intentionally still; four shots vary push-pull, lateral, vertical, and arc/orbit trajectories, with start/midpoint/end states for non-linear moves. Camera movement adapts around the named speaker instead of being removed for dialogue. Focused regression coverage was added first and failed against v5.5.0 before passing after the targeted prompt change. The user authorized unified public release and visible black-window full backup; completion requires the receipt, public checks, and independently verified backup.

- **v5.5.0 MiniMax H3 ComfyUI dialogue-first release candidate (2026-08-19)**: The single STEP4 `MiniMax H3・ComfyUI用プロンプトをコピー` action now uses the dedicated current prompt source. It instructs the authoring chat to use only `ref_image_0`, prioritize intelligible Japanese dialogue over subtitles and BGM, keep the actual named speaker as the only moving mouth, preserve title and credit placement, and guide the user to start ComfyUI at 16:9, 0.4MP, and the `normal` scheduler. The visible helper omits the previously removed Float-duration line. Focused MiniMax H3 tests, zero-warning lint, and the production build passed locally. The user authorized the unified public release and visible black-window full backup; completion requires the unified receipt, Pages/Hugging Face/C-drive public verification, and independent full-backup verification.

- **v5.4.9 provider-state convergence and STEP4 recovery (2026-08-18, local only; not released)**: Replaces the legacy split provider decision with `getEffectiveEngine(selectedEngine, enableOpenAIApi)`. Any retained OpenAI signal now converges the React display state, shared text engine, OpenAI image route, prompt family, Step 4 labels/links/logs/metadata, manual-policy guidance, and full-auto prompt mode to OpenAI. This directly addresses the observed contradictory screen: `ChatGPT` helper action with `Google AI`, `Geminiウェブ版`, and `Gemini Native Image` on the same API session. The manual-repair contract is preserved: ChatGPT retains its external-Web aspect-ratio and 2x-upscale copy prompts; Gemini shows its own external-Gemini-Web aspect-ratio and 2K copy prompts, to be sent with the generated image attached. Gemini standard image generation uses the current Interactions REST endpoint and extracts the returned image from the `model_output` step. MiniMax H3 remains provider-neutral. Live in-app-browser Gemini evidence: standard output, then external-workflow controls; the first generated output contained visible stochastic anatomy/duplicate-cast defects and is therefore not a quality pass. OpenAI live image verification remains required. Publication is forbidden until both providers, the complete regression suite, preflight, unified receipt, public verification, and the separately authorized full backup succeed.

- **v5.4.8 STEP4 pro-tools restoration release candidate (2026-08-18)**: Restored the visible STEP4 pro helper actions that had been suppressed by a provider-label condition: `画像比率修正プロンプトをコピー`, `画像2倍アップスケールプロンプトをコピー`, and `MiniMax H3・ComfyUI用プロンプトをコピー` are now always available. STEP4 also derives its provider-specific guide, generation label, policy placeholder, log label, saved metadata, and downloaded filename from the same effective OpenAI image-generation predicate used by the image route (`enableOpenAIApi || selectedEngine === 'openai'`), so a stale text-engine label cannot make an active OpenAI image route look like Gemini. The v5.4.6→v5.4.7 source diff contained no `Step4Panel.jsx` change; the harmful gate pre-existed but produced the reported visible loss when the legacy states diverged. RED regression coverage was added before the correction. Fresh validation: all 233 Node tests, zero-warning ESLint, production build, and diff checks passed. The in-app browser at local port 5173 confirmed all three controls are visible. Public repair requires the unified v5.4.8 release transaction and public verification; a new full backup has not been requested for this correction.

- **v5.4.7 inline dialogue/stage-direction bubble fix release candidate (2026-08-18)**: Fixed the generic dialogue extractor for scenario lines that contain several explicitly attributed speakers. Each quoted utterance is now separated into the matching speaker's bubble contract, while quotation-external stage directions such as `と小声`, `と笑う`, `と叫ぶ`, and `と鋭く返す` remain non-printing drawing metadata. Existing single-speaker dialogue, nested Japanese quotation, visual lettering, sound-effect, and Action extraction behavior remain intact. The exact reported multi-speaker failure shape completed through the in-app browser with OpenAI `gpt-image-2`; STEP3 emitted four panel-3 and five panel-4 speaker-mapped bubbles without stage prose, and the 1024x1536 final image passed the app's `gpt-4.1` image QA and visual review. Evidence: `scratch/dialogue-boundary-api/elder-home-dialogue-boundary-openai.png` (3,327,328 bytes, SHA-256 `71848E59F76B25AC6E121C4222FAE79C8E339F07D073C42896F482BB3B3DD5F8`). Fresh candidate checks passed 231/231 Node tests, zero-warning ESLint, Vite production build, and diff check. The user authorized the unified public release and a separate visible black-window full backup; completion remains forbidden until source, annotated tag, bilingual GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and full-backup verification all pass.

- **v5.4.6 MiniMax H3 all-character-motion release candidate (2026-08-18)**: The existing single `MiniMax H3・ComfyUI用プロンプトをコピー` action now treats every visible character, including background and crowd figures, as an active performer. Every visible character must make at least one role-appropriate, physically plausible movement in each visible shot unless the reference manga or user explicitly calls for intentional stillness. Speaking and story-critical characters perform dialogue, facial reactions, gestures, posture shifts, or purposeful actions; background and crowd figures perform individually varied, restrained secondary actions such as blinking, gaze shifts, breathing, weight shifts, hair or clothing response, prop handling, or story-fit reactions. They must not remain static cutouts, frozen poses, or duplicate loops, and their activity remains subordinate to the main action, dialogue, caption safe area, and gag. The visible single-button helper text and README state the same boundary. Local regression, lint, build, pre-deploy, browser, unified release, and visible black-window full-backup evidence remain required before treating the candidate as complete. A fresh H3/ComfyUI rendered-video check for this motion contract remains unverified unless a live H3 run is completed.

- **v5.4.5 MiniMax H3 dialogue-caption release candidate (2026-08-18)**: The existing single `MiniMax H3・ComfyUI用プロンプトをコピー` action now tells the prompt-authoring chat to preserve each legible original Japanese dialogue line verbatim as `caption_text`, while supplying the same spoken line to H3 only as verified kana inside `<d>[Japanese] ...</d>`. It requires one compact bold Japanese open caption in the reserved bottom-center safe area, exactly during the matching audible line, with local contrast by a thin outline, soft shadow, or minimal backing only as needed. Opening title and ending credit must avoid that safe area and never overlap a dialogue caption; original speech balloons remain removed. No second copy button was added: the visible text beside the existing control explains that H3 renders the subtitle in-video. Local regression, lint, build, pre-deploy, browser, unified release, and visible black-window full-backup evidence remain required before treating the candidate as complete. A fresh H3/ComfyUI rendered-video check for this new caption contract remains unverified unless a live H3 run is completed.

- **v5.4.4 MiniMax H3 unified-copy release candidate (2026-08-17)**: The user accepted the `MiniMax_H3_00006_.mp4` direction as the current baseline: original speech balloons are removed, motion is materially stronger, H3 receives kana-only Japanese dialogue, and the title plus full ending credit render inside H3. Keep title and ending-credit rendering in H3; do not switch to post-processing. H3 still rounds the requested title/credit timing and may wrap the credit, but the user explicitly excluded those presentation differences from this correction and wants the current unified single-copy workflow released. Deployment and visible black-window full backup are authorized; completion requires the unified receipt and backup verifier.

- **Local MiniMax H3 real-video correction (2026-08-17)**: The actual `MiniMax_H3_00005_.mp4` showed persistent opening-title pixels, an ending credit beginning at the start of Shot 4 instead of 00:13.200, original speech balloons with garbled Japanese, and camera directions that held each composition too still. The sole `MiniMax H3・ComfyUI用プロンプトをコピー` action now requires a title only from 00:00.000-00:01.600 with no later title pixels; no ending credit before 00:13.200; complete removal of original balloon shapes, borders, tails, and white interiors; and only two readable textual elements (title and ending credit). Environmental signs may remain as natural but unreadable background texture. H3 dialogue is now supplied in verified kana only rather than competing kanji plus guide text, and default dynamic-comedy shots require one modest purposeful camera move. The footer remains assembled from URL character tokens to prevent authoring-chat linkification. `MiniMax_H3_00006_.mp4` confirmed the balloon, movement, kana-dialogue, title, and credit improvements; its timing/wrapping behavior is recorded above as accepted for this release.

- **v5.4.3 MiniMax H3 publication-copy release candidate (2026-08-17)**: STEP4 adds a separate static `MiniMax H3・作品公開用プロンプトをコピー` action for use from any PC or browser. The copied external prompt-authoring instruction OCRs only the title from the attached four-panel image, even when that title is outside the panel grid. Outside-grid text is never video story material; only the four panels provide the story, characters, dialogue, action, and setting. It permits a short adaptive-contrast opening title, no other mid-video overlay text, and only the exact ending lines `ネームから～` and `note.com/happy_duck780`; it never adds or refers to a Nano Banana watermark. If title OCR is unreliable, it requests only the title in Japanese rather than inventing one. The user authorized the unified public release and visible black-window full backup; completion remains forbidden until every receipt stage and the backup verifier pass.

- **v5.4.2 MiniMax H3 reading and motion release candidate (2026-08-17)**: STEP4 keeps the existing MiniMax H3／ComfyUI copy button, four-step Ref2VA workflow, timed-direction rules, and continuous BGM contract. The copied instruction now preserves each Japanese dialogue transcript in `<d>[Japanese] ...</d>` and directs the prompt-authoring chat to add a kana-only reading guide for the same stable speaker, so kanji readings are not left to ambiguous interpretation. If a reading remains genuinely ambiguous from the manga and its story context, it asks only for that reading instead of emitting a potentially incorrect H3 prompt. Each shot selects only one to three compatible animation principles for lively but source-faithful acting. Next to the button, the visible default specification is limited to `15秒・16:9・BGMあり・日本語音声・標準の躍動演出`; individual duration, framing, BGM, or direction requests take precedence. The user authorized the unified public release and visible black-window full backup; completion remains forbidden until every receipt stage and the backup verifier pass.

- **v5.4.1 MiniMax H3 timed-direction copy prompt release candidate (2026-08-17)**: STEP4 keeps the existing MiniMax H3／ComfyUI copy button, four-step workflow, Ref2VA section order, and continuous BGM contract. The copied instruction now translates requested cuts, camera moves, and transitions into physically possible shot timings; every camera move has explicit start, midpoint, and end states. It preserves stable horizon, focal length, subject identity, and coherent movement, while avoiding invented camera motion. Only one spoken line that genuinely continues across a planned cut may use `<scenetrans>` at its natural Japanese phrase boundary; separate speech bubbles must never be artificially split. README and regression coverage document the same boundary. The user authorized a unified public release and a separate visible black-window full backup; completion remains forbidden until every receipt stage and the backup verifier pass.

- **v5.4.0 combined acoustic/visual direction routing release candidate (2026-08-17)**: `効果音・演出:`, `演出／効果音:`, and `SFX / 演出:` lines are now classified as visual Action instructions instead of synthetic dialogue speakers. The generic parser protects quoted sound cues on those dedicated instruction lines, keeps them out of STRICT SCRIPT / bubble-tail / eye-line metadata, and preserves the scripted cues in visual Action. The exact user-supplied `スピーカー回収騒動` scenario completed through the visible in-app browser with OpenAI `gpt-image-2`; STEP3 contained exactly five intended dialogue bubbles and no `[効果音・演出]` bubble speaker, and the 1024x1536 final image showed no explanatory prose bubbles. Evidence: `scratch/api-speaker-recall-fix-20260817.png` (3,104,250 bytes, SHA-256 `2FFD724BCF2DE03B98C5D24E8D04A8D71E4451145F4D6820F6848B85DD5AE8CF`). Fresh local checks passed 216/216 Node tests, zero-warning ESLint, production build, strict pre-deploy checks, and diff check. No API-key value was read or stored. The user authorized the unified release transaction and a separate visible black-window full backup; completion remains forbidden until every release-receipt stage and the backup verifier pass.

- **v5.3.9 MiniMax H3 continuous-BGM prompt release candidate (2026-08-16)**: STEP4 keeps the existing MiniMax H3／ComfyUI copy action and four-step guidance, but its actual copied English prompt now exactly matches the user-supplied BGM-enforcement contract. It makes enabled BGM continuous and audience-only from `00:00.000` through the final frame, requires `MANDATORY:` in `non_diegetic_music`, and keeps `N/A` only for explicit BGM disablement. Regression coverage proves the UI copy binding, BGM clauses, and byte-for-byte parity with the supplied replacement text. The user authorized the unified release transaction and a separately verified visible black-window full backup; completion remains forbidden until every release-receipt stage and the backup verifier pass.
- **v5.3.8 MiniMax H3 ComfyUI workflow clarification release candidate (2026-08-16)**: STEP4 keeps the existing MiniMax H3／ComfyUI copy action and English prompt body, but now presents the requested four-step flow: copy the instruction and send it with the generated four-panel manga to an image-capable chat, copy the returned prompt, connect that same manga as ComfyUI `Reference Image`, then confirm framing and duration before pasting the English prompt into `Prompt`. README matches that sequence and keeps the explicit boundary that the app does not generate or send video. Regression coverage checks the copy contract, UI workflow text, and README workflow boundary. The user authorized the unified release transaction and a separately verified visible black-window full backup; completion remains forbidden until every release-receipt stage and the backup verifier pass.

- **v5.3.6 public-license structure release candidate (2026-08-14)**: Restores an explicit public licensing policy after the prior README notice only stated that no standalone license existed. Original program source code is now covered by a root MIT `LICENSE`; explicitly identified original non-code content is scoped by `LICENSE-CONTENT.md` under CC BY-NC-SA 4.0; `TRADEMARKS.md`, `OUTPUTS.md`, and `THIRD_PARTY_NOTICES.md` separate brand use, generated-output handling, and direct dependency notices. This documentation-only release does not change generation logic, API-key handling, provider selection, or image generation. The user authorized the unified release transaction and a separate visible black-window full backup; completion remains forbidden until the receipt, GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and backup verifier all pass.

- **v5.3.5 release candidate — named OTS cast-instance/API-session repair (2026-08-12)**: Root cause was a contradiction in `extractCastLimitRule`: dialogue speakers alone became foreground, so an explicitly named over-the-shoulder character was simultaneously required as camera-side foreground by the eye-line rule and assigned to the background cast list. The generic repair promotes the named OTS subject into the foreground exactly once, removes that character from the background list, preserves the dialogue speaker as main focus, and emits `OTS CAST INSTANCE LOCK`; generic RED/GREEN coverage runs for both ChatGPT and Gemini without production proper-name branches. The exact `表現規制を巡る会議` five-character scenario completed through the visible in-app browser with OpenAI `gpt-image-2`; full-resolution review found panel 1 contains five unique named people with Hikari only once as the left foreground shoulder, and panels 2–4 retain their specified cast counts and meeting staging. The first image exposed an independent `少年 -> 男性` substring rewrite inside `青少年保護`; the filter now ages up only standalone person roles, and the second real API image retained the exact dialogue. Evidence: `scratch/ots-cast-instance-final.png` (1024x1536, 2,567,441 bytes, SHA-256 `90CB48BBAED9D9B25029E73186DE25575AD8A3C26AE2D3EB6B63C53BCC2FBF1E`). The same investigation fixed first-submit API preflight from 15 seconds × 2 to 45 seconds × 3 using the same in-memory input, and added workflow epochs so stopped full-auto STEP2/STEP3 results cannot overwrite a manual scenario. Fresh checks passed 210/210 Node tests, zero-warning ESLint, Vite production build, and diff check. No key value was read or stored. The user authorized the unified release transaction followed by a separate visible black-window full backup; completion remains forbidden until the receipt, GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and backup verifier all pass.

- **v5.3.4 published-status synchronization (2026-08-12)**: Corrects only release metadata and current-state documentation after v5.3.3 shipped. README now identifies v5.3.4 as the current implementation and latest release; the v5.3.3 HANDOFF record below is updated from its pre-release candidate wording to its actual published outcome. The image-quality failsafe behavior is unchanged. This follow-up must use the same unified release and full-backup evidence gates as any other patch release.

- **v5.3.3 image-quality failsafe released (2026-08-12)**: STEP4 saves and displays the first image before QA. A concrete person/hand/prop-orientation/bubble failure creates at most one issue-limited repair candidate; a passing repair replaces the original, while a still-NG repair or repair-image request failure restores the saved original and returns success to the full-auto chain with a concrete warning. An `unverified` QA result does not spend another image call and also keeps the original. Initial image API/auth/transport/missing-image failures still stop because no usable artifact exists. TDD covers original PASS, repaired PASS, repaired NG, unverified QA, repair request failure, and prompt preservation. Source commit `aa839a1` was published as annotated tag/GitHub Release `v5.3.3`; Pages, Hugging Face, and `C:\\nano-banana-pro-main` (tree SHA-256 `67D2BE956CB8ADF4C9E3C4BAC69B63DDE706D6EEE52B8A05C5310D1E0754ED99`) passed independent verification. The separate visible full backup `antigravity_full_backup_2026-08-12_010015.zip` passed seven-app parity and matching local/Drive SHA-256 `9C04122335CCC5815BA1026435EAF3C465C350A1E8D743BF6B7BE82021E20051`.

- **v5.3.2 functional-surface orientation release candidate (2026-08-11)**: Functional faces now derive their intended target from the scripted action instead of the holder: reading/operating targets self, submitting/presenting/showing targets the recipient, and explicit camera presentation targets the camera. Tabletop pages may correctly be face-up, with the text baseline oriented to the intended reader. Explicit overhead/hand-detail/close-up cameras are preserved without an invented shoulder; explicit OTS readers survive ChatGPT prompt compaction with rear head/shoulder foreground and their screen front visible to both reader and camera. The visible QA now treats the viewer as the physical camera and forbids the previous false claim that an OTS viewer must not see the reader's screen. README now includes a decision table for phones, monitors, nameplates, documents, cards, books, maps, tickets, and forms, covering reader, recipient, tabletop, OTS, and explicit camera-presentation cases. Exact bank-scenario evidence: `scratch/functional-surface-orientation-api/bank-orientation-pass-action-target-ots.png`; `gpt-image-2` completed after 151 seconds and `gpt-4.1` QA returned PASS. The exact 5,870-character single-image copy prompt also completed through `gpt-image-2` after 136 seconds and is saved as `scratch/functional-surface-orientation-api/single-image-copy-prompt-api-output.png`; the four-panel bank QA is not applicable to that scenario-free helper image. Fresh local verification passed 198/198 Node tests, ESLint, production build, diff check, and current-version pre-deploy checks. The user authorized the unified release transaction and a separate visible black-window full backup; completion remains forbidden until source, annotated tag, bilingual GitHub Release, Pages, Hugging Face, C-drive release copy, public verification, and backup verification all pass.

- **v5.3.1 API session and manga quality gate release candidate (2026-08-11)**: Fixed first-entry API connection handling with one verified browser-memory session and one bounded retry for transient preflight failures; no credential value or bootstrap flag is persisted. Replaced the seven-field dynamic-background path with lightweight `Location` plus explicit time/weather continuity, subordinate to anatomy, hand/arm laterality, prop ownership, and bubble accuracy. STEP4 now displays the image before one visible combined quality review for malformed or extra arms/hands, side swaps, prop ownership, non-dialogue bubble text, speaker-name leakage, and extra text; it never auto-regenerates after a failed review. In the in-app browser, two independent real OpenAI `gpt-image-2` 1024x1536 runs completed without API re-entry or reload; both `gpt-4.1` visual reviews returned PASS and manual inspection found exact dialogue, no speaker-name bubble labels, coherent prop ownership, and no obvious extra arms. Fresh candidate verification: 191/191 Node tests, zero-warning ESLint, production build, and diff check. User authorized the unified release transaction and full backup; completion remains forbidden until the receipt, GitHub Release, Pages, Hugging Face, C-drive release copy, and backup verifier all pass.

- **v5.3.0 generic hand/prop kinematics release candidate (2026-08-10)**: The verified generic hand/prop kinematics fix is synchronized as v5.3.0 across package metadata, runtime header, HTML title, README current-version statement/changelog, and Hugging Face metadata. The current user has authorized the unified release transaction and a full backup; completion is forbidden until the persisted receipt verifies the source, exact annotated tag, bilingual GitHub Release, GitHub Pages, Hugging Face, C-drive release copy, public verification, and full-backup stages.

- **Generic hand/prop kinematics fix locally verified (2026-08-10)**: Replaced the pointing-, two-hand-, placement-, and arm-cross-specific prompt branches with one situation-agnostic `HAND / PROP KINEMATICS LOCK` shared by ChatGPT/OpenAI and Gemini prompt families. STEP3 no longer rewrites a scripted `両手で` action to `片手で`; the contract instead assigns subject-relative anatomical left/right arms, one simultaneous role/contact per hand, one actor per prop, final-state support for released objects, and palm/thumb/finger/wrist continuity. Long ChatGPT prompts keep a compact equivalent without exceeding the 15,000-character Web-copy budget, and compression preserves existing camera, eye-line, evidence, background, wardrobe, title, and watermark semantics. TDD reproduced the old action rewrite and missing generic coverage; the exact supplied `偽画像が被災地を騒がす` scenario produced a 13,231-character live STEP3 prompt containing the generic lock and no old special-case locks. In the in-app browser, one real OpenAI `gpt-image-2` call completed after 157 seconds following one streamed partial. Full-resolution review of `scratch/generic-hand-kinematics-api/generated-disaster-center.png` found coherent two-hand phone holding in panel 2, newspaper pointing in panel 3, and phone-plus-peace-sign staging in panel 4 with no obvious extra, detached, mirrored, or grossly malformed hand. The PNG is 1024x1536, 2,872,299 bytes, SHA-256 `0C2DE257924C2D84ACC40E5922418E002E79027097D91E3826839359AE696F71`. No key value was read or stored. No commit, push, deploy, release, or backup was authorized or performed.

- **v5.2.9 README version consistency (release candidate, 2026-08-10)**: Corrected the README statement that still called v5.2.7 the current implementation after v5.2.8 had shipped. README, package metadata, HTML title, runtime version constant, and Hugging Face README now agree on v5.2.9. This patch changes public release documentation/version metadata only; it does not alter the normal manga-composition variety behavior, provider route, or image-generation behavior. The user required a fresh unified release and full backup because the prior published README was inconsistent. Completion remains forbidden until the receipt, public pages, C-drive release copy, and backup verifier all pass.

- **v5.2.8 manga composition variety and truthful image-wait copy (release candidate, 2026-08-10)**: Normal STEP2 now requires subject-relative horizontal azimuth in addition to shot size, elevation, and tilt; STEP3 carries a provider-specific page lock and per-panel staging assist. The page allows at most one flat front-on panel, varies shoulder/hip/face axes, and staggers two-hand actions in depth or height without replacing the requested action, cast, props, dialogue, or background. The exact office-server-leak scenario completed in the in-app browser through OpenAI `gpt-4.1` and `gpt-image-2`: the 14,885-character STEP3 prompt retained four camera/staging assists and the final 1024x1536 image used oblique low-angle, over-the-shoulder, foreground close-up, and oblique-wide panels. The image-wait overlay now says `通常2〜10分`, matching the runtime log. Fresh verification passed 198/198 tests, zero-warning ESLint, Vite production build, and diff checks. The user authorized the unified v5.2.8 release and full backup; completion remains forbidden until the receipt, both public surfaces, C-drive release copy, and backup verifier all pass.
- **v5.2.7 season-aware outfit selection and in-app-browser API proof (release candidate, 2026-08-09)**: News mode now derives a Japan-default spring/summer/autumn/winter hint from `targetDate`, while manual mode uses only explicit topic cues. Outfit priority is explicit user clothing; event-specific clothing; profession/safety/location/weather/indoor environment; date season; ordinary auto-selection. STEP2 shows a read-only hint such as `季節目安: 夏（対象日付から自動）`; no season control was added. Automatic scenario validation rejects empty/ambiguous outfits plus obvious summer winterwear or winter summerwear unless the context supplies a cold/warm environmental exception, and reports the exact seasonal reason before a bounded retry. Indoor ice-rink, heated-indoor, pool/sauna, safety-equipment, and explicit-outfit exceptions are covered. The existing high-severity dependency findings were also repaired; current `npm audit` reports 0 vulnerabilities. In the visible in-app browser at `http://127.0.0.1:4176/`, the user-configured OpenAI route used target date `2026-08-09`, news mode, no custom outfit, and `2026年8月の日本の屋外夏祭り会場（夜、蒸し暑い）`. STEP2 produced `Outfit: 浴衣（yukata）` with no seasonal-outfit retry; STEP3 carried that exact outfit plus the cross-panel wardrobe lock; STEP4 reached a streamed partial and final OpenAI `gpt-image-2` success. Full-resolution review confirmed four panels at a lantern-lit summer festival, all characters in yukata, and no winter coats or scarves. Evidence: `scratch/season-aware-outfit-api/generated-summer-festival.png` (1024x1536 RGB PNG, 3,210,838 bytes, SHA-256 `B340DA9B1D860A29A222E3EA8EA4F916E58E367BF6CD3DFBD58693D4B143191E`) and `scratch/season-aware-outfit-api/generated-result-viewport.png`. A second real in-app-browser run used target date `2026-12-09`, news mode, no custom outfit, and `2026年12月の日本の屋外クリスマスマーケット（夜、雪、厳しい寒さ）`. The UI changed to `季節目安: 冬（対象日付から自動）`; STEP2 produced `Outfit: 防寒着（winter clothes）` with no seasonal-outfit retry or summer-wear term; STEP3 locked that outfit in all four panels; and STEP4 completed after one streamed partial. Full-resolution review confirmed snow and an outdoor Christmas market plus coats/jackets, scarves, and gloves across the cast, with no swimsuits, short sleeves, sleeveless tops, or tank tops. Evidence: `scratch/season-aware-outfit-api/generated-winter-market.png` (1024x1536 RGB PNG, 3,109,176 bytes, SHA-256 `B780AC6686BC94D827AF4D736C291299323B477D18CFF8A4F809118FC7064AF9`). STEP2 did issue an unrelated exhausted `DYNAMIC_BACKGROUND: background_time_conflict` quality warning; the winter outfit validator itself passed and the approved scenario consistently retained night, snow, and cold. The first summer STEP4 attempt exposed browser `Failed to fetch`; `src/lib/openai.js` now retries once without streaming only for that browser-network failure, and the successful live reruns prove the normal streamed route still works. Fresh verification passed 194/194 Node tests, zero-warning ESLint, Vite production build, `npm audit` with 0 total vulnerabilities, PNG verification, and visual inspection. No key value was read or stored. User authorized the unified v5.2.7 release followed by full backup; completion remains forbidden until the receipt, both public surfaces, the C-drive release copy, and backup verification all pass.
- **v5.2.6 combined visual-direction bubble routing and OpenAI wardrobe-lock safety fix (release candidate, 2026-08-09)**: Compound labels such as `身体・表情・演出:` are now handled as visual directions across speaker detection, action extraction, structured sections, and placement inference, so their prose stays in Action and cannot become a synthetic speaker or oversized bubble. A separate live `gpt-image-2` rejection was traced by block bisection to the negated wardrobe phrase `remove garments`; both the full and compact ChatGPT/Gemini prompt contracts now preserve the same wardrobe continuity with positive keep-language and never emit that phrase. RED then GREEN coverage includes the supplied mayonnaise-style combined-label failure and the shared wardrobe contract. Fresh verification passed 181/181 Node tests, zero-error ESLint, Vite production build, and `git diff --check`. In the in-app browser, the unchanged pre-fix 10,269-character full prompt reproduced the safety rejection, while normal STEP3 from the updated code produced an unedited 10,294-character prompt with four panels/four dialogue blocks, zero negated garment-removal phrases, and the positive wardrobe lock. That exact STEP3 output completed through `gpt-image-2` after a streamed partial; full-resolution review confirmed the four-panel exhibition manga and only the intended short dialogue bubbles, with no visual-direction prose bubble. No API key value was read or stored. User authorized the unified v5.2.6 release followed by full backup; completion remains forbidden until the receipt, both public surfaces, the C-drive release copy, and backup verification all pass.
- **v5.2.5 content-aware title and safer OpenAI image-generation release candidate (2026-08-08)**: Removed the unconditional ` !?` append from STEP2 and now preserve the generator's intentional title ending: none, `!`, `?`, or `!?`. The scenario prompt explicitly discourages punctuation as a default while retaining it when the title is genuinely a surprise, question, or both. The final ChatGPT/OpenAI prompt path now generically normalizes plain school/minor-coded traits, childlike proportion wording, and overly biological anatomy wording into adult-safe instructions; declares all characters as adults aged 20 or older; sends the supported OpenAI image `moderation: "low"` option; classifies the current safety-rejection wording; preserves only the in-memory OpenAI key across Vite HMR; and reopens the key modal if an in-memory key is missing. No key value is persisted or inspected. Fresh full verification passed 179/179 Node tests, zero-error ESLint, Vite production build, and `git diff --check`. In the in-app browser, normal manual STEP2 used the new topic `成人の会社員5人が商店街の福引で巨大なぬいぐるみを当て、持ち帰り方に困るドタバタ劇`; OpenAI `gpt-4.1` generated the 1,664-character scenario and unpunctuated title `商店街福引で大誤算`. STEP3 built a 13,363-character production prompt with four verbatim dialogue blocks, an adult-age declaration, and zero residual targeted risk phrases. A single STEP4 call reached streamed partial and final completion through `gpt-image-2`, rendered at 1024x1536, with no safety rejection or retry. Browser visual review confirmed the exact title, four horizontal panels, the shopping-arcade lottery setting, giant teddy bear, and five adult office workers; it was not the stale pancake result. Evidence: `scratch/title-variety-full-api/browser-title-and-top.png` and `scratch/title-variety-full-api/browser-four-panels.png`. The user authorized the unified v5.2.5 release followed by full backup; completion remains forbidden until every receipt stage, both public surfaces, the C-drive release copy, and backup archive/hash/parity/lock checks are verified.
- **v5.2.4 Markdown visual-section dialogue/placement isolation release candidate (2026-08-07)**: The supplied `国税職員の不祥事連発!?` scenario used Markdown blocks such as `**表情:**`, `**身体/間合い:**`, `**演出:**`, `**背景:**`, and `**セリフ:**`. Those pure section headers and quoted visual descriptions were previously eligible for speaker inference, which polluted both `STRICT SCRIPT LOCK` and `PLACEMENT/IDENTITY` with prose such as `口が小さく「あ」の形` and synthetic names such as `**表情`. The parser now tracks structured scenario-section boundaries: visual-section contents remain in Action, only the dialogue section feeds bubbles, and structural headers are excluded from placement inference. RED then GREEN coverage was added for the shared parser and both ChatGPT/Gemini prompt families. Fresh verification passed 170/170 Node tests, zero-error ESLint, Vite production build, and `git diff --check`. In the in-app browser, two character sheets already parsed through OpenAI `gpt-4.1` supplied all five named characters; the exact 2,967-character scenario produced a 14,653-character corrected STEP3 prompt with four required dialogue lines, four verbatim bubble entries, zero prose leakage, and zero Markdown placement leakage. A fresh OpenAI `gpt-image-2` run completed after 157 seconds. Full-resolution review found exactly one intended bubble in each of the four panels and no expression/body/direction/background prose rendered as dialogue or captions. Evidence: `scratch/api-dialogue-markdown-fix/2026-08-07-gpt-image-2-clean.png` (2,912,895 bytes; SHA-256 `4CB4AA6C79DDC75701DA251C2BB87DBBE1128C4F73A09E8F2191F2DACE62CDE4`). No API key value was read or stored. User authorized the unified release followed by full backup; do not mark this candidate released until its receipt, public surfaces, release copy, and backup all verify.
- **Cross-panel wardrobe color continuity verified through the normal full-auto character-sheet path (2026-08-07)**: The supplied four-panel result exposed a prompt-contract conflict: named-character identity/outfit continuity was required while each `PANEL STYLE LOCK` could change an unscoped palette. The local fix adds a shared `CROSS-PANEL WARDROBE COLOR LOCK` to ChatGPT/OpenAI and Gemini prompt families, confines panel-style palette changes to environment/lighting treatment/VFX, and keeps the 15,000-character ChatGPT Web-copy budget through equivalent compact wording. TDD RED reproduced the missing contract; focused tests passed 9/9 and fresh full checks passed 167/167 Node tests, zero-warning ESLint, Vite production build, and `git diff --check`. The earlier 2,260-character edited prompt run is retained only as diagnostic evidence and is not the acceptance proof. After the user correctly rejected that shortcut, the in-app browser used the normal untouched workflow: two real character-sheet images already loaded in STEP1, OpenAI `gpt-4.1` automatically parsed five named characters, full-auto selected current categories and generated the 1,534-character `冷やし風呂大行列!?` scenario with `Outfit: 銭湯用の浴衣・ラフな部屋着`, STEP3 automatically produced an unedited 13,419-character ChatGPT-family prompt containing the wardrobe lock and no unscoped panel palette instruction, and STEP4 submitted that exact prompt to real `gpt-image-2`. Generation completed without policy editing after 174 seconds at 1024x1536. Full-resolution review passed across all four panels: Miku retained blonde-to-pink hair and the same pale blue patterned yukata; Rin retained brown twin tails, round glasses, and the same pale blue patterned yukata; Saeko retained long black hair and the same blue-gray patterned yukata; Akari retained the orange bob and the same pale lavender/blue patterned yukata; Hikari retained the blonde bob, round glasses, and the same pale blue patterned yukata. Lighting and per-panel style changed, while garment base colors stayed recognizable. A subsequent real `gpt-image-2` single-illustration check used the same parsed five-character cast and explicitly named each canonical yukata color/pattern; all five identities, glasses states, garment colors, hands, and the single-image layout passed full-resolution review. The ratio-correction copy prompt now has a source-fidelity lock that retains the original four-panel manga's face, hair, glasses, outfit items/colors/patterns, cast, dialogue, props, setting, and time while rebuilding only A4 geometry; focused regression plus the fresh full suite passed 168/168. In the in-app ChatGPT browser, the user-supplied `ChatGPT Image 2026年8月7日 15_55_00.png` was attached and regenerated through the corrected A4 prompt. The resulting 1024x1448 PNG preserved all four panel beats, the five source identities, glasses states, per-panel outfit colors, props, and vertical dialogue while keeping an even A4 page layout. Acceptance evidence: `scratch/api-wardrobe-color-lock/2026-08-07-gpt-image-2-normal-full-auto-character-sheets.png` (3,229,001 bytes), the in-app STEP4 generated result, and `C:\Users\sx717\AppData\Local\Temp\browser-use\assets\6e150fa8-42e2-448e-8ff4-667a5d57eda8\18dcd93f822d3b86`. No key value was read or stored. The user then authorized the unified release and separate full backup; `v5.2.3` must fail forward from the already tagged/released `v5.2.2` and remain incomplete until the receipt, public surfaces, release copy, and backup all verify.
- **v5.2.2 shared single-image/four-panel quality-contract release candidate (in progress, 2026-08-06)**: The existing four-panel prompt remains intact; its cast, scenario, setting, and camera requirements are not replaced. A shared image-quality contract now supplies the same setting depth, motivated lighting, anatomy/prop ownership, localized clothing-fold shadows, and clean-surface constraints to the existing ChatGPT/Gemini four-panel families and the new single-image copy prompt. The exact copy-button text produced a real five-person classroom illustration in ChatGPT Web. A real in-app-browser OpenAI `gpt-image-2` four-panel run completed at 1024x1536 and visually retained coherent classroom staging, lighting, props, and anatomy. Fresh local verification passed 166/166 Node tests, ESLint, production build, and `git diff --check`. The authorized next stages are the unified `v5.2.2` release transaction and a separate visible black-window `-NoGui` full backup. Exact five-character identity matching in the four-panel proof remains unverified because that test upload contained only three character references.
- **v5.2.1 staging-gag dialogue-routing release candidate (in progress, 2026-08-05)**: The v5.2.0 release already exists, so the subsequent generic parser correction must fail forward as v5.2.1. `ギャグ演出:` / `演出ギャグ:` labels are now structural metadata rather than speakers, including Markdown bullets, whitespace, separator, and matching bracket forms. Exact real-API evidence is saved at `scratch/api-gag-direction-fix/final-generalized-fix-1024x1536.png`: OpenAI `gpt-image-2` completed a fresh 1024x1536 four-panel image after 138 seconds, with the panel-2/3/4 direction prose absent from dialogue bubbles. Release candidate validation, the unified release receipt, public Pages/Hugging Face/release-copy verification, and the separately authorized visible black-window full backup remain pending. No credential is stored in source, notes, or this record.
- **v5.2.0 reversed gag-direction dialogue-routing fix (locally verified, 2026-08-05)**: The supplied `美術部カノジョ新作話題!?` output exposed that `ギャグ演出:` was not recognized as the existing `演出・ギャグ:` structural label. STEP3 therefore emitted `B1->[ギャグ演出]` and placed the complete panel-2/3/4 direction prose into `Dialogue (verbatim bubbles)`. RED reproduced the exact `羞恥` / `観察中…` / `バグ多発！` direction sentences as speech bubbles. The parser now recognizes both label orders plus Markdown bullets, whitespace, separator, and matching bracket variants; excludes those structural labels from speaker inference; preserves their body as visual Action; and strips the label/wrapper before prompt assembly. Focused coverage passes 26/26 across parser plus ChatGPT/Gemini prompt families; fresh full verification passes 162/162 Node tests, zero-warning ESLint, Vite production build, and `git diff --check`. In the in-app browser, the exact scenario's final STEP3 prompt contained 4 Dialogue lines / 5 real bubbles, zero direction prose in Dialogue, zero gag-label speakers, all three panel-2/3/4 direction bodies in Action, and zero remaining structural labels. A fresh post-fix OpenAI `gpt-image-2` run completed after 138 seconds with `[COMPLETE] Image successfully generated.` and a new 1024x1536 image. Full-resolution review found no rectangular scenario-description captions: only the five real dialogue bubbles were present, while `羞恥`, `観察中…`, `バグ多発！`, `バグ`, and `現実ショック` remained visual effects. Evidence image: `scratch/api-gag-direction-fix/final-generalized-fix-1024x1536.png`. No commit, push, release, deploy, release-copy change, or backup was authorized or performed.
- **v5.2.0 STEP1 character-analysis reset release candidate (in progress, 2026-08-05)**: The header now names the existing retained-character reset `シナリオ（STEP2）からやり直す` and adds `キャラクター解析（STEP1）からやり直す` between it and the full reset. The new action reuses the downstream reset, then clears character-sheet images, extracted `castList`, analysis trace, style JSON, copy state, and drag state without clearing the API key, selected engine, or engine flags. RED then GREEN regression coverage passed; fresh local validation passed 158/158 Node tests, zero-warning ESLint, and Vite production build. In the in-app browser, OpenAI GPT-4.1 completed character analysis for two uploaded sheets; the new STEP1 action removed the image thumbnails and extracted text while `✅ ChatGPT Engine` remained connected. User authorized unified v5.2.0 release followed by a visible black-window full workspace backup; public release, release-copy, and backup verification are pending.
- **v5.1.9 manga dialogue-boundary release candidate (in progress, 2026-08-05)**: STEP3 now treats structural labels such as `表情・身体`、`動作`、`ポーズ`、`姿勢`、`目線`、`視線` as directions rather than speakers, preventing descriptive prose from becoming a speech bubble. Same-speaker stage quotes are deduplicated only when they are near-identical to a formal dialogue line, so distinct dialogue remains available. The exact supplied scenario completed through the user-configured OpenAI routes in the in-app browser: STEP3 produced 2/2/3/3 dialogue entries without structural-label bubbles, and `gpt-image-2` returned a visible 1024x1536 four-panel manga without the oversized prose bubble or duplicate final-panel dialogue. Fresh local verification passed 156/156 Node tests, zero-warning ESLint, Vite production build (existing Browserslist/chunk-size warnings only), and `git diff --check`. The user authorized the unified v5.1.9 release followed by a fresh full workspace backup; public release, release-copy, and backup evidence are pending.
- **v5.1.7 non-blocking scenario-quality fix (release in progress, 2026-08-04)**: The production v5.1.6 path still let `requestSafeScenarioContent` throw after a content-hygiene rejection, bypassing the shared three-attempt scenario candidate selector and stopping STEP1. v5.1.7 integrates `SCENARIO_CONTENT` into the same quality checks and retry instructions as dynamic background, visual evidence, manual exclusions, and final-panel staging. The attempt budget is explicitly three total calls (initial attempt plus two retries), and progress now renders `試行 2/3` / `試行 3/3`. Exhausted content-hygiene quality checks return a warning and the highest-scoring usable candidate rather than terminating the workflow. RED reproduced the terminal exception; GREEN focused coverage passed 42/42 and the full suite passed 153/153, with zero-warning ESLint, Vite build, and `git diff --check` also passing. Fresh in-app-browser evidence used OpenAI `gpt-4.1` for scenario generation and all-seven-category scenario enhancement, then built STEP3 and completed STEP4 through real `gpt-image-2`; the final Base64 image rendered at 1024x1536 after one streamed partial and the log reached `[COMPLETE] Image successfully generated.` Visual review confirmed a four-panel game-shop manga retaining the update poster, game packages, manual/controller or cabinet evidence, and active final-panel staging. The authorized unified release must now be verified on Pages, Hugging Face, and `C:\nano-banana-pro-main`, followed by a separate fresh full backup and independent archive/hash/parity/lock verification.
- **v5.1.6 API-refresh protection release candidate (in progress, 2026-08-04)**: Vite/Fast Refresh no longer replays the mount-time API bootstrap against an already connected in-memory engine. The guard stores only a boolean on `window`, never an API key, and prevents the existing no-persistence path from clearing the OpenAI/Gemini key during a development update. STEP2 scenario enhancement still makes bounded quality retries without turning an exhausted soft-quality check into a terminal error: it retains the highest-scoring safe enhanced candidate, or the original scenario only when every candidate breaks a non-negotiable editing contract, and then continues to STEP3 and STEP4 with a concrete warning. In the in-app browser, the prior fixed candidate completed OpenAI GPT-4.1 all-category enhancement, STEP3 prompt construction, and visible `gpt-image-2` 1024x1536 output. Focused API-refresh and scenario-continuation tests, zero-warning lint, build, and the full suite are being run for this final candidate. User authorized unified release followed by a separate fresh full backup; v5.1.5's Hugging Face receipt is pending reconciliation and this final candidate's publication and backup evidence are pending.
- **v5.1.1 visual anatomy QA released (2026-08-04)**: The post-generation vision anatomy acceptance gate, one corrective regeneration, and fail-closed second rejection were published as source commit `cc105216eb88e3de94974bdfbdfb5e0753acfbaf`, annotated tag and bilingual GitHub Release `v5.1.1`. The persisted release receipt `backups/release_receipts/nano-banana-pro-v5.1.1-2026-08-04-anatomy-qa.json` has every stage verified. In-app-browser checks found both Pages and Hugging Face at `v5.1.1`, GitHub's rendered release contains the Japanese QA note, and independent `verify_app_release.ps1` verified the Release copy `C:\nano-banana-pro-main` against the Release ZIP tree hash. The separately authorized visible `-NoGui` full backup is being refreshed after this completion record.
- **Visible limb-anatomy QA local follow-up (2026-08-04)**: The earlier real `gpt-image-2` result exposed an apparent third hand on Saeko in panel 2. The root cause was not the existing prompt locks (which already required two arms/two hands) but an acceptance gap: STEP4 displayed the returned image without examining it. `src/lib/image-anatomy-qa.js` now asks the configured text/vision provider to review the finished four-panel image panel-by-panel for visible extra, missing, detached, or wrong-body arms/hands. A malformed QA response fails closed; one rejected image is automatically regenerated with an anatomy repair lock, and a second rejection is surfaced as an error instead of displayed. Focused RED/GREEN coverage plus the full Node suite passed (132/132), along with ESLint, Vite build, and `git diff --check`. A fresh user-credentialed external-browser OpenAI run completed `gpt-image-2` followed by real `gpt-4.1` vision QA; the live log recorded `[QA] Vision anatomy review (1/2)` then `PASS`. Full-resolution visual inspection of `generated-result-anatomy-qa.png` found no visible third hand on panel-2 Saeko. No commit, push, deploy, release, or backup was performed.
- **Final-panel activity diversity local follow-up (2026-08-04)**: Non-surreal endings now reject a passive multi-character lineup (including a moving lead with silent observers) and retry the complete scenario with story-specific actions across foreground, midground, and background. Both ChatGPT and Gemini image prompts receive the same active-staging lock; the ChatGPT copy compactor applies after final safety sanitization so the 15,000-character Web-copy soft budget remains intact. Focused RED/GREEN coverage and the full Node suite passed (126/126), as did ESLint with zero warnings, Vite production build, and `git diff --check`. A fresh local real-provider run used user-entered UI credentials and character sheets: OpenAI `gpt-4.1` generated the scenario, and `gpt-image-2` completed the four-panel image. Visual review confirmed distinct physical actions in every panel; the final panel has Miku posing, Rin clutching a book, Akari handing a book to a child, Saeko holding her head over the list, and Hikari raising a map, rather than a straight-line lineup. The output also retained the mobile-library bus, donation books, reception table, and sorting labels. The browser used for this completion was external because the user had already entered the API setup there after an earlier browser-choice error; this is not in-app-browser evidence. No commit, push, deploy, release, or backup was performed.
- **v5.1.0 rich-panel composition correction release candidate (2026-08-04)**: The rich-panel implementation from the mistakenly numbered v5.0.10 is retained, but v5.0.9 must advance to v5.1.0 under the single-digit minor/patch rule. Both provider-family image prompts require one fixed environmental anchor and at least two further physical setting cues in every panel. VFX must stay behind or around the cast and may not replace the location or story evidence. Faces, eye direction, silhouettes, hands, and key actions remain crisp and visually separated from rich lower-contrast, softer-edged backgrounds; broad blank walls, flat gradients, and black voids are disallowed except where physical emptiness is explicitly scripted. The ChatGPT compact Web-copy path preserves the same rules within its empirical 15,000-character budget. The release updater and predeploy check now reject two-digit minor/patch components and enforce carry from `…9`; RED/GREEN contract coverage passed. The prior live OpenAI `gpt-image-2` result remains the implementation evidence. User authorized correction deployment and a separate visible `-NoGui` full backup; corrected public release, release-copy verification, erroneous v5.0.10 withdrawal, and new backup evidence are next.
- **v5.0.9 dynamic-background release candidate (2026-08-04)**: Removed the STEP2 background preset dropdown, `background-rag.js`, and the fixed location dictionary. Each scenario now creates and validates a complete per-story background contract: space and circulation; foreground/midground/background layers; time, weather, and primary light; atmosphere; interaction props; two to three fixed anchors; and contradictions to avoid. A valid scenario must retain an exact anchor in every panel, use at least two anchors across the page, and may not change declared time or weather (including afternoon-to-sunset / `GOLDEN_HOUR` drift). Both provider-family image prompts carry the same dynamic continuity lock. RED/GREEN regression coverage was added; fresh full checks pass 118/118 Node tests and ESLint with zero warnings. A live OpenAI `gpt-4.1` scenario for a passenger-ship launch first exposed the noon-to-sunset defect; the generic validator was extended, the same topic was regenerated with a stable sunny daytime harbor, and `gpt-image-2` produced a 1024x1536 four-panel result that visually retained the passenger ship, flower wreath, boarding equipment or port staff, and paper streamers across the page. User authorized the unified release transaction and separately authorized a visible `-NoGui` full backup; commit, public release, release-copy verification, and new backup evidence are next.
- **v5.0.8 visual-evidence priority release candidate (2026-08-03)**: `VISUAL STORY EVIDENCE LOCK` now follows `STRICT SCRIPT LOCK` immediately and precedes `ART / RENDERING QUALITY` / provider style guidance in both prompt families; the trailing duplicate insertion and the blank separation before it were removed. Regression coverage requires `STRICT SCRIPT LOCK` < evidence lock < style guidance for ChatGPT/OpenAI and Gemini. Fresh local verification passed 116/116 Node tests, ESLint with zero warnings, Vite production build, and `git diff --check`. A live OpenAI `gpt-image-2` run completed at 1024x1536 after the same reordered editable prompt was submitted; browser review confirmed an electronics-store battery aisle with a Game Gear, AA battery packs, battery shelves, and a basket piled with batteries, with no laundromat/generic-scene substitution. User authorized the unified release transaction and separately authorized a visible `-NoGui` full backup; collect receipt, public/release-copy proof, fresh archive/manifest, parity/hash, and lock-release evidence before completion.
- **v5.0.7 story-fit background and visual-evidence release candidate (2026-08-03)**: Automatic STEP2 generation no longer preselects a curated background before the scenario exists. The model chooses one concrete setting from the actual topic and also emits 3-5 drawable `VisualEvidence` nouns that prove the specific event, places at least two distinct evidence types across at least two panel actions, and retries once when that contract is missing. STEP3 carries those terms into both provider prompts through `VISUAL STORY EVIDENCE LOCK`; explicit curated presets still add continuity anchors when selected. TDD RED reproduced the missing-evidence path, focused tests passed 20/20, the full suite passed 115/115, ESLint and Vite build exited 0, `git diff --check` passed, and production source contains no launch-ceremony sample terms. A real connected OpenAI run used `gpt-4.1` to derive `Location: 港の桟橋` and `VisualEvidence: 旅客船、テープカット、紙テープ、式典用花輪、船員` from a passenger-ship launch prompt that did not enumerate props. `gpt-image-2` completed at 163 seconds with a 1024x1536 PNG. Full-resolution browser review found passenger ships, ceremonial flower wreaths, uniformed crew/staff and a ribbon-cutting stage in panel 1; a ceremonial ribbon in panel 2; a flower wreath and paper streamers in panel 3; and a passenger ship, paper streamers, flower wreath and staff in panel 4. The event is visually recognizable as a launch ceremony, all panels remain at the port, no laundromat appears, and browser error/warning logs were empty. Commit, push, release, deploy, release-copy update, and the user-requested full backup are the next authorized stages.
- **v5.0.6 background RAG continuity release candidate (2026-08-02)**: STEP2 now exposes 24 curated, safety-filtered background presets while preserving the existing free-text location input. Hybrid auto-selection ranks safe presets against the topic and mood, selected locations carry stable environmental anchors into scenario construction, and both ChatGPT/OpenAI and Gemini image prompts add a four-panel background continuity lock for location, weather, time, lighting, and recurring props. Fresh local checks passed 111/111 Node tests, ESLint with zero warnings, production build, and `git diff --check`. The in-app browser completed a real OpenAI `gpt-4.1` scenario and `gpt-image-2` 1024x1536 image using `雨の駅前`; visual review confirmed a vertical four-panel manga with the station departure board and wet reflective tiles recurring across panels. The user authorized the unified v5.0.6 release transaction followed by the separate full backup; external publication and backup evidence are pending.
- **v5.0.5 arm-cross prop-release release candidate (2026-07-31)**: Same-character prop placement followed by crossed arms now emits a generic final-pose lock: the prop remains self-supported, both hands release it, and exactly two arms/two hands stay naturally connected to the character. The lock is limited to the same actor, so separate characters' prop handling and arm-cross poses remain unchanged. RED then GREEN regression coverage covers ChatGPT and Gemini prompt construction; fresh local checks passed 105/105 Node tests, ESLint, production build, and `git diff --check`. A real OpenAI `gpt-image-2` 1024x1536 four-panel run verified Saeko with two hands, natural crossed arms, and a self-supported file without limb/prop merging. User authorized the unified release transaction followed by the separately authorized visible `-NoGui` full backup; external evidence is pending.
- **v5.0.4 staging-and-gag bubble-routing release candidate (2026-07-31)**: Compound stage-direction labels such as `演出・ギャグ:` no longer become speakers, and quoted sound lettering in acoustic context (for example `「ギュッ」という擬音`, `「カサッ」と擦れる音`, and `「ブンッ」という風切り音`) no longer becomes dialogue. The visual Action instructions retain the staging/SFX content without the meta label. RED/GREEN regression coverage now fixes this behavior in dialogue extraction and strict ChatGPT/Gemini prompt construction. Fresh local checks passed: 103/103 Node tests, ESLint with zero warnings, production build, strict pre-deploy security/version checks, and `git diff --check`. The exact supplied four-panel scenario completed through real OpenAI `gpt-image-2`; visual review found only speaker-owned dialogue bubbles, with visual SFX and no isolated right-side non-dialogue bubble. User authorized the unified release transaction followed by the separately authorized `-NoGui` full backup; external evidence is pending.
- **v5.0.3 prompt-edit and stream-recovery release candidate (2026-07-26)**: STEP 4 now exposes an editable final-prompt field; the edited `finalPrompt` is used unchanged by copy actions and both provider image requests. OpenAI image streaming now retains the most recent valid partial image and renders it if the transport drops before the final SSE event. RED then GREEN coverage verifies the stream-drop recovery; the focused prompt-editor test verifies the UI/data path. Fresh local checks passed: 101/101 Node tests, ESLint with zero warnings, production build, strict pre-deploy checks, and `git diff --check`. In-app-browser real API verification completed random scenario generation, direct prompt editing, and final image generation with OpenAI `gpt-image-2` and Gemini `gemini-3.1-flash-image`. User authorized the unified release transaction followed by visible-window `-NoGui` full backup; publication and backup evidence are now being collected.
- **v5.0.2 ambient-sound bubble-routing release candidate (2026-07-25)**: Quoted ambient sounds such as `スプーンの「カチャ」という音` and `椅子の軋む「ギシ…」という音` are no longer promoted into speaker-owned speech bubbles. Their quoted sound lettering is removed while grammatical scene action remains (`スプーンの音` / `椅子の軋む音`). RED then GREEN regression coverage covers dialogue extraction and both ChatGPT/Gemini strict-script prompts. The exact user scenario passed a real OpenAI `gpt-image-2` run and a real Gemini `gemini-3.1-flash-image` run; both completed without extra `カチャ` or `ギシ…` bubbles. One initial Gemini request returned no image part, but the identical unchanged retry returned an 864x1232 JPEG, so this is recorded as a transient provider empty response rather than a parser defect. Unified release and the separately authorized visible-window `-NoGui` full backup are in progress.
- **v5.0.1 scenario-enhancement selection release candidate (2026-07-25)**: Replaces the final empty STEP2 enhancement-grid slot with an accessible `全カテゴリを選択` toggle. It selects or clears all seven enhancement categories; the selected state uses a bright lime glow, explicit `全選択中（全カテゴリを解除）` label, and clear-off instruction. Fresh verification passed: 94/94 Node tests, ESLint with zero warnings, production build, pre-deploy checks, and git diff --check. The local OpenAI API path generated a character-sheet scenario and completed all-category enhancement. User authorized the unified release transaction followed by a visible, -NoGui full backup; publication and backup evidence remain pending.
- **Fresh Gemini STEP2 enhancement and STEP4 image proof (2026-07-24)**: Local port 5173 used the exact `AI苦手分野4連発 !?` original scenario with Gemini `gemini-3.5-flash`, character sheets loaded, and all six non-background enhancement categories selected (`expressions`, `body`, `effects`, `camera`, `dialogue`, `gag`). The first response completed in 24 seconds at 1,793 characters with no corrective retry. A fresh independent `validateScenarioEnhancement` pass returned `ok: true`, no issue codes, and all six selected categories in `changedCategories`; title, Logline, Location, Outfit, Punchline, four-panel structure, and speaker order were preserved. Dialogue changed in two of four lines (`……なんか、手、丸くない？` / `ねえ、これ流行る？`), proving the dialogue category was effective, while background enhancement remained off. STEP4 then completed through the real Gemini image API using `gemini-3.1-flash-image` and returned one 864x1232 JPEG. Visual inspection confirmed four panels, all four intended main dialogue lines, no printed speaker-name prefixes, and the unchanged white `精神と時の部屋` setting. Gemini also produced stochastic image-level artifacts (occasional duplicated cast, short stray text, a misdirected bubble tail, and weaker-than-requested exaggeration); the user explicitly accepted these as tolerable Gemini hallucination for this parity check. The API calls had no failure; one pre-run console warning came from the existing reset path calling `setActiveEngine("")`, which immediately defaults to Gemini and did not affect either verified request.
- **Local dialogue/bubble routing fix awaiting publication (2026-07-24)**: Image-prompt dialogue now separates printable text (`TEXT`) from non-printable speaker/tail metadata (`TAILS`), explicitly forbids printed speaker names, and requires each tail tip to terminate at the mapped speaker's mouth/head silhouette. Attributable spoken quotes embedded in `状況:` (including generic roles such as a host/presenter) are promoted to dialogue while being removed grammatically from visual action; reaction-only and unowned narration quotes remain excluded. The exact `世界最小スフィンクス公開 !?` local OpenAI `gpt-image-2` run produced a 1024x1536 image with no printed speaker names, the panel-2 center bubble routed to Akari, and the host's panel-4 `この続きは次号で！` bubble present. The same prompt pasted into ChatGPT Web omitted only the host bubble while retaining the other two improvements, confirming a remaining stochastic image-model omission rather than parser loss. Local checks passed: 91/91 tests, ESLint with zero warnings, production build, pre-deploy/security checks, and `git diff --check`. No commit, push, deploy, release, release-copy update, or backup is authorized/completed for this local fix.
- **v4.9.8 documentation-alignment release candidate (2026-07-24)**: README implementation details now explicitly describe the seven STEP2 enhancement categories, selected-scope protection, dialogue/gag separation, response validation, one corrective retry, and fail-closed behavior. Application behavior is unchanged from v4.9.7. The candidate must complete the unified release transaction before the separately authorized visible-window `-NoGui` full backup runs.
- **v4.9.7 released (2026-07-24)**: Replaced the permissive whole-scenario rewrite with selected-category contracts, structural/diff validation, one corrective retry, and fail-closed acceptance. `セリフ書換` now requires an actual dialogue change while rejecting explanatory over-expansion and softened punchlines; `ギャグ演出` is a separate control that preserves dialogue unless dialogue rewriting is also selected. Metadata (including `Topic:` and omitted optional fields), panel structure, speakers, unselected dialogue/camera/emotion/background mutations, calm-tone escalation, and new anatomy/body-horror escalation are guarded. Exact user-supplied `AI苦手分野4連発 !?` browser/API proof on local port 5173 used OpenAI gpt-4.1 with every enhancement except background: all six selected categories changed, validation returned no issues, and the four final dialogue lines were `いくわよ。` / `手がパンみたいになってる……。` / `下が見えすぎる……。` / `これ、逆に新しいかも。`. Unified receipt `backups/release_receipts/nano-banana-pro-v4.9.7-2026-07-24.json` completed with every stage verified: source commit `6eb76205fda5cf766533a17866f98c5601e7f106`, annotated tag and GitHub Release `v4.9.7`, GitHub Pages commit `29c9f720ae1657d435f21e3d20108fc6821355f8`, Hugging Face commit `beadb0a6c1be46d1e6502894a0c63d25d293e57a`, and `C:\nano-banana-pro-main` synchronized from the verified Release ZIP.
- **Gemini parity proof for the same STEP2 fix**: The user connected Gemini and uploaded the same character sheets. Local port 5173 used `gemini-3.5-flash` with the exact same scenario and the same six categories (background OFF). The first response was rejected as `dialogue_overexpanded`; the automatic corrective retry returned a 1,388-character scenario that passed with all six selected categories changed, no validation issues, and dialogue `……始めるわよ。` / `……なんか、丸くない？` / `地面が、遠い……。` / `これ、流行るかな。`. Browser error count was zero. This proves the retry/validation path works for both OpenAI and Gemini providers.
- **v4.9.6 release candidate**: Adds localized crisp triangular cel-shaded shadows at overlapping and pinched clothing folds for both provider prompt families, with panel-level priority for light shirts, blouses, jackets, and sleeves. It preserves cast, script, layout, material, and lighting constraints, avoids geometric garment patterns, and retains the instruction in compact ChatGPT Web-copy prompts. Local verification passed: 79 tests, lint, build, diff check, and strict pre-deploy checks. The user completed live OpenAI and Gemini image generations; OpenAI quality is preferred, while Gemini is accepted as structurally non-broken. Release, public verification, and the separately requested visible-window/no-API-input full backup remain in progress.
- **v4.9.3 release status**: v4.9.2 exposed an HF deploy-runner defect: it deleted the Space `Dockerfile` and `nginx.conf`, producing HTTP 404 at the retired static host. The forward-fix is published as source commit `5a2430c`, tag/release `v4.9.3`, GitHub Pages (HTTP 200, title `Nano Banana Pro v4.9.3`), and the current Hugging Face Docker host `https://furuyan-nano-banana-pro.hf.space` (HTTP 200 for root and SPA fallback; runtime SHA `57c3382`; `Dockerfile` and `nginx.conf` present). `scripts/deploy_hf.ps1` now preserves both runtime files and stops before cleaning if either is missing; `tests/deploy-hf-runtime-preservation.test.mjs` is the regression guard. The canonical local release copy `C:\nano-banana-pro-main` is verified at `4.9.3`; the prior `4.9.1` copy was preserved at `C:\nano-banana-pro-main.pre-v4.9.3-20260719`. Full backup is the remaining final operation.
- **v4.9.2 release in progress**: the two-hand prop ownership lock is implemented locally. It fixes a generic depth/anatomy failure in which a character closing a document with both hands could acquire an apparent third hand from behind. The lock requires exactly two arms and two hands connected to the owning character's shoulders, keeps the prop at that character's torso, and forbids back-emerging or cross-body limbs. The exact Rin/document regression passed in both prompt families and a live OpenAI `gpt-image-2` generation; panel 2 showed no third hand. Current evidence before publication: 60/60 tests, ESLint, Vite build, `pre_deploy_check.js`, and `git diff --check` passed. Commit/push/deploy/release/local-release-copy sync/full backup remain pending.
- **v4.9.1 correction release status**: `v4.10.0` was an erroneous increment: `4.9.0` must advance to `4.9.1`. Its GitHub Release and tag are removed; `v4.9.1` is the latest release. GitHub Pages returns HTTP 200 with title `Nano Banana Pro v4.9.1`; the Hugging Face Docker host returns HTTP 200 for HTML, CSS, JS, and SPA fallback; `C:\nano-banana-pro-main` reports package/title `4.9.1` with no nested folder. The verified final full backup is `C:\Users\sx717\Antigravity\backups\antigravity_full_backup_2026-07-16_191303.zip` (SHA-256 `9E4A94EDA871E8E91734EB9284AEEBB6898674ED6C0C267B4F1CA1C65B68E6F6`); its Google Drive mirror hash matches.
- **Published application payload**: The generic hand-action allocation fix is the already-published application logic from source commit `73a7cfb`; no new application behavior was introduced while correcting the version identifiers to `v4.9.1`. The current release correction must describe this accurately and must not claim that `v4.9.1` newly added the behavior.
- **Version**: v4.9.0 Gemini-only rear-foreground staging fix is released and deployed from commit `2abb7e1`. GitHub Pages and Hugging Face Spaces serve `Nano Banana Pro v4.9.0` with `assets/index-9CEHV2dI.js`; the bilingual GitHub Release and `C:\nano-banana-pro-main` release copy are verified.
- **Previous public version**: v4.8.9 conversational depth staging release.
- **Version**: v4.8.8 conversational eye-line auto-staging fix was released and deployed.
- **Version**: v4.8.7 Super FURU title alignment is released and deployed.
- **Previous public version**: v4.8.6 enhanced-scenario narration bubble extraction fix was released, deployed, and backed up.
- **Post-v4.8.2 local fix status**: Completed as v4.8.3. The serious provider prompt-routing bug found after the user's style/footer report is fixed by explicit provider families (`chatgpt` / `gemini`) in `src/lib/prompt-assembler.js` and `src/hooks/useMangaWorkflow.js`; prompt assembly branches on normalized `providerFamily`, not the legacy `enableChatGPTMode` boolean. ChatGPT Web copy and OpenAI API generation share the same ChatGPT-family final prompt; Gemini Web copy and Gemini API generation share the same Gemini-family final prompt. Generated-image history is capped to the latest item to reduce huge Data URL memory pressure after the in-app browser crash. Free-form EMOTION descriptors are normalized to existing strong style locks. Fresh ChatGPT Web regeneration from the final rebuilt prompt remains unverified.
- **Latest changes**:
  - v4.8.7 release prep: README/app-footer references to the old long system title were aligned to `Super FURU AI 4-koma System`; `Nano Banana 2` and `ChatGPT Images 2.0` remain only where they describe supported generation engines. The deploy title precheck now accepts `Super FURU AI 4-koma System` as an allowed app title.
  - v4.8.6 release prep: enhanced-scenario narration/action lines with quoted group reactions are no longer promoted into speech-bubble speakers. Panel 4 keeps only explicit character dialogue, so quoted direction text such as a group scream or breath-stopping narration stays visual/action-only. Regression coverage was added in `tests/panel-utils-dialogue.test.mjs`; local STEP3/STEP4 browser proof generated a 1024x1536 PNG without the long narration bubble.
  - v4.8.5 release prep: STEP4 image aspect ratio correction prompt copy button now hides its base label while the temporary `繧ｳ繝斐・螳御ｺ・ feedback is visible, matching the 2x upscale copy-button feedback behavior and preventing text overlap. Regression test `tests/upscale-prompt-ui.test.mjs` now locks this copied-feedback state.
  - v4.8.4 release prep: STEP4縺ｮChatGPT 2蛟阪い繝・・繧ｹ繧ｱ繝ｼ繝ｫ譯亥・譁・・蜈磯ｭ縺ｫ `笨ｨ` 繧定ｿｽ蜉縺励∵｡亥・譁・→繧ｳ繝斐・逕ｨ繝懊ち繝ｳ縺ｮ隕冶ｪ肴ｧ繧帝ｫ倥ａ縺溘よ里蟄倥・闍ｱ隱槭い繝・・繧ｹ繧ｱ繝ｼ繝ｫ繝励Ο繝ｳ繝励ヨ縲√さ繝斐・蜃ｦ逅・∵ｯ皮紫菫ｮ豁｣繝励Ο繝ｳ繝励ヨ縲∫判蜒冗函謌舌Ο繧ｸ繝・け縺ｫ縺ｯ螟画峩縺ｪ縺励３egression test `tests/upscale-prompt-ui.test.mjs` now requires the emoji-prefixed sentence and still forbids copying the social-post title/source name.
  - Gemini stability/quality hardening: the Gemini-family prompt now adds a generic `GEMINI STABILITY / QUALITY LOCK` to reduce flat-template output, invented dominant cast/silhouette figures, shadow-as-new-character errors, dialogue duplication, extra warning phrases, and punctuation normalization. The old Gemini rule forcing Japanese dialogue to end with `縲Ａ was removed because it conflicted with verbatim script lock. The generic `classic anime styles` constraint was also replaced with a selected-style/PANEL STYLE LOCK preservation rule so Gemini does not collapse selected visual styles back to a generic anime look.
  - Provider-family architecture repair: `PROMPT_PROVIDER_FAMILIES` and `normalizePromptProviderFamily` now make Gemini/OpenAI prompt selection explicit; `useMangaWorkflow` derives the family from the active engine/API state and passes it to `buildMangaPrompt`; provider parity tests forbid Web/API split prompt variables and forbid prompt assembly branching on `enableChatGPTMode`.
  - Browser memory hardening: `src/lib/generation-history.js` caps generated image history to the latest item so giant PNG data URLs are not accumulated in the DOM.
  - Free-form EMOTION normalization: hyphen/plus/space-separated descriptors are mapped to existing strong style locks instead of silently falling back to NORMAL.
  - v4.8.1 release prep after v4.8.0: ChatGPT image prompts now stay in a web-paste-safe range by compacting repeated global/per-panel control text while preserving title, dialogue, watermarks, visual action detail, art-quality rules, cast limits, and identity/glasses locks. Dialogue labels now explicitly say `Dialogue (verbatim bubbles)` and the text rules forbid paraphrase, synonyms, softening, and added/omitted words.
  - Generic identity parser hardening: `- Character [Name]: ... glasses/no glasses ...` cast lines now feed the Identity Matrix, so glasses locks do not disappear when the cast list is already in cleaned Character-line form.
  - Generic dialogue extraction follow-up: spoken quotes embedded in action narration are extracted alongside explicit dialogue, source order is preserved, and visual text/handwriting/SFX/mood labels stay out of speech-bubble Dialogue.
  - Generic action-quote de-duplication follow-up: fallback extraction now skips unowned narration quotes and short action-quote fragments already covered by explicit dialogue, preventing extra speech bubbles such as partial setup phrases while preserving distinct spoken quotes with an inferred speaker.
  - Generic title punctuation fix: the scenario title line is captured as a full line and terminal punctuation such as `!?` is preserved instead of being stripped by prompt assembly or Step 4 rebuild.
  - Highest-priority anti-hardcoding rule documented: root `AGENTS.md` and `docs/project_standards.md` now explicitly forbid production hardcoding of proper nouns, sample words, character names, sample dialogue, product/place names, trend words, and one-off prompt fragments when fixing bugs.
  - Regression tests added for embedded spoken quote extraction, visual quote exclusion, handwriting text exclusion, title punctuation preservation, and ChatGPT prompt length/critical-content preservation.
  - Production source scan for the user's sample-specific terms returned no matches; sample terms appear only in regression test fixtures.
  - v4.8.2 API key preflight fix: entering an arbitrary string or a shortened/invalid OpenAI-style key no longer unlocks STEP 1. API entry now requires provider format validation plus a live provider preflight (`Gemini /v1beta/models` or `OpenAI /v1/models`) before setting the app's connected state. Failed provider errors are sanitized so API key fragments are not rendered in the UI. The modal now labels typed keys as unverified format guesses until preflight succeeds, saved Gemini keys are re-verified on startup, and the header/control bar/STEP1 receive an empty API-key prop while any API modal is open so stale state cannot look connected.
  - Web/API prompt parity guard: `copyPrompt` copies `finalPrompt`, API generation starts from `overridePrompt || finalPrompt`, and both `generateImageWithOpenAI` and `generateImageWithImagen` receive that same `currentPrompt`. Regression tests forbid introducing separate `webPrompt` / `apiPrompt` style variables for ChatGPT/OpenAI or Gemini.
  - ChatGPT/Gemini prompt budget note: the roughly 15,000-character ChatGPT prompt target is an empirical soft budget for manual ChatGPT Web paste, not an official OpenAI API limit and not a guarantee. Keep it separate from API transport guards such as the OpenAI image request hard cap in `src/lib/openai.js`. Do not apply that empirical ChatGPT Web budget to Gemini-family prompts; Gemini-family prompts must be allowed to remain longer when the scenario/cast/style guidance needs it.
  - STEP2 downgrade rollback: the attempted STEP2 scenario emotion-diversity guard was removed after the user rejected STEP2 spec-down. Do not re-add STEP2 restrictions or scenario-generator constraints without explicit approval.
  - ChatGPT Web style prompt restoration: the ChatGPT-family image prompt no longer collapses the selected `styleCore` into `Base style: full-color TV anime`; it now carries the full style guidance into the Web/API final prompt.
  - A4 export rollback: the extra `A4 PNG繧偵ム繧ｦ繝ｳ繝ｭ繝ｼ繝・(1024ﾃ・448)` button and deterministic A4 canvas export were removed from STEP4. Only the raw generated image download remains.
  - Strict scenario/script guard: both ChatGPT-family and Gemini-family final prompts now include `STRICT SCRIPT LOCK` before `PANEL DESCRIPTIONS`, state that a different story is a failed output, forbid replacing/rewording/omitting/adding dialogue, and make lower camera/layout/cast/style instructions yield to the locked scenario/title/dialogue.
  - Action extraction control-token leak fix: `extractActionOnly` now strips bracketed control tags such as `[EMOTION: ...]` and `[Camera: ...]` from `Action (visual only)` while preserving the actual situation/action body. This prevents hidden routing/camera/emotion tokens from being sent to image models as visual content.
  - Gag-intent additive overlay: when a comedy/gag emotion tag maps to a serious/dark style such as `HORROR`, the style is preserved and only a short comedy-expression allowance is appended. This avoids flattening dramatic comedy into plain chibi gag while still rescuing the gag intent.
  - Action quote placeholder cleanup: `extractActionOnly` no longer injects English placeholder strings such as `the listed dialogue content` or `a non-text ambient sound effect` into visual action text. Explicit visible writing contexts now include poster text, while spoken quote fragments and dangling speech verbs such as `縲ゅ→蜻溘″縲～ are removed from action text without polluting the prompt.
- **State**: v4.8.7 title alignment is released and deployed. Source commit `b8e604b6fdb208185028a62277171f1e605acc0d` was pushed to `origin/main`; tag `v4.8.7` points to that commit; GitHub Release `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.7` is published as a non-draft, non-prerelease release. GitHub Pages `gh-pages` commit `49bf0b65348660d2e6e67ded711c116cef7582a1` is deployed by successful Pages run `28789326242`; live Pages returns title `Nano Banana Pro v4.8.7`, asset `assets/index-BYfBYppp.js`, `SUPER FURU AI 4-KOMA SYSTEM`, and no old long-title variants. Hugging Face Spaces commit `9c5ec83` is pushed and the live static URL returns the same title/asset/footer checks. `C:\nano-banana-pro-main` is synced from the v4.8.7 GitHub Release ZIP to package version `4.8.7`, `index.html` title `Nano Banana Pro v4.8.7`, new footer/precheck title strings present, no old long-title variants, and no nested duplicate folder. Other README-only app references were committed and pushed: Story Maker `8790983`, Short Movie `085663a`, Character Sheet `a09d149`, Comic Translation `413309b`, and Background `098eee6`; Viral Radar had no old-title occurrence. User-requested PS1 full backup is the next operation.

## Pending Verification Tasks
- [x] Local STEP2 scenario-enhancement exact-sample proof: OpenAI gpt-4.1 on port 5173 changed all four dialogue lines, preserved metadata/speakers/calm tone, changed all six selected categories, kept background enhancement OFF without a background-mutation violation, and passed the post-response validator on the first attempt.
- [x] Local STEP2 Gemini parity proof: gemini-3.5-flash initially failed `dialogue_overexpanded`, then passed the built-in corrective retry with all six selected categories changed, background OFF, no validation issues, and zero browser errors.
- [x] Local STEP2 scenario-enhancement verification: targeted 10/10 and full 89/89 tests, ESLint, Vite build, and `git diff --check` passed.
- [x] v4.8.7 title-scope proof: all seven app folders and known local `*-main` copies returned no old long system-title variants; model/engine mentions are intentionally preserved.
- [x] v4.8.7 version prep: `node scripts\update_version.cjs 4.8.7 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was kept to version-only changes.
- [x] v4.8.7 local checks: `git diff --check -- . ':!dist'`, `node scripts\pre_deploy_check.js`, full `node --test tests\*.mjs` 40/40, `npm run lint -- --max-warnings=0`, `npm run build`, and `dist` title-string scan passed. Built asset is `assets/index-BYfBYppp.js`; `dist` contains `Nano Banana Pro v4.8.7` / `SUPER FURU AI 4-KOMA SYSTEM` and no old long-title variants.
- [x] v4.8.7 commit/push/tag/GitHub Release: source commit `b8e604b6fdb208185028a62277171f1e605acc0d` pushed to `origin/main`; tag `v4.8.7` points to that commit; GitHub Release is published at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.7`.
- [x] v4.8.7 GitHub Pages deploy proof: `npm run deploy` published `gh-pages`; the first Pages deployment failed transiently, then explicit Pages rebuild run `28789326242` succeeded. Live Pages returns title `Nano Banana Pro v4.8.7`, asset `assets/index-BYfBYppp.js`, `SUPER FURU AI 4-KOMA SYSTEM`, and no old long-title variants.
- [x] v4.8.7 Hugging Face Spaces deploy proof: `npm run deploy:hf` pushed commit `9c5ec83`; live static HTML/JS return title `Nano Banana Pro v4.8.7`, asset `assets/index-BYfBYppp.js`, `SUPER FURU AI 4-KOMA SYSTEM`, and no old long-title variants.
- [x] v4.8.7 release ZIP and `C:\nano-banana-pro-main` sync proof: GitHub Release ZIP was downloaded and synced; `C:\nano-banana-pro-main\package.json` is `4.8.7`, `index.html` title is `Nano Banana Pro v4.8.7`, new footer/precheck title strings are present, old long-title variants are absent, and `C:\nano-banana-pro-main\nano-banana-pro-main` does not exist.
- [ ] v4.8.7 requested PS1 full backup proof.
- [x] v4.8.6 local issue proof: in-app browser STEP3 rebuilt the supplied enhanced scenario and Panel 4 `Dialogue (verbatim bubbles)` contained only the explicit final speaker dialogue; the quoted group reaction/narration stayed out of speech bubbles. STEP4 OpenAI image generation completed successfully with a 1024x1536 PNG and browser warn/error logs 0.
- [x] v4.8.6 version prep: `node scripts\update_version.cjs 4.8.6 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was kept to version-only changes.
- [x] v4.8.6 local checks: `node --check src\lib\panel-utils.js`, `node --check tests\panel-utils-dialogue.test.mjs`, full `node --test tests\*.mjs` passed 40/40, `npm run lint -- --max-warnings=0` passed, `npm run build` passed with existing Browserslist/chunk warnings only, `git diff --check -- . ':!dist'` passed, and `node scripts\pre_deploy_check.js` passed.
- [x] v4.8.6 commit/push/tag/GitHub Release: source commit `d34f1865290f498df793b34b8bc2371bf4bd488c` pushed to `origin/main`; tag `v4.8.6` points to that commit; GitHub Release is published at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.6`.
- [x] v4.8.6 GitHub Pages deploy proof: `npm run deploy` published `gh-pages`; latest Pages build is `built` for `gh-pages` commit `04527b2bcf912601ceae182da44f7e5dbc0e38ce`; live Pages returns title `Nano Banana Pro v4.8.6`, asset `assets/index-Dxq0x6kQ.js`, and the group-narration guard strings.
- [x] v4.8.6 Hugging Face Spaces deploy proof: `npm run deploy:hf` pushed commit `5823b5e`; live static HTML/JS return title `Nano Banana Pro v4.8.6`, asset `assets/index-Dxq0x6kQ.js`, and the group-narration guard strings.
- [x] v4.8.6 release ZIP and `C:\nano-banana-pro-main` sync proof: GitHub Release ZIP was downloaded and synced; `C:\nano-banana-pro-main\package.json` is `4.8.6`, `index.html` title is `Nano Banana Pro v4.8.6`, and `C:\nano-banana-pro-main\nano-banana-pro-main` does not exist.
- [x] v4.8.6 requested PS1 full backup proof: visible PowerShell launched `scripts\backup_full.ps1 -NoGui`; final ZIP `backups\antigravity_full_backup_2026-07-06_090640.zip` is 136,373,907 bytes with SHA256 `311533A9B9314DF4DB3B1F072CE7405323285CFBE25AF4A044AB217B10953CED`; ZIP inspection found 15,132 entries, `testzip=OK`, required entries including `backup_meta.json`, `git_repos.txt`, `git_repos_detailed.json`, `restore_full.ps1`, `backup_full.ps1`, `root_files/AGENTS.md`, `root_files/PLAN.md`, `coordination_files/manifest.json`, and `git_worktree_snapshots/snapshot_index.json`; `root_files/PLAN.md` and `coordination_files/files/nano-banana-pro/HANDOFF.md` contain v4.8.6; Drive copy at `E:\繝槭う繝峨Λ繧､繝暴Antigravity_Backups\antigravity_full_backup_2026-07-06_090640.zip` has the same size/hash; `antigravity-system` ledger commit `99ec05f503ed370915d4daa1a122e11a5588fe4e` matches `origin/master`.
- [x] v4.8.5 focused regression proof: `node --test tests\upscale-prompt-ui.test.mjs` passed 3/3 and requires the ratio-fix button base label to be hidden while `繧ｳ繝斐・螳御ｺ・ is shown.
- [x] v4.8.5 version prep: `node scripts\update_version.cjs 4.8.5 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was kept to version-only changes.
- [x] v4.8.5 local checks so far: full `node --test` suite passed 39/39; `npm run lint -- --max-warnings=0` passed; `npm run build` passed with only existing Browserslist/chunk warnings; `git diff --check -- . ':!dist'` passed.
- [x] v4.8.5 local browser proof on port 5173: in-app browser loaded `http://127.0.0.1:5173/?codexVerify=v485-local-title` with title `Nano Banana Pro v4.8.5`; harness click proof showed the ratio-fix button `innerText` as `笨・繧ｳ繝斐・螳御ｺ・, the original-label span hidden, and the copied-feedback span visible/absolute.
- [x] v4.8.5 `node scripts\pre_deploy_check.js` after this HANDOFF update passed.
- [x] v4.8.5 commit/push/tag/GitHub Release: commit `2c437b57d297315dc0ec4e56a633bff8d23a3094` pushed to `origin/main`; tag `v4.8.5` points to that commit; GitHub Release published at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.5`.
- [x] v4.8.5 GitHub Pages deploy proof: `npm run deploy` published `gh-pages`; build `1078237152` reached `built`; live `https://furuyan1234.github.io/nano-banana-pro/` returned title `Nano Banana Pro v4.8.5`, asset `assets/index-CHp4AeiZ.js`, and JS contains `v4.8.5`, `逕ｻ蜒乗ｯ皮紫菫ｮ豁｣繝励Ο繝ｳ繝励ヨ繧偵さ繝斐・`, and `繧ｳ繝斐・螳御ｺ・.
- [x] v4.8.5 Hugging Face Spaces deploy proof: `npm run deploy:hf` committed `4c73daa` to `hf-nano-banana-pro`; live `https://furuyan-nano-banana-pro.static.hf.space/` returned title `Nano Banana Pro v4.8.5`, asset `assets/index-CHp4AeiZ.js`, and JS contains `v4.8.5`, `逕ｻ蜒乗ｯ皮紫菫ｮ豁｣繝励Ο繝ｳ繝励ヨ繧偵さ繝斐・`, and `繧ｳ繝斐・螳御ｺ・.
- [x] v4.8.5 release ZIP and `C:\nano-banana-pro-main` sync proof: GitHub Release ZIP was downloaded and synced; `C:\nano-banana-pro-main\package.json` is `4.8.5`, `index.html` title is `Nano Banana Pro v4.8.5`, and `C:\nano-banana-pro-main\nano-banana-pro-main` does not exist.
- [x] v4.8.5 requested PS1 full backup proof: visible PowerShell launched `scripts\backup_full.ps1 -NoGui`; final ZIP `backups\antigravity_full_backup_2026-07-04_182348.zip` is 136,368,750 bytes with SHA256 `ADD00CCB1CB250A7F9EFC948DAA38EA649EA5112376EFAE55CC14B98DF4489CD`; ZIP inspection found 15,131 entries, `testzip=OK`, and required entries including `backup_meta.json`, `git_repos.txt`, `git_repos_detailed.json`, `restore_full.ps1`, `backup_full.ps1`, `root_files/AGENTS.md`, `root_files/PLAN.md`, `coordination_files/manifest.json`, and `git_worktree_snapshots/snapshot_index.json`; Drive copy at `E:\繝槭う繝峨Λ繧､繝暴Antigravity_Backups\antigravity_full_backup_2026-07-04_182348.zip` has the same size/hash; `antigravity-system` ledger commit `9a2c897d0a8e5e594ff1968257a960bb7cdd71fd` matches `origin/master`.
- [x] v4.8.4 RED/GREEN proof: `node --test tests\upscale-prompt-ui.test.mjs` failed before the UI sentence had the `笨ｨ` prefix, then passed after the sentence was updated.
- [x] v4.8.4 version prep: `node scripts\update_version.cjs 4.8.4 ...` synchronized `package.json`, `src/lib/constants.js`, `index.html`, `README.md`, and `../hf-nano-banana-pro/README.md`; `package-lock.json` was limited to version-only changes; ChangeLog entries were pruned to the latest 15-ish entries.
- [x] v4.8.4 local checks so far: full `node --test` suite passed 38/38; `npm run lint -- --max-warnings=0` passed; `npm run build` passed; `git diff --check -- . ':!dist'` passed; source scan found no `Eris_Create_Lab` or `GPT Native Super Resolution Ver2.1` strings.
- [ ] v4.8.4 local browser proof on port 5173.
- [ ] v4.8.4 `node scripts\pre_deploy_check.js` after this HANDOFF update.
- [ ] v4.8.4 commit/push/tag/GitHub Release.
- [ ] v4.8.4 GitHub Pages deploy proof.
- [ ] v4.8.4 Hugging Face Spaces deploy proof.
- [ ] v4.8.4 release ZIP and `C:\nano-banana-pro-main` sync proof.
- [ ] v4.8.4 requested PS1 full backup proof.
- [x] v4.8.3 local pre-release verification: `node scripts\pre_deploy_check.js`, full `node --test` suite (36/36), `npm run lint -- --max-warnings=0`, `npm run build`, `git diff --check -- . ':!dist'`, and node syntax checks on touched JS files passed.
- [x] v4.8.3 local browser proof: in-app browser on `http://127.0.0.1:5173/?codexVerify=v483-local` loaded `Nano Banana Pro v4.8.3`, rendered `Super FURU AI 4-koma System v4.8.3`, had no A4 export button, and reported zero console warnings/errors.
- [x] v4.8.3 commit/push/tag/release: commit `f66d7b2b506e65111b2d42f9629cd499de9b33cc` was pushed to `origin/main`; tag `v4.8.3` was pushed; GitHub Release was created at `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.3`.
- [x] v4.8.3 GitHub Pages proof: live HTML returned title `Nano Banana Pro v4.8.3`, asset `assets/index-BoDe6TcM.js`, and deployed JS contains `v4.8.3`, `STRICT SCRIPT LOCK`, and `GEMINI STABILITY / QUALITY LOCK` while excluding the old `Japanese dialogue MUST end with a period` rule and `A4 PNG` label.
- [x] v4.8.3 Hugging Face Spaces proof: `npm run deploy:hf` committed `b9b598e`; live static HTML returned title `Nano Banana Pro v4.8.3`, asset `assets/index-BoDe6TcM.js`, and deployed JS matched the same lock/removal checks.
- [x] v4.8.3 release ZIP/local sync proof: GitHub Release ZIP was downloaded and synced to `C:\nano-banana-pro-main`; package version is `4.8.3`, HTML title is `Nano Banana Pro v4.8.3`, and there is no nested duplicate folder.
- [x] v4.8.3 backup policy: no full backup was run because the user did not explicitly request backup for this deploy.
- [x] Post-v4.8.2 RED/GREEN regression proof for ChatGPT full-auto prompt routing: the new test first caught the forced Gemini branch, then passed after routing full-auto prompt assembly through `enableOpenAIApi || enableChatGPTMode`.
- [x] Post-v4.8.2 per-panel style-lock regression proof: non-normal style blocks now include `PANEL STYLE LOCK` guidance for visibly distinct linework, palette, shading, and background/VFX while preserving identity/dialogue/layout/clean skin rules.
- [x] Post-v4.8.2 focused local checks: `node --test tests\prompt-budget.test.mjs tests\prompt-title.test.mjs tests\panel-utils-dialogue.test.mjs tests\full-auto-chatgpt-mode.test.mjs tests\prompt-style-exceptions.test.mjs`, `git diff --check`, `npm run lint -- --max-warnings=0`, and `npm run build` passed.
- [x] Post-v4.8.2 local browser proof after the ChatGPT prompt-routing/style-lock fix: `http://127.0.0.1:5173/?codexPromptRouteStyleLockFix=1` loaded with title `Nano Banana Pro v4.8.2`, rendered `Super FURU AI 4-koma System v4.8.2`, and reported zero browser console errors.
- [x] Web/API prompt parity proof for both provider families: `tests\prompt-provider-parity.test.mjs` verifies Web copy uses `finalPrompt`, API generation uses `currentPrompt` derived from `finalPrompt`, OpenAI/Gemini both send that same prompt, and Web/API split prompt variable names are blocked.
- [x] STEP2 rollback proof: the STEP2 scenario emotion-diversity production rules were removed, and the enforcing `tests\scenario-emotion-diversity.test.mjs` regression was deleted.
- [x] Strict scenario/script-lock proof: `tests\prompt-script-lock.test.mjs` first failed before the lock existed, then passed after both provider-family prompts placed `STRICT SCRIPT LOCK` before detailed panel descriptions and made story/dialogue changes a failed output.
- [x] Prompt budget wording and provider-boundary proof: ChatGPT prompt-budget regression tests now describe the roughly 15,000-character target as an empirical Web-copy soft budget, not an official API limit. `tests\prompt-budget.test.mjs` also verifies a Gemini-family manga prompt can exceed that soft budget while keeping the Gemini footer, style-lock markers, and a late stress marker, so the ChatGPT Web-copy budget cannot silently cap Gemini.
- [x] Provider-family architecture proof: `tests\prompt-provider-parity.test.mjs` and `tests\full-auto-chatgpt-mode.test.mjs` now require explicit `providerFamily` routing and forbid falling back to the legacy ChatGPT-mode boolean for provider selection.
- [x] Browser Data URL memory proof: `tests\generation-history-memory.test.mjs` verifies generated image history keeps only the latest data URL.
- [x] Latest local checks after the control-token leak and gag-overlay follow-up: combined provider/dialogue/style/script/budget/title/history/A4-removal/Web-quality tests passed 23/23; `node --check` on `src\lib\panel-utils.js`, `src\lib\prompts.js`, `src\lib\prompt-assembler.js`, and `src\lib\openai.js` passed; `npm run lint -- --max-warnings=0` passed; `npm run build` passed; `git diff --check -- . ':!dist'` passed with only the existing CRLF warning for `src/lib/safety-filters.js` and no content diff in that file.
- [x] Latest local checks after action-quote placeholder/dangling-verb cleanup: `tests\panel-utils-dialogue.test.mjs` passed 10/10; combined provider/dialogue/style/script/budget/title/history/A4-removal/Web-quality tests passed 25/25; `node --check src\lib\prompts.js`, `node --check src\lib\panel-utils.js`, `node --check src\lib\prompt-assembler.js`, `node --check src\lib\openai.js`, `npm run lint -- --max-warnings=0`, and `npm run build` passed.
- [x] Fresh real OpenAI API image generation after the ChatGPT-family prompt-routing/style-lock repair: after the user entered the API key in the UI, the in-app browser generated the restored `AI闡嶺ｽ懈ｨｩ隲紋ｺ峨∫樟蝣ｴ豺ｷ荵ｱ!?` scenario successfully. Treat this as API confirmation only, not the final target. It was produced before the final dangling-verb cleanup, so it is not proof of the latest rebuilt prompt.
- [x] User-provided ChatGPT Web image `C:\Users\sx717\Downloads\ChatGPT Image 2026蟷ｴ6譛・7譌･ 17_31_45.png` was inspected against the scenario/final prompt, not against another image. Verdict: practical scenario-faithful pass direction. Title, setting, main beats, required dialogue, poster text, cookie beat, and dark final panel are present; remaining gaps are minor spelling/punctuation drift and not all five characters being equally visible in panels 1/3. This is not a perfect literal-lock proof.
- [ ] Fresh ChatGPT Web regeneration from a prompt rebuilt after the final dangling-verb cleanup remains unverified.
- [x] Fresh Gemini API verification after the stability/quality hardening: STEP3 rebuilt the Gemini prompt in the in-app browser with `GEMINI STABILITY / QUALITY LOCK`, `Do NOT normalize punctuation`, and the selected-style/PANEL STYLE LOCK preservation rule; it no longer contained the old `Japanese dialogue MUST end with a period` or `classic anime styles` fixed rule. Two Gemini API generations completed without browser/API errors. The verified scenario `繧上◆縺ゅａ繧医ｊ霆ｽ縺・ヱ譏・?` followed the title, 4-panel structure, core beats, and required dialogue direction better than the earlier `AI闡嶺ｽ懈ｨｩ隲紋ｺ荏 failure, and did not show the prior black-silhouette invented-person or dialogue-duplication failure. Remaining gap: Gemini's drawing quality still looks flatter and more generic than ChatGPT/OpenAI Web; further prompt-only pressure is likely to overconstrain composition/dialogue rather than close the model-quality gap. Next quality step should be post-generation QA/retry or manual Gemini Web generation with attached character/background references, not more prompt lines.
- [x] Local prompt assembly proof for the failing scenario shape.
- [x] Production source scan for sample-specific hardcoded terms.
- [x] Local checks and build before release prep (`node --test tests\panel-utils-dialogue.test.mjs tests\prompt-title.test.mjs`, `node --check`, `git diff --check`, `npm run lint -- --max-warnings=0`, `npm run build`).
- [x] Local prompt budget regression proof: the latest long ChatGPT sample now emits a 13,841-character prompt and keeps title/dialogue/watermarks/tategaki/glasses locks, `THINGS TO AVOID`, stronger verbatim dialogue locking, and explicit Panel 4 dialogue without promoting unowned action quotes.
- [x] Local checks after prompt budget/action-quote fix: `node --test tests\panel-utils-dialogue.test.mjs tests\prompt-title.test.mjs tests\prompt-budget.test.mjs`, `git diff --check -- . ':!dist'`, `npm run lint -- --max-warnings=0`, `npm run build`, and local HTTP 200 on `http://127.0.0.1:5173/`.
- [x] Final docs validation after this HANDOFF update: `node scripts\pre_deploy_check.js` passed.
- [x] `node scripts\pre_deploy_check.js` before release commit.
- [x] GitHub Pages deploy verification for v4.8.1: live URL returned title `Nano Banana Pro v4.8.1`, asset `assets/index-DLlziH4s.js`, and deployed JS contained `v4.8.1`, `THINGS TO AVOID`, and `Dialogue (verbatim bubbles)`.
- [x] Hugging Face Spaces deploy verification for v4.8.1: live URL returned title `Nano Banana Pro v4.8.1`, asset `assets/index-DLlziH4s.js`, and deployed JS contained `v4.8.1`, `THINGS TO AVOID`, and `Dialogue (verbatim bubbles)`.
- [x] GitHub Release creation (v4.8.1): `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.1`.
- [x] Release ZIP download and `C:\nano-banana-pro-main` local sync: package version `4.8.1`, HTML title `Nano Banana Pro v4.8.1`, no nested duplicate folder.
- [x] v4.8.2 API preflight tests: invalid arbitrary text and shortened OpenAI-looking keys are rejected locally without network access; provider-shaped keys must pass live provider preflight; invalid provider responses are rejected without exposing API key fragments.
- [x] v4.8.2 UI lock regression tests: the API modal must not label unverified keys as a running engine, main UI receives an empty API-key prop while an API modal is open, and saved keys are preflight-verified before restoring connected state.
- [x] v4.8.2 local verification: `node --test tests\api-key-preflight.test.mjs tests\api-key-ui-lock.test.mjs tests\panel-utils-dialogue.test.mjs tests\prompt-title.test.mjs tests\prompt-budget.test.mjs`, `npm run lint -- --max-warnings=0`, `npm run build`, and `git diff --check -- . ':!dist'` passed.
- [x] v4.8.2 browser proof on `http://127.0.0.1:5173/?codexApiPreflightV482Browser=1`: entering a shortened OpenAI-looking dummy key shows `OpenAI蠖｢蠑擾ｼ域悴讀懆ｨｼ・荏, never shows `Engine 縺ｧ襍ｷ蜍描, fails with the Japanese error `API繧ｭ繝ｼ縺ｮ蠖｢蠑上′豁｣縺励￥縺ゅｊ縺ｾ縺帙ｓ縲・..`, keeps the header `譛ｪ謗･邯啻, keeps `main` blurred and pointer-locked, and does not render the raw OpenAI provider error or the typed key fragment.
- [x] Production source scan for the user's sample-specific terms and API key patterns returned no matches.
- [x] `node scripts\pre_deploy_check.js` after this HANDOFF update.
- [x] Commit/push v4.8.2 API preflight fix: `5f9ad7acc09011ee224a35a25acb9e704d66d1bc`.
- [x] GitHub Pages deploy verification for v4.8.2: `https://furuyan1234.github.io/nano-banana-pro/?v=20260624-v482-api-preflight-jp` returned title `Nano Banana Pro v4.8.2`, asset `assets/index-BPKQnvko.js`, Japanese API-key errors and unverified labels present, raw OpenAI error text and old English format error absent.
- [x] GitHub Pages live UI API-error proof for v4.8.2: `https://furuyan1234.github.io/nano-banana-pro/?v=20260624-v482-live-ui-api-error` rejected a shortened OpenAI-looking dummy key with Japanese format error, kept `譛ｪ謗･邯啻, kept `main` blurred/pointer-locked, and did not show raw provider errors or `Engine 縺ｧ襍ｷ蜍描.
- [x] Hugging Face Spaces deploy verification for v4.8.2: `https://furuyan-nano-banana-pro.static.hf.space/?v=20260624-v482-api-preflight-jp` returned title `Nano Banana Pro v4.8.2`, asset `assets/index-BPKQnvko.js`, Japanese API-key errors and unverified labels present, raw OpenAI error text and old English format error absent.
- [x] Hugging Face Spaces live UI API-error proof for v4.8.2: `https://furuyan-nano-banana-pro.static.hf.space/?v=20260624-v482-live-ui-api-error` rejected a shortened OpenAI-looking dummy key with Japanese format error, kept `譛ｪ謗･邯啻, kept `main` blurred/pointer-locked, and did not show raw provider errors or `Engine 縺ｧ襍ｷ蜍描.
- [x] GitHub Release creation (v4.8.2): `https://github.com/FURUYAN1234/nano-banana-pro/releases/tag/v4.8.2`.
- [x] Release ZIP download and `C:\nano-banana-pro-main` local sync for v4.8.2: package version `4.8.2`, HTML title `Nano Banana Pro v4.8.2`, API preflight test present, no nested duplicate folder.
- [x] v4.8.3 final docs status: this HANDOFF and root `PLAN.md` now record the released state and remaining unverified ChatGPT Web regeneration.

## Next Steps
- For future prompt-routing/style-diversity work, keep ChatGPT Web and OpenAI API on the same ChatGPT-family prompt, and keep Gemini Web and Gemini API on the same Gemini-family prompt. Do not split them by surface.
- Treat scenario/title/dialogue mismatch as a hard failure, not a tolerable style variation. The model must not replace the story with a different conflict, setting, sequence, ending, or punchline.
- Do not reintroduce the removed STEP2 emotion-diversity/spec-down rule unless the user explicitly asks for it. The next visual check should focus on whether the ChatGPT Web copy prompt preserves scenario/dialogue/style/identity in a fresh Web generation.
- Run a full backup only when the user explicitly requests backup.
## フルオート／連続ループの手入力保持 — v6.2.8、2026-09-16

- 要件: フルオートまたは連続ループで、STEP2の指定場所・指定服装に手入力がある場合はクリアせず、各周回で維持する。カラー／白黒、OpenAI画像品質・サイズの現在の保持挙動も確認する。
- 原因と修正: `runFullAuto` が新しい周回のカテゴリ選択後に場所・服装を無条件で空文字へ戻していた。空欄ならAIおまかせという既存仕様を保ちつつ、この2つの無条件クリアだけを削除した。連続ループも同じ`runFullAuto`を再起動するため、各周回で同じ手入力値を保持する。
- 検証: 新しい回帰は旧実装の無条件クリアに対してREDを確認後GREEN。関連15件、全Nodeテスト483件、警告0 lint、production build、`git diff --check`を確認した。実OpenAI `gpt-6-astra Web Search` STEP2は91秒で完了し、生成シナリオのLocation／Outfitに手入力値が反映された。STEP4画像生成は実行していない。
- 設定境界: カラー／白黒はフルオート、連続ループ、STEP1/STEP2リセットでは維持し、全設定クリアだけカラーへ戻す。OpenAI品質・サイズはフルオート・連続ループ・リセットを通じて再読み込みまで維持する。

## STEP API待機時間の10分統一 — v6.2.7、2026-09-16

- 要件: GPT-6系の応答やプロバイダー内の再試行を待てるよう、STEP1〜4で実行するAPIの各試行の待機上限を10分に統一する。
- 実装: Gemini／OpenAIのテキストAPI既定値、STEP2のシナリオ生成・演出強化の明示値、STEP4のGemini画像生成を600,000msに設定した。STEP4のOpenAI画像生成は従来から600,000msであり維持した。接続設定時のAPIキー事前確認はSTEP実行ではないため45秒のままにした。
- 検証: 新しい10分契約テストは旧値（STEP2 3分、共通テキスト2分、Gemini画像3分）に対してREDを確認後GREEN。全Nodeテスト481件、警告0 lint、production build、`git diff --check`を確認した。APIキー値は読み取っていない。

## OpenAIカテゴリニュース検索のプロバイダー固定 — v6.2.6、2026-09-16

- 要件: 接続時にGemini APIを選んだ場合はSTEP1〜4をGeminiだけで、OpenAI APIを選んだ場合はSTEP1〜4をOpenAIだけで実行する。カテゴリのニュースシナリオも同じ選択を維持し、別プロバイダーへ混在・フォールバックしない。
- 原因と修正: OpenAIのSTEP2はChat Completionsだけを使い、最新ニュース検索を実行していなかった。ニュースカテゴリ時だけ、選択済みOpenAIルートからResponses APIのWeb Searchを使うようにし、Gemini SDK・Google Groundingへは送らない。Geminiルートは従来どおりGoogle Groundingだけを使う。
- 検証: 実ルーターを通すモック通信でOpenAIニュース生成が`/v1/responses`、`web_search`、選択済みのOpenAIシナリオモデルだけを使うことを確認。関連テスト、警告0 lint、production build、内蔵ブラウザーのカテゴリ選択を確認。さらに、OpenAI接続で政治・経済カテゴリを実行し、画面ログの`OpenAI gpt-6-astra Web Search`と実ニュースを根拠にしたシナリオ生成を確認した。Geminiへの切替・フォールバックは発生していない。APIキー値は読み取っていない。
## 画面文字とカメラ効果の両立 — v6.3.1、2026-09-17

- 変更: 同じモニター・看板が再登場するだけで全文を毎コマ読ませる構図にならないよう、文字を読むコマと身体演技を見せるコマを分ける汎用ルールを追加した。物に印字された文字と吹き出し・字幕を分離し、明示Cameraを可読性のために移動しない。
- 実API検証: 元の「画像の外まで公開待ち」台本をSTEP2（gpt-6-astra）で再構成し、6本の台詞、必要な画面文字・数値、最終字幕を保持したままSTEP3/STEP4を実行。GPT Image 2.5 Sunburst/xhigh、1024×1536、参照2枚、自動修正OFF。原本は `scratch/camera-lettering-replan-2026-09-17/api-result.png`（2,433,087 bytes）と同フォルダーのプロンプト・ログ・検証記録。
- 目視: 俯瞰、寄り、床に近い視点、広い引きは確認できた。強いアオリ、望遠圧縮、3コマ目の机の左右は未達。内部QAはカメラ各項目をokとしたが、表面文字・小道具向きの根拠不足で総合は警告付き。6本の台詞と必要文字は画像内に残った。
- 次回の宿題: 画面を読ませる必要があるコマでは情報を一面へ集約し、他のコマは画面を側面・背面・遠景または画角外にする。文字を残したまま強いアオリと望遠圧縮を安定させる方法、左右反転を防ぐ物理配置指定、内部QAの表面向き判定を実画像で再検証する。追加生成は新しい明示承認が必要。
- 継続ルール: 今後の実API検証では、アップデート内容の確認に加えて、絵がヌルくなっていないかを独立した必須目視項目にする。カメラ高・距離・傾き・奥行き、身体演技・表情、背景の層・光・主焦点の落差を確認し、基準画像がなければ比較は`unverified`と記録する。内部QAのPASSやプロンプト上の指定だけでは代替しない。
- 公開境界: v6.3.1の正式リリース・Pages・Cドライブ公開版コピー・FourPanel note `ndf063558c1f5`更新・公式フルバックアップをユーザーが明示承認。Hugging Faceは現行契約の対象外。APIキー値は読み出し・記録しない。
## 一般シリアス結末モード（ローカル実装・API検証）— 2026-09-17

- 実装: 既存の結末セレクターを「おまかせ／シリアス／ギャグ」に整理し、両カテゴリ内のおまかせ、静かな余韻、決意・再出発、警告、問題提起、感動・救い、悲劇・喪失を追加した。既存ギャグとドキュメンタリー値は維持し、ドキュメンタリーを各カテゴリ内へ戻した。シリアス全項目に括弧の説明を追加。ブラウザ表示を確認し、ユーザーから選択肢の了承を得た。
- 自動判定: 明白な死亡・災害・被害・虐待・戦争・重篤な病気・喪失・追悼などを追加の有料分類なしでローカル判定し、AutoからSeriousAutoへ解決する。解決済みの結末種別をSTEP3/STEP4まで保持する。
- 衝突除去: 一般シリアスではSERIOUS INTENTを出し、COMEDY INTENT、GAG INTENT OVERLAY、CHIBI_GAG、chibi too、scripted surreal gagsを除外する。資料忠実モードだけの絵柄固定は一般シリアスへ広げない。
- ローカル検証: 全Nodeテスト510/510、lint、production build、git diff --checkが合格。buildのBrowserslist、混在import、chunk-size警告は既存警告。
- 実API: 洪水・犠牲者・追悼・復興を扱う手動題材をAutoで実行し、serious-requiredからSeriousAuto、Punchline「決意・再出発」へ解決した。GPT Image 2.5 Sunburst/xhigh、1024x1536、参照1枚、自動修正OFFで1枚だけ生成し、追加生成は行っていない。
- 実画像の採否: シリアス調・非ちび化、俯瞰／肩越し／ローアングル／緩い俯瞰の変化、抑えた演技、窓光と室内の層、前景・中景・背景の分離はsatisfied。3コマ目の足先までの全身構図と、画像内の大見出し表示はunmet。細かな印字面の向きと、このシナリオで指定していない望遠・ズームはunverified。内部QAの「全カメラ・タイトルOK」は実画素の採否に置き換えた。
- 公開境界: ユーザーがデプロイ・note更新・フルバックアップを明示承認。メニュー修正を含むv6.3.2を公式リリース手順で公開する。追加の画像生成はしない。進行状態はルートPLAN.mdを参照。
## v6.3.9 STEP3進捗フォーカスとSTEP4遷移 — 2026-09-19

- 状態: リリース候補。STEP3の実行開始時は主ボタン直下の実進捗ログへ移動し、最終プロンプトが完成した時だけSTEP4の先頭へ移動する。完成済みプロンプトの手動編集では画面位置を動かさない。
- 変更: `src/App.jsx`、`src/components/Step3Panel.jsx`、`src/components/Step4Panel.jsx`、`tests/step-progression-ui.test.mjs`、README。進捗ログをSTEP4からSTEP3へ移し、待機秒数を開始操作の近くで確認できるようにした。
- 検証: STEP進行の重点Nodeテスト6件、strict lint、`git diff --check`が成功。実API生成は今回のUI導線変更では行っていない。
- 公開境界: ユーザーは公式release/deployと既存FourPanel note `ndf063558c1f5`更新を明示承認。フルバックアップは今回見送りのため、`-RunFullBackup`およびバックアップ起動を行わない。

## Webコピーの手足所有チェック — ローカル実装・API検証、2026-09-20

- 実装: Webコピーの短縮経路でも、人物ごとの手首〜肩・足首〜腰の連結、自然な遮蔽／画角外、余分・分離・融合・欠損肢の禁止を残す。構図、動き、遠近、自然な遮蔽は保持し、全身表示を強制しない。
- 実API: OpenAIで、箱・ワゴン・床テープを扱う学園祭片付けの4コマをSTEP2〜4で実行。STEP3の最終プロンプトにコンパクトな `LIMB OWNERSHIP CHECK` が残ることを確認。GPT Image 2.5 Sunburst/xhigh、1024×1536、参照2枚。読順の限定修正2回を含む計3候補から最良候補を選択した。
- 目視: 各コマで見える手は腕と人物に自然につながり、余分・分離・融合した手足は見当たらない。箱・ワゴン・テープの受け渡しも明瞭。俯瞰、床に近いローアングル、肩越し、斜めの低い画角、踏み込み・しゃがみ・箱の支持による奥行きと身体演技を維持しており、単調な全身整列にはなっていない。
- 注意: 内部QAは台詞順・手・小道具・カメラを合格とした一方、文字を載せない段ボール等について表面文字／向きの証拠不足を `unverified` として警告付き採用した。追加生成は行わない。
## Astra token-efficiency / STEP2 model selection — restart handoff, 2026-09-21

### Status

- Local implementation is in progress. Do **not** commit, push, deploy, release, publish, or back up. Existing unrelated dirty file `public/diagrams/workflow-preview.png` must be preserved.
- User is about to restart the PC and wants Astra to reason over this handoff. The local Vite server was running at `http://127.0.0.1:5173/`; it will naturally stop during the restart. Start it again only if browser verification resumes.
- API keys were never read or recorded. The user entered a key in the app UI and explicitly allowed the already-completed paid STEP3 review and one STEP4 image run. Do not spend a further paid image run after restart without a fresh instruction.

### Completed model-routing work

- Added persisted STEP2 scenario-model selection (default `gpt-6-astra`) with a lower-only fallback route:
  `gpt-6-astra -> gpt-5.6-sol -> gpt-5.6-terra -> gpt-5.6-luna -> gpt-4.1 -> gpt-4.1-mini -> gpt-4.1-nano -> gpt-4o`.
- The picker explains quality/cost intent, uses a bundled official-price snapshot dated `2026-09-21`, persists until app-wide reset, and logs fixed-start/adopted model plus any fallback. The native select has explicit readable closed and option colors. The obsolete “Astraに戻す” button was removed.
- Added actual adopted scenario-model attribution to the OpenAI watermark: `ChatGPT / GPT-5.6 Terra / FURU AI 4-koma v6.4.2`; Gemini keeps its provider-specific watermark. The former `Scenario` word was intentionally omitted to avoid collision with the left watermark.
- Actual STEP2 evidence: the user selected `gpt-5.6-terra`; OpenAI Web Search scenario generation completed in 69 seconds, with fixed/adopted model both `gpt-5.6-terra` and no fallback.
- Prior focused model/watermark suite passed: 39 tests, lint, and production build. Build warnings about stale Browserslist, dynamic/static `gemini.js` import, and chunk size are pre-existing/non-blocking.

### Real STEP3/STEP4 evidence and visual QA

- User rebuilt STEP3 once and generated one image at `C:\Users\sx717\Downloads\AI_4koma_comic_ChatGPT_芝ローラーの珍交代_20260921133025.png`.
- Good: Japanese left watermark and exact right model watermark are both readable and non-overlapping; title, four panels, main cast, roller continuity, dialogue bubbles, and major hands/limbs are visually coherent.
- Failed Panel 3 conditions in that image are evidence of the **old** assembled prompt, not a model-only failure:
  - Camera was erroneously rewritten as `camera behind [リン]` instead of the source `サエコの反射帯付き安全ベストの肩越し`.
  - The rear-facing リン appears to show a chest-held document through her back. This is anatomically impossible. A chest-held document under a rear camera may be fully occluded, or show only its physical side/back/edge; it never needs to be made readable.
  - Panel 3's GEKIGA 7–8-head proportion did remain intact; unwanted chibi contamination was absent.

### Implemented generic fixes

- `src/lib/composition-variety.js`: low-angle cue no longer includes `chibi too`; it now says `preserve the scripted proportions`.
- `src/lib/prompt-assembler.js`: ChatGPT prompt compaction preserves an `EXPLICIT REAR CAMERA` eye-line rule rather than inferring a new OTS owner from speaker order.
- `src/lib/panel-utils.js`: named OTS extraction now recognizes modifiers between the character name and shoulder (for example, `サエコの反射帯付き安全ベストの肩越し`).
- `src/lib/shared-image-quality.js`: full and compact functional-surface rules now state that a rear-facing holder with a chest-held document/device may occlude it and must never project the document front or hands through the torso.

### Restart completion evidence

- The full and compact rear-facing/chest-held-object rules now retain the same physical contract while the compact wording stays within the 15,000-character Web-copy budget.
- Focused verification passed 49/49 tests, including prompt budgets, monochrome prompts, single-image synchronization, expressive direction, and STEP2 timeout coverage.
- Fresh full verification passed 591/591 Node tests, zero-error ESLint, production build, and `git diff --check`. The existing Browserslist, mixed Gemini import, and chunk-size build warnings remain non-blocking.
- The local Vite server was restored after the restart and returned HTTP 200 at `http://127.0.0.1:5173/`.
- No additional paid API image was generated. A second STEP4 image remains outside the current authorization.

### Required Astra efficiency decision after restart

- First, inspect the remaining work and explicitly choose the lowest model that can safely execute it. For the known deterministic work (repairing one test expectation, focused tests, lint/build, and diff review), hand the implementation to Sol or Terra if the restarted Codex model selector allows it.
- Keep Astra for unresolved root-cause analysis, cross-module routing contradictions, or interpretation of a new real prompt/image failure. Do not lower the model merely to save tokens if the failure is no longer deterministic.
- State the chosen model and rationale before implementation. If the user changes the task or local evidence exposes a new interaction, reassess rather than following this recommendation mechanically.

### Files changed in this task

- `README.md`
- `src/App.jsx`
- `src/components/Step2Panel.jsx`
- `src/config/openai-scenario-models.json` (new)
- `src/hooks/useMangaWorkflow.js`
- `src/index.css`
- `src/lib/composition-variety.js`
- `src/lib/fallback-chain-history.js`
- `src/lib/openai-model-routes.js`
- `src/lib/openai-text.js`
- `src/lib/panel-utils.js`
- `src/lib/prompt-assembler.js`
- `src/lib/scenario-provider.js`
- `src/lib/shared-image-quality.js`
- `tests/expressive-direction.test.mjs`
- `tests/openai-scenario-model-routing.test.mjs`
- `tests/scenario-model-selection.test.mjs` (new)
- `tests/watermark-model-attribution.test.mjs` (new)

## 4コマ可変高組版・全コマ拡大QA — ローカル実装と実API検証、2026-09-21

- 組版: 出力は1024×1536または1536×2304の2:3、タイトル帯・4コマ領域・フッター合計を固定し、各コマの相対高は生成内容どおり可変で保持する。左右余白は狭く、両ウォーターマークは最下部フッター内へ一度だけ収める。
- 汎用QA: ページ内の全コマ枠を自動検出し、各コマを拡大して同一QAへ渡す。吹き出しは台詞からB番号・話者を一般抽出し、本文位置、尻尾端点、話者頭部アンカーを独立検査する。人物名・特定コマ・特定台詞を使う本番個別ルールは禁止。
- 実API: 検証時だけSTEP2を`gpt-5.6-terra`にしてシナリオを生成し、STEP4はGPT Image 2.5 Sunburst/xhigh、1024×1536、自動修正OFFで1枚だけ生成。通常のSTEP2選択は`gpt-6-astra`へ戻した。
- 実画像で判明: 新QAは3コマ目の吹き出し読順不一致を検出したが、4コマ目で腕の先が靴状になった異常を「5本指で正常」と誤判定した。原因は指本数を先に評価し、腕端点が手か靴かを分類していなかったこと。
- 修正: 全人物・全コマ共通で肩→肘→手首→掌を追跡し、腕端点を`hand|foot|shoe|object|ambiguous`で記録する。腕が足・靴・物体で終わる場合は突起数に関係なく`anatomy`不合格、端点または手首‐掌接続が曖昧なら`unverified`にする。アカリ名や4コマ目へのハードコードはない。
- UI整理: 生成画像下は従来名の「元画像をダウンロード」だけを通常表示する。「固定比率画像をダウンロード」「加工前の元画像をダウンロード」「表示中の画像をQA再検査」は要求外のため削除した。
- 検証: 新しいUIと靴状手端点のテストを旧実装で失敗確認後に修正。全Nodeテスト608/608、ESLint、production build、`git diff --check`が成功。内蔵ブラウザでも「元画像をダウンロード」と「最初（STEP 1）に戻る」だけが表示され、削除対象2ボタンと固定比率表記がないことを確認した。
- 修正後の実API再検証: ユーザーの明示承認により、同じTerraシナリオからSTEP4を1回だけ再生成した。自動修正はOFFのままで、追加の自動生成は発生していない。
- 再検証結果: 4コマ目の「腕の先が靴になる」異常は目視で再発せず、新しい端点QAも不合格を出さなかった。一方、3コマ目の吹き出し順は引き続き逆でQA不合格。2コマ目は小道具文字混入により台詞照合が`unverified`、全コマの人物参照inventoryも欠落しており、総合合格ではない。
- 読順の追加診断: 数値`BUBBLE SLOTS`を全4コマへ残した新規API画像でも、画像モデルが吹き出し本体を各話者の近くへ寄せ、3コマ目を左→右B1/B2/B3にした。数値座標の保持だけでは不十分だった。
- 共通修正: 全シナリオ共通で「人物・背景より先に吹き出し本体を数値スロットへ描く→本体を固定→ヒゲだけを対応話者へ接続」の工程順をfull/compact両プロンプトへ追加した。QAは文字本体を変えず句読点・長音等だけを正規化して読順を照合し、OCR末尾記号の揺れで明白な逆順を`unverified`へ逃さない。人物名・コマ番号・台詞固有値による本番分岐はない。追加のRED/GREENを含む全Nodeテスト608/608、ESLint、production buildが成功した。
- 最新API検証: ユーザーの追加承認で履歴3→4の1枚だけをGPT Image 2.5 Sunburst/xhigh、1024×1536、自動修正OFFで生成した。送信済み最終プロンプトに`DRAW BODIES BEFORE ART`、3コマ目のB1/B2/B3数値スロット、同番号の話者マップがあることを事前確認したが、実画像の3コマ目は左→右B1/B2/B3となり、必要なB3/B2/B1を再び満たさなかった。したがって工程順プロンプトの追加だけでは生成側の話者近接配置を抑止できず、実画像修正は未達。新QAは末尾句読点を正規化した上でこの逆順を`bubble_order`として正しく検出した。タイトル周囲の閉じた黒枠も再発し`panel_layout`不合格。追加修正画像は生成していない。
- 境界: これ以上の追加API画像生成、deploy、release、note更新、backupは実行していない。APIキー値は読み出し・記録していない。
## v6.4.8 4コマ目帰結ゲート・Web A4寸法保持 — 2026-09-22

- 実装: 既存STEP2検証後に、1〜2コマ目の種、3コマ目までの読者予測、4コマ目の視覚的な反転・回収・帰結・再解釈を選択中シナリオモデルで監査する。弱い場合は4コマ目だけでなく1〜4コマ全体を一度だけ再構成し、既存検証と再監査の両方を通った案だけ採用する。失敗・不完全JSON・新事実混入・標語だけの結末は元の検証済み候補を警告付きで保持する。シリアスは安いギャグ反転を強制せず、ドキュメンタリーは事実・数値・時系列を発明しない。
- A4: API生成の標準1120×1584／大2240×3168（70:99）はアプリ側正規化で厳密に保持する。Web貼付用短縮プロンプトにも両寸法を残す。ChatGPT Webはアプリ正規化外なので小さな近似誤差を許容する。
- 背景文字: 台本で明示された文字だけを厳密に固定し、未指定の看板・ポスター等は場所に自然な文字を、可読／装飾、短文／長文、量・密度を問わず自由に描ける。台本にない文字だからという理由で面を抑制・簡略化・空白化しない。
- 実API: 内蔵ブラウザでユーザー指定Terraを使用。初稿監査→4コマ全体の一度きりの再構成→再監査が実行された。再構成案は直接的帰結として不採用、元案を警告付き保持したため退行なし。続けてSunburst/xhighを一回生成し、1120×1584、タイトル103px、4コマ全体1446px、透かし35pxを確認。台詞・左右の手・小道具・全4コマのカメラ検査はok、参照一致の在庫記録不足などはunverified警告。Web出力1055×1491はA4比との差0.0719%で、ユーザーが許容した。
- 公開境界: ユーザーはv6.4.8の公式release/deploy、既存FourPanel note更新、変更なしの黒い窓フルバックアップを明示承認済み。完了証拠はroot `PLAN.md`へ追記する。
## ローカル修正・API検証済み: 面文字の誤発話と周辺バグ — 2026-09-24

- 依頼: 「公立図書館の危機」3コマ目の配置表の印字が吹き出しへ変換される原因を修正し、周辺の不具合も調査・修正する。修正後に同じシナリオの実API画像を検証する。
- 境界: ローカル実装、回帰確認、内蔵ブラウザからの実API画像検証。公開・版更新・バックアップは未依頼。
- 修正: 印字・記載・表示の引用を発話動詞の誤検索から保護し、Actionに保持。文中の話者を節の主語で解決。同一行の複数話者、意図的な台詞の反復、括弧内の本文、入れ子の引用、末尾話者指定、話者未指定台詞の後の尻尾番号を保持。単独の無言プレースホルダーをActionから除外。コマ分割を入力検証と共通化し、全角・漢数字・空白・「こま目」見出しの混在と偽台詞を修正。「人物を見る」等の明示視線先を相互視線で上書きしない。
- 回帰証拠: 失敗を先に再現した汎用テストと元シナリオ全文fixtureを追加。最終の全Node `660/660`、重点`91/91`、警告0 lint、production build、diff checkが成功。ログはignoredの`output/all-tests-verified.log`、`output/final-targeted.log`、`output/lint-verified.log`、`output/build-verified.log`。既知のBrowserslist期限・mixed import・bundle-size警告のみ。productionコードへのサンプル固有語追加なし。初期作業ツリーはclean。
- 実API: `http://127.0.0.1:5173/`の内蔵ブラウザで、入力済み参照2枚＋元シナリオ全文を使用。STEP3を再構築し、Sunburst/xhigh・1120×1584・自動修正OFFで1枚生成、完了済み。3コマ目の印字をActionへ保持し、吹き出しは正しい3本のみ。全9台詞、右→左の読順、人数2/2/3/3、人物識別、4コマ、タイトルと両透かしの全体表示を目視確認。カメラ高・遠近・傾き、表情・身体演技・前景の落差も維持。
- 画像の残り: 指定カーディガンがジャケット寄り、1コマ目の手の左右と4コマ目の肩を止める動作は台本に完全一致しない。最終プロンプトには元の指定があり、画像モデルの追従差として記録。画像の全面的な完全合格は主張しない。内蔵QAは左透かし欠けと一部OCR差を指摘したが、実画像では両者を確認できず、3コマ目の面向きもunverified扱い。追加生成なし。
- 検証モデル: 今回のユーザー指定はSTEP2のGPT-6 Sol。UI選択を確認済みで、元シナリオは再生成していない。STEP3の既存レビューと画像QAはアプリ既定のGPT-4.1。準備中に担当AIがONボタンを状態と誤認してAstraの自動シナリオを開始したため、直ちに中断操作し、ユーザーへ報告した。以後Astraで新しい検証を開始していない。APIキー値の読出しなし。
- 受入: ローカルコード修正、回帰検証、元入力の実API生成と目視確認はsatisfied。画像の全演技・衣装一致はunmet。公開・版更新・バックアップは未依頼、未実行。検証結果と修正版アプリをブラウザに保持。
## v6.5.4 API生成PNG制作情報・公開作業 — 2026-09-24

- 対象: API生成画像へ、別添JSONと同じ制作情報を自動埋め込みする。APIキー、参照画像本体、ローカルパス、人物・場所の解析全文を自動除外し、PNGのみを利用者向け保存形式として案内する。
- 実装: canonical schema v3をPNG iTXtへ保存し、AI／モデル／確定シナリオ／実送信プロンプト／設定／日時／入出力SHA-256／IPTC・C2PA対応付け／プライバシー除外方針を記録する。埋め込みJSONと別添JSONは同一オブジェクトから生成する。
- API検証: OpenAI接続後にGPT-6 Lunaを明示選択し、B級ニュースのSTEP2 Web検索・生成・ペイオフ監査をフォールバックなしで完走。画像生成は追加実行していない。
- 公開境界: ユーザーがv6.5.4の公式release/deploy、既存FourPanel note `ndf063558c1f5`の更新、公式の可視黒い窓フルバックアップを明示依頼した。各工程の公開・読戻し・ZIP検証まで継続する。
## v6.5.5 生成履歴の最終採用画像一本化・公開作業 — 2026-09-24

- 対象: API画像の自動品質修正で生じる途中候補をユーザー向け生成履歴へ並べず、各生成の最終採用画像1件だけを残す。
- 根本修正: 品質ループの候補列を内部比較用に限定し、終了時に当該生成の候補画像を履歴から除いて最終採用画像だけを追加する。過去に確定した別生成の履歴は保持する。
- 検証: 回帰テストをRED→GREENで追加。全Node 665/665、警告0 lint、production buildが成功。実APIでは初回候補＋修正3候補まで比較し、修正版の退行を避けて初回画像を最終採用、画面の生成履歴が最後まで1件だけであることを確認した。
- 公開境界: ユーザーがv6.5.5の公式release/deploy、既存FourPanel note `ndf063558c1f5`の更新、公式の可視黒い窓フルバックアップを明示依頼した。公開・読戻し・新しいバックアップの独立検証まで継続する。
## v6.5.9 OpenAI/Gemini画像品質・Gemini表示・公開作業 — 2026-09-25

- 対象: OpenAI画像のA4大既定化、頭髪の枠際連続性、背景からの人物分離、プロ漫画の視線誘導、1枚絵Webコピー10,000字化に加え、Gemini経路の実画像品質とモデル・サイズ・単価表示を現行仕様へ合わせる。
- Gemini UI/Model Chain: STEP2実行ボタン直前の1行にGemini 3.8 Flashの用途と2026-09-25時点の導入価格$0.75／$3.75（100万トークン）を表示。文章・Visionの実行順とModel Chain表示を3.8→3.7→3.6→3.5→3.5 Lite→3.1 Liteへ同期し、3.8で非推奨の`temperature`送信を除去。STEP4はGemini 3.1 Flash Image、1K / 3:4、API 896×1200→A4 848×1200、品質切替なし、画像出力$0.067／枚（4候補最大$0.268）、入力$0.50／100万トークンを表示する。
- Gemini根本修正: 画像API直前でB番号・座標等の内部制御記号を自然文へ変換し、保存プロンプトはQA用に保持。Gemini修正プロンプト専用上限を65,000文字へ拡張し、一般APIガード32,000文字とは分離。QAは`日本語名 (Romaji)`を同一話者へ正規化し、ローマ字転記を話者違いと誤認しない。
- 実API確認: 参照キャラシート2枚を使いGemini `gemini-3.1-flash-image`で初回と修正版を生成。修正版は一部台詞が退行したため比較で初回候補を保持。採用画像は枠なしタイトル、全台詞、人物、手、小道具、4コマの読順・カメラ、A4余白を目視確認し、ユーザーが余白を承認した。自動修正停止ボタンも生成中だけ1行で表示され、停止操作が効くことを確認。APIキー値は取得・記録していない。
- OpenAI再検証: Sunburst/xhigh・A4大2240×3168で初回＋自動修正3回を完走し、最良候補を警告付きで採用。最終画像は枠なしタイトル、白地・黒ベタ・限定スクリーントーン、サエコの黒髪ベタ、2コマ目の右肩越し、手・小道具、カメラ、余白、STEP2採用モデル`GPT-6 Luna`のフッターを目視確認した。2コマ目の吹き出し順は脚本と逆だが会話と中央のヤギの鳴き声は自然に読め、ユーザー方針の「絵と話の面白さ優先・軽微な違反は許容」に照らして公開可能と判定した。APIキー値は取得・記録していない。
- モノクロ再検証: 白地・黒ベタ・単一スクリーントーン、限定的な頬網点、明るい肌の白地、褐色肌の連続網点、黒髪の黒ベタ＋細い白ツヤ、焦点のGペン強調、遠近時の腕短縮をすべてキャラ名・場面名なしの共通契約へ追加。最終Web圧縮後にも残り、15,000字以内であることを回帰テスト化した。
- モデル表示修正: STEP1とSTEP2が共有していた`usedModel`の非同期更新により、フルオートSTEP2→STEP3でSTEP1のGPT-4.1がフッターへ漏れる競合を修正。STEP2採用モデルを専用refへ即時保存し、今回の再構築プロンプトで`ChatGPT / GPT-6 Luna / FURU AI 4-koma v6.5.9`を確認した。
- 最終ローカル検証: 全Node `703/703`、ESLint 0エラー、production build、`pre_deploy_check.js`、`git diff --check`が成功。buildのBrowserslist期限、Geminiの静的／動的import、chunk-sizeは既知の警告である。
- 公開境界: ユーザーがv6.5.9の公式release/deploy、既存FourPanel note `ndf063558c1f5`更新、公式の可視黒い窓フルバックアップを明示依頼した。note公開クリック直前の操作確認後、公開読戻しとバックアップ独立検証まで行う。
## 長文Web貼付・API比較と分割コピー — 2026-09-26

- 方針: 画像品質を最優先し、短く効く指示を使う。32,000字は使い切る目標ではなく上限。必要な台詞・構図・人物識別・画風を守るためにだけ長くする。
- 実測: ChatGPT Webでは1回9,999字は入力欄に残り、10,000字はTXT化。15,571字を2回に分けて同じ入力欄へ貼り、1回送信で良好な4コマを生成。合計32,000字の分割貼付が入力欄に残ることは確認したが、その長さの画像生成品質は未検証。
- API: ユーザーがアプリUIで接続した既存キーとキャラシート2枚を使い、同じ15,571字の元プロンプトでSunburst/xhigh・2240×3168を1回生成。4コマと白いタイトル帯を目視確認し、タイトル上の黒い窓は見られなかった。内部QAは人物参照の識別条件に警告を出したため、完全合格は主張しない。画像の追加生成なし。キー値の読出し・記録なし。
- ローカル実装: STEP4で9,500字以下の番号付き分割コピーボタンを可変表示し、その下に一括コピーとTXT保存を残す。分割は原文を完全保持し、同じ入力欄へ順番に貼って最後に一度送信する。全文一括貼付のTXT化・生成結果変化の可能性を画面とREADMEに明記。
- 検証: 分割原文一致・長さ・Unicode境界と案内の重点6テスト、関連26テスト、ESLint、production build、ローカル画面の4分割・一括注意表示を確認。公開・版更新・バックアップは今回未実行。

- 追加ライブ検証（同日）: ユーザーの追加依頼で32,000字詳細版をAPIとWeb各1回生成。API本文31,333＋参照説明667、Sunburst/xhigh・2240×3168・自動修正OFF。Webは4分割、貼付後の自動改行1字を除き同内容、TXT化なし、1055×1491。双方4コマ・6台詞・雨具・枠なしタイトルを維持。展開されたSHADOW指定で1コマ目の顔の暗さと光る目が強まり、昨日の画風との完全一致は未達。長さだけの因果比較ではない。詳細と参照解像度の差は C:\Users\sx717\OneDrive\Documents\New project\output\web-paste-2026-09-26\results-32000.md。API内部QAは人物参照等unverifiedを残し警告付き完了。追加の再生成なし。

### 2026-09-26 分割コピー実画面提示
- ローカル実アプリ http://127.0.0.1:5173/ のSTEP4を表示。ユーザーがキーをUI入力済み。キー値は取得せず、既存台本を手入力しSTEP3のみ実行。追加の画像生成なし。
- 26,306文字から3分割（8,712 / 8,798 / 8,796）。各ボタンと全文コピーを実操作し、クリップボードの分割連結が全文に完全一致。表示だけのモックではなく実フローで確認。
- IABタブ15に分割ボタンを表示して保持。Vite5173（exec session75334）はユーザー操作用に稼働を維持。公開版への反映は未実施。
- 分割ボタンを青→薄青へ順に明るくするグラデーションに変更。分割数に応じて明度58～88%を配分し、濃紺の番号・文字とフォーカス枠を保持。ローカル実画面で3段階の配色、ESLintを確認。

## v6.6.1 公開・note・フルバックアップ — 2026-09-26
- ユーザーが現在の修正版の正式デプロイ、既存note更新、黒い窓での正式フルバックアップを明示承認。正式リリース完了→note公開読戻し→backup_launch.batと独立検証の順で実行する。
- 変更: 共通32,000文字ガード、参照説明をそろえたWeb/APIプロンプト、9,500文字以下の分割コピー、番号順に明るくなる背景、全文/TXT案内。ボタン高さは元の約22pxへ戻し、増やさない。
- 既存の15,571/32,000文字のAPI/Web生成証拠と、実画面の26,306文字3分割連結完全一致を採用。追加の画像生成は行わない。
