# 実装計画: OpenAI/Gemini状態収束の根治

旧実装の`selectedEngine`（表示・テキスト系統）と`enableOpenAIApi`（画像ルート）を別々の真実として扱わない。`getEffectiveEngine`が片方でもOpenAIならOpenAIを選び、同期effectがReact表示状態、共有テキストエンジン、画像ルート、ChatGPTプロンプトモードを収束させる。STEP4、手動救済、フルオートも同じ判定を使う。補助機能はAPI別に分離し、ChatGPTの比率修正／2倍アップスケールはOpenAI時だけ、Geminiの比率修正／2Kは生成済み画像を添付してGemini Webへ貼り付ける専用コピー、MiniMax H3は完成画像を使うプロバイダ非依存の後処理として両方に残す。Geminiアプリ内APIは通常の画像生成だけを担う。全4組合せの真理値表と実画像ルートをテスト化し、全回帰・静的検査・ビルド・公開面検証を通した後に統合リリースする。
