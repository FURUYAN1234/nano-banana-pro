# 実装計画: STEP4プロ向け補助操作の復旧

STEP4の補助操作は、APIプロバイダー固有の生成実行とは独立したコピー操作として常時表示する。STEP4でプロバイダーに依存するラベル、リンク、ログ、メタデータ、ダウンロード名は、実際に画像生成へ使用するOpenAI状態を唯一の判定に統一する。回帰テストは、補助操作をOpenAI条件でラップしないことと、古いテキスト用エンジン表示が残っていてもSTEP4の実効OpenAI表示が維持されることを固定する。全テスト、静的検査、ビルド、ローカル画面を通過後、統合リリースで公開面を検証する。
